---
phase: 03.3-probability-map-saturation-allocation-throughput
plan: 02
type: execute
wave: 2
depends_on:
  - 03.3-01
files_modified:
  - src/allocation.r
autonomous: true
requirements:
  - ALLOC-06
  - ALLOC-08
  - ALLOC-07
tags:
  - allocation
  - dinamica
  - alignment-contract
  - single-source-writer
  - saturation
must_haves:
  truths:
    - "After setup_allocation_inputs() runs, the work_dir trans_rates.csv contains zero rows where From* == To* and zero rows where Rate == 0; the row count of trans_rates.csv equals the count of %03d_id_trans_*.tif files in probability_map_dir, with no prefix gaps in the alphabetic sequence."
    - "Any active id_trans missing from alloc_params triggers stop() (not warning()) with the full list of missing ids, before any of the four Dinamica artifacts are written."
    - "After Dinamica returns posterior_path, run_allocation_one_timestep() calls write_saturation_summary() which produces work_dir/saturation_summary.csv and emits one AUDIT stage=saturation line per region to the per-worker log."
    - "The Stage 4 source rate CSV on disk (outputs/transition_tables/.../BAU-{region}-trans_rates-2022.csv) is unchanged by the filter — only the work_dir copy is filtered."
  artifacts:
    - path: src/allocation.r
      provides: Single-source writer (filter once, write four artifacts in lockstep, hard stop on missing alloc_params, inline saturation hook)
      contains: "AUDIT stage=allocation_filter"
    - path: src/allocation.r
      provides: Inline saturation hook call to write_saturation_summary
      contains: "write_saturation_summary("
  key_links:
    - from: src/allocation.r setup_allocation_inputs (~line 1670)
      to: work_dir trans_rates.csv, expansion_table.csv, patcher_table.csv
      via: single filtered_df then write.csv ×3
      pattern: "write\\.csv\\(.*trans_rates"
    - from: src/allocation.r generate_probability_maps (~line 2265)
      to: probability_map_dir/%03d_id_trans_*.tif
      via: iterate filtered_df with seq_len(nrow(...)) — no skip
      pattern: "%03d_id_trans"
    - from: src/allocation.r run_allocation_one_timestep (~line 1572)
      to: src/saturation_diagnostics.r write_saturation_summary
      via: source('src/saturation_diagnostics.r') in scripts/run_allocation.r src_files list
      pattern: "write_saturation_summary\\("
---

<objective>
Land the single-source structural fix that eliminates Dinamica row-position misalignment, and wire the inline saturation diagnostic hook. This is the substantive bug fix of Phase 3.3: after this plan lands, allocation throughput should jump from ~1% to the ≥90% per-transition floor on the BAU smoke (verified out-of-band by Wave 4).

Four coordinated edits inside `src/allocation.r` plus one edit in `scripts/run_allocation.r` to wire the new helper module into the worker's source path:

1. **Single-source filter at allocation entry (D-02, D-03):** Refactor `setup_allocation_inputs()` (~lines 1642-1782) so the work_dir `trans_rates.csv` is no longer a `file.copy()` of the Stage 4 source. Instead: read the source CSV with `check.names = FALSE`, drop persistence (From* == To*) AND zero-rate (Rate == 0) rows, sort by `id_trans` ascending, then `utils::write.csv(filtered_df, dst, row.names = FALSE)`. Reorder alloc_params to match filtered_df row order via `match(...)` plus a `stopifnot(identical(...))` defence-in-depth check (Pitfall 5).

2. **Promote alloc_params missing-ids warning to stop() (D-04):** Replace the `warning(log_msg(...))` block at ~lines 1702-1715 with `stop(log_msg(...), call. = FALSE)` listing every missing id_trans. Forces calibration regeneration rather than silently shipping a truncated expansion/patcher table.

3. **TIF writer no-skip refactor (D-02 mechanical consequence):** In `generate_probability_maps()` (~lines 2265-2316), remove the `if (from_val == to_val || rate == 0) next` skip — those rows are pre-filtered upstream. Replace the existing `if (nrow(dt_j) == 0L) next` skip with an all-NA TIF write fallback so the `%03d` alphabetic sequence stays gap-free even for active transitions with no predictions (Pitfall 2).

4. **AUDIT log line at filter entry:** Emit `AUDIT stage=allocation_filter scenario=%s region=%s year_ant=%d input_rows=%d filtered_rows=%d dropped=%d` immediately after the filter so the Phase 3.2 cross-stage audit script can pick it up.

5. **Inline saturation hook (D-05, D-06):** Insert a `write_saturation_summary(...)` call inside `run_allocation_one_timestep()` after `run_allocation_dinamica()` returns posterior_path (between current lines 1566 and 1572), wrapped in `prof_tic()`/`prof_toc()` matching the existing `dinamica` stage envelope. Add `"src/saturation_diagnostics.r"` to the `src_files` vector in `scripts/run_allocation.r:88-106` so parallel workers can resolve the helper.

Purpose: Make probability-map row position match trans_rates.csv row position by construction. Alignment becomes unreachable to violate. No runtime assertion needed (D-02 — the writer makes misalignment structurally impossible). The Stage 4 source CSV on disk is unchanged so Phase 3.2's cross-stage audit continues to see the full row set.

Output: One file modified (`src/allocation.r`), three locations changed (~lines 1670-1715, ~line 1572, ~lines 2265-2316); one supporting edit in `scripts/run_allocation.r` to add the new src file to the worker source list.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@src/allocation.r
@src/saturation_diagnostics.r
@src/utils.r
@scripts/run_allocation.r

<interfaces>
<!-- Helpers from Wave 1 src/saturation_diagnostics.r that this plan's hook call consumes. -->
<!-- Executor uses these directly — do not re-derive from saturation_diagnostics.r. -->

From src/saturation_diagnostics.r (created by Plan 01):

  compute_per_transition_metrics(
    filtered_df, prob_map_dir, anterior_path, posterior_path, threshold, exempt
  ) returns data.frame with columns:
    id_trans, from_val, to_val, rate, demanded_cells, placed_cells, placed_frac,
    coverage, p50, p90, p95, p99, pmax, demand_vs_capacity, exempt, floor_met

  write_saturation_summary(
    work_dir, scenario, region_label, region_suffix, year_ant,
    anterior_path, posterior_path, config, log_file
  ) returns invisible(summary_df)
  - Reads filtered work_dir trans_rates.csv, computes metrics, writes saturation_summary.csv,
    emits AUDIT stage=saturation log line via log_audit_saturation_line().

  log_audit_saturation_line(
    summary, scenario, region_label, year_ant, threshold, log_file
  ) returns invisible(NULL)
  - Emits: "AUDIT stage=saturation scenario=%s region=%s year_ant=%d active=%d met=%d
    exempt=%d failed=%d threshold=%.2f"

From src/utils.r (existing):

  log_msg(msg, log_file = NULL, also_console = TRUE) returns invisible(NULL)

From src/allocation.r (existing — preserve unchanged):

  prof_tic() returns opaque token or NULL
  prof_toc(token, tag_string, log_file) returns invisible(NULL)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-source filter + alloc_params hard-stop refactor in setup_allocation_inputs()</name>
  <files>src/allocation.r</files>
  <read_first>
    - src/allocation.r lines 1642-1782 (setup_allocation_inputs full function) — confirm exact line numbers for the file.copy() at 1671-1672, the read.csv at 1674, the sort-by-id_trans at 1677, the alloc_params load at ~1697-1700, and the warn-not-stop block at 1702-1715. The executor MUST read this range and base the edit on the actual current text, not on memorised offsets.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md (sections "Current rate-CSV load + sort", "Current alloc_params warn pattern", "Stop-with-log-msg analog", "Existing expansion/patcher writers", "Required change (Pitfall 5)") — exact refactor mechanics with source-pattern-anchored shapes.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md (sections "Code Examples — Single-source filter at allocation entry", "Code Examples — alloc_params coverage assertion (D-04)", "Common Pitfalls" 1, 4, 5) — locked target shapes for filter, alloc_params stop, defence-in-depth row-order alignment.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md (D-02, D-03, D-04) — locked decisions: drop persistence + zero-rate, single source df, hard stop on missing alloc_params.
    - src/utils.r (lines 1010-1025) — log_msg() signature for the AUDIT line emission.
  </read_first>
  <behavior>
    - Test 1: After setup_allocation_inputs() runs against a synthetic Stage 4 rate CSV containing 30 rows (8 persistence rows where From* == To*, 2 zero-rate rows where Rate == 0, 20 active rows), the work_dir trans_rates.csv contains exactly 20 rows, all with `From*` != `To*` and `Rate` != 0.
    - Test 2: The 20 rows in work_dir trans_rates.csv are sorted by id_trans ascending (`identical(df$id_trans, sort(df$id_trans))`).
    - Test 3: The Stage 4 source CSV on disk is byte-identical before and after setup_allocation_inputs() runs (the filter is work_dir-local).
    - Test 4: If alloc_params is missing one or more active id_trans values, the function calls stop() with a message containing every missing id (not warning, not silent truncation). The error message is also written to log_file via log_msg() before stop() unwinds.
    - Test 5: After the function returns, alloc_params (in-memory) has the same row count and same id_trans column as the filtered trans_rates_df, in the same order (`identical(alloc_params$id_trans, trans_rates_df$id_trans)` is TRUE).
    - Test 6: An `AUDIT stage=allocation_filter scenario=... region=... year_ant=... input_rows=30 filtered_rows=20 dropped=10` line is appended to log_file via log_msg().
    - Test 7: The text pattern `file.copy(trans_rate_src` is gone from src/allocation.r (the work_dir trans_rates.csv is now an explicit write.csv, not a copy).
    - Test 8: The text pattern matching `warning(log_msg(...missing.*alloc_params...))` is gone from the alloc_params section (the warn has been promoted to stop()).
  </behavior>
  <action>
    Modify `src/allocation.r` inside the `setup_allocation_inputs()` function (currently lines 1642-1782 — the executor confirms exact line numbers by reading the file first).

    Edit 1 — Replace file.copy + read.csv + sort (currently ~lines 1671-1677) with the single-source filter pattern:

    Remove the call to `file.copy(trans_rate_src, trans_rates_dst, overwrite = TRUE)` and the subsequent `read.csv(trans_rates_dst, check.names = FALSE)` followed by `trans_rates_df[order(trans_rates_df[["id_trans"]]), ]`. Replace with the following sequence of statements (RESEARCH.md `Code Examples — Single-source filter at allocation entry` is the verbatim source-of-truth for the target shape — implement exactly that pattern):

    - Read source into `trans_rates_raw` via `utils::read.csv(trans_rate_src, check.names = FALSE)` (read from SOURCE path, not the dst path which no longer exists yet).
    - Capture `n_in <- nrow(trans_rates_raw)`.
    - Filter via base R subset: keep rows where `trans_rates_raw[["From*"]] != trans_rates_raw[["To*"]] & trans_rates_raw[["Rate"]] != 0`. Assign to `trans_rates_df`.
    - Sort: `trans_rates_df <- trans_rates_df[order(trans_rates_df[["id_trans"]]), ]`.
    - Reset row names: `rownames(trans_rates_df) <- NULL`.
    - Capture `n_out <- nrow(trans_rates_df)`.
    - Write filtered df: `utils::write.csv(trans_rates_df, trans_rates_dst, row.names = FALSE)`.
    - Emit AUDIT log line via `log_msg(sprintf(...), log_file)` with format string `"AUDIT stage=allocation_filter scenario=%s region=%s year_ant=%d input_rows=%d filtered_rows=%d dropped=%d"` and args `scenario, region_label, year_ant, n_in, n_out, n_in - n_out`.

    Preserve `check.names = FALSE` on every read.csv of a Dinamica-bound artifact (Pitfall 4 — From*/To* markers).

    Edit 2 — Replace the warn block (currently ~lines 1702-1715) with a hard stop():

    The current code computes `missing_alloc_params <- setdiff(trans_rates_df[["id_trans"]], alloc_params[["id_trans"]])` and inside `if (length(missing_alloc_params) > 0)` calls `warning(log_msg(sprintf("The following id_trans values are present in trans_rates_df but missing from alloc_params: %s", paste(missing_alloc_params, collapse = ", ")), log_file))`. Replace the `warning(...)` call with `stop(...)` using format string `"Allocation params missing for active id_trans values (regenerate alloc_params for region %s period %s): %s"` and args `region_label, calibration_period, paste(missing_alloc_params, collapse = ", ")`, wrapped in `log_msg(...)` so the failure message lands in the per-worker log file before R unwinds. Pass `call. = FALSE` to stop(). Preserve the `setdiff(...)` and the `if (length(missing_alloc_params) > 0)` guard exactly.

    Edit 3 — Add defence-in-depth row-order alignment between alloc_params and trans_rates_df (Pitfall 5):

    Immediately after the alloc_params hard-stop check passes (i.e., the `if (length(missing_alloc_params) > 0) stop(...)` block returns control to the linear flow), reorder alloc_params to match trans_rates_df row order with two statements:
    - `alloc_params <- alloc_params[match(trans_rates_df[["id_trans"]], alloc_params[["id_trans"]]), ]`
    - `stopifnot(identical(alloc_params[["id_trans"]], trans_rates_df[["id_trans"]]))`

    Place these BEFORE the existing `expansion_tbl <- alloc_params[, c("From*", "To*", "Perc_expander")]` slice (~line 1717). Do not modify the downstream expansion/patcher writer logic — the slices `alloc_params[, c("From*", "To*", "Perc_expander")]` and `alloc_params[, patcher_cols]` will auto-inherit the corrected row order.

    Do NOT modify any code outside the `setup_allocation_inputs()` function body. Do NOT touch the Stage 4 source CSV on disk (D-03: the filter is work_dir-local only). Do NOT change `check.names = FALSE` to TRUE anywhere. Do NOT remove the existing `log_msg(sprintf("Expansion table written with %d rows", ...), log_file)` status messages — those stay.
  </action>
  <verify>
    <automated>grep -F "AUDIT stage=allocation_filter" src/allocation.r &amp;&amp; ! grep -E "file\.copy\(trans_rate_src" src/allocation.r &amp;&amp; grep -E 'From\*.*!=.*To\*' src/allocation.r &amp;&amp; grep -E 'Rate.*!=.*0' src/allocation.r &amp;&amp; grep -F "stop(log_msg(" src/allocation.r &amp;&amp; ! grep -F "missing from alloc_params" src/allocation.r &amp;&amp; grep -F 'match(trans_rates_df[["id_trans"]], alloc_params[["id_trans"]])' src/allocation.r &amp;&amp; grep -F 'stopifnot(identical(alloc_params[["id_trans"]], trans_rates_df[["id_trans"]]))' src/allocation.r &amp;&amp; Rscript -e 'parse(file = "src/allocation.r"); cat("parse_ok\n")' | grep -q parse_ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "AUDIT stage=allocation_filter" src/allocation.r` matches the new sprintf format string for the filter audit log line (ALLOC-06).
    - `grep -E "file\.copy\(trans_rate_src" src/allocation.r` matches zero lines (the file.copy of the Stage 4 source CSV is gone — replaced by explicit write.csv of the filtered df).
    - `grep -E 'From\*.*!=.*To\*' src/allocation.r` matches at least once (persistence filter present — D-03).
    - `grep -E 'Rate.*!=.*0' src/allocation.r` matches at least once (zero-rate filter present — D-03).
    - `grep -F "stop(log_msg(" src/allocation.r` matches at least one line in the alloc_params section (the warn → stop promotion — D-04 / ALLOC-08).
    - `grep -F "missing from alloc_params" src/allocation.r` matches zero lines (the old warn message text is gone).
    - `grep -F 'match(trans_rates_df[["id_trans"]], alloc_params[["id_trans"]])' src/allocation.r` matches at least once (defence-in-depth row-order alignment — Pitfall 5).
    - `grep -F 'stopifnot(identical(alloc_params[["id_trans"]], trans_rates_df[["id_trans"]]))' src/allocation.r` matches the row-order assertion.
    - `Rscript -e 'parse(file = "src/allocation.r"); cat("parse_ok")'` prints `parse_ok` (file still parses).
    - `grep -E "write[._]csv.*trans_rate_src" src/allocation.r` matches zero lines (the Stage 4 source CSV path is never written to — read-only from Stage 5's perspective).
  </acceptance_criteria>
  <done>setup_allocation_inputs() now writes the work_dir trans_rates.csv from a filtered, id_trans-sorted df; the warn on missing alloc_params is a hard stop; alloc_params is reordered to match the filtered df; an AUDIT stage=allocation_filter line is emitted per region. The Stage 4 source CSV on disk is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TIF writer no-skip refactor in generate_probability_maps()</name>
  <files>src/allocation.r</files>
  <read_first>
    - src/allocation.r lines 1892-2325 (generate_probability_maps full function) — confirm exact line numbers for the for-loop entry at 2265, the persistence/zero-rate skip at 2271-2273, the no-predictions skip at 2276-2278, and the existing tif_path / terra::writeRaster / log_msg block at 2280-2315. The executor MUST read this range first.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md (section "Current loop with the skip-with-`next` bug" and the Refactor targets that follow) — exact target shapes for the two skip removals and the all-NA TIF fallback.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md (sections "Pattern 3: TIF writer iterates filtered df, no skip", "Pitfall 2") — locked target shape: no persistence/zero-rate skip (those rows are gone), all-NA TIF fallback for no-predictions case (preserves alphabetic sequence).
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md (D-02 mechanical consequence) — the writer must produce a gap-free `%03d` prefix sequence.
  </read_first>
  <behavior>
    - Test 1: After generate_probability_maps() runs against a synthetic filtered trans_rates_dt of 20 active rows, the probability_map_dir contains exactly 20 TIF files with names matching `^[0-9]{3}_id_trans_[0-9]+\.tif$`.
    - Test 2: The numeric prefixes form a contiguous sequence 001..020 with no gaps. Programmatically: `fs <- list.files(prob_map_dir, pattern = "^[0-9]{3}_id_trans_.*\\.tif$"); pre <- as.integer(substr(sort(fs), 1, 3)); identical(pre, seq_along(fs))` returns TRUE.
    - Test 3: For a synthetic row where `normalized[.(k), nomatch = NULL]` returns zero rows (no predictions), an all-NA TIF is still written at the correct `%03d_id_trans_X.tif` prefix and a `WARN id_trans=... row=... has no predictions; wrote empty TIF` line is logged via log_msg.
    - Test 4: The text pattern combining `from_val == to_val || rate == 0` followed by `next` is gone from src/allocation.r (the persistence/zero-rate skip is removed — those rows are pre-filtered upstream by Task 1).
  </behavior>
  <action>
    Modify `src/allocation.r` inside the `generate_probability_maps()` function for-loop body at ~lines 2265-2316 (executor confirms exact line numbers by reading the file first).

    Edit 1 — Remove the persistence/zero-rate skip:

    Delete the three-line block at ~lines 2271-2273 that tests `if (from_val == to_val || rate == 0)` and calls `next`. Those rows are no longer present in `trans_rates_dt` after Task 1's upstream filter, so the skip is dead code and a source of `%03d` prefix gaps.

    Edit 2 — Replace the no-predictions skip with an all-NA TIF fallback:

    The current `if (nrow(dt_j) == 0L) { next }` block at ~lines 2276-2278 must be replaced with a six-line block that writes an all-NA TIF at the correct prefix before continuing. The replacement body (inside the `if (nrow(dt_j) == 0L) { ... }` guard) does, in order:
    - `tif_path <- file.path(prob_map_dir, sprintf("%03d_id_trans_%d.tif", k, id_trans))`
    - `r <- terra::setValues(anterior, NA_real_)`
    - `terra::writeRaster(r, filename = tif_path, overwrite = TRUE, NAflag = -999)`
    - `log_msg(sprintf("WARN id_trans=%d row=%d has no predictions; wrote empty TIF", id_trans, k), log_file)`
    - `next`

    The shape mirrors the existing `terra::setValues(anterior, NA_real_) + terra::writeRaster(... NAflag = -999)` write in the same function (~lines 2286-2293) — just with `NA_real_` everywhere and no `r[dt_j[["cell_id"]]] <- ...` assignment. This guarantees the alphabetic sequence stays gap-free even if a model produces no predictions for a region (Pitfall 2). Dinamica reads the cube and interprets the all-NA layer as zero-probability for that transition — no allocation happens for that row, but the row position remains valid.

    Do NOT modify the rest of the loop body (the main `tif_path <- file.path(...)` write, the `prof_tic()`/`prof_toc()` envelope at ~2284-2303, or the per-transition `log_msg(sprintf("Transition %d -> %d (id_trans=%d)...", ...))` summary at ~2304-2315). Those remain unchanged.

    Do NOT change `seq_len(nrow(trans_rates_dt))` to anything else — the loop iteration index k IS the row position by construction now.
  </action>
  <verify>
    <automated>! grep -E "if \(from_val == to_val \|\| rate == 0\)" src/allocation.r &amp;&amp; grep -F "WARN id_trans=%d row=%d has no predictions; wrote empty TIF" src/allocation.r &amp;&amp; grep -F "terra::setValues(anterior, NA_real_)" src/allocation.r &amp;&amp; grep -F 'sprintf("%03d_id_trans_%d.tif"' src/allocation.r &amp;&amp; Rscript -e 'parse(file = "src/allocation.r"); cat("parse_ok\n")' | grep -q parse_ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "if \(from_val == to_val \|\| rate == 0\)" src/allocation.r` matches zero lines (the persistence/zero-rate skip is gone — Task 1's upstream filter handles those rows).
    - `grep -F "WARN id_trans=%d row=%d has no predictions; wrote empty TIF" src/allocation.r` matches at least one line (the all-NA TIF fallback log line is present — Pitfall 2).
    - `grep -F "terra::setValues(anterior, NA_real_)" src/allocation.r` matches at least twice (once in the new fallback branch, once in the existing normal write — the existing one is preserved).
    - `grep -F 'sprintf("%03d_id_trans_%d.tif"' src/allocation.r` matches at least twice (fallback path uses the same naming as the main path).
    - `Rscript -e 'parse(file = "src/allocation.r"); cat("parse_ok")'` prints `parse_ok`.
    - `for (k in seq_len(nrow(trans_rates_dt)))` still appears verbatim in the file (the iteration index k IS the row position by construction).
  </acceptance_criteria>
  <done>The TIF writer iterates the pre-filtered trans_rates_dt with no skip; every row produces a TIF at its correct `%03d` prefix; the alphabetic sequence is gap-free by construction. Active transitions with no predictions get an all-NA TIF and a WARN line — Dinamica sees a zero-probability layer for that row but the cube alignment holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inline saturation hook in run_allocation_one_timestep() + worker source wiring</name>
  <files>src/allocation.r, scripts/run_allocation.r</files>
  <read_first>
    - src/allocation.r lines 1393-1600 (run_allocation_one_timestep) — confirm the exact location where `run_allocation_dinamica()` is called (~line 1563) and where its `posterior_path` return value is captured. The new saturation hook must be inserted AFTER posterior_path is assigned (~line 1566) and BEFORE the existing `log_msg("Completed region...")` line at ~1572. Confirm the `prof_tic()`/`prof_toc()` envelope shape used by the surrounding `dinamica` stage at ~lines 1562-1571.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md (section "`src/allocation.r` — saturation diagnostic hook in `run_allocation_one_timestep()` (~line 1572-1588)") — exact hook shape with `t_saturation <- prof_tic()` envelope, arg list, and tag string `region=%s stage=saturation_summary`.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md (Pattern 4 — Inline saturation summary) — locked target shape.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md (D-05, D-06) — locked decisions on the inline surface.
    - scripts/run_allocation.r lines 88-106 — the `src_files` vector that workers source on startup. `src/saturation_diagnostics.r` must be added so the new hook resolves inside parallel workers.
    - src/allocation.r lines 1-50 — header to confirm whether helpers from saturation_diagnostics.r are made available by sourcing in scripts/run_allocation.r (project pattern: scripts source the file list before calling run_allocation()).
  </read_first>
  <behavior>
    - Test 1: After Task 3 ships, `grep -F "write_saturation_summary(" src/allocation.r` matches at least one call site inside `run_allocation_one_timestep()`.
    - Test 2: The hook call is wrapped in `t_saturation <- prof_tic()` ... `prof_toc(t_saturation, sprintf("region=%s stage=saturation_summary", region_suffix), log_file)`.
    - Test 3: The hook receives `work_dir = region_work_dir`, `scenario`, `region_label`, `region_suffix`, `year_ant`, `anterior_path = file.path(region_work_dir, "anterior.tif")` (or the existing `region_input$anterior_path` variable in scope), `posterior_path = posterior_path` (the return value of run_allocation_dinamica), `config`, and `log_file` arguments.
    - Test 4: `scripts/run_allocation.r` `src_files` vector contains `"src/saturation_diagnostics.r"` as an entry, ordered AFTER `"src/utils.r"` (because saturation_diagnostics.r calls log_msg from utils.r) and BEFORE `"src/allocation.r"` (because allocation.r's hook calls write_saturation_summary from saturation_diagnostics.r).
    - Test 5: After both edits, a smoke-source via `Rscript -e 'setwd("."); src_files <- c("src/setup.r","src/utils.r","src/dinamica_utils.r","src/saturation_diagnostics.r","src/allocation.r"); for (f in src_files) source(f)'` succeeds without errors.
  </behavior>
  <action>
    Edit A — Insert the inline saturation hook in `src/allocation.r:run_allocation_one_timestep()`:

    Locate the `prof_toc(t_dinamica, sprintf("region=%s stage=dinamica", region_suffix), log_file)` call at ~line 1567-1571 (which closes the `dinamica` profiling stage immediately after `posterior_path <- run_allocation_dinamica(...)` at ~line 1563-1566). Insert the new saturation hook in the gap between that prof_toc and the existing `log_msg(sprintf("    Completed region: %s (ID=%d)", region_label, region_val), log_file)` at ~line 1572.

    The inserted block contains, in order:
    - A blank line and a one-line comment `# Saturation diagnostic (Phase 3.3 D-05/D-06): per-transition placed-vs-demanded summary`.
    - `t_saturation <- prof_tic()`
    - A call to `write_saturation_summary(...)` with arguments (each on its own line for readability per CONVENTIONS.md function-signature style):
      - `work_dir = region_work_dir`
      - `scenario = scenario`
      - `region_label = region_label`
      - `region_suffix = region_suffix`
      - `year_ant = year_ant`
      - `anterior_path = region_input$anterior_path` (use the existing in-scope variable that was passed into setup_allocation_inputs — confirm exact name by reading lines 1540-1553)
      - `posterior_path = posterior_path`
      - `config = config`
      - `log_file = log_file`
    - `prof_toc(t_saturation, sprintf("region=%s stage=saturation_summary", region_suffix), log_file)`

    The envelope uses the same `prof_tic()` / `prof_toc()` shape as the surrounding stages (PATTERNS.md "saturation diagnostic hook" section). The `region=%s stage=saturation_summary` tag matches `scripts/summarise_allocation_profile.r`'s grammar so post-run profile summaries pick it up.

    Edit B — Wire src/saturation_diagnostics.r into the worker source list in `scripts/run_allocation.r`:

    Locate the `src_files <- c(...)` vector at lines 88-106. Add `"src/saturation_diagnostics.r"` as an entry, positioned AFTER `"src/utils.r"` (saturation_diagnostics.r depends on log_msg) and BEFORE `"src/allocation.r"` (allocation.r's hook calls write_saturation_summary). Preserve the existing entries unchanged. The `for (src_file in src_files)` loop at ~lines 96-106 will then source the new file as part of the existing tryCatch wrapper — no other edits to scripts/run_allocation.r are required.

    Do NOT add a `source("src/saturation_diagnostics.r")` line inside `src/allocation.r` itself (that would violate the CONVENTIONS.md "No library() calls in src/" spirit and double-source it when the script driver sources both files). The single source-list edit in scripts/run_allocation.r is sufficient — that is the file workers actually execute.

    Do NOT modify any other call site of run_allocation_one_timestep() or any other line in run_allocation.r outside the src_files vector.
  </action>
  <verify>
    <automated>grep -F "write_saturation_summary(" src/allocation.r &amp;&amp; grep -F "t_saturation <- prof_tic()" src/allocation.r &amp;&amp; grep -F 'sprintf("region=%s stage=saturation_summary"' src/allocation.r &amp;&amp; grep -F '"src/saturation_diagnostics.r"' scripts/run_allocation.r &amp;&amp; Rscript -e 'parse(file = "src/allocation.r"); parse(file = "scripts/run_allocation.r"); cat("parse_ok\n")' | grep -q parse_ok &amp;&amp; Rscript -e 'env &lt;- new.env(parent = baseenv()); sys.source("src/setup.r", envir = env); sys.source("src/utils.r", envir = env); sys.source("src/dinamica_utils.r", envir = env); sys.source("src/saturation_diagnostics.r", envir = env); sys.source("src/allocation.r", envir = env); stopifnot(is.function(env$run_allocation_one_timestep)); stopifnot(is.function(env$write_saturation_summary)); cat("sourced_ok\n")' | grep -q sourced_ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "write_saturation_summary(" src/allocation.r` matches the new hook call site inside `run_allocation_one_timestep()`.
    - `grep -F "t_saturation <- prof_tic()" src/allocation.r` matches the prof_tic envelope opening.
    - `grep -F 'sprintf("region=%s stage=saturation_summary"' src/allocation.r` matches the prof_toc tag string (PATTERNS.md envelope shape).
    - `grep -F '"src/saturation_diagnostics.r"' scripts/run_allocation.r` matches the new entry in the src_files vector.
    - The order in scripts/run_allocation.r src_files vector is: `"src/utils.r"` appears before `"src/saturation_diagnostics.r"` which appears before `"src/allocation.r"` (verified by line-number comparison via grep -n on the same file).
    - `Rscript -e 'parse(file = "src/allocation.r"); parse(file = "scripts/run_allocation.r"); cat("parse_ok")'` prints `parse_ok`.
    - `Rscript -e 'sys.source("src/setup.r"); sys.source("src/utils.r"); sys.source("src/dinamica_utils.r"); sys.source("src/saturation_diagnostics.r"); sys.source("src/allocation.r")'` succeeds with no errors (the worker source order resolves correctly).
  </acceptance_criteria>
  <done>After Dinamica returns posterior_path, run_allocation_one_timestep() now calls write_saturation_summary() inside a prof_tic/prof_toc envelope. The saturation_summary.csv and AUDIT stage=saturation log line are produced per region at allocation time. Workers source src/saturation_diagnostics.r as part of the run_allocation.r src_files initialisation.</done>
</task>

</tasks>

<verification>
- All three edits land in `src/allocation.r` without breaking the file parse (`Rscript -e 'parse(file = "src/allocation.r")'` succeeds).
- The `src_files` vector in `scripts/run_allocation.r` contains `src/saturation_diagnostics.r` in the correct source-order position.
- Sourcing the full project file chain (`setup.r` -> `utils.r` -> `dinamica_utils.r` -> `saturation_diagnostics.r` -> `allocation.r`) succeeds with no errors.
- The Stage 4 source CSV path (`outputs/transition_tables/.../BAU-{region}-trans_rates-2022.csv`) is only ever read (via `read.csv(trans_rate_src, ...)`) and never written — Phase 3.2's cross-stage audit script continues to see the full row set.
- `git diff src/allocation.r` shows changes concentrated in three regions: ~1670-1720 (filter + stop), ~1572 (hook insert), ~2265-2316 (TIF writer no-skip).
- `git diff scripts/run_allocation.r` shows exactly one line added to the src_files vector.
- No file outside `files_modified:` is touched.
</verification>

<success_criteria>
- Single-source filter is in place; the work_dir trans_rates.csv has no persistence rows and no zero-rate rows; it is sorted by id_trans ascending (ALLOC-06).
- Missing alloc_params id_trans triggers stop() with the full list of missing ids (ALLOC-08).
- The TIF writer iterates the filtered df with no skip; the `%03d` alphabetic sequence is gap-free by construction; no-predictions rows get an all-NA TIF and a WARN log line (ALLOC-06 mechanical correctness).
- The inline saturation hook is wired and produces saturation_summary.csv + AUDIT stage=saturation log line per region (ALLOC-07 inline surface).
- The Stage 4 source CSV on disk is unchanged.
- src/allocation.r and scripts/run_allocation.r both parse and source cleanly in the correct order.
</success_criteria>

<output>
Create `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-02-SUMMARY.md` when done, recording:
- The four edit locations (with verified line numbers from the executor's read pass).
- A `git diff --stat` snippet showing exactly two files modified.
- A short note confirming `Rscript -e 'sys.source(...)'` on the full source chain succeeds.
- Any deviations from the plan (e.g., the actual line numbers in the file at execution time differ from PATTERNS.md's approximate references) and how the executor confirmed the right edit point.
</output>
