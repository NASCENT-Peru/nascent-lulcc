---
phase: 03-parallelism-memory-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/allocation.r
  - scripts/run_allocation.r
  - tests/testthat/test-allocation-runtime-contract.R
autonomous: true
requirements:
  - MEM-03

must_haves:
  truths:
    - "run_allocation.r selects future::multicore automatically on Linux/HPC and future::multisession on Windows/local with no manual code edit"
    - "BLAS, OMP, data.table, arrow, and GDAL thread counts are pinned to 1 before future::plan() is called"
    - "A dev-only strict-globals mode can be enabled with ALLOCATION_DEV_STRICT_GLOBALS=TRUE and logs that the guard is active"
    - "Allocation can be narrowed to one smoke-run scenario, region, and timestep through env-var filters without changing the default full-run behavior"
    - "Cgroup memory state is logged before worker launch so later smoke-run verification can prove the RAM budget in context"
  artifacts:
    - path: "src/allocation.r"
      provides: "runtime helpers for plan selection, native thread pinning, cgroup logging, and smoke-run filters"
      contains: "select_allocation_plan <- function()"
    - path: "scripts/run_allocation.r"
      provides: "ordered startup block that runs pre-flight, pins threads, enables strict globals, chooses the future backend, and logs the choice"
      contains: "pin_native_threads_to_one"
    - path: "tests/testthat/test-allocation-runtime-contract.R"
      provides: "contract tests for backend selection, thread pinning order, strict-globals gate, and smoke filters"
      contains: "ALLOCATION_YEAR_POST_FILTER"
  key_links:
    - from: "scripts/run_allocation.r"
      to: "src/allocation.r"
      via: "pin_native_threads_to_one() and select_allocation_plan() are called after pre-flight and before future::plan()"
      pattern: "select_allocation_plan|pin_native_threads_to_one"
    - from: "src/allocation.r"
      to: "run_allocation() scenario/region/year loops"
      via: "ALLOCATION_PROFILE_SCENARIO, ALLOCATION_REGION_FILTER, and ALLOCATION_YEAR_POST_FILTER narrow the smoke run"
      pattern: "ALLOCATION_PROFILE_SCENARIO|ALLOCATION_REGION_FILTER|ALLOCATION_YEAR_POST_FILTER"
---

<objective>
Establish the Phase 3 runtime control plane in `allocation.r` and `run_allocation.r`: automatic backend selection, native thread pinning, strict-globals dev mode, cgroup memory logging, and env-driven smoke-run filters for one scenario x region x timestep.

Purpose: Phase 3's memory architecture only works if the parent process sets the execution contract in the right order before any future launches. This plan creates that contract and makes the later HPC smoke run reproducible without changing the normal full-run path.
Output: Runtime helper functions in `src/allocation.r`, an updated startup block in `scripts/run_allocation.r`, and a contract test file.
</objective>

<execution_context>
@C:/Users/bblack/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-parallelism-memory-architecture/03-RESEARCH.md
@.planning/phases/03-parallelism-memory-architecture/03-PATTERNS.md
@.planning/phases/02-model-size-reduction/02-03-SUMMARY.md

<interfaces>
From `03-RESEARCH.md` "Code Examples":

```r
select_allocation_plan <- function() {
  override <- suppressWarnings(as.integer(
    Sys.getenv("ALLOCATION_NUM_WORKERS", unset = NA_character_)
  ))
  workers <- if (!is.na(override) && override > 0L) override
             else if (requireNamespace("parallelly", quietly = TRUE)) parallelly::availableCores()
             else max(1L, parallel::detectCores() - 1L)

  if (identical(tolower(Sys.getenv("ALLOCATION_PARALLEL_STRATEGY", "")), "sequential")) {
    return(list(strategy = "sequential", workers = 1L))
  }
  forced <- tolower(Sys.getenv("ALLOCATION_PARALLEL_STRATEGY", ""))
  if (forced %in% c("multicore", "multisession")) {
    return(list(strategy = forced, workers = workers))
  }
  if (requireNamespace("parallelly", quietly = TRUE) && parallelly::supportsMulticore()) {
    list(strategy = "multicore", workers = workers)
  } else {
    list(strategy = "multisession", workers = workers)
  }
}
```

```r
pin_native_threads_to_one <- function(verbose = FALSE) {
  Sys.setenv(
    OMP_NUM_THREADS         = "1",
    OPENBLAS_NUM_THREADS    = "1",
    MKL_NUM_THREADS         = "1",
    GOTO_NUM_THREADS        = "1",
    GDAL_NUM_THREADS        = "1",
    R_DATATABLE_NUM_THREADS = "1"
  )
  if (requireNamespace("RhpcBLASctl", quietly = TRUE)) {
    try(RhpcBLASctl::blas_set_num_threads(1L), silent = !verbose)
    try(RhpcBLASctl::omp_set_num_threads(1L), silent = !verbose)
  }
  if (requireNamespace("data.table", quietly = TRUE)) data.table::setDTthreads(1L)
  if (requireNamespace("arrow", quietly = TRUE)) {
    try(arrow::set_cpu_count(1L), silent = !verbose)
    try(arrow::set_io_thread_count(1L), silent = !verbose)
  }
  invisible(NULL)
}
```

```r
if (isTRUE(as.logical(Sys.getenv("ALLOCATION_DEV_STRICT_GLOBALS", "FALSE")))) {
  options(future.globals.onReference = "error")
  cat("DEV MODE: future.globals.onReference = 'error' enabled\n")
}
```

From `scripts/run_allocation.r` current startup contract:

```r
preflight_exit <- run_preflight_and_print(config = config)
if (preflight_exit != 0L) {
  quit(status = preflight_exit)
}

num_workers <- as.integer(Sys.getenv("ALLOCATION_NUM_WORKERS", unset = "4"))
cat(sprintf("Setting up parallel processing with %d workers\n", num_workers))
future::plan(future::multisession, workers = num_workers)
```

Replace this with the ordered sequence from `03-PATTERNS.md`: pre-flight -> pin_native_threads_to_one() -> strict-globals gate -> select_allocation_plan() -> future::plan().

Current smoke hook already present:

```r
profile_scenario <- Sys.getenv("ALLOCATION_PROFILE_SCENARIO", unset = "")
if (nzchar(profile_scenario)) {
  config[["scenario_names"]] <- profile_scenario
}
```

Extend this same pattern in `src/allocation.r` to add `ALLOCATION_REGION_FILTER` and `ALLOCATION_YEAR_POST_FILTER` without changing the default loop behavior when the env vars are unset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing runtime-contract tests for backend selection, thread pinning, strict-globals, and smoke filters</name>
  <files>tests/testthat/test-allocation-runtime-contract.R</files>
  <behavior>
    - Test 1: `src/allocation.r` defines `select_allocation_plan()` and contains both `"multicore"` and `"multisession"` strategy strings
    - Test 2: `scripts/run_allocation.r` calls `pin_native_threads_to_one` before the first `future::plan(` occurrence
    - Test 3: `scripts/run_allocation.r` enables `options(future.globals.onReference = "error")` only when `ALLOCATION_DEV_STRICT_GLOBALS` is true
    - Test 4: `src/allocation.r` references `ALLOCATION_REGION_FILTER` and `ALLOCATION_YEAR_POST_FILTER`
    - Test 5: `src/allocation.r` defines `prof_cgroup_snapshot()` and logs `memory.current` plus `memory.max`
  </behavior>
  <action>
Create `tests/testthat/test-allocation-runtime-contract.R` using the same repo-root resolver and text-read pattern established in the Phase 2 test files.

Write grep-style tests that intentionally fail against the current code:

1. Assert `src/allocation.r` contains `select_allocation_plan <- function()` and both `"multicore"` and `"multisession"`.
2. Read `scripts/run_allocation.r` as text, compute byte offsets for `pin_native_threads_to_one` and `future::plan(`, and assert the pin call appears first.
3. Assert `scripts/run_allocation.r` contains both `ALLOCATION_DEV_STRICT_GLOBALS` and `future.globals.onReference = "error"`.
4. Assert `src/allocation.r` contains `ALLOCATION_REGION_FILTER` and `ALLOCATION_YEAR_POST_FILTER`.
5. Assert `src/allocation.r` contains `prof_cgroup_snapshot <- function()` plus both `memory.current` and `memory.max`.

Use exact env-var names from this plan. The RED state is required: none of these hooks exist yet except the older `ALLOCATION_PROFILE_SCENARIO` path.
  </action>
  <verify>
    <automated>rg -n "select_allocation_plan|ALLOCATION_DEV_STRICT_GLOBALS|ALLOCATION_REGION_FILTER|ALLOCATION_YEAR_POST_FILTER|prof_cgroup_snapshot" tests/testthat/test-allocation-runtime-contract.R</automated>
  </verify>
  <done>`tests/testthat/test-allocation-runtime-contract.R` exists with five failing contract tests covering backend selection, startup ordering, strict-globals, smoke filters, and cgroup logging</done>
</task>

<task type="auto">
  <name>Task 2: Implement the runtime control plane in allocation.r and run_allocation.r</name>
  <files>src/allocation.r, scripts/run_allocation.r</files>
  <action>
Implement the Phase 3 runtime baseline from `03-RESEARCH.md` and `03-PATTERNS.md` exactly:

In `src/allocation.r`:
- Add `select_allocation_plan()`, `pin_native_threads_to_one()`, and `prof_cgroup_snapshot()` near the existing profiling/pre-flight helpers.
- Add `parallelly` to the `packages_expected` list inside `validate_allocation_runtime()` so the pre-flight explicitly checks the selector dependency.
- Add a small helper that applies smoke-run filters from env vars without mutating default behavior when they are unset:
  - `ALLOCATION_PROFILE_SCENARIO` keeps its existing semantics
  - `ALLOCATION_REGION_FILTER` restricts `region_names`
  - `ALLOCATION_YEAR_POST_FILTER` restricts the timestep loop to a single posterior year
- Emit explicit log lines for the chosen filters and for the cgroup snapshot before worker launch.

In `scripts/run_allocation.r`:
- Replace the fixed `future::plan(future::multisession, ...)` block with the ordered startup sequence from research:
  1. `run_preflight_and_print(config)`
  2. `pin_native_threads_to_one(verbose = TRUE)`
  3. strict-globals gate behind `ALLOCATION_DEV_STRICT_GLOBALS`
  4. `plan_choice <- select_allocation_plan()`
  5. `future::plan(future::sequential|multicore|multisession, workers = plan_choice$workers)`
- Set `options(parallelly.fork.enable = TRUE)` only for the multicore branch.
- Log the chosen strategy and worker count to stdout in the form `Parallel: strategy=<...> workers=<...>`.
- Keep the existing `future::plan(future::sequential)` cleanup at script exit.

Do not change the default scenario list, region list, or timestep list unless the corresponding env var is set. Do not yet preload models or neighbourhood rasters in this plan; that belongs to 03-02.
  </action>
  <verify>
    <automated>rg -n "select_allocation_plan|pin_native_threads_to_one|prof_cgroup_snapshot|ALLOCATION_REGION_FILTER|ALLOCATION_YEAR_POST_FILTER|future\\.globals\\.onReference = \"error\"|Parallel: strategy=" src/allocation.r scripts/run_allocation.r</automated>
  </verify>
  <done>`src/allocation.r` contains the runtime helpers and smoke filters; `scripts/run_allocation.r` pins threads, optionally enables strict-globals, chooses the future backend automatically, and logs the chosen strategy before any future launches</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment variables -> allocation runtime | Operator-provided env vars control backend selection, worker count, and smoke-run narrowing |
| R parent process -> SLURM/cgroup files | `prof_cgroup_snapshot()` reads kernel-exposed memory counters to log resource limits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `ALLOCATION_PARALLEL_STRATEGY`, `ALLOCATION_NUM_WORKERS` | accept | Env vars are operator-controlled on local/HPC runs; logs must record the selected strategy and worker count so overrides are visible |
| T-03-02 | Denial of Service | native thread libraries | mitigate | Pin BLAS, OMP, data.table, arrow, and GDAL threads to 1 before `future::plan()` to prevent hidden nested parallelism |
| T-03-03 | Tampering | smoke-run filter env vars | mitigate | Filters only narrow pre-existing scenario/region/year sets; reject unknown scenario or year values with a fail-fast `stop()` rather than silently widening the run |
</threat_model>

<verification>
Run the new contract tests and text checks:

```bash
Rscript -e "testthat::test_file('tests/testthat/test-allocation-runtime-contract.R')"
rg -n "pin_native_threads_to_one|future::plan" scripts/run_allocation.r
rg -n "select_allocation_plan|prof_cgroup_snapshot|ALLOCATION_REGION_FILTER|ALLOCATION_YEAR_POST_FILTER" src/allocation.r
```

Expected:
- the test file passes after Task 2
- `pin_native_threads_to_one` appears above the first `future::plan(` in `scripts/run_allocation.r`
- all four runtime hooks exist in `src/allocation.r`
</verification>

<success_criteria>
- `future::multicore` is selected automatically on Linux/HPC and `future::multisession` on Windows/local
- thread pinning happens before any `future::plan()` call
- `ALLOCATION_DEV_STRICT_GLOBALS=TRUE` enables `future.globals.onReference = "error"` and logs that dev mode is active
- `ALLOCATION_REGION_FILTER` and `ALLOCATION_YEAR_POST_FILTER` exist as no-default-op smoke-run filters
- `prof_cgroup_snapshot()` exists and logs both current and max memory counters
- the runtime contract tests are green
</success_criteria>

<output>
After completion, create `.planning/phases/03-parallelism-memory-architecture/03-01-SUMMARY.md`
</output>
