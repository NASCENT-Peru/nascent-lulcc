---
phase: "03.4"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PIPE-08, PIPE-09]

must_haves:
  truths:
    - "HPC git tree is at commit >= 9166fc4 (Phase 3.2 Plan 01) before any SLURM job runs"
    - "sim-trans-rates-prep SLURM job completes exit 0 with AUDIT stage=4 forbidden_excluded > 0 for all 4 regions"
    - "id_trans=34 is absent from all 4 regenerated BAU trans_rates CSVs (2022 timestep)"
    - "calibrate-allocation-params SLURM job completes exit 0 with alloc_params covering all active id_trans values"
    - "Allocation smoke run for all 4 regions completes without ALLOC-08 stop"
  artifacts:
    - path: "outputs/transition_tables/simulation-lulc-areas-scalar-9.0x/BAU/andes/BAU-andes-trans_rates-2022.csv"
      provides: "Regenerated trans_rates CSV for andes — id_trans=34 absent"
    - path: "outputs/transition_tables/simulation-lulc-areas-scalar-9.0x/BAU/cuenca_del_amazonas/BAU-cuenca_del_amazonas-trans_rates-2022.csv"
      provides: "Regenerated trans_rates CSV for cuenca_del_amazonas — id_trans=34 absent"
    - path: "outputs/transition_tables/simulation-lulc-areas-scalar-9.0x/BAU/costa_peruana/BAU-costa_peruana-trans_rates-2022.csv"
      provides: "Regenerated trans_rates CSV for costa_peruana — id_trans=34 absent"
    - path: "outputs/transition_tables/simulation-lulc-areas-scalar-9.0x/BAU/selva_andina/BAU-selva_andina-trans_rates-2022.csv"
      provides: "Regenerated trans_rates CSV for selva_andina — id_trans=34 absent"
  key_links:
    - from: "config/hpc_config.yaml (forbidden_from_classes: [mining])"
      to: "src/simulation_trans_rates_prep.r forbid_pairs_df"
      via: "sim_config[[forbidden_from_classes]] read at lines 246-263"
      pattern: "forbidden_excluded > 0 in AUDIT stage=4 log"
    - from: "outputs/transition_tables (regenerated CSVs)"
      to: "src/allocation.r ALLOC-08 hard stop"
      via: "active id_trans set vs alloc_params coverage check"
      pattern: "no ALLOC-08 stop() in allocation SLURM log"
---

<objective>
Regenerate simulation trans_rates CSVs and alloc_params on Euler HPC using Phase 3.2-corrected
code, then verify the ALLOC-08 hard stop no longer fires during an allocation smoke run.

Purpose: The stale trans_rates CSVs on HPC were produced before Phase 3.2 Plan 01 commit
9166fc4 added the `forbidden_from_classes` config key and before the reconciliation RDS was
available. As a result id_trans=34 (mining -> high_intensity_agricultural) survived into the
active transition set, triggering allocation.r's ALLOC-08 hard stop. No source-code changes are
needed — both exclusion mechanisms (Mechanism A: config-driven mining prohibition; Mechanism B:
load_unmodelled_transitions reconciliation) are correct in the committed code. The fix is
entirely a data re-run.

Output: Clean trans_rates CSVs (id_trans=34 absent), clean alloc_params, allocation smoke exits
cleanly for all four Peruvian regions (andes, cuenca_del_amazonas, costa_peruana, selva_andina),
and the Phase 3.3 Wave 4 operator gate is unblocked.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-model-manifest-pipeline-rerun/03.4-RESEARCH.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md
</context>

<tasks>

<!-- ================================================================
     TASK 1 — Pre-flight: confirm HPC git tree and prerequisites
     (PIPE-09: explicit git pull + git status before any SLURM job)
     ================================================================ -->
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator pre-flight on Euler — confirm git tree and prerequisites</name>

  <read_first>
    .planning/phases/03.4-model-manifest-pipeline-rerun/03.4-RESEARCH.md (Answer 3, Pitfalls 2 and 4, Environment Availability section)
    config/hpc_config.yaml (lines 99-103 — forbidden_from_classes key)
  </read_first>

  <action>
Log into Euler HPC and run ALL of the following checks before submitting any SLURM job.
Do not proceed to Task 2 unless every check passes.

Step 1 — Confirm git tree is current (PIPE-09):

  cd $SLURM_SUBMIT_DIR   # or: cd to your HPC project root
  git log --oneline -3

Expected: the first line shows commit 9166fc4 or a later commit on the allocation-step branch.
If the commit shown predates 9166fc4, run:
  git fetch origin
  git checkout allocation-step
  git pull origin allocation-step
Then repeat git log to confirm.

Step 2 — Confirm forbidden_from_classes key is present in hpc_config.yaml:

  grep "forbidden_from_classes" config/hpc_config.yaml

Expected output:
  forbidden_from_classes: ["mining"]

If this line is missing, the git pull in Step 1 did not succeed — do not proceed.

Step 3 — Confirm conda environments exist:

  conda env list | grep -E "trans_rate_estimation_env|allocation_params_env|allocation_env"

Expected: all three environment names appear in the list.
If an environment is missing, rebuild it with the corresponding env spec in envs/.

Step 4 — Confirm reconciliation RDS exists (Pitfall 4 guard):

  # Substitute the actual period (e.g. 2020) in the path below
  ls $HPC_SCRATCH_ROOT/outputs/transition_model_eval/*/transition_modelling_reconciliation_*.rds

Expected: at least one .rds file found for the last data period.
If no RDS found, transition_modelling() must be re-run before the trans_rates job can succeed
(load_unmodelled_transitions() will stop() with a clear error if the RDS is absent).

Step 5 — Confirm logs/ directory exists and is writable:

  ls -ld logs/
  touch logs/.preflight_check && rm logs/.preflight_check && echo "logs/ writable"
  </action>

  <acceptance_criteria>
    - git log --oneline -1 on Euler shows commit hash 9166fc4 or later on branch allocation-step
    - grep "forbidden_from_classes" config/hpc_config.yaml returns exactly: forbidden_from_classes: ["mining"]
    - conda env list shows trans_rate_estimation_env, allocation_params_env, and allocation_env
    - ls $HPC_SCRATCH_ROOT/outputs/transition_model_eval/ finds at least one transition_modelling_reconciliation_*.rds file
    - logs/ directory exists and is writable
  </acceptance_criteria>

  <verify>
    All five steps above return expected output with no errors.
  </verify>

  <done>
    All pre-flight checks pass. Operator is ready to submit SLURM jobs.
    Type "pre-flight OK" to continue to Task 2.
  </done>
</task>


<!-- ================================================================
     TASK 2 — Submit sim-trans-rates job, wait, verify audit output
     (PIPE-08: CSVs regenerated with current code)
     ================================================================ -->
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit simulation trans_rates estimation job and verify AUDIT output</name>

  <read_first>
    scripts/submit_simulation_trans_rates_estimation.sh (full file — SLURM parameters)
    .planning/phases/03.4-model-manifest-pipeline-rerun/03.4-RESEARCH.md (Answer 3 and Answer 5, sections 5a-5c)
  </read_first>

  <action>
Step 1 — Submit the job:

  cd $SLURM_SUBMIT_DIR
  sbatch scripts/submit_simulation_trans_rates_estimation.sh

Record the SLURM job ID printed (e.g. "Submitted batch job 412345").
JOBID=412345   # substitute your actual job ID

Step 2 — Monitor until completion (wall time: 4 hours max):

  watch -n 60 squeue -j $JOBID

When squeue shows the job is no longer listed (completed), continue.
Check exit code:
  sacct -j $JOBID --format=JobID,State,ExitCode | grep -v "^[[:space:]]*$"

Expected: State=COMPLETED, ExitCode=0:0

Step 3 — Confirm AUDIT stage=4 lines show forbidden_excluded > 0 for all 4 regions:

  grep "AUDIT stage=4" logs/sim-trans-rates-prep-${JOBID}.out | \
    grep -E "region=(andes|cuenca_del_amazonas|costa_peruana|selva_andina)"

Expected output format (one line per region per scenario per timestep):
  AUDIT stage=4 scenario=BAU region=andes timestep=2022 viable_input=N forbidden_excluded=K rate_csv_rows=M

CRITICAL: K (forbidden_excluded) must be > 0 for every region × scenario row.
If any row shows forbidden_excluded=0, the forbidden_from_classes YAML key was not
picked up — stop and re-check the git pull from Task 1.

Step 4 — Confirm id_trans=34 is absent from regenerated BAU CSVs (all 4 regions):

  TRANS_DIR="$HPC_SCRATCH_ROOT/outputs/transition_tables/simulation-lulc-areas-scalar-9.0x/BAU"
  for region in andes cuenca_del_amazonas costa_peruana selva_andina; do
    echo "=== $region ==="
    csv="$TRANS_DIR/$region/BAU-${region}-trans_rates-2022.csv"
    if [ -f "$csv" ]; then
      grep ",34$\|,34," "$csv" | head -5 || echo "  id_trans=34 NOT FOUND (correct)"
    else
      echo "  CSV not found: $csv"
    fi
  done

NOTE — root cause history (corrected in this phase): id_trans=34 is
106 (mining) → 104 (high_intensity_agricultural_areas), i.e. a transition FROM
mining — exactly what `forbidden_from_classes: mining` targets. Row absence
depends on TWO independent things, not one:
  (a) the constraint being applied — confirmed by forbidden_excluded > 0 in
      Step 3; this drives the mining→* flow to ~0 via the x_t == 0 constraint; AND
  (b) the Stage-4 CSV writer physically DROPPING hard-forbidden rows
      (src/simulation_trans_rates_prep.r, the mats$hard_forbid filter before write).
Before this phase the writer kept the row, so id_trans=34 leaked into the CSV at
a solver residual (~1e-11) EVEN THOUGH (a) was satisfied. That was a writer gap,
not a config/git-pull failure. The writer-drop fix lands via Task 1's code pull.

Expected: all four regions print "id_trans=34 NOT FOUND (correct)".
If any region finds a match WHILE Step 3 shows forbidden_excluded > 0: the
constraint applied but the writer-drop fix did not land — re-check the Task 1
git pull (src/simulation_trans_rates_prep.r must drop mats$hard_forbid rows
before write). Capture the AUDIT log and open a new issue before proceeding.

Step 5 — Confirm rate_csv_rows < viable_input (exclusion occurred):

  grep "AUDIT stage=4" logs/sim-trans-rates-prep-${JOBID}.out | \
    awk '{for(i=1;i<=NF;i++){if($i~/viable_input=/){vi=substr($i,index($i,"=")+1)};if($i~/rate_csv_rows=/){rc=substr($i,index($i,"=")+1)}}; if(vi+0>rc+0) print "OK excluded: viable="vi" csv="rc; else print "WARN no exclusion: viable="vi" csv="rc}' | head -20

Expected: every line starts with "OK excluded".

NOTE: viable_input > rate_csv_rows is a NECESSARY but not sufficient signal.
Persistence rows (From*==To*) and historical transitions with no id_trans are
always dropped, so this gap was nonzero even before the writer-drop fix and is
NOT by itself proof that forbidden rows were removed. With the fix in place the
gap additionally absorbs the forbidden_excluded count; the authoritative
forbidden-row check is Step 4 (id_trans=34 absent), not this magnitude test.
  </action>

  <acceptance_criteria>
    - sacct shows State=COMPLETED ExitCode=0:0 for the sim-trans-rates-prep job
    - grep "AUDIT stage=4" output contains at least one line per region (andes, cuenca_del_amazonas, costa_peruana, selva_andina) with forbidden_excluded > 0
    - For all 4 regions: grep ",34$\|,34," BAU-<region>-trans_rates-2022.csv returns zero matches
    - AUDIT lines show rate_csv_rows < viable_input for every region (exclusions took effect)
    - All 4 CSV files exist under outputs/transition_tables/simulation-lulc-areas-scalar-9.0x/BAU/
  </acceptance_criteria>

  <verify>
    All acceptance criteria above confirmed by operator. Record JOBID for audit trail.
  </verify>

  <done>
    id_trans=34 absent from all 4 regenerated CSVs. forbidden_excluded > 0 in all AUDIT lines.
    Type "trans_rates OK JOBID=&lt;id&gt;" to continue to Task 3.
  </done>
</task>


<!-- ================================================================
     TASK 3 — Submit alloc_params calibration (highmem, 4 CPUs × 28G = 112GB)
     Memory constraint: compute nodes cap at 95000 MB; the script routes
     to the highmem partition (192GB/node) so the 112GB default fits.
     ================================================================ -->
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Submit alloc_params calibration job (highmem, 4 CPUs x 28G) and verify coverage</name>

  <read_first>
    scripts/submit_calibrate_allocation_parameters.sh (full file — default SLURM params)
    .planning/phases/03.4-model-manifest-pipeline-rerun/03.4-RESEARCH.md (Answer 4 "Why alloc_params must be re-run", Answer 5 section 5d, Pitfall 1)
  </read_first>

  <action>
IMPORTANT: Do NOT pass a memory override. The default request (4 CPUs × 28G = 112GB)
exceeds the 95000 MB (95GB) cap on compute nodes, so the script routes to the highmem
partition (192GB/node) via its #SBATCH --partition=highmem directive. The 112GB default
fits with headroom there. (Earlier guidance to override to 2 CPUs × 64G = 128GB assumed a
non-existent 128GB compute tier — that override would fail on this cluster.)

Step 1 — Submit the calibration job:

  cd $SLURM_SUBMIT_DIR
  sbatch scripts/submit_calibrate_allocation_parameters.sh

Record the SLURM job ID.
ALLOC_PARAMS_JOBID=412346   # substitute your actual job ID

Step 2 — Monitor until completion (wall time: 6 hours max):

  watch -n 60 squeue -j $ALLOC_PARAMS_JOBID

When finished:
  sacct -j $ALLOC_PARAMS_JOBID --format=JobID,State,ExitCode | grep -v "^[[:space:]]*$"

Expected: State=COMPLETED, ExitCode=0:0

Step 3 — Spot-check alloc_params coverage for all 4 regions using Rscript:

Run once per region, substituting the region name. The script checks that every active
id_trans in the BAU scalar-9.0 trans_rates CSV has a matching row in alloc_params.

  cd $SLURM_SUBMIT_DIR
  for region in andes cuenca_del_amazonas costa_peruana selva_andina; do
    echo "=== Checking $region ==="
    Rscript --vanilla -e "
      source('src/setup.r'); source('src/utils.r')
      config <- get_config()
      period <- dplyr::last(config[['data_periods']])
      alloc_file <- file.path(config[['calibration_param_dir']], '${region}',
                              paste0(period, '_alloc_params.csv'))
      trans_file <- file.path(config[['trans_rate_table_dir']],
                              'simulation-lulc-areas-scalar-9.0x', 'BAU', '${region}',
                              paste0('BAU-${region}-trans_rates-2022.csv'))
      if (!file.exists(alloc_file)) stop('alloc_params not found: ', alloc_file)
      if (!file.exists(trans_file)) stop('trans_rates not found: ', trans_file)
      alloc <- read.csv(alloc_file)
      trans  <- read.csv(trans_file)
      active <- unique(trans\$id_trans[!is.na(trans\$id_trans)])
      missing <- setdiff(active, alloc\$id_trans)
      if (length(missing) == 0) {
        cat('OK: alloc_params covers all', length(active), 'active id_trans for ${region}\n')
      } else {
        cat('MISSING in ${region}:', paste(missing, collapse=', '), '\n')
        quit(status=1)
      }
    "
  done

Expected: all four regions print "OK: alloc_params covers all N active id_trans for <region>".
If any region prints MISSING, the alloc_params job did not produce clean output — investigate
the calibrate-allocation-params SLURM log before proceeding to Task 4.
  </action>

  <acceptance_criteria>
    - Job runs on the highmem partition at the script default (4 CPUs × 28G = 112GB — within the 192GB highmem node limit)
    - sacct shows State=COMPLETED ExitCode=0:0 for the calibrate-allocation-params job
    - Rscript spot-check for each of the 4 regions (andes, cuenca_del_amazonas, costa_peruana, selva_andina) prints "OK: alloc_params covers all N active id_trans for <region>"
    - No region prints MISSING id_trans values
  </acceptance_criteria>

  <verify>
    All 4 regions pass the Rscript coverage check. SLURM job exited 0.
  </verify>

  <done>
    alloc_params covers all active id_trans for all 4 regions. No ALLOC-08 triggers expected.
    Type "alloc_params OK JOBID=&lt;id&gt;" to continue to Task 4.
  </done>
</task>


<!-- ================================================================
     TASK 4 — Allocation smoke for all 4 regions; confirm ALLOC-08 gone
     ================================================================ -->
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run allocation smoke for all 4 regions and confirm ALLOC-08 stop no longer fires</name>

  <read_first>
    scripts/submit_allocation_smoke.sh (full file — env vars and SLURM params)
    .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md (region label list)
    .planning/phases/03.4-model-manifest-pipeline-rerun/03.4-RESEARCH.md (Answer 4 step 5, Pitfall 3 — scalar directory mismatch)
  </read_first>

  <action>
Submit one allocation smoke job per region, using ALLOCATION_REGION_FILTER to scope each run.
The smoke script defaults to ALLOCATION_PROFILE_SCENARIO=BAU and computes the first simulation
timestep automatically from config. Run all 4 in parallel (stagger by ~30s to avoid log collisions).

Step 1 — Pre-check: confirm selected_scalar matches a directory produced in Task 2:

  ls $HPC_SCRATCH_ROOT/outputs/transition_tables/ | grep "scalar-9.0x"

Expected: simulation-lulc-areas-scalar-9.0x appears in the listing.
If it does not, check config[["selected_scalar"]] in hpc_config.yaml and ensure it matches
a scalar value produced by the trans_rates job. If the directory is named differently
(e.g. scalar-9x vs scalar-9.0x), open an issue — do not proceed to the job submissions.

Step 2 — Submit allocation smoke for all 4 regions:

  cd $SLURM_SUBMIT_DIR

  SMOKE_JOB_ANDES=$(sbatch \
    --job-name=alloc-smoke-andes \
    --export=ALL,ALLOCATION_REGION_FILTER=andes,ALLOCATION_PROFILE_SCENARIO=BAU \
    scripts/submit_allocation_smoke.sh \
    | awk '{print $NF}')
  echo "andes smoke job: $SMOKE_JOB_ANDES"
  sleep 30

  SMOKE_JOB_AMAZON=$(sbatch \
    --job-name=alloc-smoke-amazon \
    --export=ALL,ALLOCATION_REGION_FILTER=cuenca_del_amazonas,ALLOCATION_PROFILE_SCENARIO=BAU \
    scripts/submit_allocation_smoke.sh \
    | awk '{print $NF}')
  echo "cuenca_del_amazonas smoke job: $SMOKE_JOB_AMAZON"
  sleep 30

  SMOKE_JOB_COSTA=$(sbatch \
    --job-name=alloc-smoke-costa \
    --export=ALL,ALLOCATION_REGION_FILTER=costa_peruana,ALLOCATION_PROFILE_SCENARIO=BAU \
    scripts/submit_allocation_smoke.sh \
    | awk '{print $NF}')
  echo "costa_peruana smoke job: $SMOKE_JOB_COSTA"
  sleep 30

  SMOKE_JOB_SELVA=$(sbatch \
    --job-name=alloc-smoke-selva \
    --export=ALL,ALLOCATION_REGION_FILTER=selva_andina,ALLOCATION_PROFILE_SCENARIO=BAU \
    scripts/submit_allocation_smoke.sh \
    | awk '{print $NF}')
  echo "selva_andina smoke job: $SMOKE_JOB_SELVA"

Step 3 — Monitor all 4 jobs until completion (wall time: 12 hours max each):

  watch -n 120 squeue -j ${SMOKE_JOB_ANDES},${SMOKE_JOB_AMAZON},${SMOKE_JOB_COSTA},${SMOKE_JOB_SELVA}

Step 4 — Check exit codes for all 4 jobs:

  for jid in $SMOKE_JOB_ANDES $SMOKE_JOB_AMAZON $SMOKE_JOB_COSTA $SMOKE_JOB_SELVA; do
    sacct -j $jid --format=JobID,JobName,State,ExitCode | tail -n +3
  done

Expected: all 4 jobs show State=COMPLETED ExitCode=0:0.

Step 5 — Confirm ALLOC-08 stop did not fire in any log:

  for jid in $SMOKE_JOB_ANDES $SMOKE_JOB_AMAZON $SMOKE_JOB_COSTA $SMOKE_JOB_SELVA; do
    logfile=$(ls logs/lulc-allocation-smoke-${jid}.out 2>/dev/null | head -1)
    if [ -z "$logfile" ]; then
      echo "WARNING: log for job $jid not found"
      continue
    fi
    echo "=== Job $jid ==="
    if grep -q "ALLOC-08" "$logfile"; then
      echo "  ALLOC-08 STOP FOUND — id_trans coverage gap still present"
      grep "ALLOC-08" "$logfile" | head -5
    else
      echo "  OK: no ALLOC-08 stop in $logfile"
    fi
  done

Expected: all 4 logs print "OK: no ALLOC-08 stop".

Step 6 — Optional: confirm posterior.tif exists for each region (smoke ran Dinamica):

  for region in andes cuenca_del_amazonas costa_peruana selva_andina; do
    pdir="$HPC_SCRATCH_ROOT/outputs/simulations/BAU"
    # The exact year-post subpath depends on your config simulation_start_year + step_length
    # Typically 2026 for start=2022 + step=4
    find "$pdir" -path "*${region}*" -name "posterior.tif" 2>/dev/null | head -2 \
      || echo "  WARNING: no posterior.tif found for $region"
  done
  </action>

  <acceptance_criteria>
    - 4 sbatch commands submitted, one per region: andes, cuenca_del_amazonas, costa_peruana, selva_andina
    - sacct shows State=COMPLETED ExitCode=0:0 for all 4 smoke jobs
    - grep "ALLOC-08" across all 4 SLURM .out logs returns zero matches
    - No "Error in stop()" line containing "missing alloc_params" appears in any log
    - posterior.tif exists for at least one region (confirms Dinamica ran, not just pre-flight)
  </acceptance_criteria>

  <verify>
    All 4 smoke jobs exit 0. Zero ALLOC-08 occurrences across all logs. Phase 3.3 operator gate unblocked.
  </verify>

  <done>
    All 4 regions complete allocation smoke with no ALLOC-08 stop. Phase 3.4 complete.
    Phase 3.3 Wave 4 operator gate (03.3-05-PLAN.md) is now unblocked — proceed to execute that plan.
    Type "smoke OK all-4-regions" to complete this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> HPC SLURM | Operator submits jobs from login node; SLURM executes on compute nodes with project scratch |
| R scripts -> HPC scratch filesystem | R reads config YAML, sources RDS artifacts, and writes CSV outputs to HPC_SCRATCH_ROOT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-01 | Tampering | HPC git working tree | mitigate | Task 1 Step 1 enforces git log check; operator must confirm commit >= 9166fc4 before any sbatch |
| T-03.4-02 | Information Disclosure | Stale CSV artifact | mitigate | Task 2 acceptance criteria require forbidden_excluded > 0 in AUDIT log AND grep confirms id_trans=34 absent |
| T-03.4-03 | Elevation of Privilege | Silent config degradation (null-guard on forbidden_from_classes) | mitigate | Task 2 AUDIT stage=4 check: forbidden_excluded=0 is a hard stop signal; operator must not proceed if K=0 |
| T-03.4-04 | Denial of Service | Reconciliation RDS absent on HPC | accept | Task 1 Step 4 checks for the RDS; load_unmodelled_transitions() stop() surfaces a clear error if absent; low probability given recent modelling run |
| T-03.4-05 | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; all conda envs are pre-existing |
</threat_model>

<verification>
Full phase verification (after all 4 tasks complete):

1. git log --oneline -1 on Euler shows commit >= 9166fc4
2. grep "AUDIT stage=4.*forbidden_excluded=0" logs/sim-trans-rates-prep-*.out returns zero matches
   (every audit line has forbidden_excluded > 0)
3. For all 4 regions, grep ",34$\|,34," BAU-<region>-trans_rates-2022.csv returns zero matches
4. Rscript coverage check for all 4 regions prints "OK: alloc_params covers all N active id_trans"
5. grep "ALLOC-08" logs/lulc-allocation-smoke-*.out returns zero matches
</verification>

<success_criteria>
Phase 3.4 is complete when:
- HPC working tree confirmed at commit >= 9166fc4 before any job submission (PIPE-09)
- sim-trans-rates-prep SLURM job exits 0 with forbidden_excluded > 0 in all AUDIT stage=4 lines
- id_trans=34 absent from BAU-<region>-trans_rates-2022.csv for all 4 regions:
  andes, cuenca_del_amazonas, costa_peruana, selva_andina
- calibrate-allocation-params job exits 0 with alloc_params coverage verified clean for all 4 regions
- Allocation smoke exits 0 for all 4 regions with zero ALLOC-08 occurrences
- Phase 3.3 Wave 4 operator gate (03.3-05-PLAN.md) is confirmed unblocked
</success_criteria>

<output>
Create `.planning/phases/03.4-model-manifest-pipeline-rerun/03.4-01-SUMMARY.md` when done.
Record:
- Task 1: pre-flight check results (git commit hash confirmed on Euler, conda envs present, reconciliation RDS path)
- Task 2: JOBID for sim-trans-rates-prep, forbidden_excluded values per region, confirmation that id_trans=34 is absent
- Task 3: JOBID for calibrate-allocation-params, alloc_params coverage result per region
- Task 4: JOBIDs for all 4 smoke runs, final ALLOC-08 status, posterior.tif presence
</output>
