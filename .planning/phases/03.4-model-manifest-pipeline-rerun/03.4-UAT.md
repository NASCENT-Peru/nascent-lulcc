---
status: resolved
phase: 03.4-model-manifest-pipeline-rerun
source: [03.4-01-PLAN.md]
started: 2026-06-23T07:11:27Z
updated: 2026-06-24
---

## Current Test

[resolved 2026-06-24 — the int32-cube fix was confirmed by the andes full-pipeline run
(jobs 573721/573776): Dinamica completed (DINAMICA_EXIT status=0), posterior.tif produced,
no AFS_functor assertion. See .planning/phases/03.5-.../03.5-VALIDATION.md.]

## Tests

### 1. Operator pre-flight on Euler (git tree, config, envs, reconciliation RDS, logs writable)
expected: All five pre-flight checks pass; HPC tree >= 9166fc4 on allocation-step
result: pass

### 2. sim-trans-rates job — AUDIT forbidden_excluded > 0, id_trans=34 absent (all 4 regions)
expected: Job exits 0; forbidden_excluded > 0 per region; id_trans=34 absent from regenerated BAU CSVs
result: pass

### 3. alloc_params calibration — coverage of all active id_trans (all 4 regions)
expected: Job exits 0; alloc_params covers every active id_trans for all 4 regions
result: pass

### 4. Allocation smoke (cuenca_del_amazonas, costa_peruana, selva_andina) — no ALLOC-08, posterior.tif produced
expected: Smoke runs past ALLOC-08, generates probability maps, and Dinamica allocation produces posterior.tif
result: pass
reported: "Initially failed: Expander crashed with Assertion Failed AFS_functor !probabilities.empty() (float32 cube). RESOLVED 2026-06-24 — int32 cube cast applied; andes full run (573721/573776) completed Dinamica, posterior.tif produced, no assertion."
severity: resolved

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Dinamica Expander/Patcher allocate transitions from the probability-map cube and produce posterior.tif"
  status: resolved
  resolved_by: "int32 cube cast (CreateCubeOfProbabilityMaps.ego) confirmed by andes full run 573721/573776 — posterior.tif produced, no AFS_functor assertion (2026-06-24)"
  reason: "User reported: Expander aborts with Assertion Failed (AFS_functor, !probabilities.empty()). ALLOC-08 did not fire; all 24 probability maps + cube built successfully (1735s); failure is in the AllocateTransitions submodel's Expander, not the cube loop (the assertion text interleaved at cube element 22 is buffered output)."
  severity: blocker
  test: 4
  root_cause: |
    Expander/Patcher derive transition probability RANGES from the probability map's
    cell type and require a 32-bit signed integer (confirmed by Hermann Rodrigues, CSR/UFMG:
    map 0.0 -> 0, 1.0 -> 2147483647). The probability cube fed to the Expander was float32:
    (a) src/allocation.r generate_probability_maps() writes probability_map_dir/*.tif from a
        double-backed raster with no datatype arg -> terra default FLT4S (32-bit float), 0.0-1.0; and
    (b) CreateCubeOfProbabilityMaps.ego recast every layer to cellType = .float32 via CalculateMap,
        so the cube was float32 regardless of input TIF type; and
    (c) AllocateTransitions.ego passes that cube straight into Expander/Patcher with no rescale.
    Float32 cube -> probability ranges cannot be derived -> probabilities vector empty -> assertion.
    Matches the intermittent-success reports for the same assertion (float maps sometimes work by luck).
  artifacts:
    - path: "dinamica/dinamica_model/evoland_ego_Submodels/CreateCubeOfProbabilityMaps.ego-decoded"
      issue: "Two CalculateMap casts used cellType = .float32 (expression [i1]); cube delivered float32 to Expander"
    - path: "src/allocation.r"
      issue: "generate_probability_maps() writes FLT4S probability TIFs (no datatype); float intermediate (not the proximate cause once cube cast is int32, but the cube cast is)"
  missing:
    - "CreateCubeOfProbabilityMaps.ego: change both CalculateMap casts to cellType = .int32 with expression round(i1 * 2147483647) so the cube is a 32-bit signed integer scaled 0.0->0 / 1.0->2147483647"
  fix_applied: |
    Edited dinamica/dinamica_model/evoland_ego_Submodels/CreateCubeOfProbabilityMaps.ego-decoded:
    convertToDouble and convertToDouble0 CalculateMap functors -> cellType = .int32,
    expression = [ round(i1 * 2147483647) ]. (.int32 is a valid token already used in
    CalcSimilarityOfDifferences.ego.) The repo .ego-decoded is staged/encoded into the run
    work_dir by resolve_dinamica_launch() (src/dinamica_utils.r:792-827) with overwrite=TRUE,
    so the next smoke run picks it up with no further deployment step.
  verification_pending: "Re-run scripts/submit_allocation_smoke.sh for the 3 regions; expect no AFS_functor assertion and posterior.tif produced."
