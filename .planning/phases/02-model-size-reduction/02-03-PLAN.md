---
phase: 02-model-size-reduction
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/allocation.r
  - tests/testthat/test-mlr3-predict-dispatch.R
autonomous: true
requirements:
  - MEM-04

must_haves:
  truths:
    - "predict_saved_transition_prob() dispatches to an mlr3 branch when model_obj$model_type == 'mlr3'"
    - "The mlr3 branch calls predict_newdata() and returns data.frame(.pred_0, .pred_1) with values in [0,1]"
    - "All existing dispatch branches (tidypredict_, butchered_, minimal, workflow) are unchanged"
    - "The mlr3 branch is checked FIRST, before the grepl('^tidypredict_', ...) check"
    - "A testthat test creates a mock mlr3 model object and exercises the dispatch branch"
    - "The model loader in allocation.r uses qs::qread() when the file extension is .qs (backward-compatible: .rds files still use readRDS())"
  artifacts:
    - path: "src/allocation.r"
      provides: "mlr3 dispatch branch in predict_saved_transition_prob() and extension-detecting model loader"
      contains: "model_obj$model_type == \"mlr3\""
    - path: "src/allocation.r (model loader ~line 1514)"
      provides: "qs::qread() call for .qs model files"
      contains: "qs::qread"
    - path: "tests/testthat/test-mlr3-predict-dispatch.R"
      provides: "Contract test for mlr3 predict dispatch with mock model object and loader assertion"
  key_links:
    - from: "predict_saved_transition_prob()"
      to: "learner$predict_newdata()"
      via: "model_type == 'mlr3' dispatch branch using subset_saved_transition_data()"
      pattern: "predict_newdata"
    - from: "mlr3 branch"
      to: "data.frame(.pred_0, .pred_1)"
      via: "pred$prob[, \"1\"] (named column, not positional index)"
      pattern: "pred\\$prob\\[, \"1\"\\]"
    - from: "model loader (~line 1514)"
      to: "qs::qread(mi$file_path)"
      via: "grepl('.qs$', mi$file_path) extension detection"
      pattern: "qs::qread"
---

<objective>
Add the model_type = "mlr3" dispatch branch to predict_saved_transition_prob() in allocation.r as the first check, before the existing tidypredict_ check. The branch loads the mlr3 Learner from model_obj$learner, subsets new_data via the existing subset_saved_transition_data() helper, calls predict_newdata(), and returns data.frame(.pred_0, .pred_1). All existing branches stay intact (D-04/D-06).

Also update the model loader call site (~line 1514) to detect .qs file extension and call qs::qread() instead of readRDS() — this is the missing link that allows mlr3 .qs files saved by 02-02 to actually be loaded at prediction time. Old .rds files continue to use readRDS() for backward compatibility.

Purpose: Without the dispatch branch, allocation.r cannot use mlr3 .qs model files. Without the loader fix, the .qs files cannot be deserialized even when they exist. Both are required for end-to-end mlr3 prediction. This plan and 02-02 run in parallel (Wave 2) because they touch different files.
Output: Updated src/allocation.r with mlr3 dispatch branch and extension-detecting model loader; green testthat test.
</objective>

<execution_context>
@C:/Users/bblack/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-model-size-reduction/02-CONTEXT.md
@.planning/phases/02-model-size-reduction/02-RESEARCH.md
@.planning/phases/02-model-size-reduction/02-PATTERNS.md
@.planning/phases/02-model-size-reduction/02-01-SUMMARY.md

<interfaces>
<!-- Key function signatures and dispatch pattern from src/allocation.r. Executor must read the file but these are the critical contracts. -->

predict_saved_transition_prob() is at line 501. Its internal structure:
1. Lines 506–509: log_and_stop() helper defined inside the function
2. Lines 511–517: initial log_msg() call
3. Lines 519–538: is.list() guard + predictor_names validation block — DO NOT CHANGE
4. Line 540: First dispatch check — grepl("^tidypredict_", model_obj$model_type) — INSERT BEFORE THIS LINE
5. Lines 540–575: tidypredict_ branch — DO NOT CHANGE
6. Lines 577–613: butchered_ branch — DO NOT CHANGE
7. Lines 615+: minimal and workflow branches — DO NOT CHANGE

The mlr3 branch must be inserted between the closing brace of the predictor_names validation block (line ~538) and the opening of the tidypredict_ check (line 540). Match the exact indentation and style of the butchered_ branch pattern.

Helper functions available inside predict_saved_transition_prob() (defined at lines 330–388):
- log_and_stop(msg): logs msg then calls stop(msg, call.=FALSE) — use for fatal errors
- subset_saved_transition_data(new_data, predictor_names): subsets data.table or data.frame to predictor_names — use this, do not reimplement
- get_saved_transition_predictors(model_obj): reads model_obj$predictor_names first — mlr3 objects carry this field explicitly, so this already works
- restore_ranger_importance_mode(model_obj): NOT needed for mlr3 models (do not call it in the mlr3 branch)

Output convention (all branches must return this):
```r
data.frame(.pred_0 = 1 - prob_1, .pred_1 = prob_1)
```
Caller at line ~1601 takes pred_result[[2L]] as the transition probability — .pred_1 is the second column.

mlr3 branch to insert (exact code from PATTERNS.md):
```r
if (!is.null(model_obj$model_type) && model_obj$model_type == "mlr3") {
  log_msg("        Path: mlr3 learner; predict_newdata()", log_file)
  new_data_subset <- subset_saved_transition_data(new_data, predictor_names)
  pred <- tryCatch(
    model_obj$learner$predict_newdata(newdata = new_data_subset),
    error = function(e) log_and_stop(sprintf(
      "mlr3 predict_newdata() failed: %s", conditionMessage(e)
    ))
  )
  # pred$prob is a matrix; columns named by class label — always use name, not position
  prob_1 <- pred$prob[, "1"]
  result <- data.frame(.pred_0 = 1 - prob_1, .pred_1 = prob_1)
  log_msg("        Path: mlr3 — prediction complete", log_file)
  return(result)
}
```

Model loader call site (from direct codebase reading):
Line ~1514 in allocation.r — inside the worker loop that iterates over transition model files:
```r
    t_model_load <- prof_tic()
    fitted_wf <- readRDS(mi$file_path)    # <-- THIS LINE to update
    prof_toc(
      t_model_load,
      sprintf(
        "region=%s stage=model_load from=%d to=%d",
        region_suffix,
        from_val,
        to_val
      ),
      log_file
    )
    preds_needed <- get_saved_transition_predictors(fitted_wf)
```
The variable name `fitted_wf` and the prof_tic/prof_toc wrapping must NOT change.

qs::qread() note: The model loading happens in the CALLER (the allocation worker at ~line 1514), not inside predict_saved_transition_prob(). The caller loads the file and passes the list as model_obj. Do NOT add qs::qread() inside predict_saved_transition_prob().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test for mlr3 predict dispatch branch</name>
  <files>tests/testthat/test-mlr3-predict-dispatch.R</files>
  <read_first>
    - tests/testthat/test-allocation-env-canonical.R (repo-root resolver and .read_text() helper pattern to replicate)
    - src/allocation.r (lines 330–640 — understand the full predict_saved_transition_prob() function structure and the existing dispatch branches to ensure the test targets the right insertion point)
    - .planning/phases/02-model-size-reduction/02-RESEARCH.md (Section 4 — Predict Dispatcher Analysis; the exact insertion point and mlr3 dispatch branch code)
  </read_first>
  <behavior>
    - Test 1: "allocation.r contains mlr3 dispatch branch" — reads src/allocation.r as text, asserts grepl('model_obj\\$model_type == "mlr3"', content, fixed=TRUE)
    - Test 2: "mlr3 branch appears before tidypredict_ check" — reads as text, asserts position of "model_type == \"mlr3\"" is before position of "grepl(\"^tidypredict_\"" using regexpr()
    - Test 3: "mlr3 branch uses predict_newdata and named prob column" — asserts both "predict_newdata" and 'prob[, "1"]' appear in src/allocation.r
    - Test 4: "mlr3 dispatch returns .pred_0 and .pred_1 with mock model object (integration)" — if mlr3 packages are available: creates a mock model_obj list with model_type="mlr3" and a real tiny mlr3 Learner trained on 50 synthetic rows; sources src/allocation.r (with tryCatch around source to skip if deps unavailable); calls predict_saved_transition_prob(mock_model_obj, new_data_5rows); asserts names(result) == c(".pred_0", ".pred_1") and all(result >= 0) and all(result <= 1). Skip with skip_if_not_installed("mlr3").
    - Test 5: "allocation.r model loader detects .qs extension and calls qs::qread()" — reads src/allocation.r as text, asserts grepl("qs::qread", content, fixed=TRUE)
  </behavior>
  <action>
Create tests/testthat/test-mlr3-predict-dispatch.R using the same .repo_root resolver and .read_text() helper pattern from existing test files.

Tests 1, 2, and 3 are grep-based (read allocation.r as text) — all will fail in RED state because the mlr3 branch does not exist yet.

Test 5 asserts qs::qread appears in allocation.r — will fail in RED state because the loader still uses readRDS only.

For Test 4 (integration), the mock model object construction:
```r
skip_if_not_installed("mlr3")
skip_if_not_installed("mlr3learners")
library(mlr3)
library(mlr3learners)
set.seed(42L)
n <- 50L
synth <- data.frame(
  p1 = rnorm(n), p2 = rnorm(n), p3 = rnorm(n),
  response = factor(sample(c("0","1"), n, replace=TRUE))
)
task <- mlr3::as_task_classif(synth, target="response", positive="1")
lrn_rf <- mlr3::lrn("classif.ranger", predict_type="prob",
                     num.trees=10L, save.memory=TRUE, importance="none", num.threads=1L)
lrn_rf$train(task)
mock_model_obj <- list(
  model_type      = "mlr3",
  predictor_names = c("p1","p2","p3"),
  response_levels = c("0","1"),
  learner         = lrn_rf
)
new_rows <- data.frame(p1=rnorm(5L), p2=rnorm(5L), p3=rnorm(5L))
```

Source allocation.r with tryCatch(source(file.path(.repo_root, "src/allocation.r")), error=function(e) skip(e$message)). Then call predict_saved_transition_prob(mock_model_obj, new_rows) and assert on the result.

Note: sourcing allocation.r may fail if its transitive dependencies are not available. Wrap this in tryCatch and skip rather than fail.

Commit as: `test(02-03): failing tests for mlr3 predict dispatch branch and qs::qread() loader (RED state)`
  </action>
  <verify>
    <automated>grep -c "model_type.*mlr3" tests/testthat/test-mlr3-predict-dispatch.R</automated>
  </verify>
  <acceptance_criteria>
    - File tests/testthat/test-mlr3-predict-dispatch.R exists
    - grep -c "predict_newdata" returns >= 1 in the test file
    - grep -c "skip_if_not_installed" returns >= 1 (integration test has skip guard)
    - grep -c "qs::qread" returns >= 1 (Test 5 asserts the loader uses qs::qread)
    - Tests 1-3 and Test 5 are in RED state (grep assertions fail against current allocation.r which has no mlr3 branch and no qs::qread)
  </acceptance_criteria>
  <done>Test file exists with 5 test_that() blocks committed in RED state</done>
</task>

<task type="auto">
  <name>Task 2: Add mlr3 dispatch branch to predict_saved_transition_prob() in allocation.r</name>
  <files>src/allocation.r</files>
  <read_first>
    - src/allocation.r (lines 490–650 — read this range in full; identify the exact line after the predictor_names validation closing brace and before the grepl("^tidypredict_") check; count the exact indentation of the surrounding code)
    - tests/testthat/test-mlr3-predict-dispatch.R (verify the exact strings the tests assert so the implementation matches)
    - .planning/phases/02-model-size-reduction/02-PATTERNS.md (allocation.r section — the exact mlr3 branch code block and the surrounding context at lines 150–215)
  </read_first>
  <action>
ONE surgical edit to src/allocation.r.

Locate the insertion point inside predict_saved_transition_prob():
- Find the closing `}` of the predictor_names validation block (the block ending with the closing brace at approximately line 538, which closes the `if (length(predictor_names) > 0L) {` block at line 528)
- Insert the mlr3 branch as a new `if` block AFTER that closing brace, BEFORE the existing `if (!is.null(model_obj$model_type) && grepl("^tidypredict_", model_obj$model_type))` check

The block to insert (match the two-space indent of the surrounding if blocks):

```r

    if (!is.null(model_obj$model_type) && model_obj$model_type == "mlr3") {
      log_msg("        Path: mlr3 learner; predict_newdata()", log_file)
      new_data_subset <- subset_saved_transition_data(new_data, predictor_names)
      pred <- tryCatch(
        model_obj$learner$predict_newdata(newdata = new_data_subset),
        error = function(e) log_and_stop(sprintf(
          "mlr3 predict_newdata() failed: %s", conditionMessage(e)
        ))
      )
      # pred$prob is a matrix; columns are named by class label ("0", "1")
      # Always index by name, not position — factor levels may vary (Risk 6, RESEARCH)
      prob_1 <- pred$prob[, "1"]
      result <- data.frame(.pred_0 = 1 - prob_1, .pred_1 = prob_1)
      log_msg("        Path: mlr3 — prediction complete", log_file)
      return(result)
    }
```

CRITICAL rules for this edit:
1. Do NOT modify any existing branch (tidypredict_, butchered_, minimal, workflow)
2. Do NOT call restore_ranger_importance_mode() in the mlr3 branch (mlr3 ranger models have no importance.mode inconsistency)
3. The mlr3 check uses exact string equality (model_type == "mlr3"), not grepl — this is intentional
4. The log_msg indent is 8 spaces (matching the "Path: tidypredict" and "Path: butchered" messages in the existing branches)
5. Do NOT add a qs::qread() call inside this function — the caller passes model_obj already loaded (qs::qread is added in Task 3 at the loader call site, not here)

After the edit, commit as: `feat(02-03): add mlr3 dispatch branch to predict_saved_transition_prob() (D-04, MEM-04)`
  </action>
  <verify>
    <automated>grep -c "model_obj\$model_type == \"mlr3\"" src/allocation.r && grep -c "predict_newdata" src/allocation.r && grep -c "grepl.*tidypredict_" src/allocation.r</automated>
  </verify>
  <acceptance_criteria>
    - src/allocation.r contains `model_obj$model_type == "mlr3"` (exact string equality check, not grepl)
    - src/allocation.r contains `predict_newdata` (the mlr3 prediction call)
    - src/allocation.r contains `pred$prob[, "1"]` (named column indexing, not positional)
    - src/allocation.r still contains `grepl("^tidypredict_"` (existing branch untouched)
    - src/allocation.r still contains `grepl("^butchered_"` (existing branch untouched)
    - src/allocation.r still contains `restore_ranger_importance_mode` (existing butchered/minimal branches use it; mlr3 branch does not)
    - The mlr3 check appears BEFORE the tidypredict_ check: the byte offset of "model_type == \"mlr3\"" is less than the byte offset of "grepl(\"^tidypredict_\"" in the file (verifiable with grep -n)
    - Tests 1-4 in tests/testthat/test-mlr3-predict-dispatch.R are GREEN
  </acceptance_criteria>
  <done>predict_saved_transition_prob() has the mlr3 branch as its first dispatch check; all existing branches are unchanged; dispatch tests 1-4 pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update model loader in allocation.r to use qs::qread() for .qs files</name>
  <files>src/allocation.r</files>
  <read_first>
    - src/allocation.r (lines 1505–1530 — the model loading call site at line ~1514 that uses readRDS(mi$file_path); understand the full context including prof_tic/prof_toc wrapping and the subsequent get_saved_transition_predictors call)
    - tests/testthat/test-mlr3-predict-dispatch.R (the existing test file — Test 5 already asserts qs::qread appears in allocation.r; this task makes Test 5 GREEN)
  </read_first>
  <behavior>
    - Test 5 (already written in Task 1): "allocation.r model loader detects .qs extension and calls qs::qread()" — passes when allocation.r contains qs::qread at the loader call site
  </behavior>
  <action>
ONE surgical edit to src/allocation.r — the model loader call site at approximately line 1514.

Current code (inside the worker iteration loop, wrapped by prof_tic/prof_toc):
```r
    t_model_load <- prof_tic()
    fitted_wf <- readRDS(mi$file_path)
    prof_toc(
      t_model_load,
      sprintf(
        "region=%s stage=model_load from=%d to=%d",
        region_suffix,
        from_val,
        to_val
      ),
      log_file
    )
```

Replace the single `fitted_wf <- readRDS(mi$file_path)` line with an extension-detecting loader:
```r
    t_model_load <- prof_tic()
    fitted_wf <- if (grepl("\\.qs$", mi$file_path, perl = TRUE)) {
      qs::qread(mi$file_path)
    } else {
      readRDS(mi$file_path)
    }
    prof_toc(
      t_model_load,
      sprintf(
        "region=%s stage=model_load from=%d to=%d",
        region_suffix,
        from_val,
        to_val
      ),
      log_file
    )
```

CRITICAL rules:
1. Keep the prof_tic() / prof_toc() wrapping intact — timing instrumentation must not move
2. The variable name stays `fitted_wf` — it is used in all subsequent calls (get_saved_transition_predictors, predict_saved_transition_prob)
3. readRDS() remains as the fallback for existing .rds files (backward compatibility with pre-Phase 2 models — D-04/D-06)
4. Do NOT add qs::qread() anywhere else in allocation.r — only this loader call site needs it

After the edit, commit as: `feat(02-03): update allocation.r model loader to detect .qs extension and use qs::qread() (D-05, MEM-04)`
  </action>
  <verify>
    <automated>grep -c "qs::qread" src/allocation.r</automated>
  </verify>
  <acceptance_criteria>
    - src/allocation.r contains `qs::qread` (grep -c returns >= 1 at the model-loading call site around line 1514)
    - src/allocation.r still contains `readRDS` as the fallback path for .rds files (grep -c "readRDS" returns >= 1)
    - The extension check uses grepl(".qs$", ...) — .qs files use qs::qread, .rds files use readRDS
    - grep -c "qs::qread" src/allocation.r returns >= 1
    - Test 5 in tests/testthat/test-mlr3-predict-dispatch.R is GREEN
    - All 5 tests in tests/testthat/test-mlr3-predict-dispatch.R are GREEN after this task
  </acceptance_criteria>
  <done>The model loader at ~line 1514 in allocation.r detects .qs extension and calls qs::qread(); .rds files continue to use readRDS(); Test 5 is GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model_obj → predict_newdata() | model_obj is loaded from a .qs file on the filesystem; the learner is an R6 object whose predict_newdata() is called with new_data |
| new_data → predict_newdata() | new_data comes from the allocation worker (parquet-sourced predictors); columns are already validated against predictor_names before the dispatch |
| mi$file_path → qs::qread() | File path comes from the transition model index; no user input; extension detection routes to the correct deserialiser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | model_obj$learner$predict_newdata() | accept | model_obj is loaded from the operator-controlled model output directory; no user input reaches this call path; mlr3 Learner validates column names at predict time |
| T-02-07 | Elevation of Privilege | tryCatch(predict_newdata()) | mitigate | Errors are caught and re-raised via log_and_stop() — no silent failure; the worker error bubbles to perform_transition_modelling() error handler which logs and continues rather than hanging |
| T-02-11 | Tampering | mi$file_path → qs::qread() | accept | File path is from the operator-controlled transition model directory; the extension check (.qs$) is regex-only, no code evaluation; qs::qread() on a corrupt file raises an R error caught by the worker tryCatch |
</threat_model>

<verification>
After all three tasks complete:

```bash
grep -n "model_type.*mlr3\|grepl.*tidypredict_\|grepl.*butchered_" src/allocation.r | head -20
```

The mlr3 line number must be lower (earlier in file) than the tidypredict_ line number.

```bash
grep -c "predict_newdata" src/allocation.r
grep -c 'prob\[, "1"\]' src/allocation.r
grep -c "grepl.*tidypredict_" src/allocation.r
grep -c "grepl.*butchered_" src/allocation.r
grep -c "qs::qread" src/allocation.r
```

All must return >= 1.
</verification>

<success_criteria>
- src/allocation.r has the mlr3 dispatch block inserted BEFORE the tidypredict_ check
- mlr3 branch uses: exact equality (not grepl), subset_saved_transition_data(), predict_newdata(), named prob column extraction, data.frame(.pred_0, .pred_1) return
- All existing branches (tidypredict_, butchered_, minimal, workflow) are word-for-word unchanged
- restore_ranger_importance_mode() is NOT called in the mlr3 branch
- Model loader at ~line 1514 uses qs::qread() for .qs files and readRDS() for .rds files
- grep -c "qs::qread" src/allocation.r returns >= 1
- All 5 dispatch tests pass (GREEN)
- Changes committed with TDD RED->GREEN commit pair
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-size-reduction/02-03-SUMMARY.md`
</output>
