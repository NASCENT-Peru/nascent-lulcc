---
phase: 02-model-size-reduction
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/transition_modelling.r
  - config/model_specs.yaml
  - tests/testthat/test-mlr3-training-pipeline.R
autonomous: true
requirements:
  - MEM-04

must_haves:
  truths:
    - "transition_modelling.r contains no library(tidymodels), library(workflows), library(tune), or library(parsnip) calls"
    - "transition_modelling.r contains train_mlr3_transition() function that creates a Task, trains a Learner via AutoTuner or direct $train(), and saves via qs::qsave()"
    - "The saved model object is a named list with model_type='mlr3', predictor_names, response_levels, and learner"
    - "build_transition_model_path() returns a .qs extension"
    - "Subsampling is applied before Task creation when nrow(data) > max_training_rows"
    - "File size gate: file.size() < 200 * 1024^2 check runs after every qs::qsave(); warning logged if exceeded"
    - "5-row predict sanity check runs after saving each model; asserts probabilities in [0,1] and non-NA"
    - "model_single_transition() outer signature is unchanged; inner modelling call dispatches to train_mlr3_transition()"
    - "model_specs.yaml uses mlr3 parameter names (num.trees, eta, max_depth, min_child_weight, nrounds, num.threads, nthread)"
    - "A testthat test exercises the full train-save-load-predict cycle with a synthetic 200-row dataset"
    - "model_specs.yaml contains a comment marking num_replicates as superseded by AutoTuner CV folds"
    - "train_mlr3_transition() contains a comment documenting that step_normalize is not required for classif.glmnet"
  artifacts:
    - path: "src/transition_modelling.r"
      provides: "mlr3-based training pipeline with stratified subsampling, size gate, sanity check"
      contains: "train_mlr3_transition("
    - path: "config/model_specs.yaml"
      provides: "mlr3-compatible hyperparameter specification"
      contains: "num.trees"
    - path: "tests/testthat/test-mlr3-training-pipeline.R"
      provides: "Automated RED-GREEN cycle covering train+save+load+predict for one synthetic transition"
  key_links:
    - from: "model_single_transition()"
      to: "train_mlr3_transition()"
      via: "direct call replacing multi_spec_trans_modelling() + fit_and_save_best_model() inner calls"
      pattern: "train_mlr3_transition\\("
    - from: "train_mlr3_transition()"
      to: "qs::qsave()"
      via: "saves list(model_type='mlr3', predictor_names, response_levels, learner) at .qs output_path"
      pattern: "qs::qsave"
    - from: "train_mlr3_transition()"
      to: "build_transition_model_path()"
      via: "output_path ends in .qs; cache-skip check in model_single_transition() must use .qs path"
      pattern: "build_transition_model_path"
---

<objective>
Rewrite the inner modelling stack of transition_modelling.r to use mlr3. Replace multi_spec_trans_modelling(), fit_model_with_tuning(), fit_and_save_best_model(), save_minimal_model(), and create_model_spec() with a new train_mlr3_transition() function that implements: stratified subsampling fallback (D-09/D-10), mlr3 Task creation with stratum role (D-13), per-algorithm Learner construction with AutoTuner for multi-value grids or direct $train() for single-value grids (D-03), save via qs::qsave() as a named list (D-05), file size gate (D-12), and 5-row predict sanity check (D-13). Update build_transition_model_path() to return .qs extension. Update model_specs.yaml to use mlr3 parameter names. Preserve outer function signatures: transition_modelling(), perform_transition_modelling(), model_single_transition().

Purpose: This is the core of Phase 2 — replacing the memory-heavy tidymodels pipeline with mlr3 Learners that use save.memory=TRUE and importance="none" for ranger, producing model files well under 200 MB.
Output: Rewritten src/transition_modelling.r with mlr3 inner functions; updated model_specs.yaml; green testthat test.
</objective>

<execution_context>
@C:/Users/bblack/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-model-size-reduction/02-CONTEXT.md
@.planning/phases/02-model-size-reduction/02-RESEARCH.md
@.planning/phases/02-model-size-reduction/02-PATTERNS.md
@.planning/phases/02-model-size-reduction/02-01-SUMMARY.md

<interfaces>
<!-- Outer function signatures to PRESERVE verbatim — do not change these. -->

transition_modelling(config, refresh_cache, model_dir, eval_dir, use_regions, model_specs_path, periods_to_process)
perform_transition_modelling(period, use_regions, config, ...)
model_single_transition(trans_name, refresh_cache, region, use_regions, ds_transitions, ds_static, ds_dynamic, period, config, model_dir, eval_dir, save_debug, log_file, fs_summary, model_specs_path)

<!-- build_transition_model_path() — CHANGE .rds to .qs -->
Current (line 103–111):
```r
build_transition_model_path <- function(trans_name, region, model_dir) {
  region_suffix <- ifelse(
    is.null(region) || identical(region, "National extent"),
    "national",
    gsub(" ", "_", tolower(region))
  )
  file.path(model_dir, sprintf("%s_%s.rds", trans_name, region_suffix))
}
```
Change ONLY `%s_%s.rds` to `%s_%s.qs`. Nothing else.

<!-- model_single_transition() inner call to replace (lines 1110–1130) -->
Current inner call:
```r
  results <- multi_spec_trans_modelling(
    transition_data = transition_data,
    model_specs = model_specs,
    log_file = log_file
  )
  best_model_info <- fit_and_save_best_model(
    results = results,
    full_data = transition_data,
    output_path = model_path,
    log_file = log_file,
    max_final_fit_size = model_specs$global$max_final_fit_size
  )
  results$final_model <- best_model_info
  return(results)
```
Replace with a call to train_mlr3_transition() that returns the same structure:
```r
  results <- train_mlr3_transition(
    transition_data = transition_data,
    predictor_names = pred_names,
    output_path     = model_path,
    config          = config,
    model_specs     = model_specs,
    log_file        = log_file
  )
  return(results)
```

<!-- Subsampling pattern — base R (use over dplyr inside workers per RESEARCH §6) -->
```r
max_rows <- config[["max_training_rows"]] %||% 500000L
if (nrow(transition_data) > max_rows) {
  log_msg(sprintf(
    "  Subsampling: %d rows -> %d (max_training_rows=%d)",
    nrow(transition_data), max_rows, max_rows
  ), log_file)
  response_tbl  <- table(transition_data$response)
  minority_cls  <- names(which.min(response_tbl))
  majority_cls  <- names(which.max(response_tbl))
  n_minority    <- response_tbl[[minority_cls]]
  n_majority    <- min(max_rows - n_minority, response_tbl[[majority_cls]])
  set.seed(config[["random_seed"]] %||% 123L)
  minority_rows <- transition_data[transition_data$response == minority_cls, ]
  majority_rows <- transition_data[transition_data$response == majority_cls, ]
  majority_rows <- majority_rows[sample(nrow(majority_rows), n_majority, replace = FALSE), ]
  transition_data <- rbind(minority_rows, majority_rows)
}
```

<!-- Save object contract (D-05) -->
```r
model_obj <- list(
  model_type      = "mlr3",
  predictor_names = task$feature_names,  # Task's view is authoritative
  response_levels = task$class_names,
  learner         = lrn_fitted           # at$learner if AutoTuner; lrn directly if single-value grid
)
qs::qsave(model_obj, output_path)
```

<!-- Size gate (D-12) — warn only -->
```r
size_bytes <- file.size(output_path)
if (size_bytes > 200 * 1024^2) {
  log_msg(sprintf(
    "WARNING: model file exceeds 200MB: %.1f MB — %s",
    size_bytes / 1024^2, output_path
  ), log_file)
}
```

<!-- 5-row sanity check (D-13) — assert [0,1] non-NA -->
```r
model_check <- qs::qread(output_path)
fixture_rows <- utils::head(transition_data[, predictor_names, drop = FALSE], 5L)
pred_check <- model_check$learner$predict_newdata(newdata = fixture_rows)
prob_check <- pred_check$prob[, "1"]
if (any(is.na(prob_check)) || any(prob_check < 0) || any(prob_check > 1)) {
  log_msg(sprintf(
    "ERROR: 5-row sanity check failed for %s — probabilities not in [0,1] or contain NA",
    output_path
  ), log_file)
  stop(sprintf("Sanity check failed: %s", output_path))
}
log_msg(sprintf("  Sanity check passed (5-row predict OK): %s", output_path), log_file)
```

<!-- Learner construction per algorithm -->
GLM (classif.glmnet — matches current glmnet behaviour, D-03):
```r
lrn("classif.glmnet",
  predict_type = "prob",
  alpha = params$alpha %||% to_tune(0, 1),
  s     = params$s %||% to_tune(1e-4, 1, logscale = TRUE)
)
```
For single-value grids in model_specs.yaml, set params directly without to_tune():
```r
lrn("classif.glmnet", predict_type = "prob", alpha = 1, s = 0.01)
```

RF (classif.ranger):
```r
lrn("classif.ranger",
  predict_type  = "prob",
  num.trees     = params$num.trees %||% 500L,
  mtry          = params$mtry %||% max(1L, floor(sqrt(length(predictor_names)))),
  min.node.size = params$min.node.size %||% 5L,
  importance    = "none",    # MANDATORY — suppresses importance vector
  save.memory   = TRUE,      # MANDATORY — suppresses OOB predictions matrix
  num.threads   = 1L         # MANDATORY — required for furrr parallel workers
)
```

XGBoost (classif.xgboost):
```r
lrn("classif.xgboost",
  predict_type     = "prob",
  nrounds          = params$nrounds %||% 100L,
  eta              = params$eta %||% 0.1,
  max_depth        = params$max_depth %||% 6L,
  min_child_weight = params$min_child_weight %||% 5L,
  nthread          = 1L   # MANDATORY — required for furrr parallel workers
)
```

<!-- Clone pattern (Risk 7 in RESEARCH) — always clone inside furrr worker -->
```r
lrn_fitted <- learner_spec$clone(deep = TRUE)
lrn_fitted$train(task)
```

<!-- AutoTuner — use only when any param has multiple values -->
```r
at <- mlr3tuning::auto_tuner(
  tuner      = mlr3tuning::tnr("grid_search"),
  learner    = learner_spec,
  resampling = mlr3::rsmp("cv", folds = 3L),
  measure    = mlr3::msr("classif.auc"),
  term_evals = as.integer(n_combos)
)
at$train(task)
lrn_fitted <- at$learner  # NEVER save the AutoTuner itself
```

<!-- Error return structure — must match what perform_transition_modelling() expects -->
```r
return(list(
  transition    = trans_name,
  region        = region,
  status        = "error",
  error_message = conditionMessage(e),
  cv_metrics    = NULL,
  test_metrics  = NULL,
  final_model   = NULL
))
```

<!-- Success return structure — mirrors what model_single_transition() currently returns -->
```r
return(list(
  transition  = trans_name,
  region      = region,
  status      = "success",
  model_path  = output_path,
  final_model = list(
    model_path    = output_path,
    model_type    = "mlr3",
    size_bytes    = file.size(output_path),
    n_predictors  = length(predictor_names)
  )
))
```

<!-- model_specs.yaml — current tidymodels names that must be renamed to mlr3 names -->
Mapping:
  glm:  penalty -> s, mixture -> alpha
  rf:   trees -> num.trees, min_n -> min.node.size, mtry -> mtry (same), importance -> (remove, hardcoded to "none"), replace/sample.fraction/seed -> (remove, not mlr3 params)
  xgboost: trees -> nrounds, tree_depth -> max_depth, learn_rate -> eta, min_n -> min_child_weight, mtry -> colsample_bytree (fraction)

For single-value lists like `[500]`, retain the single-element list syntax; train_mlr3_transition() detects single-value params and sets them directly (no AutoTuner).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test for mlr3 training pipeline (train-save-load-predict cycle)</name>
  <files>tests/testthat/test-mlr3-training-pipeline.R</files>
  <read_first>
    - tests/testthat/test-allocation-env-canonical.R (repo-root resolver and .read_text() helper pattern)
    - tests/testthat/test-mlr3-env-contract.R (Phase 02-01 test structure)
    - src/transition_modelling.r (lines 1–120 — to understand what train_mlr3_transition() must produce)
    - .planning/phases/02-model-size-reduction/02-RESEARCH.md (Section 2 — complete mlr3 pipeline template in Code Examples)
  </read_first>
  <behavior>
    - Test 1: "transition_modelling.r exports train_mlr3_transition function" — reads the file as text and asserts grepl("train_mlr3_transition", content, fixed=TRUE)
    - Test 2: "transition_modelling.r does not contain library(tidymodels) or library(workflows)" — reads file as text, asserts !grepl patterns for both
    - Test 3: "build_transition_model_path returns .qs extension" — reads file as text, asserts grepl(".qs") and !grepl("%s_%s.rds")
    - Test 4: "train_mlr3_transition produces a valid .qs model file (integration)" — if mlr3 packages are available, creates a 200-row synthetic dataset with a factor "response" column ("0"/"1") and 5 numeric predictor columns, calls train_mlr3_transition() with a tempfile() output path ending in .qs, asserts: file exists, file.size() > 0, qs::qread() returns a list with names(model_obj) containing c("model_type","predictor_names","response_levels","learner"), model_obj$model_type == "mlr3", all(model_obj$predictor_names %in% colnames(synthetic_data)), and a 5-row predict_newdata() returns probabilities in [0,1] non-NA. Skip with skip_if_not_installed("mlr3") if mlr3 is unavailable.
  </behavior>
  <action>
Create tests/testthat/test-mlr3-training-pipeline.R. Use the same .repo_root resolver and .read_text() helper from the existing test files.

Tests 1, 2, and 3 read src/transition_modelling.r as text (grep-based assertions). They will fail in RED state because the function does not exist yet and .rds is still the extension.

Test 4 is an integration test that requires mlr3. Wrap it in skip_if_not_installed("mlr3") and skip_if_not_installed("mlr3learners"). For the synthetic data, use:
```r
set.seed(42L)
n <- 200L
synth_data <- data.frame(
  p1 = rnorm(n), p2 = rnorm(n), p3 = rnorm(n), p4 = rnorm(n), p5 = rnorm(n),
  response = factor(sample(c("0","1"), n, replace=TRUE, prob=c(0.7,0.3)))
)
pred_names <- c("p1","p2","p3","p4","p5")
```

The config argument to train_mlr3_transition() should supply at minimum: list(max_training_rows=500000L, random_seed=42L). The model_specs argument should supply a minimal spec with algorithm="rf" and single-value params (num.trees=50L to keep test fast).

Commit as: `test(02-02): failing tests for mlr3 training pipeline (train-save-load-predict RED state)`
  </action>
  <verify>
    <automated>grep -c "train_mlr3_transition" tests/testthat/test-mlr3-training-pipeline.R</automated>
  </verify>
  <acceptance_criteria>
    - File tests/testthat/test-mlr3-training-pipeline.R exists
    - grep -c "train_mlr3_transition" returns >= 2 (appears in multiple test_that blocks)
    - File contains skip_if_not_installed("mlr3") guard on the integration test
    - Tests 1-3 are in RED state (they assert things not yet true in src/transition_modelling.r)
  </acceptance_criteria>
  <done>Test file exists with 4 test_that() blocks; file is committed in RED state</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite transition_modelling.r inner stack and update model_specs.yaml</name>
  <files>src/transition_modelling.r, config/model_specs.yaml</files>
  <read_first>
    - src/transition_modelling.r (READ THE FULL FILE — all 3,569 lines — to understand the complete structure before modifying; pay special attention to lines 1–130 for outer shells, 103–111 for build_transition_model_path, 920–1135 for model_single_transition, and 1137–end for multi_spec_trans_modelling and related inner functions that will be replaced)
    - config/model_specs.yaml (read full file — rename all tidymodels parameter keys to mlr3 names)
    - .planning/phases/02-model-size-reduction/02-RESEARCH.md (Section 2 full mlr3 API patterns, Section 6 subsampling, Code Examples section — the complete train_and_save_mlr3_transition template)
    - .planning/phases/02-model-size-reduction/02-PATTERNS.md (Key Landmines section — all 6 landmines must be addressed)
  </read_first>
  <action>
TWO edits:

**Edit 1: config/model_specs.yaml**

Rename parameter keys to mlr3 native names. Keep the single-element list syntax. Replace the entire models: block:

```yaml
global:
  num_replicates: 1     # Superseded by AutoTuner CV folds in mlr3 pipeline (Phase 2) — value ignored
  cv_folds: 3
  random_seed: 123
  max_final_fit_size: 100000  # Superseded by max_training_rows in top-level config; kept for reference

metrics:
  optimization_metric: classif.auc

models:
  glm:
    parameters:
      alpha:
        - 1      # Lasso (mixture=1 in tidymodels); matches current glmnet behaviour
      s:
        - 0.01   # penalty=0.01 in tidymodels; the lambda value for classif.glmnet

  rf:
    parameters:
      num.trees:
        - 500
      min.node.size:
        - 5
      mtry:
        - 4
      # importance and save.memory are hardcoded in train_mlr3_transition() — not tunable

  xgboost:
    parameters:
      nrounds:
        - 100
      max_depth:
        - 6
      eta:
        - 0.1
      min_child_weight:
        - 5
      colsample_bytree:
        - 0.8    # approx: mtry=4 of ~5 predictors = 0.8; operator should adjust
      # nthread is hardcoded to 1 in train_mlr3_transition() — not tunable
```

Note on num_replicates: the old `multi_spec_trans_modelling()` looped through `model_specs$global$num_replicates` (was 2 in the prior tidymodels pipeline). The mlr3 migration drops this concept entirely — AutoTuner's internal CV folds replace replicate averaging. The key is retained in YAML for backward-compatibility with any script that reads it, but the value is ignored by train_mlr3_transition().

Note on colsample_bytree: XGBoost's `mtry` in tidymodels was an integer count of features. mlr3's `classif.xgboost` uses `colsample_bytree` which is a fraction [0, 1]. When building the XGBoost search space from model_specs.yaml, if migrating an existing integer `mtry` value, convert to `colsample_bytree` fraction by dividing by `length(predictor_names)`. Example: mtry=4 with 20 predictors → colsample_bytree=0.2. The value in model_specs.yaml must already be a fraction; clamp any computed values to [0.05, 1.0].

**Edit 2: src/transition_modelling.r**

Make the following surgical changes:

**2a. build_transition_model_path() (line 110):** Change `sprintf("%s_%s.rds", ...)` to `sprintf("%s_%s.qs", ...)`. One character change.

**2b. model_single_transition() inner call (lines ~1110–1134):** Replace the block:
```r
  results <- multi_spec_trans_modelling(...)
  best_model_info <- fit_and_save_best_model(...)
  results$final_model <- best_model_info
  return(results)
```
with:
```r
  results <- tryCatch(
    withCallingHandlers(
      train_mlr3_transition(
        transition_data = transition_data,
        predictor_names = pred_names,
        trans_name      = trans_name,
        region          = region,
        output_path     = model_path,
        config          = config,
        model_specs     = model_specs,
        log_file        = log_file
      ),
      error = function(e) { captured_trace <<- sys.calls() }
    ),
    error = function(e) {
      log_msg(sprintf("  ERROR in train_mlr3_transition: %s", conditionMessage(e)), log_file)
      list(
        transition    = trans_name,
        region        = ifelse(is.null(region), "National extent", region),
        status        = "error",
        error_message = conditionMessage(e),
        cv_metrics    = NULL,
        test_metrics  = NULL,
        final_model   = NULL
      )
    }
  )
  return(results)
```

**2c. Add train_mlr3_transition() and build_mlr3_learner() as NEW functions.** Insert them after the build_transition_model_path() function (around line 111) and before the read_optional_rds() function. Do NOT remove any existing functions yet — multi_spec_trans_modelling, fit_model_with_tuning, fit_and_save_best_model, save_minimal_model stay in the file but are no longer called from model_single_transition(). They can be removed in a future cleanup.

The new functions to insert:

```r
#' Build an mlr3 Learner for the given algorithm and parameters from model_specs.
#' Detects single-value vs multi-value parameter grids to decide whether to use
#' to_tune() (for AutoTuner) or direct assignment.
build_mlr3_learner <- function(algo, params, predictor_count) {
  if (algo == "glm") {
    # classif.glmnet: regularised logistic regression matching current glmnet behaviour (D-03)
    # No normalisation pipeline needed: classif.glmnet's L1/L2 regularisation is scale-invariant.
    # step_normalize() from the tidymodels recipes stack is NOT replicated here.
    alpha_vals <- params$alpha %||% c(1)
    s_vals     <- params$s %||% c(0.01)
    lrn_obj <- mlr3::lrn("classif.glmnet", predict_type = "prob")
    if (length(alpha_vals) > 1L) {
      lrn_obj$param_set$set_values(alpha = paradox::to_tune(min(alpha_vals), max(alpha_vals)))
    } else {
      lrn_obj$param_set$set_values(alpha = alpha_vals[[1]])
    }
    if (length(s_vals) > 1L) {
      lrn_obj$param_set$set_values(s = paradox::to_tune(min(s_vals), max(s_vals), logscale = TRUE))
    } else {
      lrn_obj$param_set$set_values(s = s_vals[[1]])
    }
    return(lrn_obj)
  }

  if (algo == "rf") {
    # classif.ranger: save.memory=TRUE and importance="none" are MANDATORY for <200MB files
    num_trees_vals     <- params$num.trees %||% c(500L)
    min_node_vals      <- params$min.node.size %||% c(5L)
    mtry_vals          <- params$mtry %||% c(max(1L, floor(sqrt(predictor_count))))
    lrn_obj <- mlr3::lrn("classif.ranger",
      predict_type = "prob",
      importance   = "none",    # suppresses importance vector (size reduction)
      save.memory  = TRUE,      # suppresses OOB predictions matrix (primary size reduction)
      num.threads  = 1L         # required for furrr parallel workers
    )
    if (length(num_trees_vals) > 1L) {
      lrn_obj$param_set$set_values(num.trees = paradox::to_tune(
        paradox::p_int(lower = min(num_trees_vals), upper = max(num_trees_vals))
      ))
    } else {
      lrn_obj$param_set$set_values(num.trees = as.integer(num_trees_vals[[1]]))
    }
    if (length(min_node_vals) > 1L) {
      lrn_obj$param_set$set_values(min.node.size = paradox::to_tune(
        paradox::p_int(lower = min(min_node_vals), upper = max(min_node_vals))
      ))
    } else {
      lrn_obj$param_set$set_values(min.node.size = as.integer(min_node_vals[[1]]))
    }
    if (length(mtry_vals) > 1L) {
      lrn_obj$param_set$set_values(mtry = paradox::to_tune(
        paradox::p_int(lower = min(mtry_vals), upper = min(max(mtry_vals), predictor_count))
      ))
    } else {
      lrn_obj$param_set$set_values(mtry = min(as.integer(mtry_vals[[1]]), predictor_count))
    }
    return(lrn_obj)
  }

  if (algo == "xgboost") {
    nrounds_vals    <- params$nrounds %||% c(100L)
    max_depth_vals  <- params$max_depth %||% c(6L)
    eta_vals        <- params$eta %||% c(0.1)
    min_cw_vals     <- params$min_child_weight %||% c(5L)
    # colsample_bytree = mtry / length(predictor_names); clamp to [0.05, 1]
    # (XGBoost's mtry in tidymodels was an integer count; classif.xgboost uses a fraction [0,1])
    cbt_vals        <- params$colsample_bytree %||% c(0.8)
    lrn_obj <- mlr3::lrn("classif.xgboost",
      predict_type = "prob",
      nthread      = 1L   # required for furrr parallel workers
    )
    # Set single-value params directly; multi-value params via to_tune()
    set_single_or_tune <- function(lrn, param, vals, is_int = FALSE) {
      if (length(vals) > 1L) {
        if (is_int) {
          lrn$param_set$set_values(
            .args = setNames(list(paradox::to_tune(paradox::p_int(min(vals), max(vals)))), param)
          )
        } else {
          lrn$param_set$set_values(
            .args = setNames(list(paradox::to_tune(paradox::p_dbl(min(vals), max(vals)))), param)
          )
        }
      } else {
        lrn$param_set$set_values(.args = setNames(list(if (is_int) as.integer(vals[[1]]) else vals[[1]]), param))
      }
      lrn
    }
    lrn_obj <- set_single_or_tune(lrn_obj, "nrounds",          nrounds_vals,   is_int = TRUE)
    lrn_obj <- set_single_or_tune(lrn_obj, "max_depth",        max_depth_vals, is_int = TRUE)
    lrn_obj <- set_single_or_tune(lrn_obj, "eta",              eta_vals)
    lrn_obj <- set_single_or_tune(lrn_obj, "min_child_weight", min_cw_vals,    is_int = TRUE)
    lrn_obj <- set_single_or_tune(lrn_obj, "colsample_bytree", cbt_vals)
    return(lrn_obj)
  }

  stop(sprintf("Unknown algorithm '%s' in build_mlr3_learner()", algo))
}

#' Train one transition model using mlr3, save it as a .qs file, and return
#' a result list compatible with perform_transition_modelling()'s aggregation.
#'
#' Implements: D-01 (mlr3 replacement), D-05 (save format), D-09/D-10 (subsampling),
#' D-12 (size gate), D-13 (sanity check).
#'
#' @param transition_data data.frame with factor column "response" ("0"/"1") + predictor columns
#' @param predictor_names character vector of predictor column names
#' @param trans_name character; name of this transition (for logging and return value)
#' @param region character or NULL; region name (for return value)
#' @param output_path character; .qs file path to write
#' @param config list; full config from get_config()
#' @param model_specs list; parsed model_specs.yaml content
#' @param log_file character or NULL; path to log file
#' @return named list compatible with perform_transition_modelling() result aggregation
train_mlr3_transition <- function(
  transition_data,
  predictor_names,
  trans_name,
  region,
  output_path,
  config,
  model_specs,
  log_file = NULL
) {
  library(mlr3)
  library(mlr3learners)
  library(mlr3tuning)
  library(paradox)

  `%||%` <- function(x, y) if (is.null(x) || (is.atomic(x) && length(x) == 0L)) y else x

  # 1. Subsampling fallback (D-09, D-10)
  max_rows <- config[["max_training_rows"]] %||% 500000L
  if (nrow(transition_data) > max_rows) {
    log_msg(sprintf(
      "  Subsampling: %d rows -> %d (max_training_rows=%d, D-09)",
      nrow(transition_data), max_rows, max_rows
    ), log_file)
    response_tbl <- table(transition_data$response)
    minority_cls <- names(which.min(response_tbl))
    majority_cls <- names(which.max(response_tbl))
    n_minority   <- response_tbl[[minority_cls]]
    n_majority   <- min(max_rows - n_minority, response_tbl[[majority_cls]])
    set.seed(config[["random_seed"]] %||% 123L)  # D-10: seed from config
    min_rows <- transition_data[transition_data$response == minority_cls, ]
    maj_rows <- transition_data[transition_data$response == majority_cls, ]
    maj_rows <- maj_rows[sample(nrow(maj_rows), n_majority, replace = FALSE), ]
    transition_data <- rbind(min_rows, maj_rows)
    log_msg(sprintf("  After subsampling: %d rows", nrow(transition_data)), log_file)
  }

  # 2. Subset to predictor columns + response
  task_data <- transition_data[, c(predictor_names, "response"), drop = FALSE]

  # 3. mlr3 Task creation (stratified on response for balanced CV folds)
  task <- mlr3::as_task_classif(task_data, target = "response", positive = "1")
  task$col_roles$stratum <- "response"  # stratified resampling (see RESEARCH §2)
  log_msg(sprintf("  Task created: %d rows, %d features", task$nrow, task$ncol - 1L), log_file)

  # 4. Train one learner per algorithm in model_specs$models
  algo_results <- list()
  trained_learners <- list()

  for (algo in names(model_specs$models)) {
    params <- model_specs$models[[algo]]$parameters
    log_msg(sprintf("  Training %s learner...", algo), log_file)

    lrn_spec <- build_mlr3_learner(algo, params, length(predictor_names))

    # Detect single-value vs multi-value grid (Risk 2 in RESEARCH)
    has_tunable <- any(sapply(params, function(v) length(v) > 1L))

    if (has_tunable) {
      n_combos <- prod(sapply(params, length))
      at <- mlr3tuning::auto_tuner(
        tuner      = mlr3tuning::tnr("grid_search"),
        learner    = lrn_spec,
        resampling = mlr3::rsmp("cv", folds = as.integer(model_specs$global$cv_folds %||% 3L)),
        measure    = mlr3::msr("classif.auc"),
        term_evals = as.integer(n_combos)
      )
      at$train(task)
      lrn_fitted <- at$learner  # inner Learner only — NOT the AutoTuner (Pitfall 1)
    } else {
      # Single-value grid: train directly without AutoTuner overhead (Risk 2)
      lrn_fitted <- lrn_spec$clone(deep = TRUE)  # clone before training (Risk 7)
      lrn_fitted$train(task)
    }

    trained_learners[[algo]] <- lrn_fitted
    log_msg(sprintf("  %s trained", algo), log_file)
  }

  # 5. Select best learner (by classif.auc on a holdout or use first trained)
  # For single-value grids, all learners are comparably trained; pick RF if available,
  # else the first algorithm. In future, add CV-based selection here.
  best_algo <- if ("rf" %in% names(trained_learners)) "rf" else names(trained_learners)[[1L]]
  lrn_final <- trained_learners[[best_algo]]
  log_msg(sprintf("  Selected learner: %s (algo=%s)", class(lrn_final)[[1L]], best_algo), log_file)

  # 6. Build save object (D-05 contract)
  model_obj <- list(
    model_type      = "mlr3",
    predictor_names = task$feature_names,  # Task's view is authoritative (Risk 5)
    response_levels = task$class_names,
    learner         = lrn_final
  )

  # 7. Save
  ensure_dir(dirname(output_path))
  qs::qsave(model_obj, output_path)
  log_msg(sprintf("  Saved model: %s", output_path), log_file)

  # 8. Size gate (D-12) — warn only, do not stop
  size_bytes <- file.size(output_path)
  if (size_bytes > 200 * 1024^2) {
    log_msg(sprintf(
      "WARNING: model file exceeds 200MB: %.1f MB — %s",
      size_bytes / 1024^2, output_path
    ), log_file)
  } else {
    log_msg(sprintf("  Size OK: %.1f MB", size_bytes / 1024^2), log_file)
  }

  # 9. Predict sanity check (D-13): 5-row predict, assert [0,1] non-NA
  model_check <- qs::qread(output_path)
  fixture_rows <- utils::head(transition_data[, predictor_names, drop = FALSE], 5L)
  pred_check <- tryCatch(
    model_check$learner$predict_newdata(newdata = fixture_rows),
    error = function(e) {
      log_msg(sprintf("ERROR: sanity predict_newdata() failed: %s", conditionMessage(e)), log_file)
      stop(sprintf("Sanity check predict_newdata() failed: %s", conditionMessage(e)))
    }
  )
  prob_check <- pred_check$prob[, "1"]  # always by name, not position (Risk 6, Pitfall 2)
  if (any(is.na(prob_check)) || any(prob_check < 0) || any(prob_check > 1)) {
    log_msg(sprintf(
      "ERROR: 5-row sanity check failed for %s — probs: %s",
      output_path, paste(round(prob_check, 4), collapse = ", ")
    ), log_file)
    stop(sprintf("Sanity check failed: probabilities not in [0,1] or contain NA: %s", output_path))
  }
  log_msg(sprintf("  Sanity check passed (5-row predict OK)", ), log_file)

  # 10. Return result compatible with perform_transition_modelling() aggregation
  list(
    transition  = trans_name,
    region      = ifelse(is.null(region), "National extent", region),
    status      = "success",
    model_path  = output_path,
    cv_metrics  = NULL,
    test_metrics = NULL,
    final_model = list(
      model_path   = output_path,
      model_type   = "mlr3",
      size_bytes   = size_bytes,
      n_predictors = length(predictor_names),
      algo         = best_algo
    )
  )
}
```

After all edits, commit as: `feat(02-02): replace tidymodels inner stack with mlr3 in transition_modelling.r (D-01–D-13, MEM-04)`
  </action>
  <verify>
    <automated>
    grep -c "train_mlr3_transition" src/transition_modelling.r
    grep -c ".qs" src/transition_modelling.r
    grep -v "^#" src/transition_modelling.r | grep -c "library(tidymodels)"
    </automated>
  </verify>
  <acceptance_criteria>
    - src/transition_modelling.r contains `train_mlr3_transition(` (grep -c returns >= 2: function definition + call site)
    - src/transition_modelling.r contains `build_mlr3_learner(` (function definition)
    - src/transition_modelling.r contains `.qs` (the new extension in build_transition_model_path)
    - src/transition_modelling.r does NOT contain `library(tidymodels)` outside comments (grep -v "^#" | grep -c "library(tidymodels)" returns 0)
    - src/transition_modelling.r does NOT contain `library(workflows)` outside comments
    - src/transition_modelling.r does NOT contain `library(parsnip)` outside comments
    - src/transition_modelling.r contains `qs::qsave` (the save call)
    - src/transition_modelling.r contains `save.memory = TRUE` (ranger memory flag)
    - src/transition_modelling.r contains `importance = "none"` (ranger importance flag)
    - src/transition_modelling.r contains `200 * 1024^2` (size gate threshold)
    - src/transition_modelling.r contains `predict_newdata` (sanity check)
    - src/transition_modelling.r contains `model_type.*=.*"mlr3"` (in save object)
    - src/transition_modelling.r contains a comment about `step_normalize` not being required for classif.glmnet
    - config/model_specs.yaml contains `num.trees:` (mlr3 ranger param name)
    - config/model_specs.yaml contains `nrounds:` (mlr3 xgboost param name)
    - config/model_specs.yaml does NOT contain `trees:` under rf (old tidymodels name)
    - config/model_specs.yaml does NOT contain `learn_rate:` (old tidymodels name)
    - config/model_specs.yaml contains a comment marking `num_replicates` as superseded by AutoTuner CV folds
    - The 4 tests in tests/testthat/test-mlr3-training-pipeline.R are GREEN (Tests 1-3 pass via grep; Test 4 passes if mlr3 packages are installed)
  </acceptance_criteria>
  <done>transition_modelling.r has train_mlr3_transition() and build_mlr3_learner() functions; build_transition_model_path() returns .qs; model_specs.yaml uses mlr3 parameter names; all 4 pipeline tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config YAML → training code | max_training_rows and random_seed from config control subsampling; YAML values are operator-controlled, not user-controlled |
| output_path from config → qs::qsave() | File path for model output comes from config[["transition_model_dir"]]; path injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | output_path in train_mlr3_transition() | mitigate | Validate that output_path is within the configured transition_model_dir before calling qs::qsave(): `if (!startsWith(normalizePath(output_path, mustWork=FALSE), normalizePath(config[["transition_model_dir"]]))) stop("output_path outside model dir")` (ASVS L1: file path injection) |
| T-02-04 | Denial of Service | subsampling fallback | accept | max_training_rows is a positive integer from operator-controlled YAML; no user input reaches it; %||% default of 500000L ensures a safe ceiling even if key is missing |
| T-02-05 | Information Disclosure | qs::qsave() model file | accept | Model files contain only trained model parameters (not PII or secrets); they are written to the operator-configured model output directory |
</threat_model>

<verification>
After both tasks complete, run:

```bash
grep -c "train_mlr3_transition" src/transition_modelling.r
grep -c "save.memory = TRUE" src/transition_modelling.r
grep -c "200 \* 1024\^2" src/transition_modelling.r
grep -c "predict_newdata" src/transition_modelling.r
grep -v "^#" src/transition_modelling.r | grep -c "library(tidymodels)"
grep -c "num.trees" config/model_specs.yaml
grep -c "nrounds" config/model_specs.yaml
grep -c "Superseded by AutoTuner" config/model_specs.yaml
grep -c "step_normalize" src/transition_modelling.r
```

Expected: first four commands return >= 1; fifth returns 0; next two return >= 1; last two return >= 1.
</verification>

<success_criteria>
- src/transition_modelling.r contains train_mlr3_transition() and build_mlr3_learner() function definitions
- build_transition_model_path() returns .qs extension (not .rds)
- model_single_transition() inner call dispatches to train_mlr3_transition() (not multi_spec_trans_modelling())
- config/model_specs.yaml uses mlr3 parameter names throughout (num.trees, nrounds, eta, max_depth, min_child_weight)
- config/model_specs.yaml num_replicates key has a comment marking it as superseded by AutoTuner CV folds
- 5-row predict sanity check is inside train_mlr3_transition() (D-13)
- Size gate is inside train_mlr3_transition() (D-12)
- Stratified subsampling is before Task creation (D-09/D-10)
- ranger Learner has save.memory=TRUE and importance="none" hardcoded
- build_mlr3_learner() glm branch contains a comment that step_normalize() is not required (classif.glmnet is scale-invariant)
- colsample_bytree conversion from integer mtry is documented in XGBoost section comment
- All changes committed with TDD RED->GREEN commit pair
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-size-reduction/02-02-SUMMARY.md`
</output>
