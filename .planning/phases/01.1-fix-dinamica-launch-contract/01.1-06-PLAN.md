---
phase: 01.1-fix-dinamica-launch-contract
plan: 06
type: execute
wave: 2
depends_on:
  - 01.1-05
files_modified:
  - dinamica/container/rocker-geospatial-dinamica.def
  - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md
  - dinamica/container/README.md
  - docs/README_HPC.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements:
  - INFRA-01
  - MEM-06
user_setup:
  - service: Euler HPC
    why: "The rebuilt .sif must be built on Euler and the live --live smoke test must run on Euler — Apptainer + DinamicaConsole are Linux-only and the 1018MB .sif is impractical to ship to the workstation."
    env_vars:
      - name: DINAMICA_EGO_8_HOME
        source: "Operator's existing Euler env: absolute path to the .sif location (rebuild will OVERWRITE the existing one)"
      - name: HPC_SCRATCH_ROOT
        source: "Operator's existing Euler env"
      - name: APPTAINER_TMPDIR
        source: "Operator's Euler env: $HPC_SCRATCH_ROOT/apptainer-tmp (home quota too small for build temp)"
      - name: APPTAINER_CACHEDIR
        source: "Operator's Euler env: $HPC_SCRATCH_ROOT/apptainer-cache (same reason)"
    dashboard_config:
      - task: "Operator rebuilds dinamica-ego-8.sif on Euler from the fixed .def and runs the live smoke test"
        location: "ETH Euler: srun --pty bash, then `apptainer build` + `scripts/smoke_test_dinamica.sh --live`"

must_haves:
  truths:
    - "dinamica/container/rocker-geospatial-dinamica.def is modified per .planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md's Proposed Fix for Plan 06 — the specific edit recommended by the hypothesis-ranked synthesis lands verbatim (or with a documented justified deviation)."
    - "The rebuilt .sif on Euler accepts dinamica/dinamica_model/smoketest.ego — `bash scripts/smoke_test_dinamica.sh --live --runtime auto --artifact \"$DINAMICA_EGO_8_HOME\" --ego dinamica/dinamica_model/smoketest.ego --require-log-under logs` exits 0."
    - "The dinamica-smoke-<ts>.log produced by the live smoke test does NOT contain any of the three D-107 error patterns (Dinamica EGO exited with an error / terminate called after throwing / std::exception)."
    - "INFRA-01 SC2 (live `--live` smoke exits 0) is verified and marked Complete in .planning/REQUIREMENTS.md."
    - "MEM-06 SC5 (smoke fixture exercises launcher end-to-end) is verified and marked Complete in .planning/REQUIREMENTS.md."
    - "01.1-03-SUMMARY.md Open Issue 1 is closed with a resolution note citing 01.1-06-SUMMARY.md and the specific fix that worked."
    - "Both READMEs' runtime-caveat callouts (`dinamica/container/README.md` and `docs/README_HPC.md`) are updated to reflect Open Issue 1 RESOLVED — the caveats are either removed or rewritten to reference historical context only."
  artifacts:
    - path: "dinamica/container/rocker-geospatial-dinamica.def"
      provides: "Apptainer build recipe that produces a .sif in which DinamicaConsole accepts smoketest.ego (closes Open Issue 1)"
      contains: "Bootstrap: docker"
    - path: ".planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md"
      provides: "Updated Open Issue 1 section marking the issue RESOLVED with a backreference to 01.1-06-SUMMARY.md"
      contains: "RESOLVED"
    - path: "dinamica/container/README.md"
      provides: "Updated runtime caveat — Open Issue 1 resolved"
      contains: "Open Issue 1"
    - path: "docs/README_HPC.md"
      provides: "Updated runtime caveat — Open Issue 1 resolved"
      contains: "Open Issue 1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INFRA-01 and MEM-06 marked Complete with backreference to 01.1-06-SUMMARY.md"
      contains: "01.1-06"
  key_links:
    - from: "dinamica/container/rocker-geospatial-dinamica.def %post (modified per FINDINGS.md)"
      to: "rebuilt dinamica-ego-8.sif on Euler"
      via: "apptainer build dinamica-ego-8.sif dinamica/container/rocker-geospatial-dinamica.def"
      pattern: "apptainer build"
    - from: "rebuilt dinamica-ego-8.sif"
      to: "scripts/smoke_test_dinamica.sh --live exit 0"
      via: "apptainer exec ... bin/DinamicaEGO.sh smoketest.ego"
      pattern: "smoketest.ego"
    - from: ".planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md Proposed Fix"
      to: "dinamica/container/rocker-geospatial-dinamica.def %post edit"
      via: "Plan 06 Task 1 reads FINDINGS.md and writes the edit"
      pattern: "FINDINGS.md|Proposed Fix"
---

<objective>
Close Open Issue 1 from 01.1-03-SUMMARY.md by applying the targeted fix that Plan 05's diagnostic ladder identified, rebuilding the `.sif` on Euler, and verifying the live `--live` smoke test exits 0 against `dinamica/dinamica_model/smoketest.ego`. Once verified, update `01.1-03-SUMMARY.md` Open Issue 1 to RESOLVED, refresh the runtime caveat callouts in both READMEs, and mark INFRA-01 SC2 + MEM-06 SC5 Complete in `.planning/REQUIREMENTS.md`.

Purpose: This is the gap-closure terminus for Phase 1.1. After this plan, Phase 1.1 is fully complete — the launch contract works end-to-end and the documented runtime gap is gone.

Output: One edited `.def`, one rebuilt `.sif` on Euler, one passing live smoke test, three updated documentation files (`01.1-03-SUMMARY.md`, `dinamica/container/README.md`, `docs/README_HPC.md`), and an updated requirements traceability table in `.planning/REQUIREMENTS.md`.

Body shape note: The Task 1 edit is DERIVED FROM `.planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md` (Plan 05 Task 3 output). The Task 1 action below describes the GENERIC application procedure — "apply the Proposed Fix for Plan 06 section verbatim, with documented justification if any deviation". The exact edit cannot be encoded in this PLAN at planning time because it depends on Plan 05's evidence; the executor receives the concrete edit by reading FINDINGS.md as the first step.
</objective>

<execution_context>
@C:/Users/bblack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-04-SUMMARY.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-VERIFICATION.md
@.planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/README.md
@.planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md
@dinamica/container/rocker-geospatial-dinamica.def
@dinamica/container/README.md
@docs/README_HPC.md
@scripts/smoke_test_dinamica.sh

<interfaces>
<!-- Key contracts the executor must respect. -->

CONSTRAINT (from CONTEXT.md D-101, D-102, D-103): The fix MUST NOT change the bootstrap base image (`rocker/r-ver:4.5.3`) or the AppImage URL (`https://dinamicaego.com/nui_download/1960/`) UNLESS Plan 05's FINDINGS.md specifically implicates them. The 2026-05-17 ldd diagnostic FALSIFIED the library-compat hypothesis — base-image swap is OUT of the prior-probable fix set. Verify against FINDINGS.md before any base-image change.

CONSTRAINT (from CONTEXT.md D-104, D-105, D-106): The R-side `resolve_dinamica_launch()` HPC branch MUST remain in the apptainer-exec-bash-c shape with --home / --env DINAMICA_EGO_8_TEMP_DIR / absolute model path. Plan 06 does NOT modify `src/dinamica_utils.r` unless FINDINGS.md's Proposed Fix specifically touches the conf-seed block at lines ~270-285 (Hypothesis 4 case).

CONSTRAINT (from CONTEXT.md DD-2 and 01.1-03-SUMMARY): The production `dinamica/dinamica_model/allocation.ego-decoded` is UNCHANGED. The smoke fixture is `dinamica/dinamica_model/smoketest.ego` (committed 741 bytes). Do NOT regenerate either.

ACCEPTANCE COMMAND (from CONTEXT.md and 01.1-VERIFICATION.md missing items, both gaps):
```
bash scripts/smoke_test_dinamica.sh --live --runtime auto \
    --artifact "$DINAMICA_EGO_8_HOME" \
    --ego dinamica/dinamica_model/smoketest.ego \
    --require-log-under logs
```
Expected exit: 0. The captured `logs/dinamica-smoke-<ts>.log` must contain Dinamica's startup banner AND must NOT contain any of:
- `Dinamica EGO exited with an error`
- `terminate called after throwing`
- `std::exception`

EXISTING SMOKE TEST SHAPE (scripts/smoke_test_dinamica.sh — DO NOT MODIFY):
- `--live` mode requires HPC_SCRATCH_ROOT (line 191-193 guard, per D-105).
- LAUNCH_CMD at line 313 already uses D-104 shape with --home + --env DINAMICA_EGO_8_TEMP_DIR.
- D-107 grep at lines 362-368 produces exit code 5 on any of the three error patterns.
- The smoke test will exit 5 if Open Issue 1 is NOT yet fixed (current state) and 0 once it IS.

DEFFERRED-AT-PHASE-CLOSE TASKS (from 01.1-04-SUMMARY hand-off paragraph) — these are operator gates Plan 06 does NOT have to close, but the resume signal should note their status:
- Live env-solve test on fresh Euler scratch: `bash scripts/setup_environments.sh --env allocation_env --non-interactive` (separate gate, not blocked by Open Issue 1).
- SLURM OOM probe: `allocation worker survives the configured --mem budget` (Phase 3 work, not Phase 1.1 closure).

OPERATOR REBUILD COMMAND (from 01.1-03-SUMMARY Task 3, lines 86-92):
```bash
# Operator runs on Euler inside an srun allocation
export APPTAINER_TMPDIR="$HPC_SCRATCH_ROOT/apptainer-tmp"
export APPTAINER_CACHEDIR="$HPC_SCRATCH_ROOT/apptainer-cache"
mkdir -p "$APPTAINER_TMPDIR" "$APPTAINER_CACHEDIR"
cd "$REPO_ROOT"
apptainer build --force dinamica-ego-8.sif dinamica/container/rocker-geospatial-dinamica.def
mv dinamica-ego-8.sif "$DINAMICA_EGO_8_HOME"  # overwrites existing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the Proposed Fix from diagnostics/FINDINGS.md to dinamica/container/rocker-geospatial-dinamica.def</name>
  <files>dinamica/container/rocker-geospatial-dinamica.def</files>
  <read_first>
    - .planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md (the entire file — the "Proposed Fix for Plan 06" section is the executable specification for this task)
    - .planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/README.md (the ladder context)
    - .planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/ls-data-tree.log + strings-dinamica-env-vars.log + strace-openat.log + extract-diff.log (the evidence — read them to confirm the fix is grounded; if any contradicts FINDINGS.md, flag in the deviation report)
    - dinamica/container/rocker-geospatial-dinamica.def (current 153-line file — Task 1's edit applies HERE; preserve everything FINDINGS.md does not specifically touch)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md (D-101 through D-114 — constraints on what may NOT change without explicit FINDINGS.md justification)
  </read_first>
  <action>
    Open `.planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md` and locate the "Proposed Fix for Plan 06" section. Apply the named edit to `dinamica/container/rocker-geospatial-dinamica.def` VERBATIM with the following discipline:

    1. **If the Proposed Fix specifies an edit inside `%post`** (most likely — Hypotheses 1, 3, 4 all live there):
       - Apply the edit at the line range FINDINGS.md cites.
       - Do NOT touch unrelated `%post` lines.
       - Preserve the `set -euo pipefail` + `DEBIAN_FRONTEND=noninteractive` discipline at the top.
       - Preserve the comment-block stage headers (`# Stage 1`, `# Stage 2`, etc.) for readability.

    2. **If the Proposed Fix specifies an addition to `%environment`** (Hypothesis 2 — missing DINAMICA_EGO_8_* var):
       - Add the new export line in the `%environment` block (currently lines 112-118 of the .def).
       - Preserve the existing four exports (LD_LIBRARY_PATH, PATH, DINAMICA_EGO_8_INSTALLATION_DIRECTORY, DINAMICA_EGO_8_HOME_DIR).
       - Preserve the comment "NOTE: DINAMICA_EGO_8_TEMP_DIR is NOT exported here — apptainer --env overrides it at runtime per D-105".

    3. **If the Proposed Fix specifies a change to the conf-seed block** (Hypothesis 4):
       - Add the new key/value to the heredoc at `.def` lines 99-104.
       - ALSO add the same key/value to the runtime conf seeder in `src/dinamica_utils.r:resolve_dinamica_launch()` HPC branch (the `writeLines(...)` block — read the file with grep to find the exact line range first; reuse the same four-key block shape).

    4. **If the Proposed Fix specifies a base-image swap** (only legitimate if FINDINGS.md hypothesis 1 evidence specifically implicates a Noble vs Jammy library version that the AppImage assumes — the 2026-05-17 ldd diagnostic FALSIFIED bulk library compat):
       - Update the `From:` line.
       - Update the provenance comment block citing the new base.
       - Update `dinamica/container/README.md` provenance section.
       - Document the deviation justification in the Task 1 commit message AND in the eventual SUMMARY.
       - **Default expectation: this branch will NOT trigger; if FINDINGS.md is in INCONCLUSIVE state, Plan 06 escalates per the rollback note rather than swapping base images speculatively.**

    5. **If FINDINGS.md is in INCONCLUSIVE state for all four hypotheses:**
       - Do NOT speculatively modify the .def. Instead, write a one-paragraph "Escalation Note" at the END of `dinamica/container/rocker-geospatial-dinamica.def` as a block comment, summarising the evidence and pointing at the upstream `ethzplus/rocker-geospatial-dinamica` issue tracker as the next escalation path.
       - Skip Task 2 (operator rebuild + live smoke) — escalation does not produce a verifiable fix.
       - Document the inconclusive state clearly so future replanning can pick this up.

    Provenance: add a single-line block comment above the modified region in the .def: `# Phase 1.1 gap-closure (Plan 06): <one-line description of the fix> — see diagnostics/FINDINGS.md.`

    Anti-patterns to avoid:
    - Do NOT change the `Bootstrap: docker` line.
    - Do NOT remove the `%post -c /bin/bash` interpreter selection (necessary on Noble per 13b4590 commit; .def comment block lines 60-65 explains).
    - Do NOT change the `%test` block's POSIX-sh constraint (necessary on Euler Apptainer per 84fee65 commit; .def comment block lines 121-125 explains).
    - Do NOT delete the `Phase 1.1 — Rewrite per D-101, D-102, D-103.` header comment block.
    - Do NOT remove the `%labels` block.
    - Do NOT add a `%runscript` block (anti-pattern per 01.1-03-PLAN.md Task 1 note — apptainer-exec passes the bash -c launcher explicitly per D-104).

    If the executor finds that FINDINGS.md's Proposed Fix conflicts with any of the above anti-patterns, STOP and add an escalation note in the SUMMARY rather than violating the existing CONTEXT.md decisions.
  </action>
  <verify>
    <automated>test -f dinamica/container/rocker-geospatial-dinamica.def && grep -c 'Bootstrap: docker' dinamica/container/rocker-geospatial-dinamica.def | grep -q '^1$' && grep -c '%runscript' dinamica/container/rocker-geospatial-dinamica.def | grep -q '^0$' && grep -c 'Phase 1.1 gap-closure' dinamica/container/rocker-geospatial-dinamica.def | grep -qE '^[1-9]' && grep -c 'D-101\|D-102\|D-103' dinamica/container/rocker-geospatial-dinamica.def | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dinamica/container/rocker-geospatial-dinamica.def` passes.
    - `grep -c 'Bootstrap: docker' dinamica/container/rocker-geospatial-dinamica.def` returns 1 (unchanged from Plan 03 unless FINDINGS.md mandates base swap).
    - `grep -c '%runscript' dinamica/container/rocker-geospatial-dinamica.def` returns 0 (anti-pattern preserved).
    - `grep -c '%post' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (the block still exists).
    - `grep -c '%environment' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (the block still exists).
    - `grep -c '%test' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (the block still exists).
    - `grep -c 'Phase 1.1 gap-closure' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (provenance for the new edit).
    - `grep -c 'diagnostics/FINDINGS' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (the new comment cites the source of the fix).
    - `grep -cE 'D-101|D-102|D-103|D-104|D-105' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (existing CONTEXT.md decisions still cited).
    - `grep -c 'DINAMICA_EGO_8_INSTALLATION_DIRECTORY' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (existing %environment export preserved).
    - `grep -c '.dinamica_ego_8.conf' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (existing conf-seed reference preserved).
    - `wc -l dinamica/container/rocker-geospatial-dinamica.def` returns at least 130 (existing structure largely preserved; small fix should not gut the file).
  </acceptance_criteria>
  <done>
    `dinamica/container/rocker-geospatial-dinamica.def` is modified per FINDINGS.md's Proposed Fix for Plan 06. The CONTEXT.md anti-patterns (no %runscript, preserve %post -c /bin/bash, preserve POSIX-sh %test) are all still respected. A provenance comment block names the gap-closure plan and cites FINDINGS.md. The R-side `src/dinamica_utils.r` is touched ONLY if FINDINGS.md hypothesis 4 (conf-key missing) is the chosen fix.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator rebuilds dinamica-ego-8.sif on Euler and runs the live smoke test against smoketest.ego</name>
  <what-built>
    Task 1 modified `dinamica/container/rocker-geospatial-dinamica.def` with the fix Plan 05's FINDINGS.md identified. The `.sif` artifact must now be rebuilt on Euler (the 1018MB .sif is not transferable to the Windows workstation), and the live `--live` smoke test must run to verify exit 0.

    This is a `checkpoint:human-action` task because: (a) Apptainer is not on Windows; (b) the `.sif` build downloads ~700MB of AppImage + ~hundreds of MB of apt + R deps and writes the 1018MB output to scratch — the workstation cannot host this; (c) DinamicaConsole is Linux-only.
  </what-built>
  <how-to-verify>
    Operator runs the following on Euler (inside a `srun --pty bash` allocation with sufficient memory for the build, typically `srun --time=01:00:00 --mem=8G --pty bash`):

    1. **Pre-flight** — ensure the working tree on Euler has Task 1's `.def` changes (`git pull origin <branch>` if Task 1 was committed; otherwise `scp` the modified `.def`).
       ```bash
       cd "$REPO_ROOT"
       git log --oneline -5 dinamica/container/rocker-geospatial-dinamica.def
       # First commit should be Task 1's commit ("fix(01.1-06): ...")
       ```

    2. **Rebuild the .sif** with the standard APPTAINER_TMPDIR / APPTAINER_CACHEDIR routing:
       ```bash
       export APPTAINER_TMPDIR="$HPC_SCRATCH_ROOT/apptainer-tmp"
       export APPTAINER_CACHEDIR="$HPC_SCRATCH_ROOT/apptainer-cache"
       mkdir -p "$APPTAINER_TMPDIR" "$APPTAINER_CACHEDIR"

       apptainer build --force dinamica-ego-8.sif dinamica/container/rocker-geospatial-dinamica.def 2>&1 | tee logs/sif-build-01.1-06.log
       ```
       Expected: build exits 0; new `dinamica-ego-8.sif` is ~1 GB (size may shift ±100 MB depending on the fix); `logs/sif-build-01.1-06.log` captures the full build for posterity.

    3. **Replace the existing .sif** at `$DINAMICA_EGO_8_HOME`:
       ```bash
       echo "DINAMICA_EGO_8_HOME=$DINAMICA_EGO_8_HOME"
       ls -la "$DINAMICA_EGO_8_HOME"            # current .sif (will be replaced)
       mv dinamica-ego-8.sif "$DINAMICA_EGO_8_HOME"
       ls -la "$DINAMICA_EGO_8_HOME"            # new .sif in place
       ```

    4. **Run the live smoke test** (the canonical acceptance command from VERIFICATION.md missing items):
       ```bash
       source .env  # ensure DINAMICA_EGO_8_HOME + HPC_SCRATCH_ROOT are in env
       bash scripts/smoke_test_dinamica.sh --live --runtime auto \
           --artifact "$DINAMICA_EGO_8_HOME" \
           --ego dinamica/dinamica_model/smoketest.ego \
           --require-log-under logs
       echo "Smoke test exit code: $?"
       ```
       Expected: `Smoke test exit code: 0`. The `logs/dinamica-smoke-<ts>.log` should contain Dinamica's startup banner and the message from `smoketest.ego-decoded`'s CalculateRExpression block (`message(Sys.time(), ' - phase 1.1 smoke fixture: launch contract ok')` or similar — read `dinamica/dinamica_model/smoketest.ego-decoded` for the exact string).

    5. **Verify the D-107 grep finds nothing** (confirms no error-pattern false-negatives):
       ```bash
       LATEST=$(ls -1t logs/dinamica-smoke-*.log | head -n 1)
       echo "Inspecting: $LATEST"
       grep -cE 'Dinamica EGO exited with an error|terminate called after throwing|std::exception' "$LATEST"
       # Expected: 0
       ```

    6. **(Optional) Smoke-test the production allocation fixture** to confirm the production path is not collaterally broken:
       ```bash
       bash scripts/smoke_test_dinamica.sh --live --runtime auto \
           --artifact "$DINAMICA_EGO_8_HOME" \
           --ego dinamica/dinamica_model/allocation.ego-decoded \
           --require-log-under logs
       ```
       Expected: also exit 0 (or if `allocation.ego-decoded` legitimately requires inputs that aren't available in this smoke context, document the expected failure mode in the resume signal).

    **If step 4 fails (smoke exits 5 — D-107 pattern caught):** Plan 06's fix did not resolve Open Issue 1. The operator should:
    - Capture the `logs/dinamica-smoke-<ts>.log` content in the resume signal.
    - Re-run the diagnostic ladder from Plan 05 (`.planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/README.md`) inside the NEW .sif and overwrite the evidence files.
    - Re-trigger Plan 05 Task 3 to update FINDINGS.md with the post-Plan-06 evidence.
    - Plan 06 then re-runs from Task 1 with the updated FINDINGS.md.

    **If step 4 fails with a DIFFERENT error** (build failure, or new error pattern not in D-107's list): capture in the resume signal, flag for a follow-up plan (Plan 07).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
      - "approved | smoke exit 0 | log clean" — .sif rebuilt cleanly, live smoke exits 0, D-107 grep finds nothing in the dinamica-smoke log. Open Issue 1 CLOSED.
      - "build-failed: <error>" — `apptainer build` failed; capture the build-log tail. Task 1 fix may need revision.
      - "smoke-exit-5: <log-tail>" — build OK but smoke still hits a D-107 pattern; capture log tail; Plan 06 re-iterates per the "If step 4 fails" branch above.
      - "smoke-other-error: <details>" — build OK, smoke exits non-zero non-5 (e.g. exit 1/2/3/4); capture details; may need a follow-up plan.
      - "partial: production-fixture-failed" — smoketest.ego works (gap 1 + 2 closed), but the optional production-fixture probe in step 6 failed; document and decide whether this blocks closure (likely no — the production fixture has input dependencies the smoke context lacks).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update Open Issue 1 in 01.1-03-SUMMARY.md and refresh runtime-caveat callouts in both READMEs + REQUIREMENTS.md traceability</name>
  <files>
    .planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md
    dinamica/container/README.md
    docs/README_HPC.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md (Open Issue 1 section — lines 104-138 — the section to update with RESOLVED status)
    - .planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md (proposed fix)
    - dinamica/container/README.md (find the existing "runtime caveat" callout at the top — Plan 04 placed it there per 01.1-04-SUMMARY.md)
    - docs/README_HPC.md (find the existing runtime caveat — Plan 04 placed it in the "Recent Changes (Phase 1.1)" section per 01.1-04-SUMMARY.md)
    - .planning/REQUIREMENTS.md (lines 22 + 32 + 81 + 85 — the four INFRA-01 / MEM-06 traceability lines to update)
    - The resume signal from Task 2 (the actual fix outcome — referenced explicitly in the SUMMARY update)
  </read_first>
  <action>
    Only run this task if Task 2's resume signal was "approved | smoke exit 0 | log clean". Otherwise, skip to the deviation report and DO NOT mark anything Complete.

    **1. Update `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md`:**

    Find the "Open Issue 1" section header (line 106). Insert a new sub-section at the END of the Open Issue 1 block (before the next `###` heading) titled `### Resolution (2026-MM-DD via Plan 01.1-06)`:

    ```markdown
    ### Resolution (2026-MM-DD via Plan 01.1-06)

    **Status:** RESOLVED.

    **Root cause:** <one-paragraph description from FINDINGS.md — which of the four hypotheses was confirmed and what the underlying defect was>.

    **Fix:** Modified `dinamica/container/rocker-geospatial-dinamica.def` <line range> to <one-sentence description of the edit Task 1 made>. See `.planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md` "Proposed Fix for Plan 06" for the analytical chain; see `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-06-SUMMARY.md` for the implementation + verification record.

    **Verification:** Operator rebuilt `dinamica-ego-8.sif` on Euler and ran `bash scripts/smoke_test_dinamica.sh --live --runtime auto --artifact "$DINAMICA_EGO_8_HOME" --ego dinamica/dinamica_model/smoketest.ego --require-log-under logs` — exit 0; D-107 grep against the captured log returned 0 matches. INFRA-01 SC2 and MEM-06 SC5 are now CLOSED.

    **Phase 1.1 status:** All seven ROADMAP success criteria now CLOSED. Phase 1.1 fully complete.
    ```

    Replace `2026-MM-DD` with today's actual date (the date Task 2 completed). Replace the angle-bracketed placeholders with the actual content from FINDINGS.md and the Task 2 resume signal.

    **2. Update `dinamica/container/README.md`:**

    Find the runtime-caveat callout at the top of the file (Plan 04 placed it there citing 01.1-03-SUMMARY Open Issue 1). Replace the caveat with a resolution note:

    Before:
    ```markdown
    > **Runtime caveat:** The rebuilt `.sif` builds cleanly, but the live `--live` smoke test does NOT yet exit 0 — DinamicaConsole crashes with `std::exception` inside the rebuilt `.sif` regardless of `.ego` content. The D-107 grep correctly catches the crash with exit 5. See `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md` Open Issue 1 for the diagnostic detail and the gap-closure path.
    ```

    After:
    ```markdown
    > **Runtime status (Phase 1.1 gap-closure landed 2026-MM-DD):** The rebuilt `.sif` builds cleanly AND the live `--live` smoke test exits 0 against `dinamica/dinamica_model/smoketest.ego`. The Open Issue 1 from Plan 01.1-03 (DinamicaConsole `std::exception` under rocker/r-ver:4.5.3 Noble base) was resolved by Plan 01.1-06 — see `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-06-SUMMARY.md` for the fix detail and `.planning/phases/01.1-fix-dinamica-launch-contract/diagnostics/FINDINGS.md` for the diagnostic chain.
    ```

    Also locate any other "live smoke does not yet exit 0" references inside the file (e.g. inside the "Verifying a built image" section per 01.1-04-SUMMARY.md) and update them in the same shape.

    **3. Update `docs/README_HPC.md`:**

    Find the "Recent Changes (Phase 1.1)" section near the top (Plan 04 placed it there). Replace the runtime caveat inside it with the resolution-note shape (same wording template as the container README update above, adapted for the README_HPC context). Also locate any per-section caveat repetitions inside the smoke-test live command section and update them.

    **4. Update `.planning/REQUIREMENTS.md`:**

    - Line 22: change `- [ ] **MEM-06**: ...` to `- [x] **MEM-06**: ...` and append ` *(closed Phase 1.1 / Plan 01.1-06: live --live smoke against dinamica/dinamica_model/smoketest.ego exits 0 on rebuilt .sif; Open Issue 1 resolved per .planning/phases/01.1-fix-dinamica-launch-contract/01.1-06-SUMMARY.md)*` to the description.
    - Line 32: change `- [ ] **INFRA-01**: ...` to `- [x] **INFRA-01**: ...` with the same trailing closure annotation.
    - Line 81 (MEM-06 traceability row): change "Partial (...)" to "Complete (closed Phase 1.1 / 01.1-06; live --live smoke exit 0)".
    - Line 85 (INFRA-01 traceability row): change "Partial (...)" to "Complete (closed Phase 1.1 / 01.1-06; all seven SCs verified)".
    - Line 107 (Last updated): update the date and the closure summary to: `*Last updated: 2026-MM-DD — Phase 1.1 fully closed by gap-closure Plan 01.1-06; INFRA-01 + MEM-06 marked Complete (Open Issue 1 resolved)*`.

    Replace `2026-MM-DD` with the actual completion date.

    Self-check: after all four file edits, the project state should reflect Phase 1.1 fully complete. `grep -c 'Open Issue 1' dinamica/container/README.md docs/README_HPC.md` should now show the resolution note (mentioning "Open Issue 1" in the closing context), and `grep -cE 'INFRA-01.*Partial|MEM-06.*Partial' .planning/REQUIREMENTS.md` should return 0.
  </action>
  <verify>
    <automated>grep -c 'RESOLVED' .planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md | grep -qE '^[1-9]' && grep -c '01.1-06' .planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md | grep -qE '^[1-9]' && grep -c '01.1-06-SUMMARY' dinamica/container/README.md | grep -qE '^[1-9]' && grep -c '01.1-06-SUMMARY' docs/README_HPC.md | grep -qE '^[1-9]' && grep -c -- '- \[x\] \*\*INFRA-01\*\*' .planning/REQUIREMENTS.md | grep -q '^1$' && grep -c -- '- \[x\] \*\*MEM-06\*\*' .planning/REQUIREMENTS.md | grep -q '^1$' && grep -cE 'INFRA-01.*Partial|MEM-06.*Partial' .planning/REQUIREMENTS.md | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Resolution.*Plan 01.1-06\|RESOLVED' .planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md` returns at least 1 (the new sub-section).
    - `grep -c '01.1-06-SUMMARY' .planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md` returns at least 1 (backreference to the SUMMARY file this plan will write).
    - `grep -c '01.1-06-SUMMARY' dinamica/container/README.md` returns at least 1 (caveat resolution note cites the SUMMARY).
    - `grep -c '01.1-06-SUMMARY' docs/README_HPC.md` returns at least 1 (caveat resolution note cites the SUMMARY).
    - `grep -c 'live --live smoke does NOT yet exit 0\|does not yet exit 0' dinamica/container/README.md docs/README_HPC.md` returns 0 (the "not yet" wording is gone from both READMEs).
    - `grep -cE '^- \[x\] \*\*INFRA-01\*\*' .planning/REQUIREMENTS.md` returns 1 (marked Complete).
    - `grep -cE '^- \[x\] \*\*MEM-06\*\*' .planning/REQUIREMENTS.md` returns 1 (marked Complete).
    - `grep -cE 'INFRA-01.*Partial|MEM-06.*Partial' .planning/REQUIREMENTS.md` returns 0 (the Partial annotations are replaced with Complete).
    - `grep -c '01.1-06' .planning/REQUIREMENTS.md` returns at least 2 (both INFRA-01 and MEM-06 traceability rows cite the closing plan).
  </acceptance_criteria>
  <done>
    Open Issue 1 in 01.1-03-SUMMARY.md is marked RESOLVED with a dated Resolution sub-section citing Plan 01.1-06 and FINDINGS.md. Both READMEs' runtime caveats are updated to resolution-state. INFRA-01 and MEM-06 are marked Complete in REQUIREMENTS.md with closure citations. Phase 1.1 traceability is fully consistent — no "Partial" or "deferred to gap-closure" annotations remain for INFRA-01 / MEM-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation → Euler login node | Operator runs the rebuild and smoke test |
| Apptainer build host → dinamicaego.com | AppImage re-download during %post (same URL as Plan 03) |
| Rebuilt .sif → DinamicaConsole startup | The fix surface — if Task 1's edit is wrong, this boundary still fails |
| Modified .def → git history | Edit is committed; reviewable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-06-1 | Tampering | Task 1 applies the wrong fix from a misread of FINDINGS.md | mitigate | Acceptance criteria require FINDINGS.md to be cited verbatim in the .def comment block; the Task 2 live smoke is the empirical verification — if the fix is wrong, the smoke fails with the same exit 5 and Plan 06 iterates rather than locking in a bad fix. |
| T-01.1-06-2 | Spoofing | Operator reports "approved | smoke exit 0" without actually running the test | mitigate | Resume signal requires the actual exit code; the dinamica-smoke-<ts>.log is committed to logs/ for audit. Task 3's REQUIREMENTS.md update is contingent on the resume signal AND the executor reads the actual log (or trusts the signal — if signal is wrong, the next live operator run will catch it). |
| T-01.1-06-3 | Tampering | Task 1's .def edit accidentally introduces a new defect | mitigate | The %post stages are preserved verbatim except for the FINDINGS-named edit; acceptance criteria enforce structural invariants (Bootstrap unchanged, no %runscript, %post / %environment / %test blocks all still present). |
| T-01.1-06-4 | Denial of service | Rebuilding the .sif consumes ~1 GB of scratch and ~30 min of build time per attempt | accept | Standard cost; the existing .def already pays this. APPTAINER_TMPDIR/CACHEDIR routing per 01.1-03-SUMMARY.md keeps home quota safe. |
| T-01.1-06-5 | Repudiation | A future operator wonders why .def line X was changed | mitigate | Provenance comment in the .def names Plan 01.1-06 + FINDINGS.md; git blame backed by the Task 1 commit message ("fix(01.1-06): ...") preserves the why. |
</threat_model>

<verification>
- `dinamica/container/rocker-geospatial-dinamica.def` reflects FINDINGS.md's Proposed Fix.
- `apptainer build` succeeds on Euler with the new .def.
- `bash scripts/smoke_test_dinamica.sh --live --runtime auto --artifact "$DINAMICA_EGO_8_HOME" --ego dinamica/dinamica_model/smoketest.ego --require-log-under logs` exits 0.
- The captured `logs/dinamica-smoke-<ts>.log` contains no D-107 pattern hits.
- `01.1-03-SUMMARY.md` Open Issue 1 is marked RESOLVED with backreference to `01.1-06-SUMMARY.md`.
- `dinamica/container/README.md` and `docs/README_HPC.md` runtime-caveat callouts are updated to resolution-state.
- `.planning/REQUIREMENTS.md` marks INFRA-01 and MEM-06 Complete with closure citations to Plan 01.1-06.
</verification>

<success_criteria>
- ROADMAP Phase 1.1 SC2 (live `--live` smoke exits 0) — CLOSED.
- ROADMAP Phase 1.1 SC5 (smoke fixture exercises the launcher end-to-end via DinamicaConsole acceptance) — CLOSED.
- INFRA-01 — Complete (all six original SCs + the Phase 1.1 SC2 closure).
- MEM-06 — Complete (allocation_env package set from Phase 1 + the Phase 1.1 SC5 closure).
- Phase 1.1 is fully done. No open issues remain in the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-06-SUMMARY.md` documenting:
- The exact `.def` edit Task 1 made (with the line range and verbatim diff).
- The FINDINGS.md hypothesis that drove the fix and the supporting evidence file(s).
- The Task 2 operator outcome: .sif build duration, .sif size, live smoke exit code, dinamica-smoke log path + size.
- The Task 3 doc updates and the REQUIREMENTS.md traceability transitions.
- A "Phase 1.1 closing summary" paragraph confirming all seven ROADMAP SCs are now CLOSED.
- Any deviations from FINDINGS.md (e.g. if the executor judged a slight modification of the proposed fix was needed during Task 1, document why).
- Pointers to the gap-closure plan pair (01.1-05 + 01.1-06) so future replanning understands the full closure path.
</output>
