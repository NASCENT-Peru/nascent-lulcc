# Phase 01.1: Fix Dinamica Launch Contract — Pattern Map

**Mapped:** 2026-05-15
**Files analyzed:** 8 (6 modified, 2 new)
**Analogs found:** 7 / 8

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `src/dinamica_utils.r` (`resolve_dinamica_launch()`, lines 115–220) | utility / launch resolver | transform (env → argv) | self — same function, current body (rewrite in-place) | exact (self-rewrite) |
| `src/dinamica_utils.r` (`exec_dinamica()` post-run grep, lines 313–321) | utility / subprocess wrapper | request-response + post-hoc detection | self — existing partial pattern at lines 313–321 | exact (self-extend) |
| `scripts/smoke_test_dinamica.sh` (post-run grep + exit code 5) | shell test gate | request-response + post-hoc detection | self — exit-code-only block at lines 282–298; mirror R-side grep | exact (self-extend) |
| `scripts/setup_environments.sh` (HPC-detection refusal, lines 84–94) | shell gate / config | precondition check | `scripts/hpc_common.sh:check_stage7_contract()` (lines 35–56) | role-match (refusal pattern) |
| `dinamica/container/rocker-geospatial-dinamica.def` (full rewrite) | container definition | build-time recipe | upstream `ethzplus/rocker-geospatial-dinamica/Dockerfile` (verbatim port in RESEARCH.md §Target 2) | no in-repo analog; verbatim upstream template |
| `dinamica/container/README.md` (doc rewrite) | documentation | doc | existing `dinamica/container/README.md` (replace GHCR-bootstrap section) | self-rewrite |
| `docs/README_HPC.md` (doc update) | documentation | doc | existing `docs/README_HPC.md` (Dinamica section) | self-rewrite |
| `dinamica/dinamica_model/smoketest.ego-decoded` + `.ego` (NEW siblings) | repo asset / fixture | static fixture | `dinamica/dinamica_model/allocation.ego-decoded` (file header shape only) | role-match (fixture format) |
| `tests/testthat/test-dinamica-launch-contract-mirror.R` (NEW, per RESEARCH §Target 7) | test | request-response | `tests/testthat/test-dinamica-launcher.R` | exact (same test surface) |

## Pattern Assignments

### `src/dinamica_utils.r:resolve_dinamica_launch()` (utility, transform)

**Analog:** self — `src/dinamica_utils.r:115–220` (existing function body)

**Imports / docblock pattern** (lines 1–36, 84–114): keep verbatim; the function-header roxygen + `D-09/D-10/D-11` provenance comments already match project style. Add new `Phase 1.1 / D-104 / D-105 / D-106` provenance block at top of function body, modelled on existing `Phase 1 Plan 04 Task 1` header.

**Backend-branching pattern** (lines 137–149): preserve verbatim — `detect_dinamica_backend()` resolution + `DINAMICA_EGO_8_HOME` empty-check are correct and unchanged.

**Console-args + log-path pattern** (lines 151–162): preserve verbatim — `console_args` assembly and `resolve_dinamica_log_path()` call are correct.

**HPC branch — REPLACE lines 164–201** with new launch shape per D-104/D-105/D-106. Authoritative excerpt from RESEARCH.md §Code Examples (verbatim splice target):

```r
# Source: RESEARCH §Code Examples; D-104 + D-105 + D-106
staged_home <- file.path(Sys.getenv("HPC_SCRATCH_ROOT"), "dinamica-home")
staged_tmp  <- file.path(Sys.getenv("HPC_SCRATCH_ROOT"), "dinamica-tmp")
dir.create(staged_home, recursive = TRUE, showWarnings = FALSE)
dir.create(staged_tmp,  recursive = TRUE, showWarnings = FALSE)

# Seed .dinamica_ego_8.conf only if missing (idempotent; see Pitfall 3)
conf <- file.path(staged_home, ".dinamica_ego_8.conf")
if (!file.exists(conf)) {
  writeLines(
    c(
      'AlternativePathForR = "/usr/local/bin/Rscript"',
      'ClConfig = "0"',
      'MemoryAllocationPolicy = "1"',
      'RCranMirror = "https://cloud.r-project.org/"'
    ),
    conf
  )
}

abs_model <- normalizePath(model_path, mustWork = FALSE)
container_args <- c(
  "exec",
  "--home", staged_home,
  "--env", paste0("DINAMICA_EGO_8_TEMP_DIR=", staged_tmp),
  artifact_path,                                # the .sif
  "bash", "-c",
  sprintf("cd /opt/dinamica/usr && bin/DinamicaEGO.sh %s", shQuote(abs_model))
)
```

**Pre-flight HPC_SCRATCH_ROOT check pattern:** wrap the `staged_home`/`staged_tmp` block with the same empty-string guard idiom used at lines 141–149 for `DINAMICA_EGO_8_HOME`. Stop with a clear error if `HPC_SCRATCH_ROOT` is unset on the HPC backend.

**Local branch** (lines 203–220): preserve verbatim — local backend already invokes `DinamicaConsole` directly; D-105 last clause confirms no change.

**Return-list shape** (lines 192–200): preserve verbatim — `command`, `args`, `log_file`, `env` keys must remain identical (consumers in `exec_dinamica()` and tests depend on this).

---

### `src/dinamica_utils.r:exec_dinamica()` post-run error grep (utility, post-hoc detection)

**Analog:** self — existing partial pattern at `src/dinamica_utils.r:313–321`

**Existing partial pattern** (lines 313–321):
```r
if (
  res[["status"]] != 0L ||
    grepl("Dinamica EGO exited with an error", res[["stdout"]])
) {
  stop(
    "Dinamica registered an error. \n",
    "Rerun with echo = TRUE or check logfile to see what went wrong."
  )
}
```

**REPLACE with** (verbatim from RESEARCH.md §Code Examples — Error-string post-hoc detection):
```r
DINAMICA_ERROR_PATTERNS <- c(
  "Dinamica EGO exited with an error",
  "terminate called after throwing",
  "std::exception"
)

log_contents <- if (isTRUE(write_logfile) && file.exists(logfile_path)) {
  paste(readLines(logfile_path, warn = FALSE), collapse = "\n")
} else {
  paste(c(res[["stdout"]], res[["stderr"]]), collapse = "\n")
}

error_hits <- vapply(
  DINAMICA_ERROR_PATTERNS,
  function(p) grepl(p, log_contents, fixed = TRUE),
  logical(1)
)

if (res[["status"]] != 0L || any(error_hits)) {
  matched <- DINAMICA_ERROR_PATTERNS[error_hits]
  stop(
    "Dinamica registered an error",
    if (length(matched)) sprintf(" (matched: %s)", paste(matched, collapse = "; ")) else "",
    if (res[["status"]] != 0L) sprintf(" [exit %d]", res[["status"]]) else "",
    ".\nRerun with echo = TRUE or check logfile: ", logfile_path
  )
}
```

**Log artifact source** is the SAME file produced by the tee at lines 282–293 (`logfile_path` from `launch$log_file`). The `write_logfile = FALSE` branch falls back to `res$stdout + res$stderr` so the grep contract still holds in that mode.

**Stop-message format pattern:** keep the existing project idiom — call `stop()` with `\n`-separated guidance ("Rerun with echo = TRUE or check logfile: …"), mirroring lines 317–320. Append the matched pattern names and exit code so OBS-02 operators see actionable diagnostics.

---

### `scripts/smoke_test_dinamica.sh` post-run grep + new exit code 5 (shell gate, post-hoc detection)

**Analog:** self — existing exit-code-only block at `scripts/smoke_test_dinamica.sh:282–298`

**Existing partial pattern** (lines 282–298):
```bash
set +e
"${LAUNCH_CMD[@]}" 2>&1 | tee "$LOG_FILE"
EXIT_CODE=${PIPESTATUS[0]}
set -e

if [[ "$EXIT_CODE" -ne 0 ]]; then
    echo "[live] FAIL: Dinamica subprocess exited with status $EXIT_CODE" >&2
    echo "[live] See $LOG_FILE for output." >&2
    exit 3
fi

# Contract: a timestamped dinamica-smoke-*.log MUST exist under $LOG_ROOT.
LATEST_LOG="$(ls -1t "$LOG_ROOT"/dinamica-smoke-*.log 2>/dev/null | head -n 1 || true)"
if [[ -z "$LATEST_LOG" || ! -s "$LATEST_LOG" ]]; then
    echo "[live] FAIL: no non-empty dinamica-smoke-*.log under $LOG_ROOT" >&2
    exit 4
fi
```

**INSERT new error-grep block between lines 291 and 293** (verbatim from RESEARCH.md §Code Examples — Smoke test post-run grep):
```bash
# D-107: grep the log for error strings that Dinamica prints alongside exit 0
if grep -qE 'Dinamica EGO exited with an error|terminate called after throwing|std::exception' "$LOG_FILE"; then
    echo "[live] FAIL: Dinamica printed an error string despite exit 0" >&2
    echo "[live] Matching lines:" >&2
    grep -nE 'Dinamica EGO exited with an error|terminate called after throwing|std::exception' "$LOG_FILE" >&2
    exit 5
fi
```

**LAUNCH_CMD array — REPLACE line 253** with the D-104 shape so the smoke test mirrors what `resolve_dinamica_launch()` produces:
```bash
# Dinamica launch contract (D-104) — keep in sync with
#   src/dinamica_utils.r:resolve_dinamica_launch()
# See .planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md
LAUNCH_CMD=(
    "$RESOLVED_RUNTIME" "exec"
    "--home" "$STAGED_HOME"
    "--env" "DINAMICA_EGO_8_TEMP_DIR=$STAGED_TMP"
    "$ARTIFACT"
    "bash" "-c"
    "cd /opt/dinamica/usr && bin/DinamicaEGO.sh $(printf %q "$ABS_EGO_MODEL")"
)
```

**Exit-code documentation pattern** (lines 38–44): EXTEND the header block to document exit code 5:
```
#   5   live Dinamica subprocess returned 0 BUT printed an error string under
#       grep -E 'Dinamica EGO exited with an error|terminate called after throwing|std::exception'
#       (D-107 — Dinamica exits 0 even on std::exception; do not trust the
#       subprocess exit code as the success signal).
```

**Argument-parsing pattern** (lines 113–149): preserve verbatim; if a `--hpc` flag is added in `setup_environments.sh` per Target 6 option (a), follow the same `case` clause idiom.

---

### `scripts/setup_environments.sh` HPC-detection refusal (shell gate, precondition)

**Analog:** `scripts/hpc_common.sh:check_stage7_contract()` (lines 35–56)

**Refusal pattern from analog** (lines 44–53):
```bash
if [ ${#missing[@]} -gt 0 ]; then
    echo "ERROR: Stage 7 path contract incomplete." >&2
    echo "The following required variables are unset or empty:" >&2
    for var in "${missing[@]}"; do
        echo "  - $var" >&2
    done
    echo >&2
    echo "Source the project .env file or export these variables before submitting" >&2
    echo "the Stage 7 batch job. See .env.template for the canonical operator surface." >&2
    return 1
fi
```

**Existing silent-fallback** at `scripts/setup_environments.sh:87–91`:
```bash
if [ -n "${HPC_SCRATCH_ROOT:-}" ]; then
    ENV_BASE_PATH="$HPC_SCRATCH_ROOT/micromamba/envs"
else
    ENV_BASE_PATH="$PROJECT_ROOT/.envs"   # silent fallback — G7 violation
fi
```

**REPLACE with** (verbatim from RESEARCH.md §Code Examples — HPC-detection refusal):
```bash
# Source: D-112 + scripts/hpc_common.sh:check_stage7_contract() as the model

# HPC detection — any of three signals
on_hpc=false
if [[ -n "${SLURM_JOB_ID:-}" ]] || [[ -n "${SLURM_CLUSTER_NAME:-}" ]]; then
    on_hpc=true; hpc_signal="SLURM env var"
elif [[ -d /cluster/scratch ]]; then
    on_hpc=true; hpc_signal="/cluster/scratch present"
elif [[ "${FORCE_HPC:-false}" == "true" ]]; then
    on_hpc=true; hpc_signal="--hpc flag"
fi

if [[ "$on_hpc" == "true" ]]; then
    if [[ -z "${HPC_SCRATCH_ROOT:-}" ]]; then
        echo "ERROR: HPC context detected ($hpc_signal) but HPC_SCRATCH_ROOT is unset." >&2
        echo "       Refusing to install conda envs under \$HOME (home filesystem quota)." >&2
        echo "       Source the project .env or run: export HPC_SCRATCH_ROOT=/cluster/scratch/\$USER/nascent-lulcc" >&2
        exit 1
    fi
    ENV_BASE_PATH="$HPC_SCRATCH_ROOT/micromamba/envs"
    echo "Env install root (HPC): $ENV_BASE_PATH"
else
    ENV_BASE_PATH="$PROJECT_ROOT/.envs"
    echo "Env install root (local fallback, no HPC signals): $ENV_BASE_PATH"
fi
mkdir -p "$ENV_BASE_PATH"
```

**CLI-flag parsing pattern** (lines 38–65 of `setup_environments.sh`): if Target 6 option (a) is adopted, add a `--hpc` case-clause mirroring the existing `--non-interactive` clause:
```bash
--hpc)
    FORCE_HPC="true"
    shift
    ;;
```

**Note on `set -euo pipefail`:** the script already runs under `set -euo pipefail` (line 20), so the refusal `exit 1` produces the same fail-fast shape as `check_stage7_contract()`'s `return 1` (which is the analog's idiom because it is invoked from `setup_common_env()` which then `exit 1`s).

---

### `dinamica/container/rocker-geospatial-dinamica.def` (container definition, build recipe)

**Analog:** no in-repo Apptainer `.def` files exist. The verbatim port template lives in RESEARCH.md §Code Examples §"Upstream `.def` `%post` template" (lines 381–443) and is itself a verbatim port of `ethzplus/rocker-geospatial-dinamica/Dockerfile` (RESEARCH.md §Target 2).

**Existing `.def` preserved pattern — keep verbatim:**
- `%labels` block (lines 42–48 of current file): keep provenance labels.
- `%help` block (lines 67–75): rewrite to point at new build flow per D-114.

**Bootstrap REPLACE** (lines 1–2 of current file):
```
Bootstrap: docker
From: rocker/r-ver:4.5.3
```
Rationale: DD-1 resolved to `rocker/r-ver:4.5.3` (RESEARCH.md §Target 3) — matches upstream Dockerfile, 348 MB compressed vs 1.27 GB for `rocker/geospatial:4.5.3`.

**`%post` REPLACE** (lines 50–57 of current file) with the five-stage template from RESEARCH.md §Code Examples §"Upstream `.def` `%post` template" — apt deps, install2.r geospatial, curl AppImage, `--appimage-extract`, `cp -a squashfs-root/. .`, install_dinamica_pkg.r via `remotes::install_local()`, seed `/root/.dinamica_ego_8.conf`. Use the verbatim apt-package list from RESEARCH.md (lines 397–403).

**`%environment` ADD** (currently absent in the `.def`):
```
%environment
    export LD_LIBRARY_PATH=/opt/dinamica/usr/lib:$LD_LIBRARY_PATH
    export PATH=$PATH:/opt/dinamica/usr/bin
    export DINAMICA_EGO_8_INSTALLATION_DIRECTORY=/opt/dinamica/usr/bin
    export DINAMICA_EGO_8_HOME_DIR=/opt/dinamica
```
NOTE: do NOT export `DINAMICA_EGO_8_TEMP_DIR` here — runtime overrides it via `apptainer --env` per D-105.

**`%test` block** (current lines 59–65) REPLACE with the upstream-aligned assertion plus a smoke fixture invocation per `<specifics>` line 105:
```
%test
    set -euo pipefail
    command -v DinamicaConsole
    test -x /opt/dinamica/usr/bin/DinamicaEGO.sh
    # Optional end-to-end runnability check if smoketest.ego is staged via %files
    if [ -f /opt/dinamica/usr/share/smoketest.ego ]; then
        cd /opt/dinamica/usr && bin/DinamicaEGO.sh /opt/dinamica/usr/share/smoketest.ego
    fi
```

**Anti-pattern to avoid:** do NOT pin an `ENTRYPOINT` in the apptainer image (upstream Dockerfile does, but apptainer-exec passes the command explicitly per D-104). RESEARCH.md §Target 2 last bullet.

---

### `dinamica/dinamica_model/smoketest.ego-decoded` + `.ego` (NEW repo assets)

**Analog:** `dinamica/dinamica_model/allocation.ego-decoded` (header shape only)

**Header pattern from analog** (lines 1–3 of `allocation.ego-decoded`):
```
@charset = UTF-8
@date = 2025-Dec-04 16:00:58
@version = 8.7.0.20250814
```

**New file content** — hand-written minimal no-op `.ego-decoded` per D-109 + RESEARCH.md §Target 1 + §Target 8. Format follows the analog's header verbatim; body is a single trivial Script declaration with NO `CalculateRExpression` / `CalculatePythonExpression` chunks so `process_dinamica_script()`'s `any(!logical(0))` check passes vacuously (RESEARCH.md §Target 1):

```
@charset = UTF-8
@date = 2026-05-15 00:00:00
@version = 8.7.0.20250814

Script {{
    // Phase 1.1 smoke fixture — exercises the launch contract only.
    // No R/Python chunks; no data I/O; intentional no-op.
    smoketestConstant := NumberValue 1;
}}
```

**Generation of `smoketest.ego`** — Wave-1 Task 0 gate per RESEARCH.md §Open Questions Q2:
1. Locally: `process_dinamica_script(infile = "smoketest.ego-decoded", outfile = "smoketest.ego", mode = "encode")`.
2. Run live smoke test on Euler against rebuilt `.sif`.
3. If accepted: commit both files.
4. If rejected: fall back to D-110 — build via Dinamica EGO GUI on Windows workstation; commit `smoketest.ego` as binary asset.

**DD-2 resolution (RESEARCH.md §Target 8):** ADD as siblings; do NOT replace `allocation.ego-decoded` (production code at `src/dinamica_utils.r:512` loads that file directly via `file.copy(decoded_file, …)`).

**Update sites for the new fixture** (planner task list):
1. `scripts/smoke_test_dinamica.sh` — default/example `--ego` points to `smoketest.ego` (NOT `allocation.ego-decoded`).
2. `dinamica/container/README.md` — `--ego` example switches to `smoketest.ego`.
3. `docs/README_HPC.md` — same swap.

---

### `tests/testthat/test-dinamica-launch-contract-mirror.R` (NEW test)

**Analog:** `tests/testthat/test-dinamica-launcher.R` (lines 1–142)

**Source-loading + repo-root pattern** (lines 27–38 of analog):
```r
.repo_root <- (function() {
  here <- tryCatch(normalizePath(sys.frame(1)$ofile %||% "."), error = function(e) ".")
  if (is.null(here) || identical(here, "")) here <- "."
  is_dir <- tryCatch(file.info(here)$isdir, error = function(e) NA)
  if (isTRUE(is_dir)) here <- file.path(here, "x")
  normalizePath(file.path(dirname(dirname(dirname(here)))), mustWork = FALSE)
})()

.env <- new.env(parent = baseenv())
sys.source(file.path(.repo_root, "src", "setup.r"), envir = .env)
sys.source(file.path(.repo_root, "src", "utils.r"), envir = .env)
sys.source(file.path(.repo_root, "src", "dinamica_utils.r"), envir = .env)
```
Reuse verbatim.

**`resolve_dinamica_launch()` invocation pattern** (lines 47–61 of analog):
```r
withr::local_envvar(
  DINAMICA_EGO_8_HOME = "/tmp/dinamica.sif",
  DINAMICA_BACKEND    = "hpc",
  HPC_SCRATCH_ROOT    = "/tmp/scratch"      # NEW — required by D-105 post-rewrite
)
launch <- .env$resolve_dinamica_launch(
  model_path       = "/abs/test.ego",
  backend          = "hpc",
  runtime_override = "apptainer",
  probe_runtime    = FALSE
)
```

**Assertion pattern** (lines 63–80 of analog):
```r
expect_identical(launch$backend, "hpc")
expect_identical(launch$runtime, "apptainer")
expect_true(any(grepl("--home", launch$args)))
expect_true(any(grepl("DINAMICA_EGO_8_TEMP_DIR=", launch$args, fixed = TRUE)))
expect_true(any(grepl("bin/DinamicaEGO.sh", launch$args, fixed = TRUE)))
expect_true(any(grepl("bash", launch$args, fixed = TRUE)))
```

**Mirror-assertion against shell** (RESEARCH.md §Target 7):
```r
shell_out <- system2(
  "bash",
  c(file.path(.repo_root, "scripts/smoke_test_dinamica.sh"),
    "--dry-run", "--runtime", "apptainer",
    "--artifact", "/tmp/dinamica.sif",
    "--ego", "/abs/test.ego"),
  stdout = TRUE, stderr = TRUE
)
resolved_line <- grep("^resolved command", shell_out, value = TRUE)
expect_match(resolved_line, "--home")
expect_match(resolved_line, "DINAMICA_EGO_8_TEMP_DIR=", fixed = TRUE)
expect_match(resolved_line, "bash -c", fixed = TRUE)
expect_match(resolved_line, "bin/DinamicaEGO.sh", fixed = TRUE)
```

**Skip guard pattern** (line 48 of analog): use `skip_if_not(is.function(.env$resolve_dinamica_launch))` so the test no-ops on hosts without the source loaded.

---

### `dinamica/container/README.md` + `docs/README_HPC.md` (documentation)

**Analog:** self — current README structure is correct; only the build-flow and launch-command sections change.

**Sections to update in `dinamica/container/README.md`:**
- "Provenance" (current lines 11–19): swap GHCR reference for "verbatim port of upstream Dockerfile inlined in `%post`" + note pinned base image `rocker/r-ver:4.5.3` (D-102).
- "Build flow" (current lines 31–55): keep `apptainer build …` command; remove any GHCR-auth mentions; add the rebuild trigger conditions per D-102 (upstream Dinamica version bump → new AppImage URL number + new `@version` in `smoketest.ego-decoded`).
- Demote the `docker save → docker-archive://` workaround from canonical to fallback-note per D-114.
- Switch `--ego` example to `smoketest.ego` per DD-2.

**Sections to update in `docs/README_HPC.md`:**
- Dinamica launch-command examples: rewrite from `apptainer exec <sif> DinamicaConsole <model>` to `apptainer exec --home <staged-home> --env DINAMICA_EGO_8_TEMP_DIR=<staged-tmp> <sif> bash -c 'cd /opt/dinamica/usr && bin/DinamicaEGO.sh <abs-model>'`.
- `setup_environments.sh` operator notes: document the new HPC-detection refusal + `--hpc` flag (if adopted).
- Smoke test command: swap `--ego allocation.ego-decoded` for `--ego smoketest.ego`.

---

## Shared Patterns

### Error-string post-hoc detection (D-107)
**Source patterns:** RESEARCH.md §Code Examples — R-side and Shell-side blocks.
**Apply to:** `src/dinamica_utils.r:exec_dinamica()` AND `scripts/smoke_test_dinamica.sh` (both grep the SAME three patterns against the SAME teed log artifact).
**Pattern constant (R-side):**
```r
DINAMICA_ERROR_PATTERNS <- c(
  "Dinamica EGO exited with an error",
  "terminate called after throwing",
  "std::exception"
)
```
**Pattern constant (shell-side):**
```bash
'Dinamica EGO exited with an error|terminate called after throwing|std::exception'
```
**Drift mitigation (RESEARCH.md §Target 7):** add a canonical comment block in both files referencing each other and a mirror-assertion test (see `test-dinamica-launch-contract-mirror.R` above).

### HPC detection (D-112, three-signal OR)
**Source:** RESEARCH.md §Code Examples — HPC-detection refusal block; mirrors `scripts/hpc_common.sh:check_stage7_contract()` shape.
**Apply to:** `scripts/setup_environments.sh` (only — the rest of `hpc_common.sh` already validates `HPC_SCRATCH_ROOT` after this gate fires).
**Signal precedence:** `SLURM_JOB_ID` / `SLURM_CLUSTER_NAME` → `/cluster/scratch` existence → `FORCE_HPC=true` / `--hpc` flag.

### Idempotent scratch-dir + conf seeding (D-105, Pitfall 3)
**Source:** RESEARCH.md §Code Examples — `resolve_dinamica_launch()` excerpt; `dir.create(..., recursive = TRUE, showWarnings = FALSE)` + `if (!file.exists(conf)) writeLines(...)`.
**Apply to:** `src/dinamica_utils.r:resolve_dinamica_launch()` HPC branch only. Local branch does not need staged-home/staged-tmp.
**Race mitigation (Pitfall 3):** RESEARCH.md notes Wave-1 pre-flight seeding via `validate_allocation_runtime()` is the cleanest path. Planner-level decision: either pre-seed in pre-flight OR write conf atomically via tempfile + `file.rename()`. PATTERNS.md flags both options; both are acceptable.

### Stop-message format (R-side error surfacing)
**Source:** `src/dinamica_utils.r:317–320` (existing `exec_dinamica()` stop block).
**Apply to:** All new `stop()` calls in `resolve_dinamica_launch()` and the rewritten error grep.
**Shape:** Single `stop()` call; `\n`-separated guidance; reference the log file path explicitly; use `call. = FALSE` consistently (see analog at line 80, 130, 147 for examples).

### Roxygen + provenance block per change
**Source:** `src/dinamica_utils.r:8–35` (file-header `Phase 1 Plan 04 Task 1 — D-09, D-10, D-11` block).
**Apply to:** Every R function modified — add a `Phase 1.1 — D-104/D-105/...` annotation in a block-comment header above the changed code, with one-line citation of the decision IDs being implemented.

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `dinamica/container/rocker-geospatial-dinamica.def` (`%post` body) | container build recipe | build-time | No other Apptainer `.def` files in repo. Authoritative template is the upstream `ethzplus/rocker-geospatial-dinamica/Dockerfile` reproduced verbatim in RESEARCH.md §Target 2 + §Code Examples §"Upstream `.def` `%post` template". Planner should consume RESEARCH.md directly. |

(All other files have a clean self- or sibling- analog in the repo.)

## Metadata

**Analog search scope:**
- `src/dinamica_utils.r` (full file, focus lines 88–324, 335–606)
- `scripts/smoke_test_dinamica.sh` (full file, focus lines 38–44, 251–298)
- `scripts/setup_environments.sh` (full file, focus lines 38–94)
- `scripts/hpc_common.sh` (full file, focus lines 32–56)
- `tests/testthat/test-dinamica-launcher.R` (full file)
- `dinamica/container/rocker-geospatial-dinamica.def` (full file)
- `dinamica/container/README.md` (lines 1–60)
- `dinamica/dinamica_model/allocation.ego-decoded` (header lines 1–5)
- `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md` §Code Examples + §Targets 2/3/4/5/6/7/8

**Files scanned:** 9 (8 in-repo + 1 upstream Dockerfile reproduced verbatim in RESEARCH.md)
**Pattern extraction date:** 2026-05-15
