---
phase: "03.2"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/transition_feature_selection.r
  - src/simulation_trans_rates_prep.r
  - config/local_config.yaml
  - config/hpc_config.yaml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "`transition_feature_selection()` does not crash with 'object final_summary not found' on return"
    - "`simulation_trans_rates_prep.r` reads year_steps from `config[[\"simulation_year_steps\"]]` (an explicit ordered list in both YAMLs) and stop()s at pipeline entry if the list violates any of: `simulation_year_steps[1] == simulation_start_year`, `tail(simulation_year_steps, 1) == simulation_end_year`, `all(diff(simulation_year_steps) > 0)`"
    - "`simulation_trans_rates_prep.r` reads scalars from `config[[\"simulation_trans_rates_params\"]][[\"scale_factor\"]]` instead of the hardcoded `c(1.0, seq(3.0, 9.0, by = 2))`"
    - "The mining prohibition in `simulation_trans_rates_prep.r` is read from `config[[\"simulation_trans_rates_params\"]][[\"forbidden_from_classes\"]]` and logged when applied"
    - "Both config YAMLs declare `forbidden_from_classes: [\"mining\"]` under `simulation_trans_rates_params`"
    - "After `perform_feature_selection()` runs for a region, the log contains `AUDIT stage=1->2 period=P whole_map_transitions=N` — N can be verified against `nrow(filter(viable_transitions_lists.csv, region_name == 'whole_map', !is.na(rate_P)))` to confirm FS loaded the correct input set"
    - "PIPE-01 and PIPE-02 are marked `[x]` complete in `.planning/REQUIREMENTS.md` with a Phase 3.2 note; `grep -r 'xlsx\\|\\.xls' src/simulation_trans_rates_prep.r src/allocation.r` returns zero matches"
  artifacts:
    - path: "src/transition_feature_selection.r"
      provides: "Bug-free orchestrator function (no undefined final_summary return)"
      contains: "return(invisible(NULL))"
    - path: "src/simulation_trans_rates_prep.r"
      provides: "Config-driven year_steps, scalars, and mining exclusion"
      contains: "forbidden_from_classes"
    - path: "config/local_config.yaml"
      provides: "forbidden_from_classes key under simulation_trans_rates_params"
      contains: "forbidden_from_classes"
    - path: "config/hpc_config.yaml"
      provides: "forbidden_from_classes key under simulation_trans_rates_params"
      contains: "forbidden_from_classes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PIPE-01 and PIPE-02 marked complete"
      contains: "[x] **PIPE-01**"
  key_links:
    - from: "src/simulation_trans_rates_prep.r (forbid_pairs_df construction)"
      to: "config[[\"simulation_trans_rates_params\"]][[\"forbidden_from_classes\"]]"
      via: "direct YAML key read"
      pattern: "forbidden_from_classes"
    - from: "src/simulation_trans_rates_prep.r (year_steps)"
      to: "config[[\"simulation_year_steps\"]] (explicit ordered list in both YAMLs)"
      via: "direct read + stop()-validated invariants against simulation_start_year and simulation_end_year (3 invariants: start match, end match, monotonic diffs)"
      pattern: "simulation_year_steps"
---

<objective>
Fix three source-level bugs discovered during Phase 3.2 research that would cause crashes or silent mismatches if left unfixed:

1. `transition_feature_selection()` returns `final_summary` (line 63) which is never assigned — crash on return.
2. `year_steps` in `simulation_trans_rates_prep.r` is hardcoded as `c(2022, 2024, 2028, ...)`. Per `03.2-CONTEXT.md` D-01..D-04, this is replaced with an explicit `simulation_year_steps` list in both YAMLs (faithfully reproducing the current sequence including the intentional 2-year first step), read directly with no derivation, plus a stop()-level entry-point validation against `simulation_start_year`, `simulation_end_year`, and strictly-monotonic diffs. Pure `seq()`-based derivation is rejected because it silently relocates the irregularity from the start to the end of the sequence.
3. `scalars` in `simulation_trans_rates_prep.r` is hardcoded as `c(1.0, seq(3.0, 9.0, by = 2))` instead of read from `config[["simulation_trans_rates_params"]][["scale_factor"]]`.
4. The mining prohibition in `simulation_trans_rates_prep.r` (lines 243-248) is hardcoded and unlogged — move to a config key `forbidden_from_classes` under `simulation_trans_rates_params` and log when applied.

Also: verify PIPE-01 and PIPE-02 have no regression (both pre-satisfied per RESEARCH.md) and mark them complete in REQUIREMENTS.md.

Purpose: These are blocking correctness bugs. Fix them before adding cross-stage audit hooks so the pipeline actually runs cleanly.

Output: Modified `src/transition_feature_selection.r`, `src/simulation_trans_rates_prep.r`, `config/local_config.yaml`, `config/hpc_config.yaml`, `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@C:/Users/black/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/black/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-transition-pipeline-consistency/03.2-RESEARCH.md

<interfaces>
<!-- Key facts extracted from source files — executor must not re-read to discover these. -->

From src/transition_feature_selection.r line 39-63:
```r
# The orchestrator calls purrr::map() over periods_to_process; the map result
# is NOT assigned. Then line 63:
return(final_summary)   # BUG: final_summary is never assigned
# Fix: replace with return(invisible(NULL))
```

From src/simulation_trans_rates_prep.r lines 288-310 (commented block + replacement):
```r
# COMMENTED-OUT seq()-based derivation (lines 288-295) — REJECTED per 03.2-CONTEXT.md D-01,
# both the active hardcoded vector below AND this commented block are removed in Change 3:
# year_steps <- unique(c(
#   seq(
#     config[["simulation_start_year"]],
#     config[["simulation_end_year"]],
#     by = config[["step_length"]]
#   ),
#   config[["simulation_end_year"]]
# ))

# HARDCODED version to replace (lines 297-309):
year_steps <- c(
  2022, 2024, 2028, 2032, 2036, 2040, 2044, 2048, 2052, 2056, 2060
)
```

From src/simulation_trans_rates_prep.r (run_scalar_optimization_loop function, lines 1247-1258):
```r
run_scalar_optimization_loop <- function(
  config,          # <-- config IS a parameter here
  lulc_schema,
  initial_lulc_areas,
  lulc_demand,
  df_trans_source,
  year_steps,
  step_length,
  T_steps_val,
  forbid_pairs_df,
  trans_rate_table_dir
)
```
# NOTE: sim_config is NOT a parameter of run_scalar_optimization_loop().
# sim_config is a local variable of the calling function prepare_region_trans_rates()
# and is NOT in scope inside run_scalar_optimization_loop().

From src/simulation_trans_rates_prep.r lines 1284-1286:
```r
# scalars <- config$simulation_trans_rates_params$scale_factor  # commented out (line 1284)
# scalars <- 5                                                   # commented out (line 1285)
scalars <- c(1.0, seq(3.0, 9.0, by = 2))                       # HARDCODED — replace with config read
```

From src/simulation_trans_rates_prep.r lines 243-259 (mining prohibition):
```r
# Current hardcoded form:
forbid_pairs_df <- tidyr::expand_grid(
  region_name = NA_character_,
  From = "mining",
  To = setdiff(lulcs_global, "mining")
) %>%
  tibble::as_tibble()
```

From config/local_config.yaml lines 77-95 (simulation_trans_rates_params block):
```yaml
simulation_trans_rates_params:
  margin: 0.50
  ...
  forbid_inflow: []
  num_workers: 6
  scale_factor: [1.0, 3.0, 5.0, 9.0]
# Add after scale_factor:
# forbidden_from_classes: ["mining"]
```

Config keys already present:
- `config[["simulation_start_year"]]` — present in configuration_settings (line 71 local, line 75 hpc); retained as a canonical anchor that `simulation_year_steps[1]` is validated against
- `config[["simulation_end_year"]]` — present in configuration_settings (line 72 local, line 76 hpc); retained as a canonical anchor that `tail(simulation_year_steps, 1)` is validated against (the 2060-must-be-reached invariant)
- `config[["step_length"]]` — present in configuration_settings (line 62 local, line 67 hpc); retained as documentation of the regular cadence but NOT used to derive the sequence
- `config[["simulation_trans_rates_params"]][["scale_factor"]]` — present as YAML list

Config key to ADD (Change 3 of Task 2):
- `config[["simulation_year_steps"]]` — new top-level ordered list. Initial value reproduces the current hardcoded sequence exactly: `[2022, 2024, 2028, 2032, 2036, 2040, 2044, 2048, 2052, 2056, 2060]`. Lives in both YAMLs at the same relative location with byte-identical key shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the transition_feature_selection.r crash bug and add stage 1→2 audit log</name>
  <files>src/transition_feature_selection.r</files>
  <read_first>
    - src/transition_feature_selection.r (MANDATORY — read the entire file before making any edits; lines 1-65 are critical)
  </read_first>
  <action>
Make two changes to `src/transition_feature_selection.r`:

**Change A — Fix undefined `final_summary` crash (line 63):**

Replace:
```r
  return(final_summary)
```
With:
```r
  return(invisible(NULL))
```
This is the only change needed. The `purrr::map()` result on line 39 is already not assigned, which is correct — the function is called for its side effects (writing RDS files). The return value is unused by callers. `invisible(NULL)` is the idiomatic R return for side-effect functions.

**Change B — Add stage 1→2 audit log inside `perform_feature_selection()`:**

Locate the block starting at line 127:
```r
  message(sprintf(
    "Loaded %d viable transitions for period %s",
    nrow(transitions_info),
    period
  ))
```

Replace with:
```r
  n_viable_stage1 <- nrow(transitions_info)
  message(sprintf(
    "AUDIT stage=1->2 period=%s whole_map_transitions=%d (loaded from viable_transitions_lists.csv)",
    period,
    n_viable_stage1
  ))
```

No other changes in this file.
  </action>
  <verify>
    <automated>Rscript -e "source('src/transition_feature_selection.r'); cat('parse OK\n')" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "return(final_summary)" src/transition_feature_selection.r` returns zero lines
    - `grep -n "return(invisible(NULL))" src/transition_feature_selection.r` returns exactly one line in the `transition_feature_selection` function body
    - `grep -n "AUDIT stage=1->2" src/transition_feature_selection.r` returns one line inside `perform_feature_selection`
    - `Rscript -e "source('src/transition_feature_selection.r')"` exits with code 0 (no parse error)
  </acceptance_criteria>
  <done>The `transition_feature_selection()` function no longer references an undefined `final_summary`; it returns `invisible(NULL)`. The stage 1→2 audit log line `AUDIT stage=1->2 period=P whole_map_transitions=N` is emitted inside `perform_feature_selection()` immediately after loading viable transitions. N equals `nrow(filter(viable_transitions_lists.csv, region_name == 'whole_map', !is.na(rate_P)))` and can be cross-checked by operators against the CSV directly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded year_steps, scalars, and mining prohibition with config-driven values in simulation_trans_rates_prep.r</name>
  <files>src/simulation_trans_rates_prep.r, config/local_config.yaml, config/hpc_config.yaml</files>
  <read_first>
    - src/simulation_trans_rates_prep.r (MANDATORY — read lines 240-330 for the forbid_pairs/year_steps block and lines 1278-1295 for the scalars block before editing)
    - config/local_config.yaml (read lines 60-100 to understand the configuration_settings and simulation_trans_rates_params block structure, and to confirm where simulation_year_steps will be added)
    - config/hpc_config.yaml (read lines 65-103 to confirm identical structure)
    - .planning/phases/03.2-transition-pipeline-consistency/03.2-CONTEXT.md (MANDATORY — D-01..D-04 lock the year_steps approach; this task implements D-01..D-03 and is the response to D-04)
  </read_first>
  <action>
Make four changes across three files:

**Change 1 — Add `forbidden_from_classes` key to both config YAMLs:**

In `config/local_config.yaml`, under `simulation_trans_rates_params`, add after the `scale_factor` line:
```yaml
  forbidden_from_classes: ["mining"]  # LULC classes for which ALL outgoing transitions are globally forbidden
```

Apply the identical change in `config/hpc_config.yaml` at the same relative location.

**Change 2 — Replace hardcoded mining prohibition in `simulation_trans_rates_prep.r` (lines ~243-248):**

Replace:
```r
  forbid_pairs_df <- tidyr::expand_grid(
    region_name = NA_character_,
    From = "mining",
    To = setdiff(lulcs_global, "mining")
  ) %>%
    tibble::as_tibble()
```
With:
```r
  forbidden_from_classes <- sim_config[["forbidden_from_classes"]]
  if (is.null(forbidden_from_classes)) forbidden_from_classes <- character(0)
  if (length(forbidden_from_classes) > 0) {
    message(sprintf(
      "  - Applying global class exclusions from config (forbidden_from_classes): %s",
      paste(forbidden_from_classes, collapse = ", ")
    ))
  }
  forbid_pairs_df <- if (length(forbidden_from_classes) > 0) {
    tidyr::expand_grid(
      region_name = NA_character_,
      From = forbidden_from_classes,
      To = setdiff(lulcs_global, forbidden_from_classes)
    ) %>%
      tibble::as_tibble()
  } else {
    tibble::tibble(region_name = character(), From = character(), To = character())
  }
```

Note: `sim_config` is assigned at line 59 as `config[["simulation_trans_rates_params"]]` and is in scope at lines 243-248 inside `prepare_region_trans_rates()`. This change is within that same function.

**Change 3 — Replace hardcoded `year_steps` (lines ~288-309) with explicit-list read + validation (per CONTEXT.md D-01..D-03):**

Three parts, all in `prepare_region_trans_rates()`:

**3a. Add `simulation_year_steps` to both YAMLs.**

In `config/local_config.yaml`, add a new top-level key under `configuration_settings` (alongside the existing `simulation_start_year`, `simulation_end_year`, `step_length`):
```yaml
  simulation_year_steps: [2022, 2024, 2028, 2032, 2036, 2040, 2044, 2048, 2052, 2056, 2060]  # explicit list — see 03.2-CONTEXT.md D-01
```

Apply the identical change to `config/hpc_config.yaml` at the same relative location with byte-identical key shape (operator-detected divergence between the two YAMLs is treated as a bug — Phase 1 convention).

**3b. Remove the hardcoded block AND the previously-commented derivation block.**

Delete the active hardcoded vector at `src/simulation_trans_rates_prep.r:297-309`:
```r
  year_steps <- c(
    2022,
    2024,
    2028,
    2032,
    2036,
    2040,
    2044,
    2048,
    2052,
    2056,
    2060
  )
```

Also delete the previously-commented derivation block at lines 288-295 (the `# year_steps <- unique(c(`...`))` block) — it is the rejected approach per CONTEXT.md D-01 rationale and leaving it in source as a comment will mislead future readers.

**3c. Replace both deleted blocks with a single config read + 3-invariant validation:**
```r
  year_steps <- config[["simulation_year_steps"]]
  if (is.null(year_steps) || length(year_steps) == 0) {
    stop("config[[\"simulation_year_steps\"]] is missing or empty. Add an explicit ordered list to your config YAML — see 03.2-CONTEXT.md D-01.")
  }
  sim_start <- config[["simulation_start_year"]]
  sim_end   <- config[["simulation_end_year"]]
  if (year_steps[1] != sim_start) {
    stop(sprintf(
      "simulation_year_steps[1] is %d but simulation_start_year is %d — update one to match the other before continuing.",
      year_steps[1], sim_start
    ))
  }
  if (tail(year_steps, 1) != sim_end) {
    stop(sprintf(
      "simulation_year_steps ends at %d but simulation_end_year is %d — update one to match the other before continuing.",
      tail(year_steps, 1), sim_end
    ))
  }
  if (!all(diff(year_steps) > 0)) {
    stop(sprintf(
      "simulation_year_steps must be strictly increasing — observed diffs: %s",
      paste(diff(year_steps), collapse = ", ")
    ))
  }
```

`config` is in scope at this point inside `prepare_region_trans_rates()` (the function reads `config` throughout). `tail()` is base-R, no namespace import needed. The validation runs once per call, immediately when `year_steps` is first needed, so any config drift fails fast.

**Change 4 — Replace hardcoded `scalars` (line ~1286) inside `run_scalar_optimization_loop()`:**

Replace:
```r
  scalars <- c(1.0, seq(3.0, 9.0, by = 2))
```
With:
```r
  scalars <- config[["simulation_trans_rates_params"]][["scale_factor"]]
  if (is.null(scalars) || length(scalars) == 0) {
    stop("config[[\"simulation_trans_rates_params\"]][[\"scale_factor\"]] is missing or empty. Add it to your config YAML.")
  }
```

Use `config[["simulation_trans_rates_params"]][["scale_factor"]]` directly — `sim_config` is a local variable of the calling function `prepare_region_trans_rates()` and is NOT passed to `run_scalar_optimization_loop()`, so it is out of scope there. `config` IS a parameter of `run_scalar_optimization_loop()` (confirmed: line 1248) and is the correct access path.
  </action>
  <verify>
    <automated>Rscript -e "source('src/simulation_trans_rates_prep.r'); cat('parse OK\n')" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "year_steps <- c(" src/simulation_trans_rates_prep.r` returns zero lines (the hardcoded vector is gone)
    - `grep -n "seq(" src/simulation_trans_rates_prep.r | grep -i "simulation_start_year\|simulation_end_year"` returns zero lines (the rejected seq()-based derivation, both active and commented, is gone)
    - `grep -n "simulation_year_steps" src/simulation_trans_rates_prep.r` returns at least one line in the active (non-commented) year_steps assignment
    - `grep -n "simulation_year_steps" config/local_config.yaml` returns exactly one line under `configuration_settings`
    - `grep -n "simulation_year_steps" config/hpc_config.yaml` returns exactly one line under `configuration_settings`
    - `grep -n "simulation_start_year is" src/simulation_trans_rates_prep.r` returns at least one line (the start-year validation stop() message)
    - `grep -n "simulation_end_year" src/simulation_trans_rates_prep.r` returns at least one line in the end-year validation stop() message
    - `grep -n "strictly increasing" src/simulation_trans_rates_prep.r` returns at least one line (the monotonic-diffs validation stop() message)
    - Negative test: a synthetic config with `simulation_year_steps: [2022, 2024, 2028, 2056]` (missing 2060) causes `Rscript -e 'config <- yaml::yaml.load_file("<tmpfile>"); source("src/simulation_trans_rates_prep.r"); prepare_region_trans_rates(config, ...)'` (or equivalent invocation) to exit non-zero with a message containing "ends at 2056 but simulation_end_year is 2060"
    - `grep -v '^#' src/simulation_trans_rates_prep.r | grep -c 'scalars <- c(1.0'` returns 0 (no active hardcoded scalars line)
    - `grep -n "scale_factor" src/simulation_trans_rates_prep.r` returns at least one line where scalars is read from config
    - `grep -n '"mining"' src/simulation_trans_rates_prep.r` returns zero lines in non-comment code (the hardcoded string is gone)
    - `grep -n "forbidden_from_classes" src/simulation_trans_rates_prep.r` returns at least two lines (the read + the log message)
    - `grep -n "forbidden_from_classes" config/local_config.yaml` returns one line
    - `grep -n "forbidden_from_classes" config/hpc_config.yaml` returns one line
    - `Rscript -e "source('src/simulation_trans_rates_prep.r')"` exits with code 0
  </acceptance_criteria>
  <done>`simulation_trans_rates_prep.r` reads year_steps directly from `config[["simulation_year_steps"]]` (an explicit ordered list in both YAMLs, initialised to the current sequence `[2022, 2024, 2028, 2032, 2036, 2040, 2044, 2048, 2052, 2056, 2060]`) and stop()s at the assignment point if any of the three invariants fail: `year_steps[1] == simulation_start_year`, `tail(year_steps, 1) == simulation_end_year`, `all(diff(year_steps) > 0)`. The pure-derivation alternative (seq() + unique(c(..., end))) is rejected because it relocates the irregularity from start to end; both the active hardcoded vector and the previously-commented derivation block are removed from source. Reads scalars from `config[["simulation_trans_rates_params"]][["scale_factor"]]` directly inside `run_scalar_optimization_loop()` (where `config` is in scope as a parameter); builds forbid_pairs_df from `sim_config[["forbidden_from_classes"]]` with a log message listing the excluded classes. Both config YAMLs declare `forbidden_from_classes: ["mining"]` under `simulation_trans_rates_params` and `simulation_year_steps: [2022, 2024, 2028, 2032, 2036, 2040, 2044, 2048, 2052, 2056, 2060]` under `configuration_settings`.</done>
</task>

<task type="auto">
  <name>Task 3: Verify PIPE-01/PIPE-02 no-regression and mark both complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (MANDATORY — read the full file to understand PIPE-01/PIPE-02 entries and the Traceability table format before editing)
  </read_first>
  <action>
**Step 1 — Grep verification (run these commands; if either returns matches, stop and report):**

```bash
grep -rn "xlsx\|\.xls" src/simulation_trans_rates_prep.r
grep -rn "xlsx\|\.xls" src/allocation.r
```

Expected result: zero matches in both files. The RESEARCH.md confirms PIPE-01 is pre-satisfied (line 155 of simulation_trans_rates_prep.r uses `config[["lulc_demand_path"]]`). If any xlsx/xls match is found outside of a comment, do NOT mark PIPE-01 complete — instead stop and report the regression.

**Step 2 — Update REQUIREMENTS.md:**

In the `### Pipeline Correctness` section, update both entries:

Replace:
```markdown
- [ ] **PIPE-01**: `simulation_trans_rates_prep.r` reads LULC demand from the config-driven CSV path (`config[["lulc_demand_path"]]`) — the hardcoded Windows xlsx path is removed
- [ ] **PIPE-02**: The CVXR convex optimisation loop is ported from `src/old/simulation_transition_rates_estimation.R` into `simulation_trans_rates_prep.r` section G and produces valid transition rate tables
```

With:
```markdown
- [x] **PIPE-01**: `simulation_trans_rates_prep.r` reads LULC demand from the config-driven CSV path (`config[["lulc_demand_path"]]`) — the hardcoded Windows xlsx path is removed *(pre-satisfied — verified no xlsx/xls regression in Phase 3.2; grep src/simulation_trans_rates_prep.r src/allocation.r returns zero matches)*
- [x] **PIPE-02**: The CVXR convex optimisation loop is ported from `src/old/simulation_transition_rates_estimation.R` into `simulation_trans_rates_prep.r` section G and produces valid transition rate tables *(pre-satisfied — full CVXR loop `optimize_region_scenario`/`build_mats`/`run_scalar_optimization_loop` confirmed present; Phase 3.2 hardens its inputs only)*
```

**Step 3 — Update the Traceability table:**

Find the traceability table rows for PIPE-01 and PIPE-02:

Replace:
```
| PIPE-01 | Phase 1 | Pending |
| PIPE-02 | Phase 4 | Pending |
```

With:
```
| PIPE-01 | Phase 3.2 | Complete (pre-satisfied; verified no regression in Phase 3.2 — grep src/simulation_trans_rates_prep.r src/allocation.r returns zero xlsx/xls matches) |
| PIPE-02 | Phase 3.2 | Complete (pre-satisfied; full CVXR loop confirmed present; Phase 3.2 hardens inputs only) |
```

Also update the "Last updated" line at the bottom of REQUIREMENTS.md to add:
```
*Last updated: 2026-05-22 — Phase 3.2 closed PIPE-01 + PIPE-02 (pre-satisfied, verified no regression)*
```
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*PIPE-01\*\*" .planning/REQUIREMENTS.md && grep -c "\[x\] \*\*PIPE-02\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "xlsx\|\.xls" src/simulation_trans_rates_prep.r src/allocation.r` returns zero lines (no regression)
    - `grep -c "[x] \*\*PIPE-01\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "[x] \*\*PIPE-02\*\*" .planning/REQUIREMENTS.md` returns 1
    - The Traceability table rows for PIPE-01 and PIPE-02 both show "Complete" with Phase 3.2 and a note
  </acceptance_criteria>
  <done>PIPE-01 and PIPE-02 are marked `[x]` complete in `.planning/REQUIREMENTS.md` with notes confirming pre-satisfaction and Phase 3.2 regression verification. The Traceability table reflects Phase 3.2 and Complete for both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config YAML → R code | Config values read at runtime; malformed YAML or missing keys must fail fast with actionable error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.2-01-1 | Denial of Service | `simulation_trans_rates_prep.r`: `forbidden_from_classes` key absent from config | mitigate | Null-guard: `if (is.null(forbidden_from_classes)) forbidden_from_classes <- character(0)` defaults to empty list; pipeline continues without mining exclusion |
| T-3.2-01-2 | Denial of Service | `run_scalar_optimization_loop()`: `scale_factor` absent from config | mitigate | `stop()` with actionable message before any optimization runs |
| T-3.2-01-3 | Information Disclosure | `forbidden_from_classes` could exclude transitions silently | mitigate | `message()` log line lists all excluded classes when list is non-empty |
</threat_model>

<verification>
After all three tasks complete:
1. `Rscript -e "source('src/transition_feature_selection.r')"` exits 0
2. `Rscript -e "source('src/simulation_trans_rates_prep.r')"` exits 0
3. `grep -rn "return(final_summary)" src/transition_feature_selection.r` returns nothing
4. `grep -n "year_steps <- c(" src/simulation_trans_rates_prep.r` returns nothing (hardcoded vector gone)
4a. `grep -n "simulation_year_steps" src/simulation_trans_rates_prep.r config/local_config.yaml config/hpc_config.yaml` returns at least three lines (one per file: the active R read + the YAML key in each config)
4b. `grep -n "seq(" src/simulation_trans_rates_prep.r | grep -i "simulation_start_year\|simulation_end_year"` returns nothing (rejected seq()-based derivation, both active and commented, is gone)
5. `grep -v '^#' src/simulation_trans_rates_prep.r | grep -c 'scalars <- c(1.0'` returns 0
6. `grep -n "forbidden_from_classes" config/local_config.yaml config/hpc_config.yaml` returns two lines (one per file)
7. `grep -rn "xlsx\|\.xls" src/simulation_trans_rates_prep.r src/allocation.r` returns nothing (PIPE-01 no regression)
8. `grep -c "[x] \*\*PIPE-01\*\*" .planning/REQUIREMENTS.md` returns 1
9. `grep -c "[x] \*\*PIPE-02\*\*" .planning/REQUIREMENTS.md` returns 1
</verification>

<success_criteria>
- `transition_feature_selection()` returns `invisible(NULL)` without referencing an undefined variable
- Stage 1→2 audit log emits `AUDIT stage=1->2 period=P whole_map_transitions=N` — N verifiable against viable_transitions_lists.csv row count for whole_map
- `simulation_trans_rates_prep.r` reads `year_steps` directly from `config[["simulation_year_steps"]]` (an explicit ordered list in both YAMLs, initialised to `[2022, 2024, 2028, 2032, 2036, 2040, 2044, 2048, 2052, 2056, 2060]`) and stop()s if any of the three invariants fail: `year_steps[1] == simulation_start_year`, `tail(year_steps, 1) == simulation_end_year`, `all(diff(year_steps) > 0)`
- `simulation_trans_rates_prep.r` reads `scalars` from `config[["simulation_trans_rates_params"]][["scale_factor"]]` with a stop if absent
- Mining prohibition is config-driven via `sim_config[["forbidden_from_classes"]]` with a log line, not a hardcoded string
- Both config YAMLs declare `forbidden_from_classes: ["mining"]`
- Both source files parse without error after the changes
- PIPE-01 and PIPE-02 are marked `[x]` complete in REQUIREMENTS.md with Phase 3.2 notes
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-transition-pipeline-consistency/03.2-01-SUMMARY.md`
</output>
