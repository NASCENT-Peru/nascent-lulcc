---
phase: 01-repair-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/setup.r
  - src/calibration_predictor_prep.r
  - src/simulation_trans_rates_prep.r
  - config/local_config.yaml
  - config/hpc_config.yaml
  - .env.template
autonomous: true
requirements:
  - PIPE-01
  - PIPE-03
must_haves:
  truths:
    - "Stage 7 and its prerequisite prep scripts resolve machine-specific paths from shared config/env rules instead of hardcoded workstation or user paths."
    - "A fresh local or HPC checkout can point `lulc_demand_path` and `TERRA_TEMP` to valid locations without editing R source."
    - "The same path/env contract is available to later Stage 7 pre-flight and shell entrypoints."
  artifacts:
    - path: "src/setup.r"
      provides: "Shared path/env resolution helpers and config expansion rules"
    - path: "src/calibration_predictor_prep.r"
      provides: "Config/env-driven terra temp resolution"
    - path: "src/simulation_trans_rates_prep.r"
      provides: "Config-driven LULC demand input path"
    - path: "config/hpc_config.yaml"
      provides: "Authoritative HPC path keys aligned to env overrides"
    - path: ".env.template"
      provides: "Documented runtime overrides for local/HPC checkouts"
  key_links:
    - from: "src/simulation_trans_rates_prep.r"
      to: "config/local_config.yaml"
      via: "config[[\"lulc_demand_path\"]]"
      pattern: "lulc_demand_path"
    - from: "src/calibration_predictor_prep.r"
      to: ".env.template"
      via: "TERRA_TEMP env override"
      pattern: "TERRA_TEMP"
---

<objective>
Establish the shared R-side path and environment contract for Phase 1, then remove the two active hardcoded-path breakpoints that currently block fresh HPC execution.

Purpose: Make YAML authoritative with explicit env overrides so later pre-flight, shell wrappers, and Dinamica launch logic all validate the same contract.
Output: Shared config/env helpers, cleaned config/template files, and repaired prep scripts for LULC demand + terra temp resolution.
</objective>

<execution_context>
@C:/Users/black/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/black/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repair-visibility/01-CONTEXT.md
@.planning/phases/01-repair-visibility/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@src/setup.r
@src/calibration_predictor_prep.r
@src/simulation_trans_rates_prep.r
@config/local_config.yaml
@config/hpc_config.yaml
@.env.template

<interfaces>
From src/setup.r:
```r
get_config <- function(config_file = NULL, force_environment = NULL)
detect_environment <- function()
build_full_config <- function(yaml_config)
```

Current contract constraints:
```text
config YAML is the authoritative path map.
Environment overrides are needed for machine-specific temp/beegfs/Dinamica values.
Later plans will consume the same resolved keys from get_config().
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared Stage 7 path/env contract in R config helpers</name>
  <files>src/setup.r, config/local_config.yaml, config/hpc_config.yaml, .env.template</files>
  <behavior>
    - Config loading expands a small, explicit set of env overrides for machine-specific runtime paths.
    - HPC-local/user-specific paths are no longer embedded in touched active code or operator template files.
    - Later code can query one resolved contract for scratch/temp/Dinamica/runtime paths.
  </behavior>
  <action>Refactor `build_full_config()` and related helpers so YAML remains authoritative per D-12 and D-13, but supports explicit overrides for machine-specific values needed by Stage 7 (`TERRA_TEMP`, `HPC_SCRATCH_ROOT`, `HPC_TMP_ROOT`, `DINAMICA_EGO_8_HOME`, plus one backend/runtime hint key consumed by later plans). Expose one callable resolver in `src/setup.r` named `get_stage7_runtime_paths()` that returns those resolved values for later plans and verification. Replace user-specific literals in the touched config/template files per D-14, and ensure the contract is documented in `.env.template` rather than scattered through comments. Do not add ad hoc path detection inside downstream scripts; this task must create the single contract later plans reuse.</action>
  <verify>
    <automated>bash -lc "set -euo pipefail; rg -n 'get_stage7_runtime_paths|TERRA_TEMP|HPC_SCRATCH_ROOT|HPC_TMP_ROOT|DINAMICA_EGO_8_HOME' src/setup.r config/local_config.yaml config/hpc_config.yaml .env.template >/dev/null; if rg -n '/.*/black|E:/terra_temp' src/setup.r config/local_config.yaml config/hpc_config.yaml .env.template; then exit 1; fi"</automated>
  </verify>
  <done>`src/setup.r` exposes one shared runtime path resolution flow, the touched config/template files no longer hardcode user-specific HPC paths, and the env override names needed by later plans are explicit and consistent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the active prep-script path breakpoints to consume the shared contract</name>
  <files>src/calibration_predictor_prep.r, src/simulation_trans_rates_prep.r</files>
  <behavior>
    - `calibration_predictor_prep()` uses env/config-driven terra temp resolution instead of `E:/terra_temp`.
    - `simulation_trans_rates_prep()` reads demand from the configured CSV path instead of a hardcoded XLSX.
    - Fresh HPC checkout behavior follows the same contract introduced in Task 1.
  </behavior>
  <action>Update `calibration_predictor_prep.r` to resolve terra temp via the shared contract and `Sys.getenv("TERRA_TEMP", unset = tempdir())` semantics per D-12 and D-13, satisfying PIPE-03 without introducing workstation fallbacks. Update `simulation_trans_rates_prep.r` to use `config[["lulc_demand_path"]]` and the existing CSV path per PIPE-01; remove the active hardcoded XLSX dependency instead of keeping a "temporary" branch. Keep any numeric cleaning/transformation logic that still applies to the CSV input.</action>
  <verify>
    <automated>bash -lc "set -euo pipefail; if rg -n 'E:/terra_temp|LULC_demand_results\\.xlsx' src/calibration_predictor_prep.r src/simulation_trans_rates_prep.r; then exit 1; fi; rg -n 'TERRA_TEMP|lulc_demand_path' src/calibration_predictor_prep.r src/simulation_trans_rates_prep.r >/dev/null"</automated>
  </verify>
  <done>The two active prep hotspots no longer require source edits for HPC use, and both now consume the shared config/env contract from Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/env -> R runtime | Untrusted machine-specific path values enter Stage 7 configuration here. |
| repo config -> file system | Config-selected paths determine which files are read and where temp artifacts are written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/setup.r` | mitigate | Validate override paths as explicit absolute paths rooted in approved config/env keys before use; avoid ad hoc concatenation. |
| T-01-02 | R | prep script inputs | mitigate | Make the demand/temp paths come from named config keys and log the resolved source in later pre-flight rather than silently using embedded literals. |
| T-01-03 | D | temp path handling | mitigate | Fail when required scratch/temp overrides are absent on HPC instead of falling back to unusable defaults that fail mid-run. |
</threat_model>

<verification>
Run the grep gates above, then manually inspect the resolved keys in the touched config/template files to confirm the same override names are used everywhere.
</verification>

<success_criteria>
`simulation_trans_rates_prep.r` and `calibration_predictor_prep.r` no longer embed workstation-specific paths, and the R-side runtime contract is explicit enough for later pre-flight and shell plans to consume unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repair-visibility/01-repair-visibility-01-SUMMARY.md`
</output>
