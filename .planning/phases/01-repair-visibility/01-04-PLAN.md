---
phase: 01-repair-visibility
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/dinamica_utils.r
  - scripts/smoke_test_dinamica.sh
  - docs/README_HPC.md
  - dinamica/dinamica_model/allocation.ego-decoded
  - dinamica/container/rocker-geospatial-dinamica.def
  - dinamica/container/README.md
autonomous: true
requirements:
  - INFRA-01
  - PIPE-07
must_haves:
  truths:
    - "Stage 7 calls one Dinamica entrypoint, and backend selection happens inside that adapter based on runtime environment."
    - "On Euler HPC, `DINAMICA_EGO_8_HOME` points to the external Dinamica `.sif` file, and `exec_dinamica()` invokes that exact artifact through `apptainer exec` or `singularity exec` without changing caller code."
    - "Dinamica subprocess logs land in the central `logs/` directory and their paths are surfaced for fast triage."
    - "A smoke test executes a minimal Dinamica model on Euler and proves success by writing a timestamped `logs/` artifact."
    - "The repository contains the container definition provenance and build instructions required to reproduce the external Dinamica image, without committing the built image itself."
  artifacts:
    - path: "src/dinamica_utils.r"
      provides: "Unified Dinamica launcher with local/HPC backends and central log handling"
    - path: "scripts/smoke_test_dinamica.sh"
      provides: "Operator-facing HPC smoke test contract"
    - path: "docs/README_HPC.md"
      provides: "Documented Dinamica external artifact + smoke test usage"
    - path: "dinamica/dinamica_model/allocation.ego-decoded"
      provides: "Smoke-testable allocation model asset wired through the unified launcher"
    - path: "dinamica/container/rocker-geospatial-dinamica.def"
      provides: "Committed container definition anchored to the required upstream image provenance"
    - path: "dinamica/container/README.md"
      provides: "Operator-facing build instructions and provenance notes for INFRA-01"
  key_links:
    - from: "src/dinamica_utils.r"
      to: "scripts/smoke_test_dinamica.sh"
      via: "shared backend/env contract"
      pattern: "apptainer|singularity|DINAMICA_EGO_8_HOME"
    - from: "src/dinamica_utils.r"
      to: "logs/"
      via: "timestamped Dinamica subprocess logfile"
      pattern: "dinamica\\.log|logs"
    - from: "dinamica/container/rocker-geospatial-dinamica.def"
      to: "docs/README_HPC.md"
      via: "documented provenance + build flow for the external SIF artifact"
      pattern: "ethzplus/rocker-geospatial-dinamica|apptainer build|singularity build"
---

<objective>
Finish the Dinamica execution repair by hiding local/HPC backend differences behind one adapter, moving subprocess logs to the central log surface, and providing a smoke test operators can run before real allocation.

Purpose: Satisfy the Phase 1 Dinamica-on-Euler contract without scattering container logic through the allocation code or committing repo-owned container images.
Output: Unified launcher in `src/dinamica_utils.r`, central Dinamica log placement, smoke-test script, and HPC documentation for the external image/wrapper contract.
</objective>

<execution_context>
@C:/Users/black/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/black/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repair-visibility/01-CONTEXT.md
@.planning/phases/01-repair-visibility/01-RESEARCH.md
@src/dinamica_utils.r
@scripts/submit_allocation.sh
@docs/README_HPC.md
@dinamica/dinamica_model/allocation.ego-decoded

<interfaces>
From src/dinamica_utils.r:
```r
exec_dinamica <- function(
  model_path,
  disable_parallel = TRUE,
  log_level = NULL,
  write_logfile = TRUE,
  echo = FALSE
)

run_allocation_dinamica <- function(work_dir, project_root = NULL, ...)
```

Required backend contract per D-09 to D-11:
```text
one R entrypoint;
local backend = direct DinamicaConsole;
HPC backend = `DINAMICA_EGO_8_HOME` is the absolute path to the external `.sif` artifact on Euler;
`exec_dinamica()` consumes that env var directly as the image argument to `apptainer exec` / `singularity exec`,
then runs `DinamicaConsole <model_path>` inside the container;
no caller outside `src/dinamica_utils.r` knows about container details;
smoke test before real allocation.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor the Dinamica adapter to a single local/HPC backend entrypoint with central logs</name>
  <files>src/dinamica_utils.r</files>
  <behavior>
    - `exec_dinamica()` remains the only caller-facing entrypoint.
    - Local runs invoke `DinamicaConsole` directly.
    - HPC runs probe `apptainer` first, `singularity` second, and execute against the `.sif` path stored in `DINAMICA_EGO_8_HOME`.
    - Timestamped Dinamica subprocess logs are written under central `logs/` and surfaced for later diagnosis.
    - A resolution helper exposes the selected backend, runtime command, exact `.sif` artifact path, and logfile path without executing Dinamica.
  </behavior>
  <action>Refactor `src/dinamica_utils.r` so backend selection happens inside the existing adapter per D-09, not in callers. Make the HPC contract explicit end-to-end for INFRA-01: on Euler, `DINAMICA_EGO_8_HOME` must be an absolute path to the external Dinamica `.sif` image, and `exec_dinamica()` must consume it directly as the container image argument in `apptainer exec <DINAMICA_EGO_8_HOME> DinamicaConsole <model_path>` with `singularity exec` as the only fallback runtime spelling. Do not leave any "SIF or wrapper" ambiguity in code or docs for this phase. Add a non-executing `resolve_dinamica_launch()` helper that returns the selected backend, runtime command (`apptainer` or `singularity` on HPC), exact `.sif` artifact path from `DINAMICA_EGO_8_HOME`, the full argument vector, and the central logfile path; `exec_dinamica()` should consume that helper rather than re-deriving its own launch contract. Make the helper support an explicit runtime override / no-probe mode so dry-run verification can prove HPC command resolution on a workstation where neither `apptainer` nor `singularity` is installed, while the real smoke test still performs the live runtime probe on Euler. Update log handling per D-07 and PIPE-07 so raw Dinamica stdout/stderr are preserved in `logs/` with timestamps, while key launch/exit events are easy for allocation-side logging to mirror. Keep argument passing shell-safe and avoid scattering container-specific conditionals into `src/allocation.r`.</action>
  <verify>
    <automated>micromamba run -n allocation_env Rscript --vanilla -e "source('src/setup.r'); source('src/utils.r'); source('src/dinamica_utils.r'); Sys.setenv(DINAMICA_EGO_8_HOME='/tmp/dinamica.sif'); launch <- resolve_dinamica_launch('dinamica/dinamica_model/allocation.ego-decoded', backend = 'hpc', runtime_override = 'apptainer', probe_runtime = FALSE); stopifnot(launch$runtime == 'apptainer', identical(normalizePath(launch$artifact_path, winslash='/', mustWork = FALSE), normalizePath('/tmp/dinamica.sif', winslash='/', mustWork = FALSE)), any(grepl('DinamicaConsole', launch$args)), grepl('logs[/\\\\].+\\.log$', launch$log_file))"</automated>
  </verify>
  <done>The Dinamica adapter hides local/HPC backend differences behind one function, treats `DINAMICA_EGO_8_HOME` as the Euler `.sif` path on HPC, and writes subprocess logs to the central log surface expected by Phase 1 diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Euler smoke-test contract and operator documentation</name>
  <files>scripts/smoke_test_dinamica.sh, docs/README_HPC.md, dinamica/dinamica_model/allocation.ego-decoded, dinamica/container/rocker-geospatial-dinamica.def, dinamica/container/README.md</files>
  <action>Create an operator-facing smoke-test script per D-11 that validates the explicit INFRA-01 contract before any real Stage 7 run: on Euler, `DINAMICA_EGO_8_HOME` must point to the external Dinamica `.sif`, the script probes `apptainer` first then `singularity`, and the live command must execute the minimal model as `apptainer exec "$DINAMICA_EGO_8_HOME" DinamicaConsole "$EGO_MODEL"` (or the `singularity exec` equivalent) with the same bind/temp rules `exec_dinamica()` uses. Make the script support both `--dry-run` and live execution, but require the live path to exit non-zero unless Dinamica completes successfully and a timestamped logfile is created under `logs/` (for example `logs/dinamica-smoke-*.log`). Commit the container definition/provenance/build-instructions artifacts required by INFRA-01 and Roadmap Phase 1 success criterion 6: a definition file rooted in the required `ethzplus/rocker-geospatial-dinamica` provenance, plus a short README that documents the exact `apptainer build`/`singularity build` flow and makes clear the built image stays external per D-10. Update the touched HPC docs to describe the exact `DINAMICA_EGO_8_HOME=/absolute/path/to/dinamica.sif` contract, required env vars, where the definition/build instructions live, the dry-run command, and the live Euler smoke-test command. If the existing allocation model asset needs a deterministic minimal-mode entry or comments for the smoke test, update `dinamica/dinamica_model/allocation.ego-decoded` directly instead of creating a second, divergent launcher path.</action>
  <verify>
    <automated>bash -lc "set -euo pipefail; bash -n scripts/smoke_test_dinamica.sh; bash scripts/smoke_test_dinamica.sh --dry-run --runtime apptainer --artifact /tmp/dinamica.sif --ego dinamica/dinamica_model/allocation.ego-decoded > smoke.out; rg -n 'DINAMICA_EGO_8_HOME|apptainer|/tmp/dinamica.sif|allocation.ego-decoded' smoke.out; rg -n 'DINAMICA_EGO_8_HOME=.*/dinamica\\.sif|ethzplus/rocker-geospatial-dinamica|apptainer build|singularity build|external artifact' dinamica/container/rocker-geospatial-dinamica.def dinamica/container/README.md docs/README_HPC.md; export DINAMICA_EGO_8_HOME=/project/<project>/containers/dinamica-ego-8.sif; bash scripts/smoke_test_dinamica.sh --runtime auto --artifact \"$DINAMICA_EGO_8_HOME\" --ego dinamica/dinamica_model/allocation.ego-decoded --live --require-log-under logs; latest_log=$(ls -1t logs/dinamica-smoke-*.log | head -n 1); test -n \"$latest_log\"; rg -n 'Dinamica|completed|success' \"$latest_log\""</automated>
  </verify>
  <done>Operators have one documented dry-run command and one live Euler smoke-test command for Dinamica wiring; the live smoke test succeeds only when the minimal model runs and writes a timestamped logfile under `logs/`; the repo documents the exact `.sif` contract plus committed container definition/provenance/build instructions without introducing repo-owned container build outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/config -> Dinamica launcher | External image/wrapper paths and bind roots enter subprocess execution here. |
| R runtime -> shell/container runtime | The adapter crosses from R into local binaries or HPC container executables. |
| Dinamica subprocess -> filesystem logs | External process output is persisted for diagnosis and must not pollute model directories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | E | `exec_dinamica()` backend selection | mitigate | Resolve the executable path explicitly, prefer `apptainer` then `singularity`, and log the chosen backend/image path before launch. |
| T-01-11 | T | bind/image path handling | mitigate | Accept only explicit absolute artifact/bind paths from the shared contract and pass them as structured `processx` args rather than shell-built strings where possible. |
| T-01-12 | R | Dinamica log artifacts | mitigate | Write timestamped subprocess logs to central `logs/` and record their paths so crash triage has a durable source of truth. |
</threat_model>

<verification>
Run the syntax/loader checks above, then execute the live Euler smoke test with `DINAMICA_EGO_8_HOME` set to the deployed `.sif` path. Completion requires exit code 0, a success marker in the smoke-test stdout/log, and a timestamped `logs/dinamica-smoke-*.log` artifact.
</verification>

<success_criteria>
Phase 1 has one Dinamica execution contract: local direct launch, HPC container/wrapper launch, central logs, and a smoke test that proves the Euler wiring before real allocation work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repair-visibility/01-repair-visibility-04-SUMMARY.md`
</output>
