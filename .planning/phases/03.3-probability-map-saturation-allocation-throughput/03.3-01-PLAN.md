---
phase: 03.3-probability-map-saturation-allocation-throughput
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/local_config.yaml
  - config/hpc_config.yaml
  - src/saturation_diagnostics.r
  - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md
autonomous: true
requirements:
  - ALLOC-10
tags:
  - allocation
  - dinamica
  - saturation
  - probability-map
  - region-reconnaissance
must_haves:
  truths:
    - "The authoritative list of Peruvian region labels (from inputs/regionalization/regions.json) is recorded in a reconnaissance markdown file under the phase directory, with one label per line, so Wave 4 can iterate them via ALLOCATION_REGION_FILTER without hardcoding."
    - "config/local_config.yaml and config/hpc_config.yaml both contain saturation_threshold (default 0.90) and saturation_exempt ([]) under simulation_trans_rates_params with byte-identical key shape."
    - "src/saturation_diagnostics.r exports pure-function helpers (compute_per_transition_metrics, write_saturation_summary, log_audit_saturation_line) that the Wave 2 inline hook and the Wave 3 standalone script can both call."
  artifacts:
    - path: .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md
      provides: Authoritative region label list for D-09 verification scope
      contains: "## Region labels"
    - path: config/local_config.yaml
      provides: saturation_threshold + saturation_exempt keys
      contains: "saturation_threshold:"
    - path: config/hpc_config.yaml
      provides: mirror of saturation_threshold + saturation_exempt keys
      contains: "saturation_threshold:"
    - path: src/saturation_diagnostics.r
      provides: Reusable metric helpers (compute_per_transition_metrics, write_saturation_summary, log_audit_saturation_line)
      min_lines: 120
  key_links:
    - from: src/saturation_diagnostics.r
      to: src/utils.r (log_msg)
      via: source order — log_msg() is loaded before saturation_diagnostics.r in both inline (allocation.r entry) and standalone (scripts/diagnose_allocation_saturation.r) call sites
      pattern: "log_msg\\("
    - from: src/saturation_diagnostics.r
      to: config[["simulation_trans_rates_params"]][["saturation_threshold"]]
      via: function arg threshold passed by caller after get_config()
      pattern: "saturation_threshold"
---

<objective>
Lay the parallel-safe Wave 1 groundwork for Phase 3.3:

1. Run a reconnaissance pass against `inputs/regionalization/regions.json` to enumerate the actual Peruvian region labels and pin that list into the phase directory so Wave 4 verification can iterate them without hardcoding (D-09).
2. Add `saturation_threshold` (default 0.90) and `saturation_exempt` (empty list) keys to both `config/local_config.yaml` and `config/hpc_config.yaml` under `simulation_trans_rates_params`, mirroring the existing block shape (D-08).
3. Create `src/saturation_diagnostics.r` — a new helper module exporting `compute_per_transition_metrics()`, `write_saturation_summary()`, and `log_audit_saturation_line()` — that both the Wave 2 inline hook in `src/allocation.r` and the Wave 3 standalone script `scripts/diagnose_allocation_saturation.r` will call (D-05, D-06).

These three changes have zero file overlap with the Wave 2 single-source writer refactor, so they ship in the same wave as parallel-safe preparation.

Purpose: Anchor the D-09 verification scope to data (not assumptions), unlock the config-driven threshold/exempt-list pattern Wave 2 will consume, and ship reusable metric helpers with no upstream allocation.r dependencies so they can be unit-tested in Wave 3 against synthetic raster fixtures.

Output: One reconnaissance markdown, two config edits, one new src/ helper file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@config/local_config.yaml
@config/hpc_config.yaml
@src/utils.r
@src/calibrate_allocation_parameters.r
</context>

<tasks>

<task type="auto">
  <name>Task 1: Region reconnaissance — pin the four Peruvian region labels into the phase directory</name>
  <files>.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md</files>
  <read_first>
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md (sections "Q2 — The actual region set", "Pitfall 6", "Environment Availability") — research already established that regions.json is a data-filesystem artifact (E:/nascent-lulcc-agg/inputs/regionalization/regions.json locally, $HPC_SCRATCH_ROOT/inputs/regionalization/regions.json on HPC) not in git, and that the executor must read it at runtime.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md (decision D-09) — confirms BAU × all Peruvian regions × first simulation timestep is the verification scope.
    - config/local_config.yaml — find the data_basepath value to resolve the regions.json absolute path locally.
    - src/region_prep.r (lines 1-50) — confirm regions.json is keyed by a "label" field (lowercased, spaces→underscores from regions.shp column Workshop_r).
  </read_first>
  <action>
    Resolve the absolute path to regions.json using config[["reg_dir"]] (local: E:/nascent-lulcc-agg/inputs/regionalization/regions.json). If the local file is reachable from the executor, run `Rscript -e 'cat(jsonlite::fromJSON("E:/nascent-lulcc-agg/inputs/regionalization/regions.json")$label, sep = "\n")'` (or use jq if regions.json is plain JSON without R) to print the label vector. If the local path is NOT reachable, fall back to scanning `outputs/transition_models/` (or wherever .qs files live per config[["transition_model_dir"]]) for the suffix after the last underscore in `*-*_{region_label}.qs` filenames and union those labels.

    Write the result to `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md` with sections:
    - `## Region labels` — fenced block listing each label one-per-line, in the order returned by jsonlite::fromJSON (or alphabetical if derived from .qs filename scan). Include the count.
    - `## Source` — exact command run and absolute path of regions.json (or `.qs` directory scanned).
    - `## Verification scope` — restate D-09: "BAU × {label1, label2, ...} × 2022→2026 first timestep" with the labels substituted in.
    - `## Notes` — if the four-region count from CONTEXT.md does not match what regions.json returned, raise the discrepancy and stop. Do NOT silently re-cast.

    Do NOT hardcode region names anywhere. Do NOT proceed if neither regions.json nor a .qs scan resolves the label set — instead, write the file with an explicit `## BLOCKED` section explaining what the executor tried and exit non-zero so Wave 4 verification has authoritative input.
  </action>
  <verify>
    <automated>test -f .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md &amp;&amp; grep -q "^## Region labels$" .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md &amp;&amp; grep -q "^## Source$" .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md &amp;&amp; grep -q "^## Verification scope$" .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md` exists with the three top-level H2 sections (Region labels, Source, Verification scope).
    - The `## Region labels` section contains a fenced block with one label per line and a recorded count.
    - The `## Source` section names the exact data file scanned (regions.json or .qs filename scan) with an absolute path.
    - If the data filesystem was unreachable, the file contains a `## BLOCKED` section instead and the task exits non-zero.
    - No region label is hardcoded outside this reconnaissance file (grep across `src/` and `scripts/` produced no new `costa_peruana` / `selva_andina` literals introduced by this task).
  </acceptance_criteria>
  <done>The authoritative Peruvian region label list is committed to the phase directory; Wave 4 verification can read it without re-deriving and without hardcoding.</done>
</task>

<task type="auto">
  <name>Task 2: Add saturation_threshold and saturation_exempt to both config YAMLs</name>
  <files>config/local_config.yaml, config/hpc_config.yaml</files>
  <read_first>
    - config/local_config.yaml (lines 70-100, current `simulation_trans_rates_params:` block) — confirm key indentation (2 spaces), value style (no quotes on numerics, inline `[]` for empty lists per the `forbid_inflow: []` precedent).
    - config/hpc_config.yaml (lines 75-103, mirror block) — confirm the same shape.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md (sections "`config/local_config.yaml` — add `saturation_threshold` + `saturation_exempt` keys" and "`config/hpc_config.yaml` — mirror keys") — exact insertion lines and key style.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md (Q3) — config key location rationale.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md (D-08) — defaults: threshold 0.90, exempt list initially empty.
  </read_first>
  <action>
    In `config/local_config.yaml` under the `simulation_trans_rates_params:` block (currently ending at the `scale_factor:` line before `transition_identification_params:`), append two new keys at the end of the block with 2-space indentation:
    - `saturation_threshold: 0.90`
    - `saturation_exempt: []  # entries: {from_lulc: <int>, to_lulc: <int>, reason: <text>}`

    Apply the exact same two-line addition to `config/hpc_config.yaml` at the corresponding location (the mirror block under `simulation_trans_rates_params:`). The values are identical across environments because the threshold and exempt list are scientific decisions, not infrastructure-specific (PATTERNS.md "Apply pattern" note).

    Do NOT add the keys under any other parent (no new top-level section). Do NOT change any existing key. Keep the trailing comment annotation `# entries: {from_lulc: <int>, to_lulc: <int>, reason: <text>}` so future maintainers understand the schema without grepping documentation.
  </action>
  <verify>
    <automated>grep -A1 "^simulation_trans_rates_params:" config/local_config.yaml | head -200 &gt; /dev/null &amp;&amp; grep -E "^[[:space:]]+saturation_threshold:[[:space:]]*0\.90" config/local_config.yaml &amp;&amp; grep -E "^[[:space:]]+saturation_exempt:[[:space:]]*\[\]" config/local_config.yaml &amp;&amp; grep -E "^[[:space:]]+saturation_threshold:[[:space:]]*0\.90" config/hpc_config.yaml &amp;&amp; grep -E "^[[:space:]]+saturation_exempt:[[:space:]]*\[\]" config/hpc_config.yaml &amp;&amp; Rscript -e 'cfg &lt;- yaml::read_yaml("config/local_config.yaml"); stopifnot(identical(cfg[["simulation_trans_rates_params"]][["saturation_threshold"]], 0.90)); stopifnot(is.list(cfg[["simulation_trans_rates_params"]][["saturation_exempt"]]) || identical(cfg[["simulation_trans_rates_params"]][["saturation_exempt"]], list()))'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^[[:space:]]+saturation_threshold:[[:space:]]*0\.90" config/local_config.yaml` matches exactly one line.
    - `grep -E "^[[:space:]]+saturation_exempt:[[:space:]]*\[\]" config/local_config.yaml` matches exactly one line.
    - Both keys are nested under the `simulation_trans_rates_params:` block (verified by `Rscript -e 'yaml::read_yaml("config/local_config.yaml")[["simulation_trans_rates_params"]][["saturation_threshold"]]'` returning 0.9).
    - `config/hpc_config.yaml` contains the identical two lines under the same parent block.
    - `yaml::read_yaml()` on both files succeeds without parse errors after the change.
    - No other key in either file is modified (verified via `git diff --stat` showing exactly two files changed and a small line-count delta on each).
  </acceptance_criteria>
  <done>Wave 2's allocation.r inline hook and Wave 3's standalone script can both read `config[["simulation_trans_rates_params"]][["saturation_threshold"]]` and `[["saturation_exempt"]]` without further config wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/saturation_diagnostics.r with reusable metric helpers</name>
  <files>src/saturation_diagnostics.r</files>
  <read_first>
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md (sections "Pattern 4: Inline saturation summary", "Code Examples", "Q6 — Demand denominator semantics", "Pitfall 3", "Pitfall 4") — full metric semantics, the area_t identity (`Rate × count(anterior_vals == from_val)`), and the check.names = FALSE requirement on all CSV round-trips.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md (sections "`src/saturation_diagnostics.r` (NEW)", "Shared Patterns" — log_msg, AUDIT log line grammar, terra::values(mat = FALSE) idiom, CSV round-trip with check.names = FALSE) — header style, function signature style, demand-denominator pattern, metric-row construction pattern.
    - src/calibrate_allocation_parameters.r (lines 1000-1015, 700-760) — terra::global / terra::values per-pair counting analog and per-region CSV writer pattern.
    - src/utils.r (lines 1010-1025) — log_msg() signature.
    - src/allocation.r (lines 16-44, 1642-1655) — header docstring style and function signature style (each arg on own line, no default config arg for internal helpers).
    - scripts/summarise_allocation_profile.r (lines 79-128) — data.frame metric-row aggregation via do.call(rbind, lapply(...)) with stringsAsFactors = FALSE.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md (D-05, D-06, D-07) — locked decisions: both surfaces share helpers, exact metric set, spatial autocorrelation NOT computed.
  </read_first>
  <behavior>
    - Test 1 (compute_per_transition_metrics on synthetic 10×10 raster with from=101→to=102, Rate=0.5, anterior has 50 from-class cells, posterior has 20 of those cells flipped to to-class):
      - `demanded_cells` == 25 (`round(0.5 × 50)`)
      - `placed_cells` == 20
      - `placed_frac` == 20/25 == 0.8
    - Test 2 (compute_per_transition_metrics column set is exactly the 16 columns in the saturation_summary.csv schema from RESEARCH.md `Data Structures and Join Keys`): id_trans, from_val, to_val, rate, demanded_cells, placed_cells, placed_frac, coverage, p50, p90, p95, p99, pmax, demand_vs_capacity, exempt, floor_met. No more, no fewer.
    - Test 3 (coverage on a probability raster with values c(0, 0, 0.5, 0.5, NA)): coverage == 2 / 4 == 0.5 (NA excluded from denominator).
    - Test 4 (percentiles on prob_vals == c(0.1, 0.5, 0.9, 0.95, 0.99)): p50 == 0.5, p90 == 0.9, p95 == 0.95, p99 == 0.99, pmax == 0.99.
    - Test 5 (exempt flag is TRUE when (from_val, to_val) appears in the exempt list passed in): for exempt = list(list(from_lulc = 101L, to_lulc = 102L, reason = "test")) and transition (101, 102), `exempt == TRUE` and `floor_met` is computed but the orchestration treats this row as a passthrough.
    - Test 6 (log_audit_saturation_line emits the exact AUDIT prefix grammar from PATTERNS.md): the emitted line starts with `AUDIT stage=saturation scenario=` and contains `active=`, `met=`, `exempt=`, `failed=`, `threshold=` keys in that order.

    Implementation notes:
    - All read.csv calls use `check.names = FALSE` (Pitfall 4).
    - All raster ops use `terra::values(mat = FALSE)` once per raster then aggregate in base R (PATTERNS.md "terra::values(mat = FALSE) + base-R vector ops").
    - All log lines go through `log_msg(sprintf(...), log_file)`, never `message()` or `cat()` directly.
    - No spatial autocorrelation computation (D-07).
  </behavior>
  <action>
    Create new file `src/saturation_diagnostics.r` matching the project's R source-file conventions (lowercase .r, snake_case functions, 2-space indent, `<-` assignment, no `library()` calls, namespace::function references — `terra::`, `stats::`, `utils::`).

    Header docstring block: a comment block in the shape of `src/allocation.r:16-44` (narrative paragraph followed by a `Usage:` block, with `#`-line conventions per CONVENTIONS.md). Document the metric definitions verbatim from RESEARCH.md D-06:
      - coverage — fraction of valid (non-NA) cells with prob > 0
      - p50, p90, p95, p99 — quantile of the probability map distribution
      - pmax — max(prob_vals)
      - demanded_cells — round(Rate × count(anterior_vals == from_val))
      - placed_cells — count((anterior_vals == from_val) & (posterior_vals == to_val))
      - placed_frac — placed_cells / demanded_cells
      - demand_vs_capacity — demanded_cells / count(prob_vals >= p50)

    Export three functions:

    1. `compute_per_transition_metrics(filtered_df, prob_map_dir, anterior_path, posterior_path, threshold, exempt)` — returns a data.frame with the 16 columns listed in the Behavior section (id_trans, from_val, to_val, rate, demanded_cells, placed_cells, placed_frac, coverage, p50, p90, p95, p99, pmax, demand_vs_capacity, exempt, floor_met). Implementation iterates `seq_len(nrow(filtered_df))` with `lapply(...)` → `do.call(rbind, ...)` (PATTERNS.md metric-row construction pattern). For each row k, loads `file.path(prob_map_dir, sprintf("%03d_id_trans_%d.tif", k, id_trans))` via terra::rast and computes the per-transition metrics. The exempt match uses `vapply(exempt, function(e) e$from_lulc == from_val && e$to_lulc == to_val, logical(1))`. The `floor_met` flag is `!is.na(placed_frac) && placed_frac >= threshold`. All vectorisation: pull `anterior_vals <- terra::values(anterior, mat = FALSE)` and `posterior_vals <- terra::values(posterior, mat = FALSE)` ONCE before the loop, then do base-R aggregation per transition.

    2. `write_saturation_summary(work_dir, scenario, region_label, region_suffix, year_ant, anterior_path, posterior_path, config, log_file)` — orchestrator. Reads `filtered_df` from `file.path(work_dir, "trans_rates.csv")` (the work_dir copy written by setup_allocation_inputs after Wave 2's refactor — for now, in Wave 1, the function reads it agnostically; Wave 2 guarantees it's the filtered copy). Extracts `threshold <- config[["simulation_trans_rates_params"]][["saturation_threshold"]]` and `exempt <- config[["simulation_trans_rates_params"]][["saturation_exempt"]]`. Computes `prob_map_dir <- file.path(work_dir, "probability_map_dir")`. Calls `compute_per_transition_metrics(...)` to get the summary df. Writes it to `file.path(work_dir, "saturation_summary.csv")` via `utils::write.csv(summary_df, ..., row.names = FALSE)`. Calls `log_audit_saturation_line(summary_df, scenario, region_label, year_ant, threshold, log_file)`. Returns `invisible(summary_df)`.

    3. `log_audit_saturation_line(summary, scenario, region_label, year_ant, threshold, log_file)` — emits exactly the line shape from PATTERNS.md "AUDIT log line shape":
       `AUDIT stage=saturation scenario=%s region=%s year_ant=%d active=%d met=%d exempt=%d failed=%d threshold=%.2f`
       where `active = nrow(summary)`, `met = sum(summary$floor_met)`, `exempt = sum(summary$exempt)`, `failed = sum(!summary$floor_met & !summary$exempt)`. Emit via `log_msg(sprintf(...), log_file)`.

    Function signatures: each arg on its own line, no default config arg (CONVENTIONS.md "Purely internal helpers have no default config arg"). Use `stringsAsFactors = FALSE` everywhere data.frame is constructed. Use `check.names = FALSE` on the read.csv of trans_rates.csv (Pitfall 4).

    Do NOT include any spatial autocorrelation, Moran's I, or patch-count computation (D-07 deferred). Do NOT compute demand-vs-capacity using `Rate × total_valid_cells_in_region` — it must be `Rate × count(anterior_vals == from_val)` (Pitfall 3). Do NOT use `library()` at the top of the file (CONVENTIONS.md "No library() calls in src/").
  </action>
  <verify>
    <automated>Rscript -e 'env &lt;- new.env(parent = baseenv()); sys.source("src/utils.r", envir = env); sys.source("src/saturation_diagnostics.r", envir = env); stopifnot(is.function(env$compute_per_transition_metrics)); stopifnot(is.function(env$write_saturation_summary)); stopifnot(is.function(env$log_audit_saturation_line))' &amp;&amp; grep -E "^compute_per_transition_metrics <- function" src/saturation_diagnostics.r &amp;&amp; grep -E "^write_saturation_summary <- function" src/saturation_diagnostics.r &amp;&amp; grep -E "^log_audit_saturation_line <- function" src/saturation_diagnostics.r &amp;&amp; grep -F "check.names = FALSE" src/saturation_diagnostics.r &amp;&amp; grep -F "terra::values" src/saturation_diagnostics.r &amp;&amp; grep -F "AUDIT stage=saturation" src/saturation_diagnostics.r &amp;&amp; ! grep -E "library\(" src/saturation_diagnostics.r &amp;&amp; ! grep -iE "moran|autocorr|spatial.?auto" src/saturation_diagnostics.r</automated>
  </verify>
  <acceptance_criteria>
    - File `src/saturation_diagnostics.r` exists with all three exported helpers defined at top-level (`compute_per_transition_metrics`, `write_saturation_summary`, `log_audit_saturation_line`).
    - Header docstring block contains the seven metric definitions verbatim (coverage, p50, p90, p95, p99, pmax, demanded_cells, placed_cells, placed_frac, demand_vs_capacity) per RESEARCH.md D-06.
    - `grep -F "check.names = FALSE" src/saturation_diagnostics.r` matches at least once (every read.csv of a Dinamica-bound artifact preserves From*/To* markers — Pitfall 4).
    - `grep -F "terra::values" src/saturation_diagnostics.r` matches at least once and `grep -F "mat = FALSE" src/saturation_diagnostics.r` matches (terra::values pulled once per raster, not re-walked — Shared Patterns).
    - `grep -F "AUDIT stage=saturation scenario=" src/saturation_diagnostics.r` matches the sprintf format string (AUDIT log grammar — PATTERNS.md "AUDIT log line shape").
    - `grep -E "^library\(" src/saturation_diagnostics.r` matches zero lines (CONVENTIONS.md "No library() calls in src/").
    - `grep -iE "moran|autocorr|spatial.?auto" src/saturation_diagnostics.r` matches zero lines (D-07 deferred — no spatial autocorrelation).
    - `Rscript -e 'sys.source("src/utils.r"); sys.source("src/saturation_diagnostics.r")'` succeeds with no errors (file parses and source order is correct — utils.r before saturation_diagnostics.r).
  </acceptance_criteria>
  <done>The shared metric helper module is in place. Wave 2 inline hook and Wave 3 standalone script can both source `src/saturation_diagnostics.r` after `src/utils.r` and call `write_saturation_summary()` / `compute_per_transition_metrics()` without further dependencies on `src/allocation.r`. Synthetic-raster unit tests in Wave 3 will verify the behavior contracts above.</done>
</task>

</tasks>

<verification>
- All three Wave 1 artifacts are committed: reconnaissance markdown, two config edits, one new src/ file.
- `Rscript -e 'sys.source("src/utils.r"); sys.source("src/saturation_diagnostics.r")'` succeeds.
- `yaml::read_yaml("config/local_config.yaml")` and `yaml::read_yaml("config/hpc_config.yaml")` both succeed and both return `simulation_trans_rates_params$saturation_threshold == 0.90`.
- The reconnaissance file's `## Region labels` block contains a count that Wave 4 verification can iterate.
- No file in `files_modified:` of Plan 02 (src/allocation.r) is touched here.
</verification>

<success_criteria>
- The authoritative Peruvian region label list is pinned in the phase directory and Wave 4 can read it without re-deriving (D-09).
- The two new config keys are present in both YAMLs under `simulation_trans_rates_params` with matching defaults (D-08).
- `src/saturation_diagnostics.r` exports the three helpers that the Wave 2 inline hook and the Wave 3 standalone script will both call (D-05, D-06).
- No source file outside Wave 1's `files_modified:` list was modified.
</success_criteria>

<output>
Create `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-SUMMARY.md` when done, recording:
- The exact Peruvian region label list and the source path (regions.json or .qs filename scan).
- Confirmation of the two config keys present in both YAMLs.
- The three exported helpers in `src/saturation_diagnostics.r` with their signatures.
- Any deviations from the plan (e.g., regions.json unreachable → BLOCKED reconnaissance file) and the operator action required to unblock Wave 4.
</output>
