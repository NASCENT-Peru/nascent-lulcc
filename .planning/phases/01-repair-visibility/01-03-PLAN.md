---
phase: 01-repair-visibility
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/allocation.r
  - src/dinamica_utils.r
  - src/utils.r
  - scripts/run_allocation.r
  - scripts/diagnose_alloc_crash.sh
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
must_haves:
  truths:
    - "Running Stage 7 with missing prerequisites aborts before any region work with one actionable list of all gaps."
    - "Per-region logs contain structured lifecycle messages and crash sentinels with scenario, region, timestep, and transition context."
    - "`setup_allocation_inputs()` and `run_allocation_dinamica(..., log_file)` both write structured messages into the same per-region log."
    - "RSS profiling emits real numeric values on local Windows and HPC Linux runs."
    - "One post-mortem command surfaces SLURM OOM evidence and points to the relevant region and Dinamica logs."
  artifacts:
    - path: "src/allocation.r"
      provides: "Portable RSS profiling, Stage 7 pre-flight, worker breadcrumbing"
    - path: "src/dinamica_utils.r"
      provides: "Log-file-aware Dinamica worker helper that mirrors critical lifecycle events"
    - path: "src/utils.r"
      provides: "Structured logging helpers and worker-log support"
    - path: "scripts/run_allocation.r"
      provides: "Thin allocation entrypoint that can run pre-flight without self-healing installs"
    - path: "scripts/diagnose_alloc_crash.sh"
      provides: "Post-mortem OOM/log correlation command"
  key_links:
    - from: "scripts/run_allocation.r"
      to: "src/allocation.r"
      via: "pre-flight-only or full run entry"
      pattern: "run_allocation"
    - from: "src/allocation.r"
      to: "src/dinamica_utils.r"
      via: "worker passes per-region log path into `run_allocation_dinamica(..., log_file=...)`"
      pattern: "run_allocation_dinamica\\(.*log_file"
    - from: "src/allocation.r"
      to: "scripts/diagnose_alloc_crash.sh"
      via: "shared sentinel/log naming contract"
      pattern: "SENTINEL|STATE|PROFILE"
---

<objective>
Add the consolidated Stage 7 operator gate and the logging/diagnosis surface that makes crashes explainable within minutes.

Purpose: Close the visibility gap before deeper runtime fixes by making missing prerequisites, worker crashes, and RSS behavior observable from one per-region log plus one diagnosis command.
Output: Portable RSS profiling, standalone pre-flight helper, structured worker breadcrumbs/sentinels, and a post-mortem diagnostic script.
</objective>

<execution_context>
@C:/Users/bblack/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repair-visibility/01-CONTEXT.md
@.planning/phases/01-repair-visibility/01-RESEARCH.md
@src/allocation.r
@src/utils.r
@scripts/run_allocation.r
@scripts/submit_allocation_profile.sh

<interfaces>
From src/allocation.r:
```r
run_allocation <- function(config = get_config())
setup_allocation_inputs <- function(...)
prof_tic <- function()
prof_toc <- function(t0, tag, log_file = NULL)
```

From src/utils.r:
```r
log_msg <- function(msg, log_file = NULL, also_console = TRUE)
initialize_worker_log <- function(log_dir, trans_name)
```

Required crash context per D-06:
```text
last-known stage, scenario, region, timestep, transition context
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement portable RSS profiling and the consolidated Stage 7 pre-flight gate</name>
  <files>src/allocation.r, scripts/run_allocation.r</files>
  <behavior>
    - `rss_before/after/delta` are real numeric values on Windows and Linux via `ps`.
    - Peak/HWM remains available on Linux as optional enrichment, not the only RSS source.
    - `run_allocation()` invokes a standalone pre-flight helper before any allocation work.
    - Missing env vars, packages, model files, or Dinamica backend availability are reported as one actionable failure list.
  </behavior>
  <action>Replace the `/proc`-only RSS path with a portable `ps::ps_memory_info()`-based implementation for real RSS metrics per OBS-01, keeping Linux-only peak enrichment if useful. Add a standalone pre-flight helper called by `run_allocation()` per D-01 through D-04 that validates only Stage 7 runtime prerequisites per D-02, aggregates all gaps into one failure report per D-03, and can be invoked from `scripts/run_allocation.r` in a pre-flight-only mode for verification. Make that pre-flight-only path testable with multiple injected missing prerequisites in one run by accepting a fixture/override input (for env vars, packages, files, and Dinamica backend expectations) so verification can assert one consolidated actionable failure list without mutating the real workspace. Ensure `run_allocation()` returns before `future::plan()`, worker-log initialization, or any region allocation work when the gate fails. Remove or bypass any runtime package self-healing in the entry script so missing packages fail through the new gate instead of mutating the environment.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$tmp = Join-Path $env:TEMP ('preflight-' + [guid]::NewGuid().ToString()); New-Item -ItemType Directory -Path $tmp | Out-Null; $fixture = Join-Path $tmp 'fixture.json'; Set-Content -Path $fixture -Value '{\"env\":[\"MISSING_ENV_A\",\"MISSING_ENV_B\"],\"packages\":[\"definitelyMissingPkg\"],\"files\":[\"missing/model.rds\"],\"dynamica\":{\"backend\":\"hpc\",\"runtime\":\"apptainer\",\"artifact\":\"missing/dinamica.sif\"}}'; $out = Join-Path $tmp 'out.txt'; & micromamba run -n allocation_env Rscript --vanilla scripts/run_allocation.r --preflight-only --preflight-fixture $fixture *> $out; if ($LASTEXITCODE -eq 0) { exit 1 }; $lines = Get-Content $out; if ((($lines | Select-String 'Allocation pre-flight failed').Count) -ne 1) { exit 1 }; foreach ($needle in 'MISSING_ENV_A','MISSING_ENV_B','definitelyMissingPkg','missing/model.rds','missing/dinamica.sif') { if (-not ($lines | Select-String -SimpleMatch $needle)) { exit 1 } }; if ($lines | Select-String 'future::plan|Initializing worker|Starting allocation') { exit 1 }"</automated>
  </verify>
  <done>`run_allocation()` cannot start region work until the consolidated pre-flight passes, the pre-flight emits exactly one actionable failure list even when several prerequisites are missing at once, and profiling log lines report numeric RSS values instead of `NAMB`/`NA` for the core RSS fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add structured worker breadcrumbs, crash sentinels, and one-command post-mortem diagnosis</name>
  <files>src/allocation.r, src/dinamica_utils.r, src/utils.r, scripts/diagnose_alloc_crash.sh</files>
  <behavior>
    - Major Stage 7 lifecycle events are written as structured messages to the per-region log.
    - Worker failure paths emit one sentinel containing the last-known scenario/region/timestep/transition state.
    - `setup_allocation_inputs()` receives the region log path and logs inside the worker path.
    - `run_allocation_dinamica()` accepts `log_file` explicitly and mirrors Dinamica launch/exit/log-path events into that per-region log per D-05 and OBS-03.
    - `diagnose_alloc_crash.sh` surfaces `sacct`/optional `seff`/cgroup evidence and points at the matching log artifacts.
  </behavior>
  <action>Thread the per-region log path deeper into allocation internals per D-05, at minimum into `setup_allocation_inputs()` and directly into `run_allocation_dinamica(..., log_file)` in `src/dinamica_utils.r`, with the worker caller in `src/allocation.r` passing the same region log path through both helpers. Standardize only critical lifecycle messages per D-08, maintain an in-memory breadcrumb state, and flush a sentinel record on handled errors or unexpected worker termination context per D-06. Require `run_allocation_dinamica()` to emit structured launch, external-log-path, and exit/failure messages into the per-region log so OBS-03 is satisfied even before raw Dinamica stdout/stderr is inspected. Add `scripts/diagnose_alloc_crash.sh` to query SLURM accounting (`sacct`, optional `seff`, cgroup memory when present), correlate the latest region/sentinel artifacts, and print one actionable crash summary satisfying OBS-02. Do not attempt to normalize every log line in Phase 1.</action>
  <verify>
    <automated>micromamba run -n allocation_env Rscript --vanilla -e "source('src/utils.r'); source('src/dinamica_utils.r'); log_file <- tempfile(fileext='.log'); run_allocation_dinamica('dinamica/dinamica_model/allocation.ego-decoded', log_file = log_file, dry_run = TRUE); lines <- readLines(log_file, warn = FALSE); stopifnot(any(grepl('DINAMICA_(START|LOG_PATH|EXIT|FAIL)', lines)))" && bash -lc "set -euo pipefail; tmp=$(mktemp -d); printf 'JobIDRaw|State|MaxRSS\\n123|OUT_OF_MEMORY|128000K\\n' > \"$tmp/sacct.txt\"; printf 'Memory Efficiency: 99.1%% of 125.0 GB\\n' > \"$tmp/seff.txt\"; printf '{\"event\":\"SENTINEL\",\"scenario\":\"BAU\",\"region\":\"Andes\",\"timestep\":2030,\"transition\":\"forest_to_crop\"}\\n' > \"$tmp/region.log\"; bash scripts/diagnose_alloc_crash.sh --fixture-dir \"$tmp\" > \"$tmp/out.txt\"; rg -n 'SENTINEL|OUT_OF_MEMORY|MaxRSS|forest_to_crop' \"$tmp/out.txt\""</automated>
  </verify>
  <done>Per-region logs show structured state transitions and a crash sentinel when a worker fails, and the diagnosis script can be run as the single post-mortem command referenced in the phase goal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem/env -> Stage 7 pre-flight | Runtime prerequisites from the local/HPC machine are untrusted until validated. |
| worker state -> log artifacts | Failure context must cross from volatile worker memory into durable logs. |
| SLURM accounting -> diagnosis script | External cluster accounting output is ingested and summarized for operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | D | `run_allocation()` pre-flight | mitigate | Validate all required env vars, packages, model files, and backend executables before `future::plan()` or any region work starts. |
| T-01-08 | R | worker crash logging | mitigate | Emit structured lifecycle messages and a terminal sentinel with last-known state so SIGKILL-style failures leave durable evidence. |
| T-01-09 | T | `diagnose_alloc_crash.sh` | mitigate | Consume `sacct`/`seff` via quoted arguments and print raw evidence paths/IDs instead of shell-evaluating cluster output. |
</threat_model>

<verification>
Run the pre-flight command and the diagnosis help command. After implementation, induce a controlled missing-package or missing-env-var failure and confirm the output is one consolidated actionable list before any work starts.
</verification>

<success_criteria>
The roadmap observability criteria are reachable from one allocation entrypoint and one diagnosis script: pre-flight fails fast with one list, logs carry structured breadcrumbs/sentinels, and RSS values are numeric.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repair-visibility/01-repair-visibility-03-SUMMARY.md`
</output>
