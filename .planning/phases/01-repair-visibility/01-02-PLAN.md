---
phase: 01-repair-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - environments/allocation_env.yml
  - scripts/hpc_common.sh
  - scripts/setup_environments.sh
  - scripts/submit_allocation.sh
  - scripts/submit_allocation_profile.sh
autonomous: true
requirements:
  - MEM-06
  - PIPE-04
must_haves:
  truths:
    - "Stage 7 batch jobs activate the canonical allocation environment rather than a drifted training environment."
    - "All prediction-time packages required by Stage 7 are provisioned in the environment file the submit scripts actually use."
    - "HPC shell helpers derive paths from `$USER` and explicit scratch/temp env vars instead of hardcoded `black` paths."
  artifacts:
    - path: "environments/allocation_env.yml"
      provides: "Canonical Stage 7 dependency definition"
    - path: "scripts/submit_allocation.sh"
      provides: "Stage 7 batch entrypoint activating the canonical env"
    - path: "scripts/submit_allocation_profile.sh"
      provides: "Profile batch entrypoint activating the canonical env"
    - path: "scripts/hpc_common.sh"
      provides: "Shared HPC bootstrap honoring explicit scratch/temp contract"
    - path: "scripts/setup_environments.sh"
      provides: "Environment creation flow aligned with canonical allocation env"
  key_links:
    - from: "scripts/submit_allocation.sh"
      to: "environments/allocation_env.yml"
      via: "ENV_NAME allocation_env"
      pattern: "allocation_env"
    - from: "scripts/hpc_common.sh"
      to: ".env.template"
      via: "shared scratch/temp variable names"
      pattern: "TERRA_TEMP|NASCENT_LULCC"
---

<objective>
Make the allocation environment canonical for Stage 7 and propagate the shared HPC path contract through the shell/bootstrap layer.

Purpose: Resolve the environment drift behind MEM-06 and ensure HPC operators can run allocation on a fresh checkout without editing helper scripts.
Output: Completed `allocation_env.yml`, shell bootstrap scripts aligned to `$USER`/explicit scratch vars, and submit scripts that activate the same environment they validate.
</objective>

<execution_context>
@C:/Users/black/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/black/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repair-visibility/01-CONTEXT.md
@.planning/phases/01-repair-visibility/01-RESEARCH.md
@environments/allocation_env.yml
@scripts/hpc_common.sh
@scripts/setup_environments.sh
@scripts/submit_allocation.sh
@scripts/submit_allocation_profile.sh
@.env.template

<interfaces>
Current submit-script contract:
```bash
ENV_NAME="transition_model_env"
ENV_PATH="$ENV_BASE_PATH/$ENV_NAME"
setup_common_env
activate_env "$ENV_PATH"
```

Required Stage 7 package set from REQUIREMENTS.md:
```text
r-parsnip, r-recipes, r-ranger, r-xgboost (1.7.x), r-tidypredict,
r-butcher, r-ps, r-lobstr, r-bundle, r-qs, r-rhpcblasctl
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonicalize the Stage 7 execution environment and activation target</name>
  <files>environments/allocation_env.yml, scripts/submit_allocation.sh, scripts/submit_allocation_profile.sh</files>
  <behavior>
    - The environment file used by Stage 7 contains the full prediction/runtime package set from MEM-06.
    - Allocation submit scripts activate that same environment, not `transition_model_env`.
    - Batch verification uses the same env that pre-flight will later validate.
  </behavior>
  <action>Expand `environments/allocation_env.yml` to include the full prediction-time dependency set required by MEM-06, keeping the `r-xgboost` 1.7.x pin and the existing Stage 7 runtime packages. Update both allocation submit scripts so Stage 7 activates `allocation_env` atomically with this file per the user constraint on MEM-06 drift; do not "fix" the YAML while leaving batch execution on `transition_model_env`. Keep profile-mode variables and SLURM options intact.</action>
  <verify>
    <automated>rg -n "allocation_env|transition_model_env|r-parsnip|r-recipes|r-ranger|r-xgboost|r-tidypredict|r-butcher|r-ps|r-lobstr|r-bundle|r-qs|r-rhpcblasctl" environments/allocation_env.yml scripts/submit_allocation.sh scripts/submit_allocation_profile.sh</automated>
  </verify>
  <done>The Stage 7 submit scripts point at `allocation_env`, and `allocation_env.yml` contains the complete prediction/runtime package contract required for Phase 1 verification.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the shared HPC path contract to bootstrap and setup scripts</name>
  <files>scripts/hpc_common.sh, scripts/setup_environments.sh</files>
  <action>Refactor the shared shell helpers to honor the contract from Plan 01 per D-12 through D-15: derive home/beegfs-sensitive paths from `$USER` or explicit env vars, require explicit HPC temp/beegfs variables instead of inventing hardcoded defaults, and remove active `black` literals from the touched operational helpers per PIPE-04. Add a noninteractive validation entrypoint at `scripts/hpc_common.sh --check-stage7-contract` that exits non-zero with a clear message naming each missing required variable (`HPC_SCRATCH_ROOT`, `HPC_TMP_ROOT`, and any companion temp var adopted in Plan 01). Make `scripts/setup_environments.sh` the canonical bootstrap path for later Phase 1 R verification by ensuring it can provision/update `allocation_env` noninteractively on a fresh workstation or HPC checkout before any `Rscript`-based plan verification runs. Keep micromamba probing and environment creation behavior, but ensure failures point to one actionable fix path instead of silently creating the wrong directories.</action>
  <verify>
    <automated>bash -lc "set -euo pipefail; bash -n scripts/hpc_common.sh scripts/setup_environments.sh; if env -u HPC_SCRATCH_ROOT -u HPC_TMP_ROOT bash scripts/hpc_common.sh --check-stage7-contract > stage7-contract.out 2>&1; then exit 1; fi; rg -n 'HPC_SCRATCH_ROOT|HPC_TMP_ROOT' stage7-contract.out >/dev/null; bash scripts/setup_environments.sh --env allocation_env --non-interactive >/tmp/allocation-env-bootstrap.log 2>&1; micromamba run -n allocation_env Rscript --version >/tmp/allocation-rscript.txt 2>&1; rg -n 'Rscript' /tmp/allocation-rscript.txt >/dev/null"</automated>
  </verify>
  <done>The shared shell helpers no longer contain active hardcoded `black` paths, they bootstrap allocation using the same explicit scratch/temp contract that Stage 7 pre-flight will enforce, and the canonical `allocation_env` bootstrap path yields a runnable `Rscript` for downstream Phase 1 verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell env -> batch job | User/HPC-provided env vars control environment activation and temp/beegfs locations. |
| environment YAML -> runtime packages | The package manifest determines whether Stage 7 runs reproducibly or mutates itself at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `scripts/hpc_common.sh` | mitigate | Only derive paths from `$USER` and explicit contract vars; reject missing scratch/temp inputs instead of constructing hidden defaults. |
| T-01-05 | D | `scripts/submit_allocation*.sh` | mitigate | Ensure the submit scripts activate the same canonical env file that pre-flight validates so missing packages fail before work starts. |
| T-01-06 | R | `allocation_env.yml` | mitigate | Put the full Stage 7 package set under version control and stop relying on runtime package repair or training-env drift. |
</threat_model>

<verification>
Run the grep/package gate and `bash -n` syntax checks above. If micromamba is available in the execution environment, additionally dry-run the environment activation path without submitting a job.
</verification>

<success_criteria>
Stage 7 shell entrypoints and environment provisioning agree on one canonical execution env, and the touched HPC helpers no longer embed user-specific path assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repair-visibility/01-repair-visibility-02-SUMMARY.md`
</output>
