---
phase: 03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor
plan: 03
type: execute
wave: 3
depends_on: ["03.5-01", "03.5-02"]
files_modified:
  - .planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-VALIDATION.md
autonomous: false
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "selva_andina smoke proves the threading speed-up: stage=predict / stage=setup_inputs wall time drops measurably vs the 20665s baseline"
    - "selva_andina smoke proves the lazy-read memory-floor reduction: stage=predictor_preload line is gone (replaced by per-from-class predictor_load) and loop peak_rss drops vs ~94 GB baseline"
    - "Numerical equivalence is proven: lazy+1thread probability TIFs bit-for-bit vs eager; +threads within max|Δ| < 1e-6"
    - "Per-transition cell counts are identical to baseline (no Pitfall 4 coverage loss)"
    - "The central falsifiable risk (A3 lazy-aggregate-win) is settled with a measured Σ predictor_load vs the 167s eager preload, with ALLOCATION_PREDICTOR_LAZY=0 as the recorded bisect fallback"
  artifacts:
    - path: ".planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-VALIDATION.md"
      provides: "Before/after profile table (vs job 571309), two-stage isolation results, equivalence + cell-count check, honest residual-floor assessment"
      contains: "571309"
  key_links:
    - from: "scripts/submit_allocation_smoke.sh"
      to: "selva_andina profile lines"
      via: "ALLOCATION_REGION_FILTER=selva_andina + ALLOCATION_PROFILE_SCENARIO=BAU"
      pattern: "ALLOCATION_REGION_FILTER=selva_andina"
    - from: "PROFILE log lines"
      to: "03.5-VALIDATION.md acceptance table"
      via: "stage=predictor_preload / predictor_load / predict / setup_inputs / peak_rss deltas vs 571309"
      pattern: "stage=(predictor_preload|predictor_load|predict|setup_inputs)"
---

<objective>
Validate Phase 3.5 on the canonical hard case — the Andes (`selva_andina`) — by running the allocation smoke before and after, isolating the two changes (lazy read, then threading), and asserting the memory-floor and wall-time deltas against the quantified job-571309 baseline. Settle the central falsifiable risk (research A3: does the lazy per-from-class scan beat the eager preload in aggregate?) with measured numbers and record the `ALLOCATION_PREDICTOR_LAZY=0` bisect fallback.

Purpose: Plans 01 and 02 land code + unit tests, but the real validation is the HPC smoke profile (REQUIREMENTS.md: no full automated test suite; HPC batch is the deployment + validation mechanism). The Andes is the throughput proof (its preload is cheap at 167s/+13.7 GB; the ~5.7 h `setup_inputs` predict loop is the cost that threading must cut). Numerical equivalence is a hard constraint — lazy+1thread must be bit-for-bit vs eager; +threads within 1e-6 (FP-reorder tolerance, RESEARCH Pitfall 3).

Output: `03.5-VALIDATION.md` with the before/after profile table vs 571309, two-stage isolation results, the equivalence + per-transition cell-count check, and an honest residual-floor assessment (the ~10-20 GB target may land 20-30 GB — record what actually happened).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-RESEARCH.md
@.planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-01-SUMMARY.md
@.planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-02-SUMMARY.md

<interfaces>
<!-- Baseline numbers (job 571309, BAU, selva_andina, 2026-06-22) to anchor acceptance. -->

PROFILE line shape: "PROFILE region=... stage=... elapsed=...s rss_delta=... peak_rss=...".

Baseline 571309 (selva_andina, BAU):
  stage=predictor_preload  elapsed=167.564s  rss_delta=+13.7GB  peak_rss=59.4GB   (30,822,155 cells × 23 static + 15 dynamic cols)
  stage=setup_inputs       elapsed=20665s (~5.7h)  — dominated by the per-transition ranger predict loop; loop peak RSS ~94 GB
  stage=write_total        elapsed=301s
  Run then dinamica_fail'd for an UNRELATED, already-tracked reason (staged PyEnvironment lost +x on beegfs) — OUT OF SCOPE; validation stops after probability maps + profile lines, before the Dinamica allocation step.

scripts/submit_allocation_smoke.sh knobs (Plans 01/02 added the new echoes):
  ALLOCATION_REGION_FILTER=selva_andina  ALLOCATION_PROFILE_SCENARIO=BAU
  ALLOCATION_PARALLEL_STRATEGY=sequential  ALLOCATION_NUM_WORKERS=${SLURM_CPUS_PER_TASK:-4}
  ALLOCATION_PREDICTOR_LAZY (default ON; =0 = eager bisect)  ALLOCATION_PREDICT_NUM_THREADS (default auto)
  ALLOCATION_PREDICT_BATCH_ROWS (optional, Phase 3.4)
Run on a highmem (188 GB) Rundeck node. Per-transition cell count is logged at src/allocation.r L2477 as "%d cells".
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Plans 01 + 02 landed: scoped threaded ranger prediction (ALLOCATION_PREDICT_NUM_THREADS) and lazy per-from-class predictor reads (ALLOCATION_PREDICTOR_LAZY), both default-on, with the eager/single-thread escape hatches. The Andes is the validation region per the operator directive (every other region already produced probability maps via the smoke).
  </what-built>
  <how-to-verify>
    Operator action required — the smoke runs on a Rundeck-reserved highmem (188 GB) node; the local agent cannot reach the cluster or the 68M-cell predictor Parquet. On the cluster, confirm the branch contains Plan 01 + Plan 02 commits, then run THREE smoke jobs for selva_andina (each: `sbatch scripts/submit_allocation_smoke.sh` with `ALLOCATION_REGION_FILTER=selva_andina ALLOCATION_PROFILE_SCENARIO=BAU` on a highmem node), capturing the logs/lulc-allocation-smoke-<job>.out for each:
      1. Stage-1 (lazy read isolated): ALLOCATION_PREDICTOR_LAZY=1 ALLOCATION_PREDICT_NUM_THREADS=1
      2. Stage-2 (+ threading): ALLOCATION_PREDICTOR_LAZY=1 (ALLOCATION_PREDICT_NUM_THREADS unset = auto)
      3. Eager bisect control (A3 fallback): ALLOCATION_PREDICTOR_LAZY=0 ALLOCATION_PREDICT_NUM_THREADS=1
    Each job may stop at/after probability-map generation; the Dinamica allocation step is OUT OF SCOPE (the 571309 dinamica_fail is the unrelated +x-bit issue — do not treat its failure as a Phase 3.5 failure). Provide the three job IDs / log paths (or paste the PROFILE + "%d cells" lines) so the agent can populate 03.5-VALIDATION.md.
  </how-to-verify>
  <resume-signal>Provide the three smoke job IDs / log file paths (or paste their PROFILE lines, per-transition "%d cells" lines, and the probability_map_dir TIF locations), then type "validated".</resume-signal>
  <read_first>
    - scripts/submit_allocation_smoke.sh (the harness + its ALLOCATION_* knobs)
    - .planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-RESEARCH.md (Q5 validation table + two-stage isolation; Pitfall 3/4 equivalence)
  </read_first>
  <acceptance_criteria>
    - Three selva_andina smoke logs exist (lazy+1thread, lazy+auto-threads, eager+1thread control), each containing PROFILE lines for the prediction path and per-transition "%d cells" lines.
    - Each run reached at least end-of-probability-map generation (setup_inputs completed); a dinamica_fail at the +x-bit step does NOT invalidate the run.
    - Job IDs / log paths (or the pasted profile + cell-count + TIF-path lines) are provided to the agent.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 1: Populate 03.5-VALIDATION.md with before/after deltas, isolation, equivalence, residual-floor</name>
  <read_first>
    - .planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-RESEARCH.md (Q5 validation table — the exact signals + falsify conditions; Q2 residual-floor breakdown; A3/A4 honest-assessment guidance)
    - the three smoke logs / pasted profile lines from the checkpoint
    - src/allocation.r L2477 (the "%d cells" per-transition log) and L2191-2195 / L2298-2307 (predictor_preload vs predictor_load profile stages)
  </read_first>
  <action>
    Create 03.5-VALIDATION.md. Build the before/after table keyed to the RESEARCH Q5 signals, each row citing the 571309 baseline and the measured value: (a) stage=predictor_preload — assert GONE on the lazy runs (replaced by per-transition stage=predictor_load); flag the lazy path as not-taken if it still appears at full cost. (b) Σ stage=predictor_load across transitions — compare to the 167.564s eager preload; this settles risk A3 (the lazy aggregate-win question) — if Σ ≫ baseline, record it and recommend ALLOCATION_PREDICTOR_LAZY=0 fallback. (c) stage=predict on the large forest transition — assert ≥2× faster on the threaded run (Stage-2) vs the single-thread run (Stage-1); flag threading-not-applied if flat (Pitfall 5). (d) stage=setup_inputs total — assert measurably lower than 20665s on Stage-2; Phase 4 SC#2 wants ≥3× overall. (e) loop peak_rss — assert lower than ~94 GB baseline; record the actual number against the ~10-20 GB target HONESTLY (A4: a single forest from-class frame may push it to 20-30 GB — still a 3-4× win; do not assert the target if it was not met). (f) per-transition "%d cells" — assert IDENTICAL across lazy and eager control runs (Pitfall 4 coverage check). (g) equivalence — record max|Δ| between Stage-1 (lazy+1thread) probability TIFs and the eager control (target bit-for-bit), and between Stage-2 (+threads) and Stage-1 (target < 1e-6, FP-reorder tolerance). Add a "Residual floor assessment" section documenting the dominant remaining RSS terms (anterior_dt, resident models, largest from-class frame, gather list) per RESEARCH Q2, and whether a follow-up (tighter per-transition projection or gather-flush) is warranted for the Amazon. Add an "A3 verdict" line: lazy path wins / loses / inconclusive, with the bisect command recorded. Record that the costa_peruana regression smoke (small/fast) should follow as a no-regression check (note it; operator runs it). State explicitly that the Dinamica step is out of scope and any dinamica_fail at the +x-bit stage does not affect this phase's acceptance.
  </action>
  <verify>
    <automated>grep -Eq "571309" .planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-VALIDATION.md && grep -Eq "predictor_preload" .planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-VALIDATION.md && grep -Eq "1e-6|bit-for-bit" .planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-VALIDATION.md && echo VALIDATION_OK</automated>
  </verify>
  <done>03.5-VALIDATION.md contains the before/after table vs 571309, the two-stage isolation results, the equivalence + per-transition cell-count check, the A3 verdict + bisect command, and the honest residual-floor assessment.</done>
  <acceptance_criteria>
    - 03.5-VALIDATION.md has a table with rows for stage=predictor_preload, Σ stage=predictor_load, stage=predict, stage=setup_inputs, loop peak_rss, per-transition cell count, each citing the 571309 baseline and the measured value with a pass/fail verdict.
    - predictor_preload is recorded as GONE (or flagged not-taken) on the lazy runs.
    - stage=setup_inputs Stage-2 value is recorded against 20665s with a measured speedup factor.
    - peak_rss is recorded against ~94 GB with an HONEST statement of whether the ~10-20 GB target was met (20-30 GB acceptable, stated as such).
    - Per-transition cell counts are recorded as identical (or a Pitfall-4 coverage-loss flag is raised).
    - Equivalence: max|Δ| lazy+1thread-vs-eager (bit-for-bit target) and +threads-vs-1thread (< 1e-6) are recorded.
    - An "A3 verdict" (lazy wins/loses/inconclusive) and the ALLOCATION_PREDICTOR_LAZY=0 bisect command are present.
    - The Dinamica-out-of-scope note is present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → cluster | Validation runs on a trusted Rundeck/SLURM node; no external/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.5-04 | Repudiation / correctness | claiming the speed-up without the baseline anchor | mitigate | Every VALIDATION.md row cites the 571309 baseline and a measured value with an explicit pass/fail; A3 verdict + bisect command recorded. |
| T-03.5-NA | — | external network / web | accept | No external attack surface — internal HPC compute validation. |

(No package installs — no supply-chain gate required.)
</threat_model>

<verification>
- 03.5-VALIDATION.md exists and the grep gate confirms it cites 571309, the predictor_preload stage, and the equivalence tolerance.
- The before/after table covers all RESEARCH Q5 signals with pass/fail verdicts anchored to the baseline.
- The two-stage isolation (lazy alone vs +threads) and the eager bisect control are recorded.
- Numerical equivalence (bit-for-bit lazy+1thread; <1e-6 +threads) and per-transition cell-count identity are recorded.
</verification>

<success_criteria>
- The Andes smoke proves the threading wall-time win (setup_inputs / predict measurably faster than 20665s baseline).
- The lazy-read memory-floor reduction is recorded (predictor_preload gone; loop peak_rss lower than ~94 GB) with an honest target assessment.
- Numerical equivalence and per-transition coverage are proven.
- The A3 falsifiable risk is settled with measured numbers + a recorded bisect fallback.
</success_criteria>

<output>
Create `.planning/phases/03.5-reduce-allocation-memory-floor-lazy-per-transition-predictor/03.5-03-SUMMARY.md` when done.
</output>
