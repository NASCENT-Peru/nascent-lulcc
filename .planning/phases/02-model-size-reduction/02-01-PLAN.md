---
phase: 02-model-size-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - environments/allocation_env.yml
  - environments/transition_model_env.yml
  - config/local_config.yaml
  - config/hpc_config.yaml
  - tests/testthat/test-mlr3-env-contract.R
autonomous: true
requirements:
  - MEM-04

must_haves:
  truths:
    - "allocation_env.yml lists all six mlr3 packages (r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk, r-glmnet)"
    - "transition_model_env.yml lists all five mlr3 training packages (r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk)"
    - "local_config.yaml has max_training_rows: 500000 under configuration_settings"
    - "hpc_config.yaml has max_training_rows: 1000000 under configuration_settings"
    - "A testthat test asserts these presence conditions and passes"
  artifacts:
    - path: "environments/allocation_env.yml"
      provides: "mlr3 package declarations for training and prediction"
      contains: "r-mlr3"
    - path: "environments/transition_model_env.yml"
      provides: "mlr3 package declarations for training inside transition_model_env conda environment"
      contains: "r-mlr3"
    - path: "config/local_config.yaml"
      provides: "max_training_rows config key for subsampling fallback"
      contains: "max_training_rows"
    - path: "config/hpc_config.yaml"
      provides: "max_training_rows config key for subsampling fallback"
      contains: "max_training_rows"
    - path: "tests/testthat/test-mlr3-env-contract.R"
      provides: "Grep-based assertions that the mlr3 package block and config keys exist"
  key_links:
    - from: "environments/allocation_env.yml"
      to: "src/transition_modelling.r (Phase 02-02)"
      via: "r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk loaded via library() at training time"
      pattern: "r-mlr3"
    - from: "environments/transition_model_env.yml"
      to: "src/transition_modelling.r (Phase 02-02)"
      via: "r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk loaded via library() at training time inside transition_model_env"
      pattern: "r-mlr3"
    - from: "config/local_config.yaml"
      to: "src/transition_modelling.r (Phase 02-02)"
      via: "config[[\"max_training_rows\"]] %||% 500000L used in subsampling fallback"
      pattern: "max_training_rows"
---

<objective>
Lay the prerequisite foundation for the mlr3 migration: add the six required mlr3 packages (including r-glmnet for classif.glmnet) to allocation_env.yml, add the five mlr3 training packages to transition_model_env.yml, add the max_training_rows subsampling threshold to both YAML configs, and write a testthat contract test that asserts all conditions are met.

Purpose: Plans 02-02 and 02-03 both assume these packages are declared and the config key is present. This plan makes those assumptions true before any code is written.
Output: Updated allocation_env.yml, transition_model_env.yml, both config YAMLs, and a green testthat test file.
</objective>

<execution_context>
@C:/Users/bblack/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-model-size-reduction/02-CONTEXT.md
@.planning/phases/02-model-size-reduction/02-RESEARCH.md
@.planning/phases/02-model-size-reduction/02-PATTERNS.md

<interfaces>
<!-- Key patterns the executor must replicate exactly. Extracted from codebase. -->

From environments/allocation_env.yml (lines 66–92) — the MEM-06 block structure to follow:
```yaml
  # ---------------------------------------------------------------------------
  # MEM-06: prediction-time package set
  # ---------------------------------------------------------------------------
  # These packages are required for Stage 7 to load and execute the
  # tidymodels-trained transition probability models without runtime
  # `install.packages()` calls. Order matches REQUIREMENTS.md MEM-06.

  # tidymodels engines used at prediction time
  - r-parsnip
  - r-recipes
  - r-workflows
  - r-ranger
  - r-xgboost=1.7
  - r-tidypredict

  # Model object slimming + (de)serialisation
  - r-butcher
  - r-bundle
  - r-qs

  # Process/memory introspection (cross-platform RSS reporting; OBS-* uses ps)
  - r-ps
  - r-lobstr

  # BLAS thread control inside parallel workers
  - r-rhpcblasctl
```
Append the mlr3 block AFTER the r-rhpcblasctl line. The file ends at line 92 with no trailing blank line.

From environments/transition_model_env.yml (lines 36–56) — the Machine learning and modelling block:
```yaml
  # Machine learning and modelling
  - r-tidymodels
  - r-ranger       # Random Forest
  - r-glmnet       # Regularized regression
  - r-xgboost=1.7  # Gradient boosting - pin to 1.7.x for tidypredict compatibility
  - r-yardstick    # Model evaluation metrics
  - r-tune         # Hyperparameter tuning
  - r-workflows    # modelling workflows
  - r-recipes      # Data preprocessing
  - r-parsnip>=1.2.1  # Unified model interface (>=1.2.0 fixes xgboost 1.7/2.x API deprecations)
  - r-rsample      # Resampling
  - r-dials        # Tuning parameters
```
r-glmnet is already present. Append only r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk after the `- r-glmnet       # Regularized regression` line. Do NOT add r-glmnet again.

From config/local_config.yaml (lines 58–73) — the configuration_settings block:
```yaml
# Calibration/simulation configurations
configuration_settings:
  ref_grid_target_cellsize: 100  # in meters
  reference_crs: "epsg:2056"
  step_length: 4
  data_periods: ["2018_2022"] #"2010_2014", "2014_2018", 
  regionalization: true
  scenario_names: ["BAU", "NAT", "CUL", "SOC"]
  scenario_to_ssp_mapping: 
    BAU: "ssp245"
    NAT: "ssp126"
    CUL: "ssp126"
    SOC: "ssp126"
  simulation_start_year: 2022
  simulation_end_year: 2060
  selected_scalar: 9.0
```
Insert `  max_training_rows: 500000  # Subsampling ceiling for transition model training (Phase 2, D-11)` after the `selected_scalar: 9.0` line, before the blank line that precedes `simulation_trans_rates_params:`.

From config/hpc_config.yaml (lines 62–77) — identical block structure. Insert:
`  max_training_rows: 1000000  # Higher threshold on HPC nodes; operator should tune to actual node RAM (Phase 2, D-11)`
after the `selected_scalar: 9.0` line in that file.

From tests/testthat/test-allocation-env-canonical.R — the text-reading test pattern:
```r
.repo_root <- (function() {
  here <- tryCatch(normalizePath(sys.frame(1)$ofile %||% "."), error = function(e) ".")
  if (file.info(here)$isdir %||% FALSE) here <- file.path(here, "x")
  normalizePath(file.path(dirname(dirname(dirname(here)))), mustWork = FALSE)
})()

.read_text <- function(rel) {
  paste(readLines(file.path(.repo_root, rel), warn = FALSE), collapse = "\n")
}

test_that("...", {
  yml <- .read_text("environments/allocation_env.yml")
  expect_true(
    grepl(paste0("(?m)^\\s*-\\s*", pkg, "(\\s|=|$)"), yml, perl = TRUE),
    info = "..."
  )
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract test for mlr3 env block and config keys</name>
  <files>tests/testthat/test-mlr3-env-contract.R</files>
  <read_first>
    - tests/testthat/test-allocation-env-canonical.R (the established test pattern to replicate — repo-root resolver, .read_text() helper, grepl with perl=TRUE)
    - environments/allocation_env.yml (current state — assertions will fail against this pre-change content)
    - environments/transition_model_env.yml (current state — r-mlr3 absent; test must fail)
    - config/local_config.yaml (current state — max_training_rows is absent; test must fail)
    - config/hpc_config.yaml (current state — max_training_rows is absent; test must fail)
  </read_first>
  <behavior>
    - Test 1: allocation_env.yml declares all six mlr3 Phase 2 packages: r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk, r-glmnet
    - Test 2: allocation_env.yml does NOT contain a second/duplicate r-mlr3 entry (sanity guard)
    - Test 3: local_config.yaml contains `max_training_rows:` under configuration_settings
    - Test 4: hpc_config.yaml contains `max_training_rows:` under configuration_settings
    - Test 5: transition_model_env.yml declares all five mlr3 training packages (r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk) — note: r-glmnet already present and is NOT asserted here
  </behavior>
  <action>
Create `tests/testthat/test-mlr3-env-contract.R` using the exact same .repo_root resolver and .read_text() helper pattern from test-allocation-env-canonical.R. Define `%||%` locally at the top if not already sourced (copy from the existing test file).

Write five test_that() blocks:

1. "allocation_env.yml declares all Phase 2 mlr3 packages (MEM-04)" — loops over c("r-mlr3", "r-mlr3learners", "r-mlr3tuning", "r-paradox", "r-bbotk", "r-glmnet") and for each asserts grepl(paste0("(?m)^\\s*-\\s*", pkg, "(\\s|=|$)"), yml, perl = TRUE).

2. "allocation_env.yml does not duplicate r-mlr3 entries" — counts matches of the strict pattern "(?m)^\\s*-\\s*r-mlr3\\s*$" and expects exactly 1 match.

3. "local_config.yaml has max_training_rows under configuration_settings" — reads config/local_config.yaml as text, asserts grepl("(?m)^\\s+max_training_rows:", yml, perl = TRUE).

4. "hpc_config.yaml has max_training_rows under configuration_settings" — same assertion on hpc_config.yaml.

5. "transition_model_env.yml declares all Phase 2 mlr3 training packages (MEM-04)" — reads environments/transition_model_env.yml as text, loops over c("r-mlr3", "r-mlr3learners", "r-mlr3tuning", "r-paradox", "r-bbotk") and for each asserts grepl(paste0("(?m)^\\s*-\\s*", pkg, "(\\s|=|$)"), yml, perl = TRUE). Note: r-glmnet is already present in this file and is NOT asserted here.

All five tests MUST fail when run against the current (pre-change) files — this is the RED state. Commit as: `test(02-01): failing contract tests for mlr3 env block and max_training_rows config keys`
  </action>
  <verify>
    <automated>grep -c "max_training_rows" tests/testthat/test-mlr3-env-contract.R</automated>
  </verify>
  <done>File tests/testthat/test-mlr3-env-contract.R exists and contains five test_that() blocks; grep confirms "max_training_rows" appears at least twice in the test file; the tests are in RED state (they assert things not yet present in the files they read)</done>
</task>

<task type="auto">
  <name>Task 2: Add mlr3 package block to allocation_env.yml, transition_model_env.yml, and max_training_rows to both configs</name>
  <files>environments/allocation_env.yml, environments/transition_model_env.yml, config/local_config.yaml, config/hpc_config.yaml</files>
  <read_first>
    - environments/allocation_env.yml (read the full file — must append after line 92 r-rhpcblasctl; must NOT add duplicate r-xgboost or r-ranger entries)
    - environments/transition_model_env.yml (read the full file — r-glmnet is already present; append only r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk after the r-glmnet line under the # Machine learning and modelling block; do NOT add r-glmnet again; keep r-xgboost=1.7 pin unchanged)
    - config/local_config.yaml (read the full file — insert after line 73 `selected_scalar: 9.0`, before blank line before `simulation_trans_rates_params:`)
    - config/hpc_config.yaml (read the full file — insert after line 77 `selected_scalar: 9.0`, before blank line before `simulation_trans_rates_params:`)
    - tests/testthat/test-mlr3-env-contract.R (verify the exact package names and key names the tests assert)
  </read_first>
  <action>
FOUR file edits, each independent:

**Edit 1: environments/allocation_env.yml**
After the `  - r-rhpcblasctl` line (the last line of the MEM-06 block), append the following block verbatim (two-space indent, matching all other entries):

```yaml

  # ---------------------------------------------------------------------------
  # Phase 2 (MEM-04): mlr3 training and prediction packages
  # ---------------------------------------------------------------------------
  # Required for loading and calling $predict_newdata() on mlr3 Learner objects
  # saved by the rewritten transition_modelling.r. The r-xgboost=1.7 pin above
  # must remain unchanged — mlr3learners 0.14 supports xgboost 1.7 (RESEARCH §5).
  # r-glmnet is required by classif.glmnet (regularised logistic regression,
  # matching the current glmnet-based GLM behaviour).
  # r-mlr3pipelines NOT included: classif.glmnet regularisation is scale-invariant; step_normalize() not required
  - r-mlr3
  - r-mlr3learners
  - r-mlr3tuning
  - r-paradox
  - r-bbotk
  - r-glmnet
```

Do NOT add r-xgboost or r-ranger (already present in the MEM-06 block at lines 78–79). Do NOT change the r-xgboost=1.7 pin.

**Edit 2: environments/transition_model_env.yml**
After the `  - r-glmnet       # Regularized regression` line, insert the following lines verbatim (two-space indent, matching all other entries in this file):

```yaml
  - r-mlr3         # mlr3 training framework (Phase 2, MEM-04)
  - r-mlr3learners # classif.ranger / classif.glmnet / classif.xgboost learners
  - r-mlr3tuning   # AutoTuner for hyperparameter optimisation
  - r-paradox      # Search space definitions for tuning
  - r-bbotk        # Black-box optimisation toolkit (AutoTuner dependency)
```

Do NOT add r-glmnet (already present immediately above). Do NOT add r-mlr3pipelines. Keep the r-xgboost=1.7 pin unchanged on its existing line.

**Edit 3: config/local_config.yaml**
Insert one line after `  selected_scalar: 9.0` and before the blank line that precedes `# Simulation transition rates preparation configuration`:

```yaml
  max_training_rows: 500000  # Subsampling ceiling for transition model training (Phase 2, D-11)
```

Use two-space indent matching all other keys in configuration_settings. No quotes around the integer. Inline comment follows the existing `# comment text` convention.

**Edit 4: config/hpc_config.yaml**
Insert one line after `  selected_scalar: 9.0` and before the blank line that precedes `# Simulation transition rates preparation configuration`:

```yaml
  max_training_rows: 1000000  # Higher threshold on HPC nodes; operator should tune to actual node RAM (Phase 2, D-11)
```

After all edits, commit as: `feat(02-01): add mlr3 packages to allocation_env.yml and transition_model_env.yml, max_training_rows to configs (MEM-04, D-09, D-11)`
  </action>
  <verify>
    <automated>grep -c "r-mlr3" environments/allocation_env.yml && grep -c "r-mlr3" environments/transition_model_env.yml && grep -c "r-glmnet" environments/allocation_env.yml && grep -c "max_training_rows" config/local_config.yaml && grep -c "max_training_rows" config/hpc_config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - environments/allocation_env.yml contains `r-mlr3` as a dependency entry (grep -c returns >= 1)
    - environments/allocation_env.yml contains `r-mlr3learners` as a dependency entry
    - environments/allocation_env.yml contains `r-mlr3tuning` as a dependency entry
    - environments/allocation_env.yml contains `r-paradox` as a dependency entry
    - environments/allocation_env.yml contains `r-bbotk` as a dependency entry
    - environments/allocation_env.yml contains `r-glmnet` as a dependency entry
    - environments/allocation_env.yml still contains `r-xgboost=1.7` (unchanged pin)
    - environments/allocation_env.yml still contains `r-ranger` (unchanged)
    - environments/transition_model_env.yml contains `r-mlr3` as a dependency entry
    - environments/transition_model_env.yml contains `r-mlr3learners` as a dependency entry
    - environments/transition_model_env.yml contains `r-mlr3tuning` as a dependency entry
    - environments/transition_model_env.yml contains `r-paradox` as a dependency entry
    - environments/transition_model_env.yml contains `r-bbotk` as a dependency entry
    - environments/transition_model_env.yml still contains `r-glmnet` exactly once (no duplicate added)
    - environments/transition_model_env.yml still contains `r-xgboost=1.7` (unchanged pin)
    - config/local_config.yaml contains `max_training_rows: 500000` (grep -c returns 1)
    - config/hpc_config.yaml contains `max_training_rows: 1000000` (grep -c returns 1)
    - All five test_that() blocks in tests/testthat/test-mlr3-env-contract.R now pass (GREEN state)
    - No r-mlr3 duplicate entries in allocation_env.yml: grep -c "^  - r-mlr3$" environments/allocation_env.yml returns exactly 1
  </acceptance_criteria>
  <done>All six mlr3 packages appear in allocation_env.yml; all five mlr3 training packages appear in transition_model_env.yml (r-glmnet unchanged, no duplicate); max_training_rows appears in both config files; all five contract tests are GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config files → R runtime | max_training_rows value read via get_config(); no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | environments/allocation_env.yml | accept | YAML is under version control; changes are reviewed via git diff; no runtime user input |
| T-02-02 | Tampering | environments/transition_model_env.yml | accept | YAML is under version control; changes are reviewed via git diff; no runtime user input |
| T-02-03 | Tampering | config/local_config.yaml, config/hpc_config.yaml | accept | Config files are under version control; max_training_rows is a data threshold, not a path or secret |
</threat_model>

<verification>
After both tasks complete:

```bash
grep -E "^\s+-\s+r-mlr3(learners|tuning|pipelines)?\s*$" environments/allocation_env.yml
grep -E "^\s+-\s+r-(paradox|bbotk|glmnet)\s*$" environments/allocation_env.yml
grep -E "^\s+-\s+r-mlr3(learners|tuning)?\s" environments/transition_model_env.yml
grep -E "^\s+-\s+r-(paradox|bbotk)\s" environments/transition_model_env.yml
grep "max_training_rows" config/local_config.yaml config/hpc_config.yaml
grep "r-xgboost=1.7" environments/allocation_env.yml environments/transition_model_env.yml
```

All commands must return matches. The r-xgboost=1.7 pin must be unchanged in both environment files.
</verification>

<success_criteria>
- environments/allocation_env.yml has exactly 6 new entries under the Phase 2 MEM-04 comment block: r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk, r-glmnet
- environments/transition_model_env.yml has 5 new entries appended after the r-glmnet line: r-mlr3, r-mlr3learners, r-mlr3tuning, r-paradox, r-bbotk; r-glmnet is present exactly once (no duplicate)
- r-xgboost=1.7 pin is unchanged in both environment files
- config/local_config.yaml has max_training_rows: 500000 in configuration_settings
- config/hpc_config.yaml has max_training_rows: 1000000 in configuration_settings
- tests/testthat/test-mlr3-env-contract.R exists with 5 test_that() blocks in GREEN state
- All changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-size-reduction/02-01-SUMMARY.md`
</output>
