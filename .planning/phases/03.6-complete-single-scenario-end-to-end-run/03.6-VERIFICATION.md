---
status: passed
phase: 03.6-complete-single-scenario-end-to-end-run
verified: 2026-06-28
score: 6/6 must-haves
requirements: [RUN-01, RUN-02, RUN-03, RUN-04, RUN-05, RUN-06]
scenario_verified: NAT
---

# Phase 3.6 Verification — Complete Single-Scenario End-to-End Run

**Verdict: PASSED.** The capstone goal — a single scenario run to completion across all
regions × all timesteps, with national mosaics for every timestep, declared done by the
run-manifest (not log-eyeballing) — is achieved. Run executed for **NAT** (operator
decision; the schedule/machinery is scenario-agnostic).

## Capstone evidence (operator-confirmed on ZALF HPC, 2026-06-28)
```
STATE manifest verdict=PASS scenario=NAT cells=40/40 mosaics=10/10 differs_failures=0 degenerate=0 sat_failed_total=27 warnings=0
```
`outputs/simulations/NAT/run_manifest.csv` (40 rows) is the durable artifact: 4 regions ×
10 timesteps (2024…2060), every per-region `posterior.tif` present + valid + differing from
its anterior, and all 10 national `posterior_<year>.tif` mosaics assembled.

## Requirement traceability
| Req | Verdict | Evidence |
|-----|---------|----------|
| RUN-01 (run to completion; manifest PASS) | ✓ | `cells=40/40`, `mosaics=10/10`, `verdict=PASS` |
| RUN-02 (schedule from `simulation_year_steps` to 2060; blocking rate-table pre-flight) | ✓ | reaches 2060 (10 steps); `preflight_rate_tables.sh OK` before launch |
| RUN-03 (per-region decoupled chaining; fat/highmem, never bare compute) | ✓ | per-region jobs chain own `posterior.tif`; routed andes/cuenca/selva→fat, costa→highmem |
| RUN-04 (post-hoc national mosaic series, byte-comparable) | ✓ | `mosaics=10/10` via `assemble_national_mosaic.r` |
| RUN-05 (differs-from-anterior bar; saturation report-only; degeneracy QA) | ✓ | `differs_failures=0`, `degenerate=0`; `sat_failed_total=27` report-only (did not gate) |
| RUN-06 (atomic writes; timestep-level resume) | ✓ | `write_raster_atomic` (D-10) + driver contiguous resume (D-09), exercised across the run |

## Plans
01–05 all complete (SUMMARY.md present for each). Code independently re-reviewed this cycle:
all 12 code-review findings resolved (`03.6-REVIEW.md`, status: resolved), plus a parquet
guard rail (`e69dd64`) added after the stray-file incident.

## Notes (non-blocking)
- `sat_failed_total=27`: some `cuenca_del_amazonas`/`costa_peruana` transitions under-saturate
  in later years — a scientific signal for review (in `run_manifest.csv`), not a pipeline fault.
- ZALF operational lessons (env detection, per-region memory routing, stray-parquet-file guard)
  captured in `03.6-05-SUMMARY.md`.
