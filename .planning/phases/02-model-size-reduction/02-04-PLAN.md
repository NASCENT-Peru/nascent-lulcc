---
phase: 02-model-size-reduction
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - scripts/retrain_all_models.r
autonomous: true
requirements:
  - MEM-04

must_haves:
  truths:
    - "scripts/retrain_all_models.r exists and is executable via Rscript scripts/retrain_all_models.r"
    - "The script sources src/setup.r, src/utils.r, src/transition_modelling.r and calls get_config()"
    - "The script calls transition_modelling(config=config, refresh_cache=force_retrain)"
    - "The script accepts --force flag (sets refresh_cache=TRUE) and --dry-run flag (prints what would run without training)"
    - "The script accepts --region <name> flag to filter to a single region"
    - "With --dry-run, the script prints the expected transition-region pairs without training"
    - "The script logs a summary table of completed/skipped/failed transitions at the end"
    - "The script follows the run_transition_modelling.r skeleton pattern"
  artifacts:
    - path: "scripts/retrain_all_models.r"
      provides: "Utility to re-train all transition models using the new mlr3 pipeline"
      contains: "transition_modelling("
  key_links:
    - from: "scripts/retrain_all_models.r"
      to: "transition_modelling() in src/transition_modelling.r"
      via: "direct call passing config and refresh_cache=force_retrain"
      pattern: "transition_modelling\\("
    - from: "scripts/retrain_all_models.r"
      to: "get_config() in src/setup.r"
      via: "config <- get_config() call after sourcing src/setup.r"
      pattern: "get_config"
---

<objective>
Create scripts/retrain_all_models.r — the utility script that orchestrates re-training of all ~140–160 transition-region model pairs using the new mlr3 pipeline (D-07, D-08). The script calls transition_modelling() directly (which already handles parallel execution via furrr::future_map() internally). It accepts --force, --dry-run, and --region flags. It follows the established script skeleton pattern from scripts/run_transition_modelling.r.

Purpose: Gives the operator a single command to kick off mlr3 re-training of all transitions, replacing the now-obsolete tidymodels .rds files with mlr3 .qs files. Without this, the operator must invoke transition_modelling() manually from an R session.
Output: New scripts/retrain_all_models.r that delegates to the already-tested transition_modelling() from 02-02.
</objective>

<execution_context>
@C:/Users/bblack/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-model-size-reduction/02-CONTEXT.md
@.planning/phases/02-model-size-reduction/02-RESEARCH.md
@.planning/phases/02-model-size-reduction/02-PATTERNS.md
@.planning/phases/02-model-size-reduction/02-02-SUMMARY.md
@.planning/phases/02-model-size-reduction/02-03-SUMMARY.md

<interfaces>
<!-- Script skeleton patterns from existing scripts — read these before writing -->

From scripts/run_transition_modelling.r — the script skeleton this new script must follow:
```r
#!/usr/bin/env Rscript
# retrain_all_models.r
# Re-train all transition models using the new mlr3 pipeline.
# ...

start_time <- Sys.time()

cat("\n========================================\n")
cat("Re-training All Transition Models (mlr3)\n")
cat("========================================\n\n")

# Working directory resolution (run_transition_modelling.r pattern)
script_path <- commandArgs(trailingOnly = FALSE)
script_path <- script_path[grepl("--file=", script_path)]
if (length(script_path) > 0) {
  script_dir   <- dirname(sub("--file=", "", script_path))
  project_root <- dirname(script_dir)
} else {
  project_root <- getwd()
  if (basename(project_root) == "scripts") project_root <- dirname(project_root)
}
setwd(project_root)

# Source files — DO NOT call install.packages() (run_allocation.r lines 82-87)
src_files <- c("src/setup.r", "src/utils.r", "src/transition_modelling.r")
for (src_file in src_files) {
  tryCatch(source(src_file), error = function(e) {
    cat(sprintf("ERROR sourcing %s: %s\n", src_file, e$message))
    quit(status = 1)
  })
}

config <- tryCatch(get_config(), error = function(e) {
  cat(sprintf("ERROR getting config: %s\n", e$message))
  quit(status = 1)
})
```

From scripts/run_allocation.r — argument parsing pattern:
```r
.cli_args     <- commandArgs(trailingOnly = TRUE)
force_retrain <- "--force" %in% .cli_args
dry_run       <- "--dry-run" %in% .cli_args
region_idx    <- which(.cli_args == "--region")
region_filter <- if (length(region_idx) > 0 && region_idx < length(.cli_args)) {
  .cli_args[[region_idx + 1L]]
} else {
  NULL
}
```

transition_modelling() signature (from 02-02):
```r
transition_modelling(
  config           = config,
  refresh_cache    = force_retrain,   # TRUE when --force flag passed
  use_regions      = NULL,            # reads from config
  model_specs_path = NULL,            # reads from config
  periods_to_process = NULL           # reads from config
)
```

Summary file pattern (from run_transition_modelling.r lines 186–213):
```r
end_time <- Sys.time()
elapsed  <- difftime(end_time, start_time, units = "hours")
cat(sprintf("Total runtime: %.2f hours\n", as.numeric(elapsed)))

summary_file <- file.path(
  config[["transition_model_eval_dir"]],
  sprintf("retrain_summary_%s.txt", Sys.getenv("SLURM_JOB_ID", unset = "local"))
)
if (!dir.exists(dirname(summary_file))) dir.create(dirname(summary_file), recursive = TRUE)
writeLines(c(
  sprintf("Job ID: %s", Sys.getenv("SLURM_JOB_ID", unset = "local")),
  sprintf("Start:   %s", format(start_time)),
  sprintf("End:     %s", format(end_time)),
  sprintf("Runtime: %.2f hours", as.numeric(elapsed)),
  sprintf("Force:   %s", force_retrain),
  sprintf("Dry run: %s", dry_run),
  sprintf("Region:  %s", region_filter %||% "all")
), summary_file)
```

Dry-run mode — print expected transition-region pairs without training. Use perform_transition_modelling()'s internal logic: read viable_transitions_lists.csv, filter to non-self non-NA rate_2018_2022 rows (region != "whole_map"), print each pair. The simplest approach is to call transition_modelling() with refresh_cache=FALSE and dry_run=TRUE if that parameter is supported; otherwise, implement dry-run by reading viable_transitions directly:
```r
if (dry_run) {
  viable_path <- config[["viable_transitions_lists"]]
  if (!file.exists(viable_path)) {
    cat(sprintf("Dry run: viable_transitions_lists.csv not found at %s\n", viable_path))
    cat("Cannot enumerate transition-region pairs without this file.\n")
    quit(status = 0)
  }
  viable <- utils::read.csv(viable_path, stringsAsFactors = FALSE)
  # Filter: non-self transitions, non-NA rate_2018_2022, exclude whole_map
  viable <- viable[
    !is.na(viable$rate_2018_2022) &
    viable$rate_2018_2022 != 0 &
    viable$region_name != "whole_map" &
    viable$from_class != viable$to_class,
  ]
  if (!is.null(region_filter)) {
    viable <- viable[viable$region_name == region_filter, ]
  }
  cat(sprintf("[DRY RUN] Would re-train %d transition-region pairs:\n", nrow(viable)))
  for (i in seq_len(nrow(viable))) {
    cat(sprintf("  %s -> %s (%s)\n",
      viable$from_class[[i]], viable$to_class[[i]], viable$region_name[[i]]))
  }
  cat("\nRe-run without --dry-run to execute training.\n")
  quit(status = 0)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/retrain_all_models.r</name>
  <files>scripts/retrain_all_models.r</files>
  <read_first>
    - scripts/run_transition_modelling.r (read the full file — this is the primary analog for the script skeleton: shebang, start_time, working directory resolution, source loop, get_config(), tryCatch wrapper, summary file write pattern)
    - scripts/run_allocation.r (read lines 1–100 — argument parsing pattern with commandArgs, no install.packages(), tryCatch sourcing)
    - .planning/phases/02-model-size-reduction/02-02-SUMMARY.md (confirms transition_modelling() signature and return structure after the 02-02 rewrite)
    - src/transition_modelling.r (lines 19–54 — transition_modelling() signature and parameter defaults, to verify the call is correct)
  </read_first>
  <action>
Create scripts/retrain_all_models.r as a NEW file. It does not exist yet.

The script structure (in order):
1. Shebang: `#!/usr/bin/env Rscript`
2. Comment block: script purpose, usage examples (--force, --dry-run, --region <name>)
3. start_time <- Sys.time()
4. Banner cat() block
5. Argument parsing (--force, --dry-run, --region):
   - `force_retrain <- "--force" %in% .cli_args`
   - `dry_run       <- "--dry-run" %in% .cli_args`
   - `--region <name>` as next positional arg after --region flag
6. Working directory resolution (script_path pattern from run_transition_modelling.r)
7. Source loop: src/setup.r, src/utils.r, src/transition_modelling.r (fail-fast with quit(status=1) on error; NO install.packages())
8. Config: `config <- tryCatch(get_config(), error = function(e) { cat(...); quit(status=1) })`
9. Display resolved settings (config env, model dir, force flag, dry run flag, region filter)
10. Dry-run mode: if dry_run is TRUE, read viable_transitions_lists.csv from config[["viable_transitions_lists"]], filter and print pairs, then quit(status=0)
11. Main call:
    ```r
    cat("\nStarting mlr3 model re-training...\n")
    tryCatch(
      transition_modelling(
        config           = config,
        refresh_cache    = force_retrain,
        use_regions      = if (!is.null(region_filter)) TRUE else NULL,
        model_specs_path = NULL,
        periods_to_process = NULL
      ),
      error = function(e) {
        cat(sprintf("FATAL ERROR: %s\n", conditionMessage(e)))
        quit(status = 1)
      }
    )
    ```
    Note: --region filter is best handled by passing it directly to transition_modelling() if that function supports region filtering, or by documenting that the operator should set appropriate config values. Since transition_modelling() uses use_regions from config by default, document that --region is informational in this version and full per-region filtering will be wired in a future enhancement. The flag is accepted but use_regions is still read from config. Add a cat() note when region_filter is set: "NOTE: --region flag accepted; full per-region filtering is via config regionalization settings."

12. Summary: end_time, elapsed, summary file written to config[["transition_model_eval_dir"]]
13. Final status message and quit(status=0)

The `%||%` operator: either define it locally at the top of the script or rely on it being available after sourcing src/setup.r / src/utils.r. Check by examining if the operator is defined in those files; if not, define it locally.

Commit as: `feat(02-04): add scripts/retrain_all_models.r mlr3 re-training utility (D-07, D-08)`
  </action>
  <verify>
    <automated>grep -c "transition_modelling(" scripts/retrain_all_models.r && grep -c "get_config" scripts/retrain_all_models.r && grep -c "dry.run\|dry_run" scripts/retrain_all_models.r</automated>
  </verify>
  <acceptance_criteria>
    - scripts/retrain_all_models.r exists
    - grep -c "transition_modelling(" returns >= 1
    - grep -c "get_config" returns >= 1
    - grep -c "dry_run\|dry-run" returns >= 2 (flag definition and conditional use)
    - grep -c "force_retrain\|--force" returns >= 2 (flag definition and use in transition_modelling() call)
    - grep -c "region_filter\|--region" returns >= 2 (flag parsing and display)
    - grep -c "install.packages" returns 0 (no runtime package installation)
    - grep -c "src/setup.r" returns >= 1 (script sources setup.r)
    - grep -c "src/transition_modelling.r" returns >= 1 (script sources transition_modelling.r)
    - grep -c "quit(status" returns >= 2 (fail-fast exits present)
    - The file starts with `#!/usr/bin/env Rscript` (bash -n not applicable to R but header must be present)
    - File is syntactically valid R (no obvious parse errors readable by inspection)
  </acceptance_criteria>
  <done>scripts/retrain_all_models.r exists and contains all required sections: arg parsing, source loop, get_config(), dry-run handler, transition_modelling() call, and summary file write</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (--force, --dry-run, --region) → script | commandArgs() values are passed by the operator; no external user input |
| config[["viable_transitions_lists"]] → file read | Path from operator-controlled YAML; dry-run mode reads this CSV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | --region CLI argument | accept | Used for display and informational filtering only in this version; full filtering deferred; no file writes depend on the unvalidated region string |
| T-02-09 | Denial of Service | transition_modelling() call with refresh_cache=TRUE | accept | --force flag is operator-only; forces re-training of all ~160 pairs; expected to be a long-running operation; operator is aware of cost |
| T-02-10 | Information Disclosure | summary file in transition_model_eval_dir | accept | Summary contains timing, paths, and status only — no PII or secrets; written to the operator-configured eval directory |
</threat_model>

<verification>
After task completion:

```bash
grep -c "transition_modelling(" scripts/retrain_all_models.r
grep -c "get_config" scripts/retrain_all_models.r
grep -c "install.packages" scripts/retrain_all_models.r
grep -c "dry_run" scripts/retrain_all_models.r
```

Expected: first two return >= 1; third returns 0; fourth returns >= 2.

Functional check (requires R and sourced dependencies):
```bash
Rscript scripts/retrain_all_models.r --dry-run
```
Expected: prints "[DRY RUN] Would re-train N transition-region pairs:" and exits 0. If viable_transitions_lists.csv is not present, prints a clear message and exits 0 (no crash).
</verification>

<success_criteria>
- scripts/retrain_all_models.r exists with shebang, argument parsing, source loop, get_config(), dry-run handler, transition_modelling() call, and summary file write
- --force flag sets refresh_cache=TRUE in the transition_modelling() call
- --dry-run flag reads viable_transitions_lists.csv and prints pairs without training
- No install.packages() calls anywhere in the script
- Script fails fast on source errors (quit(status=1))
- Summary file written to config[["transition_model_eval_dir"]] after training completes
- Changes committed
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-size-reduction/02-04-SUMMARY.md`
</output>
