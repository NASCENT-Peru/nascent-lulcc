---
phase: 01.1-fix-dinamica-launch-contract
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dinamica/container/rocker-geospatial-dinamica.def
  - dinamica/dinamica_model/smoketest.ego-decoded
  - dinamica/dinamica_model/smoketest.ego
autonomous: false
requirements:
  - INFRA-01
  - MEM-06
user_setup:
  - service: Dinamica EGO 8 AppImage
    why: "Container build downloads the AppImage from dinamicaego.com/nui_download/1960/ at build time; download URL pinned per D-102"
    env_vars: []
    dashboard_config:
      - task: "Operator must rebuild dinamica-ego-8.sif on Euler after this plan lands"
        location: "On Euler: apptainer build dinamica-ego-8.sif dinamica/container/rocker-geospatial-dinamica.def && mv to $DINAMICA_EGO_8_HOME location"

must_haves:
  truths:
    - "dinamica/container/rocker-geospatial-dinamica.def bootstraps from docker://rocker/r-ver:4.5.3 (DD-1 resolution per D-101/D-102) — no GHCR auth required."
    - ".def %post inlines the upstream Dockerfile's five stages (apt deps, R geospatial via install2.r, AppImage download + extract, dinamica R pkg install, conf seed) verbatim."
    - "The Dinamica AppImage URL is pinned to https://dinamicaego.com/nui_download/1960/ (the 8.7.0.20250814 release)."
    - "dinamica/dinamica_model/smoketest.ego-decoded is a hand-written ~5-line no-op .ego-decoded source that DinamicaConsole accepts after process_dinamica_script() round-trip."
    - "dinamica/dinamica_model/smoketest.ego exists as a committed binary asset (output of process_dinamica_script encoding OR Dinamica EGO GUI export per D-110 fallback)."
    - "dinamica/dinamica_model/allocation.ego-decoded is UNCHANGED (production code at src/dinamica_utils.r:512 loads it directly; DD-2 resolved to add a sibling, NOT replace)."
    - ".def %environment exports PATH and LD_LIBRARY_PATH but does NOT pin an ENTRYPOINT (apptainer exec passes the command explicitly per D-104)."
  artifacts:
    - path: "dinamica/container/rocker-geospatial-dinamica.def"
      provides: "Apptainer build recipe rebuildable on Euler with no GHCR auth and no workstation docker-save transfer"
      contains: "rocker/r-ver:4.5.3"
    - path: "dinamica/dinamica_model/smoketest.ego-decoded"
      provides: "Hand-written minimal no-op smoke fixture source"
      contains: "smoketestConstant"
    - path: "dinamica/dinamica_model/smoketest.ego"
      provides: "Binary smoke fixture accepted by DinamicaConsole"
      contains: "@charset"
  key_links:
    - from: "dinamica/container/rocker-geospatial-dinamica.def %post"
      to: "https://dinamicaego.com/nui_download/1960/"
      via: "curl -fSL + --appimage-extract"
      pattern: "1960"
    - from: "dinamica/dinamica_model/smoketest.ego-decoded"
      to: "dinamica/dinamica_model/smoketest.ego"
      via: "process_dinamica_script(mode='encode')"
      pattern: "smoketest"
---

<objective>
Rewrite `dinamica/container/rocker-geospatial-dinamica.def` so it bootstraps from `docker://rocker/r-ver:4.5.3` and inlines the upstream `ethzplus/rocker-geospatial-dinamica` Dockerfile's five `%post` stages verbatim (D-101/D-102/D-103), making the `.sif` rebuildable on Euler with no GHCR auth and no workstation `docker save` workaround. Add `dinamica/dinamica_model/smoketest.ego-decoded` + `dinamica/dinamica_model/smoketest.ego` as siblings of `allocation.ego-decoded` (D-109/DD-2) — production allocation continues to use `allocation.ego-decoded` unchanged (D-111).

Purpose: Close INFRA-01 SC1 (container rebuildable from this repo with no manual transfer) and MEM-06 SC5 (true minimal smoke fixture).

Output: One rewritten `.def` and two new repo assets. The actual `.sif` build + acceptance is an operator-side checkpoint that runs on Euler.
</objective>

<execution_context>
@C:/Users/black/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/black/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md
@dinamica/container/rocker-geospatial-dinamica.def
@dinamica/dinamica_model/allocation.ego-decoded
@src/dinamica_utils.r

<interfaces>
<!-- Existing assets the executor must NOT modify. -->

Production allocation path (unchanged):
- `src/dinamica_utils.r:run_allocation_dinamica()` lines 411-... loads `dinamica/dinamica_model/allocation.ego-decoded` directly via `file.copy(decoded_file, ...)`.
- DD-2 RESOLUTION: the smoke fixture is added as a NEW sibling file. `allocation.ego-decoded` is UNCHANGED.

Existing .ego-decoded header format (analog for smoketest.ego-decoded):
```
@charset = UTF-8
@date = 2025-Dec-04 16:00:58
@version = 8.7.0.20250814
```

process_dinamica_script() at src/dinamica_utils.r:335-394 (existing):
- `process_dinamica_script(infile, outfile, mode = "encode")` reads text, base64-encodes CalculateRExpression / CalculatePythonExpression blocks, writeChar()s output.
- For a no-op .ego-decoded with NO Calculate* blocks, the function performs a literal text copy (verified in RESEARCH.md Target 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dinamica/container/rocker-geospatial-dinamica.def with D-101 bootstrap + verbatim upstream %post</name>
  <files>dinamica/container/rocker-geospatial-dinamica.def</files>
  <read_first>
    - dinamica/container/rocker-geospatial-dinamica.def (current file — 76 lines; preserve %labels and %help skeleton, replace Bootstrap / From / %post / %test / %environment)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md (section "dinamica/container/rocker-geospatial-dinamica.def" lines 262-301 — preservation + replacement directives)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md (section "Code Examples — Upstream .def %post template" lines 381-443 — verbatim splice target; also Target 2 lines 507-570 for the upstream Dockerfile evidence)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md (D-101, D-102, D-103, D-114)
  </read_first>
  <action>
    REPLACE the entire content of `dinamica/container/rocker-geospatial-dinamica.def` with the following definition. Preserve the %labels block (provenance) but update the description; preserve %help and rewrite for the new build flow.

    ```
    Bootstrap: docker
    From: rocker/r-ver:4.5.3

    # -----------------------------------------------------------------------------
    # Apptainer / Singularity definition file for the external Dinamica EGO 8
    # image used by Stage 7 on Euler.
    #
    # Phase 1.1 — Rewrite per D-101, D-102, D-103.
    #
    # Provenance
    # ----------
    # This definition inlines the upstream `ethzplus/rocker-geospatial-dinamica`
    # Dockerfile's install steps verbatim in %post, bootstrapping from the
    # publicly-pullable rocker/r-ver:4.5.3 base image instead of a GHCR-published
    # composite image (D-101 — the upstream repo is Dockerfile-only because
    # Dinamica's license forbids redistribution).
    #
    # Pinned versions (D-102 — rebuild trigger conditions documented below):
    #   - rocker/r-ver:4.5.3
    #   - Dinamica EGO 8.7.0.20250814 via https://dinamicaego.com/nui_download/1960/
    #
    # When to rebuild:
    #   - Upstream Dinamica EGO bumps to 8.8 → update the download URL number
    #     AND the @version line in dinamica/dinamica_model/smoketest.ego-decoded.
    #   - rocker/r-ver bumps to 4.6+ → update the `From` tag here.
    #   - Otherwise, the .sif is byte-reproducible from this .def.
    #
    # Build flow (operators only)
    # ---------------------------
    # Apptainer (preferred on Euler):
    #   apptainer build dinamica-ego-8.sif \
    #       dinamica/container/rocker-geospatial-dinamica.def
    #
    # Singularity (fallback spelling):
    #   singularity build dinamica-ego-8.sif \
    #       dinamica/container/rocker-geospatial-dinamica.def
    #
    # After building, place the `.sif` outside the repository (typically under
    # `/project/<project>/containers/dinamica-ego-8.sif` per D-10) and
    # export DINAMICA_EGO_8_HOME to that absolute path.
    #
    # The fallback `docker save → docker-archive://` workstation transfer (D-114)
    # is documented in dinamica/container/README.md as a fallback note ONLY,
    # not the canonical path.
    #
    # See dinamica/container/README.md for the full operator-facing build flow.
    # See docs/README_HPC.md for the DINAMICA_EGO_8_HOME contract and the
    # apptainer launch command shape (D-104).
    # -----------------------------------------------------------------------------

    %labels
        org.opencontainers.image.title         "nascent-lulcc Dinamica EGO 8 runtime"
        org.opencontainers.image.source        "https://github.com/ethzplus/rocker-geospatial-dinamica"
        org.opencontainers.image.description   "External SIF for Stage 7 allocation; consumed by exec_dinamica() via apptainer/singularity exec with --home + --env DINAMICA_EGO_8_TEMP_DIR (D-104, D-105)."
        nascent-lulcc.requirement              "INFRA-01"
        nascent-lulcc.contract.env             "DINAMICA_EGO_8_HOME=/absolute/path/to/dinamica.sif"
        nascent-lulcc.upstream                 "ethzplus/rocker-geospatial-dinamica"
        nascent-lulcc.phase                    "1.1"

    %post
        set -euo pipefail
        export DEBIAN_FRONTEND=noninteractive
        export DINAMICA_TARGET_DIR=/opt/dinamica
        export DINAMICA_EGO_DOWNLOAD_URL=https://dinamicaego.com/nui_download/1960/

        # Stage 1 — apt geospatial deps (verbatim from upstream install_geospatial.sh)
        apt-get update && apt-get install -y --no-install-recommends \
            cmake gdal-bin lbzip2 libabsl-dev libfftw3-dev libgdal-dev libgeos-dev \
            libgsl0-dev libhdf4-alt-dev libhdf5-dev libnetcdf-dev libproj-dev \
            libprotobuf-dev libsqlite3-dev libssl-dev libudunits2-dev netcdf-bin \
            pkg-config protobuf-compiler sqlite3 curl ca-certificates \
            && rm -rf /var/lib/apt/lists/*

        # Stage 2 — R geospatial packages (matches upstream install_geospatial.sh tail)
        install2.r --error sf stars terra

        # Stage 3 — Dinamica AppImage download + extract
        mkdir -p "$DINAMICA_TARGET_DIR"
        curl -fSL "$DINAMICA_EGO_DOWNLOAD_URL" -o "$DINAMICA_TARGET_DIR/DinamicaEGO.AppImage"
        chmod +x "$DINAMICA_TARGET_DIR/DinamicaEGO.AppImage"
        cd "$DINAMICA_TARGET_DIR"
        ./DinamicaEGO.AppImage --appimage-extract
        cp -a squashfs-root/. .
        rm -rf squashfs-root DinamicaEGO.AppImage

        # Stage 4 — Dinamica R package install from extracted tarball
        R -e "install.packages('remotes', repos='https://cloud.r-project.org/'); \
              remotes::install_local(list.files( \
                file.path(Sys.getenv('DINAMICA_TARGET_DIR'), 'usr', 'bin', 'Data', 'R'), \
                pattern = 'Dinamica_.*\\\\.tar\\\\.gz', recursive = TRUE, full.names = TRUE))"

        # Stage 5 — seed /root/.dinamica_ego_8.conf (D-105 — replicated at runtime
        # via apptainer --home <staged-home>)
        cat > /root/.dinamica_ego_8.conf <<'CONF'
    AlternativePathForR = "/usr/local/bin/Rscript"
    ClConfig = "0"
    MemoryAllocationPolicy = "1"
    RCranMirror = "https://cloud.r-project.org/"
    CONF

        # Ensure the in-image staging dir for DINAMICA_EGO_8_TEMP_DIR exists; the
        # runtime override via apptainer --env may point elsewhere, but the
        # default /tmp/dinamica path must be present so DinamicaConsole startup
        # does not abort when no --env is passed (used by %test below).
        mkdir -p /tmp/dinamica

    %environment
        export LD_LIBRARY_PATH=/opt/dinamica/usr/lib:$LD_LIBRARY_PATH
        export PATH=$PATH:/opt/dinamica/usr/bin
        export DINAMICA_EGO_8_INSTALLATION_DIRECTORY=/opt/dinamica/usr/bin
        export DINAMICA_EGO_8_HOME_DIR=/opt/dinamica
        # NOTE: DINAMICA_EGO_8_TEMP_DIR is NOT exported here — apptainer --env
        # overrides it at runtime per D-105.

    %test
        set -euo pipefail
        # Lightweight build-time verification: the entrypoint binaries exist.
        # The real end-to-end smoke runs from outside the container via
        # scripts/smoke_test_dinamica.sh --live against this .sif.
        command -v DinamicaConsole >/dev/null 2>&1
        test -x /opt/dinamica/usr/bin/DinamicaEGO.sh
        DinamicaConsole -version || true

    %help
        # Phase 1.1 launch contract (D-104):
        #   apptainer exec \
        #       --home   "$HPC_SCRATCH_ROOT/dinamica-home" \
        #       --env    DINAMICA_EGO_8_TEMP_DIR="$HPC_SCRATCH_ROOT/dinamica-tmp" \
        #       "$DINAMICA_EGO_8_HOME" \
        #       bash -c 'cd /opt/dinamica/usr && bin/DinamicaEGO.sh /abs/path/to/model.ego'
        #
        # Build:
        #   apptainer build dinamica-ego-8.sif \
        #       dinamica/container/rocker-geospatial-dinamica.def
        #
        # Live smoke test (after building and exporting DINAMICA_EGO_8_HOME +
        # HPC_SCRATCH_ROOT):
        #   bash scripts/smoke_test_dinamica.sh --live --runtime auto \
        #       --artifact "$DINAMICA_EGO_8_HOME" \
        #       --ego     dinamica/dinamica_model/smoketest.ego \
        #       --require-log-under logs
        #
        # See dinamica/container/README.md and docs/README_HPC.md.
    ```

    KEY anti-pattern to avoid: do NOT add an `%runscript` block that pins `DinamicaConsole "$@"` as the runscript. Phase 1's failure G6 proved that `apptainer exec <sif> DinamicaConsole <model>` is unsupported by this image; our launch path uses `bash -c 'cd ... && bin/DinamicaEGO.sh'` from outside (D-104). The `.def` must expose the binaries via PATH but not pin a runscript that conflicts with the explicit exec.

    Also: do NOT vendor `install_geospatial.sh` or `install_dinamica_pkg.r` from upstream as %files — inline the content verbatim in %post (Stages 1/2 and Stage 4). The upstream repo is a moving target; inlining keeps the build byte-reproducible from this `.def` alone.
  </action>
  <verify>
    <automated>grep -c 'rocker/r-ver:4.5.3' dinamica/container/rocker-geospatial-dinamica.def | grep -q '^1$' &amp;&amp; grep -c 'dinamicaego.com/nui_download/1960' dinamica/container/rocker-geospatial-dinamica.def | grep -q '^1$' &amp;&amp; grep -c 'DinamicaEGO.sh' dinamica/container/rocker-geospatial-dinamica.def | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ghcr.io/ethzplus' dinamica/container/rocker-geospatial-dinamica.def` returns 0 (old bootstrap reference is gone; the upstream attribution in %labels uses the github.com URL, not the GHCR registry path).
    - `grep -c 'From: rocker/r-ver:4.5.3' dinamica/container/rocker-geospatial-dinamica.def` returns 1.
    - `grep -c 'Bootstrap: docker' dinamica/container/rocker-geospatial-dinamica.def` returns 1.
    - `grep -c 'dinamicaego.com/nui_download/1960' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1.
    - `grep -c -- '--appimage-extract' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1.
    - `grep -c 'install2.r' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (Stage 2 R geospatial install).
    - `grep -c 'remotes::install_local' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (Stage 4 Dinamica R pkg install).
    - `grep -c '.dinamica_ego_8.conf' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (Stage 5 conf seed).
    - `grep -c 'AlternativePathForR' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (Stage 5 conf content).
    - `grep -c 'LD_LIBRARY_PATH' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (%environment).
    - `grep -c 'DINAMICA_EGO_8_INSTALLATION_DIRECTORY' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1.
    - `grep -c 'DinamicaEGO.sh' dinamica/container/rocker-geospatial-dinamica.def` returns at least 1 (in %help and %test).
    - `grep -c 'D-101' dinamica/container/rocker-geospatial-dinamica.def` ≥ 1 (provenance).
    - `grep -c 'D-104' dinamica/container/rocker-geospatial-dinamica.def` ≥ 1.
    - `grep -c '%runscript' dinamica/container/rocker-geospatial-dinamica.def` returns 0 (no pinned entrypoint per the anti-pattern).
    - `grep -c 'apt-get install' dinamica/container/rocker-geospatial-dinamica.def` ≥ 1.
    - The verbatim apt package list from RESEARCH.md Code Examples lines 398-403 is present: at minimum `grep -c 'libgdal-dev' ...` ≥ 1, `grep -c 'libproj-dev' ...` ≥ 1, `grep -c 'libnetcdf-dev' ...` ≥ 1.
  </acceptance_criteria>
  <done>
    dinamica/container/rocker-geospatial-dinamica.def bootstraps from docker://rocker/r-ver:4.5.3, inlines all five upstream-Dockerfile %post stages verbatim (apt deps + install2.r geospatial + AppImage extract + Dinamica R pkg install + conf seed), exports the right %environment vars, has a %test block that asserts DinamicaConsole and bin/DinamicaEGO.sh exist, and has a %help block documenting the new D-104 launch shape. No GHCR auth, no docker-save workaround references in the canonical build path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dinamica/dinamica_model/smoketest.ego-decoded as a hand-written minimal no-op fixture</name>
  <files>dinamica/dinamica_model/smoketest.ego-decoded</files>
  <read_first>
    - dinamica/dinamica_model/allocation.ego-decoded (first 10 lines — header format analog: @charset, @date, @version)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md (section "dinamica/dinamica_model/smoketest.ego-decoded + .ego" lines 305-342)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md (Target 1 lines 493-505 — no-op .ego-decoded semantics; Target 8 lines 703-738 — DD-2 sibling resolution)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md (D-109, DD-2)
  </read_first>
  <action>
    Create `dinamica/dinamica_model/smoketest.ego-decoded` with the following EXACT content (no R/Python chunks so `process_dinamica_script()`'s check fires vacuously per RESEARCH.md Target 1):

    ```
    @charset = UTF-8
    @date = 2026-05-15 00:00:00
    @version = 8.7.0.20250814

    Script {{
        // Phase 1.1 smoke fixture — exercises the launch contract only.
        // No R/Python chunks; no data I/O; intentional no-op.
        // See dinamica/dinamica_model/smoketest.ego (encoded binary) and
        // .planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md
        // Target 1 + Target 8 for the format rationale (D-109, DD-2).
        smoketestConstant := NumberValue 1;
    }}
    ```

    Notes:
    - The file is committed as a TEXT file. `@version = 8.7.0.20250814` matches the upstream Dockerfile + allocation.ego-decoded.
    - The `@date` is the Phase 1.1 origination date.
    - The body MUST NOT contain `CalculateRExpression` or `CalculatePythonExpression` chunks — that triggers the `process_dinamica_script(check=TRUE)` rejection path.
    - allocation.ego-decoded is UNCHANGED — verify via `git diff dinamica/dinamica_model/allocation.ego-decoded` returning empty after this plan completes.
  </action>
  <verify>
    <automated>test -f dinamica/dinamica_model/smoketest.ego-decoded &amp;&amp; grep -c 'smoketestConstant' dinamica/dinamica_model/smoketest.ego-decoded | grep -q '^[1-9]' &amp;&amp; grep -c '@version = 8.7.0.20250814' dinamica/dinamica_model/smoketest.ego-decoded | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dinamica/dinamica_model/smoketest.ego-decoded` passes (file exists).
    - `grep -c '@charset = UTF-8' dinamica/dinamica_model/smoketest.ego-decoded` returns 1.
    - `grep -c '@version = 8.7.0.20250814' dinamica/dinamica_model/smoketest.ego-decoded` returns 1.
    - `grep -c 'smoketestConstant' dinamica/dinamica_model/smoketest.ego-decoded` returns at least 1.
    - `grep -c 'CalculateRExpression' dinamica/dinamica_model/smoketest.ego-decoded` returns 0 (no R chunks).
    - `grep -c 'CalculatePythonExpression' dinamica/dinamica_model/smoketest.ego-decoded` returns 0 (no Python chunks).
    - `wc -l dinamica/dinamica_model/smoketest.ego-decoded` returns ≤ 20 (true minimal fixture).
    - `git status dinamica/dinamica_model/allocation.ego-decoded` shows the file unchanged (no diff).
  </acceptance_criteria>
  <done>
    dinamica/dinamica_model/smoketest.ego-decoded exists as a 10-15 line file with the @charset / @date / @version header, an empty-body Script block, and no R/Python chunks. allocation.ego-decoded is untouched.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator generates dinamica/dinamica_model/smoketest.ego (D-110 gating experiment)</name>
  <what-built>
    Plan 03 Task 1 has rewritten the .def; Plan 03 Task 2 has created the no-op `smoketest.ego-decoded` source. We now need the binary `.ego` form that DinamicaConsole accepts.

    D-110 specifies a primary path (use the existing `process_dinamica_script(mode="encode")` round-trip) and a fallback path (Dinamica EGO GUI on a Windows workstation). RESEARCH.md Target 1 + Pitfall 5 establish HIGH confidence that the primary path WORKS for a no-op fixture, but the final verification is decidable only by running on Euler.

    This is a `checkpoint:human-action` task because Claude cannot run a Linux Dinamica binary on a Windows workstation, cannot build a `.sif` without Apptainer (Apptainer is not available on the Windows host), and the binary-format question must be answered against a built `.sif`. The operator handles this on Euler.
  </what-built>
  <how-to-verify>
    Operator runs the following on the **Windows workstation** (primary path D-110):

    1. From the project root, in an R session inside `allocation_env`:
       ```R
       source("src/dinamica_utils.r")
       process_dinamica_script(
         infile  = "dinamica/dinamica_model/smoketest.ego-decoded",
         outfile = "dinamica/dinamica_model/smoketest.ego",
         mode    = "encode",
         check   = TRUE
       )
       ```
       Expected: the function writes `dinamica/dinamica_model/smoketest.ego` and returns without error. Because the source has no `Calculate(R|Python)Expression` blocks, `process_dinamica_script()` performs a literal text copy with `.ego` extension.

    2. Verify the file exists:
       ```bash
       test -f dinamica/dinamica_model/smoketest.ego && echo OK
       ```

    3. Stage the file: `git add dinamica/dinamica_model/smoketest.ego` (commit as binary asset).

    Then on **Euler** (D-110 acceptance — the gating live verification):

    4. Build the new `.sif` from the rewritten `.def` (Plan 03 Task 1):
       ```bash
       cd $REPO_ROOT
       apptainer build dinamica-ego-8.sif dinamica/container/rocker-geospatial-dinamica.def
       mv dinamica-ego-8.sif /project/<project>/containers/dinamica-ego-8.sif   # or your $DINAMICA_EGO_8_HOME path
       export DINAMICA_EGO_8_HOME=/project/<project>/containers/dinamica-ego-8.sif
       ```
       Expected: `apptainer build` exits 0 with no GHCR auth prompt and no manual workstation transfer.

    5. Run the live smoke test against the new fixture (Plan 02 Task 1 produces the new `--ego smoketest.ego` shape; until Plan 04 lands the docs may still reference `allocation.ego-decoded` — pass `--ego dinamica/dinamica_model/smoketest.ego` explicitly):
       ```bash
       source .env
       bash scripts/smoke_test_dinamica.sh --live --runtime auto \
           --artifact "$DINAMICA_EGO_8_HOME" \
           --ego dinamica/dinamica_model/smoketest.ego \
           --require-log-under logs
       ```
       Expected: exit code 0. `logs/dinamica-smoke-*.log` contains Dinamica's startup banner and a successful end-of-script message — NO `std::exception`, NO `terminate called`, NO `Dinamica EGO exited with an error`.

    **If step 5 fails with `terminate called` or `unsupported model script format`:** the fallback path is D-110's GUI route:

    6a. On the Windows workstation, open Dinamica EGO 8 GUI.
    6b. Open `dinamica/dinamica_model/smoketest.ego-decoded`. The GUI accepts `.ego-decoded` source files.
    6c. File → Save As → `dinamica/dinamica_model/smoketest.ego` (GUI writes the proprietary binary `.ego`).
    6d. Overwrite the round-trip-produced file with the GUI-saved binary.
    6e. Re-run step 5 on Euler. Expected: exit 0.

    If the GUI path also fails, ROADMAP SC2/SC5 cannot close. Capture the log and revisit `smoketest.ego-decoded` content (currently `Script {{ smoketestConstant := NumberValue 1; }}` — this is the absolute minimum Dinamica accepts based on the analog format).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
      - "approved" — primary path D-110 worked, `smoketest.ego` committed via `git add dinamica/dinamica_model/smoketest.ego`, live Euler smoke test exited 0
      - "fallback-used" — D-110 fallback (GUI build on workstation) was needed, file committed
      - "failed: <details>" — both paths failed; capture the log and Assumption A1 / Pitfall 5 escalation
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host network → dinamicaego.com download | The %post stage downloads a third-party binary at build time |
| Container build → /opt/dinamica filesystem | Inlined upstream Dockerfile steps run as root inside the build VM |
| Committed smoketest.ego binary asset → DinamicaConsole parser | Hand-written / round-tripped binary file consumed at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-11 | Tampering | Dinamica AppImage download integrity | accept | Phase 1.1 inherits the upstream Dockerfile's trust model (download from dinamicaego.com, no checksum). Adding a checksum gate is deferrable (out of scope per `<deferred>` ideas in CONTEXT.md). Pin the numbered URL `1960` per D-102 so version drift is detectable. |
| T-01.1-12 | Spoofing | Bootstrap from a tampered rocker/r-ver tag | accept | We pin `rocker/r-ver:4.5.3` (D-102) — tag pinning is the standard Docker idiom for reproducibility. Adding sigstore signature verification is out of scope for this phase. |
| T-01.1-13 | Tampering | Operator-committed smoketest.ego binary asset has been silently modified | mitigate | The file is committed to git; any change shows up in `git diff` and is reviewable. The smoketest fixture is a no-op script; even if tampered, the worst case is the smoke test reports a Dinamica error (which is now caught by D-107 grep). |
| T-01.1-14 | Information disclosure | %post echoes credentials | accept | %post is fully self-contained — no credentials are written or echoed. The only network operation is the public AppImage URL. |
</threat_model>

<verification>
- `dinamica/container/rocker-geospatial-dinamica.def` bootstraps from `docker://rocker/r-ver:4.5.3` with no GHCR reference in the canonical build path.
- `dinamica/dinamica_model/smoketest.ego-decoded` exists with the @version 8.7.0.20250814 header and a no-op Script body.
- `dinamica/dinamica_model/smoketest.ego` exists as a committed binary asset (operator-side gate).
- `dinamica/dinamica_model/allocation.ego-decoded` is byte-unchanged.
- Operator-side gate (Task 3): `apptainer build` succeeds on Euler and the live smoke test against the new fixture exits 0.
</verification>

<success_criteria>
- ROADMAP SC1 (apptainer build with no GHCR auth and no workstation transfer) — VERIFIED by the Task 3 operator gate.
- ROADMAP SC2 (smoke test exit 0 against rebuilt .sif with non-empty log) — VERIFIED by Task 3 gate combined with Plan 02 Task 1.
- ROADMAP SC5 (committed minimal .ego fixture; production encode-then-execute unchanged) — VERIFIED by Tasks 2 and 3 + the unchanged `allocation.ego-decoded`.
- INFRA-01 container-build path closed; MEM-06 smoke-fixture sanity closed.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md` summarising:
- Rewritten .def's bootstrap line and the five inlined %post stages.
- smoketest.ego-decoded body (verbatim).
- Operator gate outcome: "approved" / "fallback-used" / "failed".
- The .sif rebuild + smoke test command used on Euler.
- Whether the primary process_dinamica_script() path or the GUI fallback produced the committed smoketest.ego.
</output>
