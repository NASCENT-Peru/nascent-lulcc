---
phase: 03.3-probability-map-saturation-allocation-throughput
plan: 04
status: complete-with-verification-gap
completed: 2026-05-28
requirements:
  - ALLOC-06
  - ALLOC-07
  - ALLOC-08
  - ALLOC-10
files_modified:
  - tests/testthat/test-allocation-single-source-writer.R
  - tests/testthat/test-saturation-diagnostics.R
---

# Phase 03.3 Plan 04: Saturation Regression Test Summary

## Outcome

Added two new testthat files for the Phase 3.3 saturation and alignment contracts:

| Test file | Test blocks | Requirement coverage |
|---|---:|---|
| `tests/testthat/test-allocation-single-source-writer.R` | 8 | ALLOC-06, ALLOC-08 |
| `tests/testthat/test-saturation-diagnostics.R` | 7 | ALLOC-07, ALLOC-10 |

`test-allocation-single-source-writer.R` is a fast text-pattern regression suite over `src/allocation.r` and `scripts/run_allocation.r`. It locks in the Plan 02 single-source writer filter, removal of the old `file.copy(trans_rate_src)` path, `id_trans` sorting, no-skip probability TIF writer, all-NA fallback, alloc_params hard stop, inline saturation hook, and worker source ordering.

`test-saturation-diagnostics.R` sources `src/utils.r` and `src/saturation_diagnostics.r` into an isolated environment, builds inline synthetic raster fixtures via `terra::rast(matrix(...))`, and checks the documented 16-column schema, demanded/placed arithmetic, coverage NA handling, quantiles, exempt matching, and AUDIT grammar.

## Verification

Static verification completed:

| Check | Result |
|---|---|
| Both test files exist with uppercase `.R` extension | pass |
| `test-allocation-single-source-writer.R` has `library(testthat)` only | pass |
| `test-saturation-diagnostics.R` has `library(testthat)` and `library(terra)` | pass |
| Source-text assignments are present | pass |
| `expect_match()` and `expect_no_match()` guards are present | pass |
| Synthetic test uses `withr::local_tempdir()` | pass |
| Synthetic test uses `check.names = FALSE` | pass |
| Synthetic test emits/checks `AUDIT stage=saturation` grammar | pass |
| No binary fixtures were created under `tests/testthat/fixtures/` | pass |
| Test file does not reference `scripts/diagnose_allocation_saturation.r` | pass |

Verification gap: `Rscript` is not available on this workstation shell (`Rscript` is not recognized on `PATH`), so parse, focused test, and full-suite test gates could not be executed here. Run these on an R-capable host:

```bash
Rscript -e 'parse(file = "tests/testthat/test-allocation-single-source-writer.R"); cat("parse_ok\n")'
Rscript -e 'parse(file = "tests/testthat/test-saturation-diagnostics.R"); cat("parse_ok\n")'
Rscript -e 'library(testthat); test_file("tests/testthat/test-allocation-single-source-writer.R", reporter = "summary")'
Rscript -e 'library(testthat); test_file("tests/testthat/test-saturation-diagnostics.R", reporter = "summary")'
Rscript -e 'library(testthat); test_dir("tests/testthat", reporter = "progress")'
```

## Deviations

No binary fixtures were added. The synthetic raster helper uses inline matrices and writes only temporary GeoTIFFs under `withr::local_tempdir()`. The R verification gates remain pending solely because the local shell cannot find `Rscript`.
