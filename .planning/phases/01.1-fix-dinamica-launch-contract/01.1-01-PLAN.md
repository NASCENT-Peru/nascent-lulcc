---
phase: 01.1-fix-dinamica-launch-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dinamica_utils.r
  - tests/testthat/test-dinamica-launcher.R
  - tests/testthat/test-dinamica-error-detection.R
autonomous: true
requirements:
  - INFRA-01
  - OBS-02
user_setup: []

must_haves:
  truths:
    - "resolve_dinamica_launch(backend='hpc', runtime_override='apptainer', probe_runtime=FALSE) returns args containing '--home', '--env DINAMICA_EGO_8_TEMP_DIR=', 'bash', '-c', and 'cd /opt/dinamica/usr && bin/DinamicaEGO.sh'."
    - "resolve_dinamica_launch() creates $HPC_SCRATCH_ROOT/dinamica-home/ and $HPC_SCRATCH_ROOT/dinamica-tmp/ when missing; seeds .dinamica_ego_8.conf when missing; idempotent on re-run."
    - "resolve_dinamica_launch() raises a clear error when HPC backend is selected and HPC_SCRATCH_ROOT is unset."
    - "The model path passed to bin/DinamicaEGO.sh is always absolute (normalizePath applied)."
    - "exec_dinamica() reads back the teed log file (or falls back to res$stdout+res$stderr when write_logfile=FALSE) and calls stop() when any of three Dinamica error strings appear, regardless of subprocess exit code."
    - "Local backend continues to invoke DinamicaConsole directly with no --home / --env flags (unchanged)."
  artifacts:
    - path: "src/dinamica_utils.r"
      provides: "resolve_dinamica_launch() HPC branch with apptainer --home/--env + bash -c launch shape and exec_dinamica() post-run error-string grep"
      contains: "bin/DinamicaEGO.sh"
    - path: "tests/testthat/test-dinamica-launcher.R"
      provides: "Updated unit tests asserting the new HPC launch args shape (no longer DinamicaConsole; now bash -c + bin/DinamicaEGO.sh + --home + --env)"
      contains: "bin/DinamicaEGO.sh"
    - path: "tests/testthat/test-dinamica-error-detection.R"
      provides: "Unit tests covering D-107 (three-pattern grep) and D-108 (log artifact reuse) via a faked processx::run result"
      contains: "std::exception"
  key_links:
    - from: "src/dinamica_utils.r:resolve_dinamica_launch()"
      to: "$HPC_SCRATCH_ROOT/dinamica-home/.dinamica_ego_8.conf"
      via: "dir.create + writeLines (idempotent)"
      pattern: "dinamica-home"
    - from: "src/dinamica_utils.r:exec_dinamica()"
      to: "logs/dinamica/<ts>_dinamica.log"
      via: "readLines() + grepl(DINAMICA_ERROR_PATTERNS, fixed=TRUE)"
      pattern: "DINAMICA_ERROR_PATTERNS"
---

<objective>
Rewrite the HPC branch of `src/dinamica_utils.r:resolve_dinamica_launch()` to produce the supported apptainer launch shape (D-104/D-105/D-106) and replace the half-version error check in `exec_dinamica()` with the three-pattern post-hoc log grep (D-107/D-108). Update the existing testthat tests for the new args shape and add a new error-detection test file.

Purpose: Close INFRA-01 (Dinamica launches via the supported `bin/DinamicaEGO.sh` wrapper with `--home`/`--env` flags Dinamica EGO 8 needs at runtime) and OBS-02 (silent `std::exception` exits no longer slip through — operators see a stop() within minutes).

Output: `src/dinamica_utils.r` with the new HPC launch contract and three-pattern error grep, plus the unit tests proving both contracts.
</objective>

<execution_context>
@C:/Users/black/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/black/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md
@src/dinamica_utils.r
@tests/testthat/test-dinamica-launcher.R

<interfaces>
<!-- Existing exports the executor must preserve verbatim. Do NOT rename or change signatures. -->

From src/dinamica_utils.r (current public API — unchanged):
```r
resolve_dinamica_launch(model_path,
                        backend = "auto",
                        disable_parallel = TRUE,
                        log_level = NULL,
                        runtime_override = NULL,
                        probe_runtime = TRUE,
                        work_dir = NULL)
# returns list(backend, runtime, artifact_path, command, args, log_file, env)

exec_dinamica(model_path,
              disable_parallel = TRUE,
              log_level = NULL,
              write_logfile = TRUE,
              echo = FALSE,
              backend = "auto",
              runtime_override = NULL,
              work_dir = NULL)
# returns invisible(processx::run result)

resolve_dinamica_log_path(base_dir)
# returns <repo>/logs/dinamica/<ISO-ts>_dinamica.log
```

The return-list keys (`backend`, `runtime`, `artifact_path`, `command`, `args`, `log_file`, `env`) MUST remain identical — consumers in `exec_dinamica()` and `tests/testthat/test-dinamica-launcher.R` depend on these.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite resolve_dinamica_launch() HPC branch to the apptainer --home/--env + bash -c launch shape (D-104/D-105/D-106)</name>
  <files>src/dinamica_utils.r, tests/testthat/test-dinamica-launcher.R</files>
  <read_first>
    - src/dinamica_utils.r (lines 1-220 — read the full current resolve_dinamica_launch and the .DINAMICA_BACKENDS / .DINAMICA_RUNTIMES_HPC constants above it)
    - tests/testthat/test-dinamica-launcher.R (full file — five existing tests reference args/launch shape; you will UPDATE the HPC assertions and ADD new ones for D-105 idempotent staged-home seeding)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md (section "src/dinamica_utils.r:resolve_dinamica_launch() (utility, transform)" lines 23-72 — verbatim splice target)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md (section "Code Examples — Apptainer launch shape" lines 283-316 — the verbatim R block to splice in)
  </read_first>
  <behavior>
    Tests that MUST pass after this task (RED before → GREEN after):
    - test_that("resolve_dinamica_launch() HPC backend uses --home + --env + bash -c + bin/DinamicaEGO.sh"): with `DINAMICA_EGO_8_HOME=/tmp/dinamica.sif`, `DINAMICA_BACKEND=hpc`, `HPC_SCRATCH_ROOT=<tempdir>`, calling `resolve_dinamica_launch(model_path="/abs/model.ego", backend="hpc", runtime_override="apptainer", probe_runtime=FALSE)` returns `launch$args` such that:
        * `any(grepl("^--home$", launch$args))` is TRUE
        * `any(grepl("^--env$", launch$args))` is TRUE
        * `any(grepl("^DINAMICA_EGO_8_TEMP_DIR=", launch$args))` is TRUE
        * `any(launch$args == "bash")` is TRUE
        * `any(launch$args == "-c")` is TRUE
        * `any(grepl("cd /opt/dinamica/usr && bin/DinamicaEGO.sh", launch$args, fixed=TRUE))` is TRUE
        * `any(grepl("/tmp/dinamica.sif", launch$args, fixed=TRUE))` is TRUE
        * NO arg equals `"DinamicaConsole"` (D-104 replaces direct DinamicaConsole invocation)
    - test_that("resolve_dinamica_launch() HPC uses absolute model path even when given a relative one"): with the same env, calling with `model_path="model.ego"` (relative) returns `launch$args` whose last bash-c payload contains a normalized absolute path (test by `grepl("^/", normalizePath_substring)` against the resolved arg).
    - test_that("resolve_dinamica_launch() HPC creates staged-home + staged-tmp idempotently"): with `HPC_SCRATCH_ROOT=tempdir`, call resolve_dinamica_launch() twice in a row; assert `dir.exists(file.path(HPC_SCRATCH_ROOT, "dinamica-home"))` and `dir.exists(file.path(HPC_SCRATCH_ROOT, "dinamica-tmp"))` are TRUE after first call and remain TRUE after second; assert `file.exists(file.path(HPC_SCRATCH_ROOT, "dinamica-home", ".dinamica_ego_8.conf"))` is TRUE and the file content matches the four `AlternativePathForR / ClConfig / MemoryAllocationPolicy / RCranMirror` lines from RESEARCH.md Code Examples lines 294-305.
    - test_that("resolve_dinamica_launch() HPC fails clearly when HPC_SCRATCH_ROOT is unset"): with `DINAMICA_EGO_8_HOME=/tmp/dinamica.sif`, `DINAMICA_BACKEND=hpc`, `HPC_SCRATCH_ROOT=NA`, expect_error(...) with regexp `"HPC_SCRATCH_ROOT"`.
    - test_that("resolve_dinamica_launch() local backend is unchanged"): with `DINAMICA_BACKEND=local`, `DINAMICA_EGO_8_HOME=/opt/dinamica/install`, `runtime_override=NULL`, `probe_runtime=FALSE`, the returned `launch$args` MUST NOT contain `--home` or `--env`; `launch$command` is `"DinamicaConsole"`; `launch$runtime` is `"DinamicaConsole"`.
    - test_that("resolve_dinamica_launch() return list keys are unchanged"): assert `setequal(names(launch), c("backend","runtime","artifact_path","command","args","log_file","env"))`.
  </behavior>
  <action>
    Step A: Update `tests/testthat/test-dinamica-launcher.R` FIRST.

    1. The existing test at line 47 (`"resolve_dinamica_launch() resolves the HPC backend with DINAMICA_EGO_8_HOME as the .sif image"`) currently asserts `any(grepl("DinamicaConsole", launch$args))` at line 73. REPLACE that assertion with the new D-104 assertions listed in `<behavior>`. Add `HPC_SCRATCH_ROOT = tempfile()` to the `withr::local_envvar()` block at lines 51-54 (use `dir.create(staged, recursive=TRUE)` first so the env var resolves to an existing dir).
    2. Add a new test_that("resolve_dinamica_launch() HPC uses absolute model path even when given a relative one") covering D-106; use the same env block plus `model_path = "model.ego"` (relative) and assert the last arg (the bash -c payload) contains a normalized absolute path.
    3. Add test_that("resolve_dinamica_launch() HPC creates staged-home + staged-tmp idempotently") covering D-105; create a tempdir, set HPC_SCRATCH_ROOT to it via withr, call resolve_dinamica_launch() TWICE, assert dirs and conf exist with the exact four-line content from RESEARCH.md Code Examples 294-305.
    4. Add test_that("resolve_dinamica_launch() HPC fails clearly when HPC_SCRATCH_ROOT is unset") — withr::local_envvar(HPC_SCRATCH_ROOT = NA, ...), expect_error(..., regexp = "HPC_SCRATCH_ROOT").
    5. The existing test "resolve_dinamica_launch() resolves the local backend with direct DinamicaConsole launch" (lines 83-104) and "resolve_dinamica_launch() falls back to singularity when override == 'singularity'" (lines 106-121) and "resolve_dinamica_launch() fails clearly on HPC when DINAMICA_EGO_8_HOME is unset" (lines 123-141) remain unchanged in intent. The singularity test must also be updated to pass `HPC_SCRATCH_ROOT = tempfile()` in withr::local_envvar (the new HPC branch requires it).
    6. Run the test file once to confirm RED (the existing DinamicaConsole assertion now fails, idempotent staged-home test fails because no implementation yet).

    Step B: Rewrite `src/dinamica_utils.r:resolve_dinamica_launch()` HPC branch (lines 164-201 of the current file).

    1. Add a provenance header comment block above line 164 (HPC branch start):
       ```r
       # ---- Phase 1.1 — D-104, D-105, D-106 ----
       # Apptainer launch shape: `apptainer exec --home <staged-home> --env DINAMICA_EGO_8_TEMP_DIR=<staged-tmp> <sif> bash -c 'cd /opt/dinamica/usr && bin/DinamicaEGO.sh <abs-model>'`.
       # Direct `apptainer exec <sif> DinamicaConsole <model>` is unsupported by the upstream image and produces silent std::exception failures.
       # MUST stay in sync with scripts/smoke_test_dinamica.sh LAUNCH_CMD array.
       ```
    2. Keep backend resolution (lines 137-149), console_args assembly is no longer needed in the HPC branch (the launcher takes only the model path, no `-disable-parallel-steps` flag — but the upstream `bin/DinamicaEGO.sh` is a thin wrapper around DinamicaConsole and DOES forward flags; preserve `console_args` minus the model_path so disable_parallel and log_level still flow through). Keep `log_file <- resolve_dinamica_log_path(base_dir)` at line 162.
    3. Inside `if (identical(backend, "hpc")) { ... }` (currently lines 164-201) REPLACE the body with:
       ```r
       artifact_path <- dinamica_home  # unchanged — DINAMICA_EGO_8_HOME is the .sif path on HPC

       if (!is.null(runtime_override)) {
         if (!runtime_override %in% .DINAMICA_RUNTIMES_HPC) {
           stop("runtime_override must be one of: ",
                paste(.DINAMICA_RUNTIMES_HPC, collapse = ", "),
                "; got '", runtime_override, "'.", call. = FALSE)
         }
         runtime <- runtime_override
       } else if (isTRUE(probe_runtime)) {
         runtime <- .probe_hpc_runtime()
       } else {
         stop("resolve_dinamica_launch(): probe_runtime=FALSE requires an explicit runtime_override on the HPC backend.", call. = FALSE)
       }

       # D-105 staged-home + staged-tmp under HPC_SCRATCH_ROOT
       hpc_scratch_root <- Sys.getenv("HPC_SCRATCH_ROOT", unset = "")
       if (!nzchar(hpc_scratch_root)) {
         stop(
           "HPC_SCRATCH_ROOT is not set. Phase 1.1 D-105 requires HPC_SCRATCH_ROOT ",
           "for the apptainer --home and --env DINAMICA_EGO_8_TEMP_DIR staging. ",
           "Source the project .env or export HPC_SCRATCH_ROOT=/beegfs/$USER/nascent-lulcc.",
           call. = FALSE
         )
       }
       staged_home <- file.path(hpc_scratch_root, "dinamica-home")
       staged_tmp  <- file.path(hpc_scratch_root, "dinamica-tmp")
       dir.create(staged_home, recursive = TRUE, showWarnings = FALSE)
       dir.create(staged_tmp,  recursive = TRUE, showWarnings = FALSE)

       conf <- file.path(staged_home, ".dinamica_ego_8.conf")
       if (!file.exists(conf)) {
         # Write atomically via tempfile + file.rename() to avoid Pitfall 3 race
         tmp_conf <- tempfile(tmpdir = staged_home, fileext = ".conf.tmp")
         writeLines(
           c(
             'AlternativePathForR = "/usr/local/bin/Rscript"',
             'ClConfig = "0"',
             'MemoryAllocationPolicy = "1"',
             'RCranMirror = "https://cloud.r-project.org/"'
           ),
           tmp_conf
         )
         file.rename(tmp_conf, conf)
       }

       # D-106 absolute model path
       abs_model <- normalizePath(model_path, winslash = "/", mustWork = FALSE)

       # D-104 launch shape
       bash_payload <- sprintf(
         "cd /opt/dinamica/usr && bin/DinamicaEGO.sh %s",
         shQuote(paste(c(setdiff(console_args, model_path), abs_model), collapse = " "))
       )
       container_args <- c(
         "exec",
         "--home", staged_home,
         "--env", paste0("DINAMICA_EGO_8_TEMP_DIR=", staged_tmp),
         artifact_path,
         "bash", "-c", bash_payload
       )
       env_vec <- c("current")
       return(list(
         backend       = "hpc",
         runtime       = runtime,
         artifact_path = artifact_path,
         command       = runtime,
         args          = container_args,
         log_file      = log_file,
         env           = env_vec
       ))
       ```
       NOTE on `console_args` handling: `console_args` is built at lines 152-159 of the current file ending with `model_path`. For the HPC branch we want `-disable-parallel-steps` and `-log-level N` flags STILL to flow through (they are accepted by `bin/DinamicaEGO.sh`), but with the model path normalized. Build the bash_payload as: `cd /opt/dinamica/usr && bin/DinamicaEGO.sh <flags> <abs_model>`. Use `console_args` with the trailing model_path replaced by `abs_model`. Implementation:
       ```r
       flags <- character()
       if (isTRUE(disable_parallel)) flags <- c(flags, "-disable-parallel-steps")
       if (!is.null(log_level)) flags <- c(flags, "-log-level", as.character(log_level))
       launcher_argline <- paste(c(flags, shQuote(abs_model)), collapse = " ")
       bash_payload <- sprintf("cd /opt/dinamica/usr && bin/DinamicaEGO.sh %s", launcher_argline)
       ```
       Place this build *after* setting `staged_home` / `staged_tmp` (so the function fails fast on missing HPC_SCRATCH_ROOT before doing arg construction).
    4. Keep the local branch (lines 203-220 of the current file) verbatim — D-105 last clause: local invocation skips both `--home` and `--env` flags.
    5. Update the function's roxygen docblock (lines 84-114) to reflect the new HPC args shape: replace the line `c("exec", <sif_path>, "DinamicaConsole", <ego model passthrough args>)` with `c("exec", "--home", <staged-home>, "--env", "DINAMICA_EGO_8_TEMP_DIR=<staged-tmp>", <sif_path>, "bash", "-c", "cd /opt/dinamica/usr && bin/DinamicaEGO.sh <abs-model> [flags]")`. Add `@note` mentioning D-104/D-105/D-106 and "MUST stay in sync with scripts/smoke_test_dinamica.sh LAUNCH_CMD".

    Step C: Run tests. From repo root:
    ```bash
    Rscript -e "testthat::test_file('tests/testthat/test-dinamica-launcher.R')"
    ```
    All seven tests must pass. If `Rscript` not on PATH (Windows workstation), this gate is satisfied by inspecting the test file structure; Wave-2 / Wave-3 operator gate will run on Euler.
  </action>
  <verify>
    <automated>Rscript -e "testthat::test_file('tests/testthat/test-dinamica-launcher.R')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"--home"' src/dinamica_utils.r` returns at least one match inside the HPC branch (line range 160-230).
    - `grep -n 'DINAMICA_EGO_8_TEMP_DIR=' src/dinamica_utils.r` returns at least one match.
    - `grep -n 'bin/DinamicaEGO.sh' src/dinamica_utils.r` returns at least one match.
    - `grep -n 'HPC_SCRATCH_ROOT' src/dinamica_utils.r` returns at least one match (in resolve_dinamica_launch).
    - `grep -nE 'normalizePath\(model_path' src/dinamica_utils.r` returns at least one match.
    - `grep -nE '\.dinamica_ego_8\.conf' src/dinamica_utils.r` returns at least one match.
    - In the HPC branch of resolve_dinamica_launch (lines after `if (identical(backend, "hpc"))` and before the next `# Local backend`), `grep -c 'DinamicaConsole'` returns 0 (DinamicaConsole no longer appears in the HPC launch args path; local branch may still reference it).
    - `grep -n 'D-104' src/dinamica_utils.r` and `grep -n 'D-105' src/dinamica_utils.r` and `grep -n 'D-106' src/dinamica_utils.r` each return at least one match (provenance annotation).
    - `tests/testthat/test-dinamica-launcher.R` contains at least one assertion against `bin/DinamicaEGO.sh` and at least one against `--home` (grep returns ≥1 match each).
    - `tests/testthat/test-dinamica-launcher.R` contains at least one `expect_error` with `regexp = "HPC_SCRATCH_ROOT"`.
    - When `Rscript` is available: `Rscript -e "testthat::test_file('tests/testthat/test-dinamica-launcher.R')"` exits 0 with "OK" for every test in the file. When `Rscript` is unavailable (Windows host), the static-grep gates above are sufficient; defer live run to the Wave-2 operator gate.
  </acceptance_criteria>
  <done>
    src/dinamica_utils.r HPC branch produces apptainer exec args matching the D-104 shape verbatim; idempotent dir/conf seeding works; absolute model path is enforced; HPC_SCRATCH_ROOT unset fails fast; local branch is byte-identical to before; test-dinamica-launcher.R asserts the new shape and existing HPC tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace exec_dinamica() error check with three-pattern post-hoc log grep (D-107/D-108)</name>
  <files>src/dinamica_utils.r, tests/testthat/test-dinamica-error-detection.R</files>
  <read_first>
    - src/dinamica_utils.r (lines 239-324 — exec_dinamica function body, especially the existing error check at lines 313-321 and the logfile write block at 274-301)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md (section "src/dinamica_utils.r:exec_dinamica() post-run error grep" lines 75-125 — verbatim splice target)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md (section "Code Examples — Error-string post-hoc detection" lines 318-350 — the verbatim R block to splice in)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md (D-107, D-108 — exact three error strings: "Dinamica EGO exited with an error", "terminate called after throwing", "std::exception")
  </read_first>
  <behavior>
    Tests that MUST pass after this task (in NEW file tests/testthat/test-dinamica-error-detection.R):
    - test_that("DINAMICA_ERROR_PATTERNS contains the three D-107 strings"): source dinamica_utils.r into a private env, assert `setequal(.env$DINAMICA_ERROR_PATTERNS, c("Dinamica EGO exited with an error", "terminate called after throwing", "std::exception"))` OR (if the constant is function-local) verify the function body contains all three strings via deparse + grepl.
    - test_that("exec_dinamica error grep fires when log contains std::exception and exit=0"): create a temp log file containing the line "terminate called after throwing an instance of 'std::exception'", construct a fake `res` list with `status=0L, stdout="", stderr=""`, run the grep block in isolation (extract it as an internal helper if needed) and assert it raises stop() with a message matching `"std::exception"`.
    - test_that("exec_dinamica error grep fires when log contains 'Dinamica EGO exited with an error' and exit=0"): same shape, log line contains the first pattern, expect stop().
    - test_that("exec_dinamica error grep fires when log contains 'terminate called after throwing' and exit=0"): same, expect stop().
    - test_that("exec_dinamica error grep does NOT fire when log is clean and exit=0"): log file contains "Dinamica EGO 8.7.0 model completed successfully", fake res with status=0, the block must NOT call stop() (no error raised).
    - test_that("exec_dinamica error grep falls back to res$stdout+res$stderr when write_logfile=FALSE"): set `write_logfile=FALSE`, no log file written, res$stdout contains "std::exception", expect stop().
    - test_that("exec_dinamica error grep stop message includes the matched pattern names and logfile path"): the stop() message must include the matched pattern string (e.g. "std::exception") AND the logfile_path (substring check via expect_match).
  </behavior>
  <action>
    Step A: Create `tests/testthat/test-dinamica-error-detection.R` FIRST (RED).

    Use the same `.repo_root` / `sys.source` boilerplate from tests/testthat/test-dinamica-launcher.R lines 27-38 (verbatim copy of the loader pattern). The new test file must NOT redefine those — just copy the pattern and source `src/dinamica_utils.r` into a private `.env`.

    Implementation idiom for testing the grep block without invoking processx (because we cannot reliably spawn a fake Dinamica subprocess in unit tests):

    The grep logic in exec_dinamica() will be EXTRACTED into a helper that takes `(res, write_logfile, logfile_path)` and either returns `invisible(res)` or calls `stop()`. This helper is the test surface. Name it: `.check_dinamica_post_run(res, write_logfile, logfile_path)` (leading dot = internal). Place it inside `src/dinamica_utils.r` just above `exec_dinamica()`. Tests source dinamica_utils.r and call `.env$.check_dinamica_post_run(...)`.

    Test fixtures: write the log file under `withr::local_tempdir()`; build the fake res via `list(status = 0L, stdout = "", stderr = "")`. Use `expect_error(...)` with `regexp = "std::exception"` etc.

    Run the test file once to confirm RED — the function doesn't exist yet so `is.function(.env$.check_dinamica_post_run)` is FALSE; use `skip_if_not(is.function(.env$.check_dinamica_post_run))` and accept that all tests SKIP at RED stage; that's the RED proxy.

    Step B: In `src/dinamica_utils.r`, ADD the internal helper `.check_dinamica_post_run()` just above `exec_dinamica()` (insert before line 222 of the current file, after `resolve_dinamica_launch()`'s closing brace at line 220):

    ```r
    # ---- Phase 1.1 — D-107, D-108 ----
    # Post-hoc error-string detection: DinamicaConsole / bin/DinamicaEGO.sh return
    # exit 0 on std::exception and on parse failures. Grep the captured log
    # (same artifact resolve_dinamica_log_path() returns; D-108) for the three
    # canonical error strings and stop() on any match, regardless of exit code.
    # MUST stay in sync with scripts/smoke_test_dinamica.sh grep pattern set.
    DINAMICA_ERROR_PATTERNS <- c(
      "Dinamica EGO exited with an error",
      "terminate called after throwing",
      "std::exception"
    )

    .check_dinamica_post_run <- function(res, write_logfile, logfile_path) {
      log_contents <- if (isTRUE(write_logfile) && !is.null(logfile_path) && file.exists(logfile_path)) {
        paste(readLines(logfile_path, warn = FALSE), collapse = "\n")
      } else {
        paste(c(res[["stdout"]], res[["stderr"]]), collapse = "\n")
      }

      error_hits <- vapply(
        DINAMICA_ERROR_PATTERNS,
        function(p) grepl(p, log_contents, fixed = TRUE),
        logical(1)
      )

      if (res[["status"]] != 0L || any(error_hits)) {
        matched <- DINAMICA_ERROR_PATTERNS[error_hits]
        stop(
          "Dinamica registered an error",
          if (length(matched)) sprintf(" (matched: %s)", paste(matched, collapse = "; ")) else "",
          if (res[["status"]] != 0L) sprintf(" [exit %d]", res[["status"]]) else "",
          ".\nRerun with echo = TRUE or check logfile: ",
          if (!is.null(logfile_path)) logfile_path else "(no logfile — write_logfile=FALSE)",
          call. = FALSE
        )
      }
      invisible(res)
    }
    ```

    Step C: REPLACE the existing check at lines 313-321 of `src/dinamica_utils.r` with a single call to the new helper:

    Current code (lines 313-321):
    ```r
    if (
      res[["status"]] != 0L ||
        grepl("Dinamica EGO exited with an error", res[["stdout"]])
    ) {
      stop(
        "Dinamica registered an error. \n",
        "Rerun with echo = TRUE or check logfile to see what went wrong."
      )
    }
    ```

    REPLACE with:
    ```r
    # D-107, D-108 — three-pattern grep on the teed log artifact
    .check_dinamica_post_run(
      res            = res,
      write_logfile  = write_logfile,
      logfile_path   = if (isTRUE(write_logfile)) logfile_path else NULL
    )
    ```

    Note: `logfile_path` is defined inside the `if (isTRUE(write_logfile))` block at line 275 — its scope reaches line 321 because R has function scope, but it's only defined when `write_logfile=TRUE`. The conditional above handles that.

    Step D: Re-run the new test file:
    ```bash
    Rscript -e "testthat::test_file('tests/testthat/test-dinamica-error-detection.R')"
    ```
    All seven tests must pass (GREEN).

    Step E: Re-run test-dinamica-launcher.R to confirm Task 1 is unbroken.
  </action>
  <verify>
    <automated>Rscript -e "testthat::test_dir('tests/testthat', filter = 'dinamica')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'DINAMICA_ERROR_PATTERNS' src/dinamica_utils.r` returns at least 2 matches (definition + use).
    - `grep -n '.check_dinamica_post_run' src/dinamica_utils.r` returns at least 2 matches (definition + call site replacing the old check).
    - `grep -c 'Dinamica EGO exited with an error' src/dinamica_utils.r` returns ≥ 1 (in DINAMICA_ERROR_PATTERNS).
    - `grep -c 'terminate called after throwing' src/dinamica_utils.r` returns ≥ 1.
    - `grep -c 'std::exception' src/dinamica_utils.r` returns ≥ 1.
    - `grep -n 'D-107' src/dinamica_utils.r` and `grep -n 'D-108' src/dinamica_utils.r` each return ≥ 1.
    - The OLD check at lines 313-321 is gone: `grep -cE 'grepl\("Dinamica EGO exited with an error", res\[\["stdout"\]\]\)' src/dinamica_utils.r` returns 0 (filter comments first if needed: `grep -v '^[[:space:]]*#' src/dinamica_utils.r | grep -c 'grepl("Dinamica EGO exited with an error"'` should be 0 because the new helper uses the constant, not the literal string here).
    - `tests/testthat/test-dinamica-error-detection.R` exists and contains at least 5 `test_that(...)` blocks; `grep -c 'test_that' tests/testthat/test-dinamica-error-detection.R` ≥ 5.
    - When `Rscript` is available: `Rscript -e "testthat::test_dir('tests/testthat', filter = 'dinamica')"` exits 0 with all dinamica tests passing.
  </acceptance_criteria>
  <done>
    src/dinamica_utils.r exposes DINAMICA_ERROR_PATTERNS + .check_dinamica_post_run() at file scope; exec_dinamica()'s error check is reduced to a single call to the helper; test-dinamica-error-detection.R has seven passing tests covering each pattern, the clean-log case, the write_logfile=FALSE fallback, and the stop-message format; test-dinamica-launcher.R still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R subprocess → contained apptainer process | Untrusted in the sense that DinamicaConsole exits 0 on internal errors; the R side must independently verify success via post-hoc log grep |
| HPC_SCRATCH_ROOT filesystem → staged-home conf file | Multiple parallel allocation workers may attempt to seed `.dinamica_ego_8.conf` simultaneously (Pitfall 3) |
| User-supplied `model_path` → bash -c payload | The model path is interpolated into a `bash -c` string; if attacker-controlled, shell injection is possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | `.dinamica_ego_8.conf` seeding race in `resolve_dinamica_launch()` | mitigate | Atomic write via `tempfile(tmpdir=staged_home) + file.rename()` so concurrent workers see either the full file or no file; the `if (!file.exists(conf))` gate keeps the operation idempotent. |
| T-01.1-02 | Information disclosure | `exec_dinamica()` swallowing real Dinamica errors as success | mitigate | `.check_dinamica_post_run()` greps for all three D-107 patterns on the captured log; stop() with the matched pattern names and logfile path surfaces failures in the per-region log. |
| T-01.1-03 | Elevation of privilege | Shell injection via `model_path` in `bash -c` payload | mitigate | `shQuote(abs_model)` (or `shQuote()` on the entire launcher argline) escapes shell metacharacters. `normalizePath()` reduces the value to an absolute path before quoting. The HPC code path is invoked only from internal callers (`run_allocation_dinamica()` and the test surface), not from user input. |
| T-01.1-04 | Denial of service | Missing `HPC_SCRATCH_ROOT` causes silent fallback to user's $HOME (out-of-quota) | mitigate | `resolve_dinamica_launch()` fails fast with a clear stop() when `HPC_SCRATCH_ROOT` is unset on the HPC backend, before any I/O happens. |
| T-01.1-05 | Repudiation | Operator cannot confirm which error pattern fired | mitigate | The stop() message names the matched pattern(s) AND the logfile path; downstream `worker_state_flush_sentinel()` captures the stop() into the per-region log. |
</threat_model>

<verification>
- All seven existing + new tests in `tests/testthat/test-dinamica-launcher.R` pass.
- All seven tests in the new `tests/testthat/test-dinamica-error-detection.R` pass.
- `src/dinamica_utils.r` HPC branch produces args matching: `apptainer exec --home <staged-home> --env DINAMICA_EGO_8_TEMP_DIR=<staged-tmp> <sif> bash -c 'cd /opt/dinamica/usr && bin/DinamicaEGO.sh <abs-model> <flags>'`.
- `exec_dinamica()` calls `.check_dinamica_post_run()` exactly once after processx returns; the helper raises stop() on any of the three patterns OR non-zero exit.
- Local backend (`resolve_dinamica_launch(backend="local")`) is byte-identical to the pre-rewrite state.
</verification>

<success_criteria>
- ROADMAP SC4 (launch shape) — VERIFIED by test-dinamica-launcher.R new HPC assertions.
- ROADMAP SC3 (error detection regardless of exit code) — VERIFIED by test-dinamica-error-detection.R clean-log + dirty-log + exit-0 cases.
- INFRA-01 R-side surface ready for Wave-2 cross-contract mirror test against the shell-side smoke test.
- OBS-02 detection contract closed at the R surface; operator gate (live Euler smoke test) handled in Wave-2/Wave-3.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-01-SUMMARY.md` summarising:
- Final `src/dinamica_utils.r` HPC launch args structure (one-line example).
- Final DINAMICA_ERROR_PATTERNS list.
- Number of tests added (≥ 4 new tests in test-dinamica-launcher.R, ≥ 5 new tests in test-dinamica-error-detection.R).
- Verbatim verification of acceptance grep gates.
</output>
