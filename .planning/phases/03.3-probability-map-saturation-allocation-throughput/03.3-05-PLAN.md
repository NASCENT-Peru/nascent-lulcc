---
phase: 03.3-probability-map-saturation-allocation-throughput
plan: 05
type: execute
wave: 4
depends_on:
  - 03.3-02
  - 03.3-03
  - 03.3-04
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md
autonomous: false
requirements:
  - ALLOC-06
  - ALLOC-07
  - ALLOC-08
  - ALLOC-09
  - ALLOC-10
tags:
  - requirements
  - roadmap
  - operator-gate
  - hpc-verification
  - saturation
must_haves:
  truths:
    - "ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10 are listed in .planning/REQUIREMENTS.md under a new ## Allocation Throughput section, each with a one-line description matching the table in RESEARCH.md `## Phase Requirements`, and each appears in the Traceability table with Phase 3.3 and a pending/complete status."
    - "The Phase 3.3 entry in .planning/ROADMAP.md's `**Requirements:**` line is updated from `TBD — to be added during /gsd-discuss-phase` to the explicit list `ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10`."
    - "An operator-gate task is presented for live HPC verification: run scripts/run_allocation.r on Euler for BAU × all Peruvian regions (the list pinned by Plan 01's reconnaissance file) × first simulation timestep (2022 → 2026); inspect each region's work_dir/saturation_summary.csv for ≥90% per-transition throughput (or documented exemption); commit the saturation_summary.csv files + posterior.tif evidence into the phase directory; run scripts/diagnose_allocation_saturation.r against each region for the deeper post-hoc report."
    - "After the operator gate completes, .planning/phases/03.3-.../03.3-SUMMARY.md is written documenting per-region throughput numbers, the root-cause confirmation (alignment fix unlocks the floor), and the final status of each of the five requirement IDs."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: New ALLOC-06..10 requirement entries + traceability table rows
      contains: "ALLOC-06"
    - path: .planning/ROADMAP.md
      provides: Phase 3.3 Requirements line updated from TBD to explicit ID list
      contains: "ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10"
    - path: .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md
      provides: Phase 3.3 closeout summary with per-region throughput numbers and requirement status
      min_lines: 80
  key_links:
    - from: .planning/REQUIREMENTS.md (Traceability table)
      to: .planning/ROADMAP.md (Phase 3.3 Requirements line)
      via: requirement IDs cross-referenced
      pattern: "ALLOC-0[6-9]|ALLOC-10"
    - from: scripts/run_allocation.r (operator gate run)
      to: work_dir/saturation_summary.csv (inline diagnostic output from Plan 02 hook)
      via: ALLOCATION_REGION_FILTER env var iterating the Plan 01 reconnaissance region list
      pattern: "saturation_summary\\.csv"
    - from: scripts/diagnose_allocation_saturation.r (post-hoc deeper report)
      to: work_dir/saturation_diagnosis.csv (Plan 03 deliverable)
      via: Rscript scripts/diagnose_allocation_saturation.r <work_dir> [--dinamica-log <path>]
      pattern: "saturation_diagnosis\\.csv"
---

<objective>
Close Phase 3.3 in three coordinated steps:

1. **Requirements consolidation (Task 1 — autonomous):** Add the five new requirement IDs (ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10) to `.planning/REQUIREMENTS.md` under a new `## Allocation Throughput` section. Append corresponding rows to the Traceability table at the bottom of the file with `Phase 3.3` and pending status (operator gate flips them to complete in Task 3).

2. **Roadmap consolidation (Task 2 — autonomous):** Update the Phase 3.3 entry in `.planning/ROADMAP.md` to replace `**Requirements**: TBD — to be added during /gsd-discuss-phase` with the explicit list `**Requirements**: ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10`. Update the `**Plans**` line from `TBD (run /gsd-plan-phase 3.3 to break down)` to `5 plans`. Update the `Plans:` checklist to enumerate the five plans in their wave order with one-line objective each. Update the `## Progress` table's `0/TBD` for Phase 3.3 to `5/5` if all five plans are complete by the operator gate, or leave `4/5` if the operator gate is the last step pending.

3. **Operator gate (Task 3 — checkpoint:human-verify, BLOCKING):** Present an operator-runnable verification recipe for the HPC live smoke covering BAU × all Peruvian regions (the list pinned by Plan 01's reconnaissance file) × first simulation timestep (2022→2026). The operator runs the recipe on Euler, inspects each region's `work_dir/saturation_summary.csv` for ≥90% per-transition throughput (or documents an exemption with rationale), commits the saturation_summary.csv files + posterior.tif evidence into the phase directory, and reports the per-region floor-met counts back. Claude then writes `03.3-SUMMARY.md` from the operator's report and updates the REQUIREMENTS.md traceability table rows to `Complete`.

This plan is wave 4 — it depends on Plan 02 (the inline saturation hook produces `saturation_summary.csv`), Plan 03 (the standalone diagnostic script for the post-hoc deeper report), and Plan 04 (the test suite that locks in the structural fix). Tasks 1 and 2 are autonomous documentation edits Claude completes independently. Task 3 is a blocking human-verify checkpoint — Claude prepares the recipe; the operator executes it on Euler; Claude resumes after the operator reports back.

Purpose: Close Phase 3.3 with full requirement traceability and live HPC evidence that the alignment fix unlocks the ≥90% per-transition throughput floor across all Peruvian regions on the BAU smoke. This is the empirical confirmation that the structural fix Phase 3.1's user-observed manual reorder predicted is correct by construction.

Output: Two documentation edits (REQUIREMENTS.md, ROADMAP.md), one operator gate, one phase summary.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-PATTERNS.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-SUMMARY.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-02-SUMMARY.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-03-SUMMARY.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-04-SUMMARY.md
@.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md
@.planning/phases/03.1-allocation-correctness-and-dinamica/03.1-01-SUMMARY.md
@.planning/phases/03.1-allocation-correctness-and-dinamica/03.1-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ALLOC-06..10 requirement entries to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file) — confirm exact section structure (## v1 Requirements with subsections ### Observability, ### Memory & Parallelism, ### Performance & Resumability, ### Infrastructure, ### Pipeline Correctness) and the Traceability table shape at the bottom (single column for `| Requirement | Phase | Status |` rows).
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-RESEARCH.md (section "## Phase Requirements" — the verbatim one-line descriptions for ALLOC-06..10 that this task copies into REQUIREMENTS.md).
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-CONTEXT.md (sections D-02, D-04, D-05, D-06, D-08 — the decision IDs each ALLOC-* requirement maps to, for the Phase column reference style).
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` in two locations.

    **Edit 1 — Add new `### Allocation Throughput` subsection at the end of `## v1 Requirements`.** Locate the end of the existing `### Pipeline Correctness` subsection (currently ending at line ~42 with the `PIPE-07` bullet). Insert a new subsection IMMEDIATELY AFTER `### Pipeline Correctness` and BEFORE the `## v2 Requirements` heading at line 44:

    ```markdown
    ### Allocation Throughput

    - [ ] **ALLOC-06**: The four work_dir Dinamica artifacts (`trans_rates.csv`, `expansion_table.csv`, `patcher_table.csv`, `probability_map_dir/*.tif`) are written from a single filtered, id_trans-sorted source data frame; persistence and zero-rate rows are dropped before any artifact is written; no `%03d_id_trans_*.tif` prefix gaps exist
    - [ ] **ALLOC-07**: Per-transition allocation throughput (placed cells / demanded cells) reaches ≥`saturation_threshold` (default 0.90) for every active transition in BAU × all Peruvian regions × 2022→2026, except transitions enumerated in `saturation_exempt`; residual unallocated counts logged per region in `saturation_summary.csv` + `AUDIT stage=saturation ...` line
    - [ ] **ALLOC-08**: `alloc_params.csv` row coverage of the active `id_trans` set is enforced as a hard precondition: if any active `id_trans` lacks an `alloc_params` row, allocation `stop()`s with the full missing list
    - [ ] **ALLOC-09**: A standalone `scripts/diagnose_allocation_saturation.r` produces a post-hoc per-transition saturation report comparing the demanded change matrix against actual posterior counts, reusing the same metric helpers as the inline summary
    - [ ] **ALLOC-10**: Probability-map quality metrics (coverage; P50/P90/P95/P99/max; demand-vs-capacity ratio) are computed and logged for every active transition during each region run
    ```

    Copy the one-line descriptions VERBATIM from RESEARCH.md's `## Phase Requirements` table (the Description column). Preserve the `- [ ]` checkbox shape used by every other v1 requirement bullet in the file. Do NOT change descriptions to be shorter or more concise — the verbatim copy ensures the requirement text matches what Plan 02/03/04 actually delivers.

    **Edit 2 — Append five rows to the Traceability table.** Locate the Traceability table at the bottom of the file (currently lines 70-92). Find the last row (`PIPE-07 | Phase 1 | Pending`) and APPEND five new rows immediately after it, BEFORE the `**Coverage:**` summary block at line ~94:

    ```markdown
    | ALLOC-06 | Phase 3.3 | Pending |
    | ALLOC-07 | Phase 3.3 | Pending |
    | ALLOC-08 | Phase 3.3 | Pending |
    | ALLOC-09 | Phase 3.3 | Pending |
    | ALLOC-10 | Phase 3.3 | Pending |
    ```

    The status column is `Pending` initially — Task 3's operator gate flips these to `Complete (closed Phase 3.3 / Plan 03.3-05)` after the live HPC verification reports per-region floor-met counts.

    **Edit 3 — Update the Coverage block.** The current `**Coverage:**` block (lines 94-96) reports `v1 requirements: 21 total`. Update this to `26 total` and add a new line under `**Phase totals:**` after the Phase 4 line:

    `- Phase 3.3 (Allocation Throughput): 5 requirements — ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10`

    Also update the `Mapped to phases: 21 ✓` line to `26 ✓`.

    **Edit 4 — Update the `*Last updated:*` footer.** Currently `*Last updated: 2026-05-17 — Phase 1.1 closed PIPE-04 + OBS-02; INFRA-01 + MEM-06 partial (deferred to gap-closure on 01.1-03 Open Issue 1)*`. Append a new line below it (or replace if the executor prefers a single-line footer per the file's convention):

    `*Last updated: 2026-05-26 — Phase 3.3 added ALLOC-06..10 (saturation alignment + diagnostic surfaces); status pending operator gate*`

    Do NOT modify any other line of the file. Do NOT change the format/casing of existing requirement IDs. Do NOT renumber existing PIPE-* or MEM-* or OBS-* requirements.
  </action>
  <verify>
    <automated>grep -F "ALLOC-06" .planning/REQUIREMENTS.md &amp;&amp; grep -F "ALLOC-07" .planning/REQUIREMENTS.md &amp;&amp; grep -F "ALLOC-08" .planning/REQUIREMENTS.md &amp;&amp; grep -F "ALLOC-09" .planning/REQUIREMENTS.md &amp;&amp; grep -F "ALLOC-10" .planning/REQUIREMENTS.md &amp;&amp; grep -F "### Allocation Throughput" .planning/REQUIREMENTS.md &amp;&amp; grep -F "| ALLOC-06 | Phase 3.3 | Pending |" .planning/REQUIREMENTS.md &amp;&amp; grep -F "| ALLOC-10 | Phase 3.3 | Pending |" .planning/REQUIREMENTS.md &amp;&amp; grep -F "Phase 3.3 (Allocation Throughput): 5 requirements" .planning/REQUIREMENTS.md &amp;&amp; grep -F "26 total" .planning/REQUIREMENTS.md &amp;&amp; [ "$(grep -c '^### ' .planning/REQUIREMENTS.md)" -ge 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a new `### Allocation Throughput` subsection under `## v1 Requirements`.
    - Each of ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10 appears exactly once as a `- [ ] **ALLOC-NN**: ...` bullet under that subsection.
    - The one-line description for each ALLOC-* requirement matches verbatim the corresponding row in RESEARCH.md's `## Phase Requirements` Description column.
    - The Traceability table contains five new rows, one per ALLOC-* ID, with Phase=Phase 3.3 and Status=Pending.
    - The `**Coverage:**` block reports `v1 requirements: 26 total` and `Mapped to phases: 26 ✓` (incremented from 21).
    - A new line under `**Phase totals:**` reads `Phase 3.3 (Allocation Throughput): 5 requirements — ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10`.
    - The `*Last updated:*` footer is updated to reflect the 2026-05-26 Phase 3.3 addition.
    - `git diff --stat .planning/REQUIREMENTS.md` shows additions only, no deletions of existing requirement lines.
  </acceptance_criteria>
  <done>REQUIREMENTS.md has five new requirement entries (ALLOC-06..10) under a new ### Allocation Throughput subsection, five new traceability rows pointing at Phase 3.3, and an updated Coverage block reflecting 26 total v1 requirements. Status starts as Pending; Task 3's operator gate flips them to Complete.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 3.3 entry in ROADMAP.md with explicit Requirements + Plans list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 163-172, the current Phase 3.3 entry) — confirm exact current line shapes: `**Requirements**: TBD — to be added during /gsd-discuss-phase` (line 166); `**Plans**: TBD (run /gsd-plan-phase 3.3 to break down)` (line 172).
    - .planning/ROADMAP.md (lines 140-161, Phase 3.2 entry) — analog format for how a Requirements + Plans section looks when populated, including the wave-grouped Plans checklist.
    - .planning/ROADMAP.md (lines 192-202, the ## Progress table) — confirm the current Phase 3.3 row reads `| 3.3. Probability Map Saturation & Allocation Throughput | 0/TBD | Not started (INSERTED 2026-05-26) | - |`.
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-PLAN.md through 03.3-04-PLAN.md — confirm the exact plan objectives for the one-line checklist entries (the `<objective>` first sentence of each).
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` in three locations.

    **Edit 1 — Update the Phase 3.3 Requirements line (line 166).** Replace:
    `**Requirements**: TBD — to be added during /gsd-discuss-phase`
    with:
    `**Requirements**: ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10`

    **Edit 2 — Replace the Plans line + Plans checklist (lines 172).** Replace:
    `**Plans**: TBD (run /gsd-plan-phase 3.3 to break down)`
    with the following block (matching the Phase 3.2 format from lines 152-161):

    ```markdown
    **Plans**: 5 plans

    Plans:

    **Wave 1**
    - [ ] 03.3-01-PLAN.md — Region reconnaissance (regions.json discovery), saturation_threshold + saturation_exempt config keys, src/saturation_diagnostics.r helper module.

    **Wave 2** *(blocked on Wave 1 completion)*
    - [ ] 03.3-02-PLAN.md — Single-source writer refactor in src/allocation.r (filter persistence + zero-rate, alloc_params stop, TIF writer no-skip, inline saturation hook).

    **Wave 3** *(blocked on Wave 2 completion — Plan 03 and Plan 04 run in parallel)*
    - [ ] 03.3-03-PLAN.md — Standalone scripts/diagnose_allocation_saturation.r post-hoc diagnostic with saturation_class classification + Dinamica-log Remaining-Transitions cross-validation.
    - [ ] 03.3-04-PLAN.md — Test coverage: tests/testthat/test-allocation-single-source-writer.R (ALLOC-06/08) + tests/testthat/test-saturation-diagnostics.R (ALLOC-07/10).

    **Wave 4** *(blocked on Wave 2, Wave 3 completion; has operator-gate checkpoint)*
    - [ ] 03.3-05-PLAN.md — Add ALLOC-06..10 to REQUIREMENTS.md; update ROADMAP.md; operator-gate live HPC verification (BAU × all Peruvian regions × 2022→2026); write 03.3-SUMMARY.md.
    ```

    The exact plan objective summaries should match the first sentence of each plan's `<objective>` block. Adjust wording if needed but keep each one-line entry concise (target one wrapped line in a markdown editor; do not expand beyond ~30 words).

    **Edit 3 — Update the ## Progress table row for Phase 3.3 (line ~200).** Replace:
    `| 3.3. Probability Map Saturation & Allocation Throughput | 0/TBD | Not started (INSERTED 2026-05-26) | - |`
    with:
    `| 3.3. Probability Map Saturation & Allocation Throughput | 4/5 | Wave 1-3 plans complete; operator gate pending (03.3-05) | partial — Wave 1-3 2026-05-26 |`

    (After Task 3's operator gate completes and the SUMMARY is written, the executor at the END of Plan 05 will revisit this line and change `4/5` to `5/5`, `partial — Wave 1-3 2026-05-26` to the actual completion date, and `Wave 1-3 plans complete; operator gate pending (03.3-05)` to `Complete`. For now, the Wave 4 documentation task pre-stages this row.)

    Do NOT modify any other Phase entry. Do NOT renumber phases. Do NOT change the `## Overview` paragraph or the `## Phases` overview list at the top.
  </action>
  <verify>
    <automated>grep -F "**Requirements**: ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10" .planning/ROADMAP.md &amp;&amp; ! grep -F "TBD — to be added during /gsd-discuss-phase" .planning/ROADMAP.md &amp;&amp; ! grep -F "TBD (run /gsd-plan-phase 3.3 to break down)" .planning/ROADMAP.md &amp;&amp; grep -F "03.3-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -F "03.3-02-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -F "03.3-03-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -F "03.3-04-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -F "03.3-05-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -F "4/5" .planning/ROADMAP.md &amp;&amp; grep -F "Wave 1" .planning/ROADMAP.md &amp;&amp; grep -F "Wave 4" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 3.3 `**Requirements**` line in ROADMAP.md reads exactly `**Requirements**: ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10`.
    - The old `TBD — to be added during /gsd-discuss-phase` text is gone from the Phase 3.3 section.
    - The old `TBD (run /gsd-plan-phase 3.3 to break down)` text is gone.
    - The Plans checklist enumerates all five plans (03.3-01 through 03.3-05) grouped by wave (Wave 1, Wave 2, Wave 3, Wave 4).
    - Plan 03 and Plan 04 are documented as parallel within Wave 3.
    - Plan 05 (this plan) is documented under Wave 4 with the operator-gate annotation.
    - The ## Progress table row for Phase 3.3 reads `4/5` (or `5/5` after Task 3 completes — pre-staging the 4/5 is correct).
    - `git diff --stat .planning/ROADMAP.md` shows changes concentrated in the Phase 3.3 entry and the Progress table row.
  </acceptance_criteria>
  <done>ROADMAP.md's Phase 3.3 entry has an explicit Requirements list (ALLOC-06..10), an explicit Plans checklist enumerating all 5 plans in 4 waves, and an updated Progress table row. The roadmap accurately reflects what Phase 3.3 actually shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator gate — live HPC verification BAU × all Peruvian regions × 2022→2026</name>
  <what-built>
    Phase 3.3 Plans 01-04 have shipped autonomously:
    - Plan 01: Region reconnaissance file pinning the actual Peruvian region labels from inputs/regionalization/regions.json; saturation_threshold (default 0.90) + saturation_exempt ([]) config keys added to both local_config.yaml and hpc_config.yaml; src/saturation_diagnostics.r module exporting compute_per_transition_metrics + write_saturation_summary + log_audit_saturation_line.
    - Plan 02: src/allocation.r single-source writer refactor (drops persistence + zero-rate rows from work_dir trans_rates.csv; sorts by id_trans; writes the four Dinamica artifacts from one filtered df in lockstep; promotes alloc_params missing-ids warning to stop(); TIF writer no longer skips on persistence/zero-rate; all-NA TIF fallback for no-predictions rows; inline saturation hook in run_allocation_one_timestep() calls write_saturation_summary() after Dinamica returns); scripts/run_allocation.r src_files vector updated to source src/saturation_diagnostics.r between utils.r and allocation.r.
    - Plan 03: scripts/diagnose_allocation_saturation.r standalone post-hoc diagnostic CLI (reads work_dir + optional dinamica log; classifies each transition as aligned_ok / saturation_bound / exempt; cross-validates against the Dinamica Remaining-Transitions matrix when --dinamica-log is supplied; writes saturation_diagnosis.csv).
    - Plan 04: tests/testthat/test-allocation-single-source-writer.R (text-pattern regression coverage for ALLOC-06 + ALLOC-08); tests/testthat/test-saturation-diagnostics.R (synthetic raster fixture coverage for ALLOC-07 + ALLOC-10).
    - This plan (Task 1, Task 2): REQUIREMENTS.md + ROADMAP.md edits adding ALLOC-06..10 with traceability + Phase 3.3 plans listed.

    The structural fix is in place: row position N in the work_dir trans_rates.csv now equals alphabetic position N in probability_map_dir/*.tif by construction. Phase 3.1's user-observed manual reorder predicted this unlocks the per-transition throughput floor; the operator gate verifies this empirically across all Peruvian regions on the BAU smoke.
  </what-built>
  <how-to-verify>
    **Operator runs on ETH Euler HPC** (the rebuilt apptainer .sif is in place from Phase 1.1; data filesystem is mounted at $HPC_SCRATCH_ROOT; Phase 3.2 audit infrastructure is in place per ROADMAP dependency).

    **Step 0 — Read the pinned region list.**
    Open `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-RECONNAISSANCE.md` and copy the labels from the `## Region labels` block. Confirm the count matches your expectations (the user said "four regions"; if the file's count differs, raise it before proceeding).

    **Step 1 — Confirm the test suite passes locally first** (sanity check before HPC submit):
    ```bash
    Rscript -e "testthat::test_dir('tests/testthat', reporter = 'progress')"
    ```
    Expected: 0 failures, 0 warnings. The two new test files from Plan 04 must pass alongside the existing 14 test files. If any test fails, STOP and report — do not submit to HPC.

    **Step 2 — Submit the HPC live smoke for each region.**
    For EACH region label from the reconnaissance file (e.g. `costa_peruana`, `selva_andina`, plus the other two/three from the pinned list), submit a separate sbatch job with `ALLOCATION_REGION_FILTER` set to that region label. The exact submission shape mirrors Phase 3.1's `scripts/submit_allocation_dinamica_only.sh:58` pattern but with `submit_allocation.sh` (or whichever submit wrapper currently runs allocation.r end-to-end) and the year filter pinned to 2026 (first simulation timestep):
    ```bash
    for REGION in <pasted-from-reconnaissance>; do
      ALLOCATION_REGION_FILTER="$REGION" \
      ALLOCATION_YEAR_POST_FILTER="2026" \
      sbatch scripts/submit_allocation.sh
    done
    ```
    (If your wrapper does not accept these env vars in this shape, fall back to manually editing the submit script for one region at a time. Phase 3.1 used `submit_allocation_dinamica_only.sh` which pins region in the script header — that flow is also acceptable.)

    Each job should produce, under the BAU scenario directory:
    `outputs/simulations/BAU/2026/region_<REGION>/` containing at minimum:
    - `anterior.tif`, `posterior.tif`
    - `trans_rates.csv` (the filtered work_dir copy — should have ZERO persistence rows and ZERO zero-rate rows)
    - `expansion_table.csv`, `patcher_table.csv`
    - `probability_map_dir/` with `%03d_id_trans_*.tif` files in a CONTIGUOUS sequence (001, 002, 003, ... with no gaps)
    - **NEW (Plan 02 deliverable):** `saturation_summary.csv` written by the inline hook.

    Wait for all jobs to finish (`squeue -u $USER` should show no remaining jobs).

    **Step 3 — Inspect saturation_summary.csv per region.**
    For each region's work_dir:
    ```bash
    cd outputs/simulations/BAU/2026/region_<REGION>
    cat saturation_summary.csv
    ```
    For each row check:
    - `placed_frac` should be `>= 0.90` (the default threshold), OR
    - The row's `exempt` column should be `TRUE` (meaning the (from_val, to_val) pair is in `saturation_exempt` per CONTEXT.md D-08).

    Count for each region:
    - Total active transitions (= `nrow(saturation_summary.csv)`)
    - Number with `floor_met == TRUE`
    - Number with `exempt == TRUE`
    - Number with `floor_met == FALSE && exempt == FALSE` (= "failed" transitions — these are the ones that need scrutiny)

    Also grep the per-region worker log for the AUDIT line:
    ```bash
    grep "AUDIT stage=saturation" worker_logs/*.log
    ```
    The line should match: `AUDIT stage=saturation scenario=BAU region=<REGION> year_ant=2022 active=<N> met=<M> exempt=<E> failed=<F> threshold=0.90`.

    **Step 4 — Run the standalone post-hoc diagnostic per region.**
    For deeper inspection of any region with failing transitions, locate the Dinamica log under `logs/dinamica-*.log` (the most recent one for that region's job) and run:
    ```bash
    Rscript scripts/diagnose_allocation_saturation.r outputs/simulations/BAU/2026/region_<REGION> \
      --dinamica-log logs/dinamica-<timestamp>.log
    ```
    Expected output:
    - A `saturation_diagnosis.csv` file appears alongside `saturation_summary.csv` in the region work_dir.
    - The console prints a `table(saturation_class)` summary showing counts per `aligned_ok` / `saturation_bound` / `exempt`.
    - When `--dinamica-log` was supplied, the per-row `dinamica_log_drift` column should be near zero (< 0.05 absolute) for all rows — confirms the inline placed_frac arithmetic matches Dinamica's own bookkeeping.

    **Step 5 — Commit the evidence into the phase directory.**
    Create a new subdirectory `.planning/phases/03.3-probability-map-saturation-allocation-throughput/uat-evidence/` and copy into it:
    - The `saturation_summary.csv` from each region's work_dir (rename to `saturation_summary_<region>.csv` so they don't collide).
    - The `saturation_diagnosis.csv` from each region (similarly renamed).
    - A small text file `posterior_cell_counts.txt` recording, for each region, the per-class cell count in `posterior.tif` (e.g. via `Rscript -e 'terra::freq(terra::rast("outputs/simulations/BAU/2026/region_<REGION>/posterior.tif"))'`). The posterior.tif files themselves are large — DO NOT commit them; the freq table is sufficient evidence.
    - A small text file `audit_log_lines.txt` containing the grepped `AUDIT stage=saturation ...` lines from each region's worker log.

    **Step 6 — Report results back to Claude.**
    Reply with:
    - The per-region counts: `<REGION>: active=N met=M exempt=E failed=F`.
    - For any region with `failed > 0`: which (from_val, to_val) pairs failed, and your judgment on whether each is (a) a genuine capacity-bound transition that should be added to `saturation_exempt` (with rationale), or (b) an indication that the alignment fix did not fully unlock the floor for that pair and the deferred D-10 remediation knobs need to be considered.
    - Any unexpected behavior (e.g., a region whose run did not produce `saturation_summary.csv`, a region with empty work_dir, etc.).

    Claude will then write `03.3-SUMMARY.md` from your report, update the REQUIREMENTS.md traceability rows from `Pending` to `Complete (closed Phase 3.3 / Plan 03.3-05; live HPC verification per region: <counts>)`, and update the ROADMAP.md Progress row from `4/5` to `5/5` with the completion date.
  </how-to-verify>
  <resume-signal>
    Reply with `approved <per-region counts>` if all regions hit the floor (or have documented exemptions); reply with `regression <details>` if any region's floor_met count is implausibly low (suggests the alignment fix did NOT take — Plan 02 needs investigation); reply with `needs-exemption <list>` if you have identified physics-bound transitions that should be added to saturation_exempt in config and re-run.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write 03.3-SUMMARY.md after operator gate completes</name>
  <files>.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - The operator's reply from Task 3 (per-region counts, any exempt additions, any regression notes).
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-01-SUMMARY.md through 03.3-04-SUMMARY.md (all four prior plan summaries) — to compose the per-plan rollup section of 03.3-SUMMARY.md.
    - .planning/phases/03.1-allocation-correctness-and-dinamica/03.1-01-SUMMARY.md — analog phase summary format (sections: Outcome, Fixes Applied, Verification, Open Items / Follow-up Concerns, Operator Evidence).
    - .planning/phases/03.3-probability-map-saturation-allocation-throughput/uat-evidence/*.csv + *.txt files committed by the operator in Task 3.
  </read_first>
  <action>
    **This task only runs AFTER Task 3 completes with an `approved` resume signal** (or after the operator and Claude have negotiated any exemption additions or regression remediations).

    Edit 1 — Write `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md` (new file) with the following structure (mirror `.planning/phases/03.1-allocation-correctness-and-dinamica/03.1-01-SUMMARY.md` shape):

    - `# Phase 3.3 Summary — Probability Map Saturation & Allocation Throughput`
    - `## Outcome` — One short paragraph stating whether the alignment fix unlocked the ≥90% per-transition floor across all Peruvian regions on BAU × 2022→2026. Quote the per-region counts the operator reported. Reference Phase 3.1's empirical observation (manual row-reorder unlocked the majority of transitions) as the predicted-and-now-confirmed result.
    - `## Fixes Applied` — Five subsections (one per plan):
      - `### Plan 01 — Region reconnaissance + config keys + saturation_diagnostics.r helpers`
      - `### Plan 02 — Single-source writer refactor (alignment by construction)`
      - `### Plan 03 — Standalone scripts/diagnose_allocation_saturation.r`
      - `### Plan 04 — Test coverage`
      - `### Plan 05 — Requirements + roadmap + operator gate`
      Each subsection: 2-3 lines summarising the fix and the requirement IDs it closes. Cross-reference each plan's own SUMMARY.md for detail.
    - `## Verification — per-region throughput numbers` — A table:

      | Region | Active transitions | floor_met | exempt | failed | Threshold |
      |--------|--------------------|-----------|--------|--------|-----------|
      | <region 1> | N | M | E | F | 0.90 |
      | <region 2> | N | M | E | F | 0.90 |
      | ... | ... | ... | ... | ... | ... |

      Plus 1-2 lines of commentary. If any region needed exempt additions, document the (from_val, to_val) pair + rationale in a sub-block.
    - `## Open Items / Follow-up Concerns` — Empty if all regions hit the floor with no exemptions. Otherwise list (a) any transitions added to saturation_exempt (with rationale), and (b) any transitions where the alignment fix did NOT unlock the floor — these become candidates for Phase 4's deferred D-10 remediation menu (Patcher tuning / Expander rebalancing / probability post-processing).
    - `## Evidence` — File-list pointer to `.planning/phases/03.3-probability-map-saturation-allocation-throughput/uat-evidence/` and what each file contains.
    - `## Requirement Status` — A small table:

      | ID | Description | Status |
      |----|-------------|--------|
      | ALLOC-06 | Single-source writer / no %03d gaps | Complete |
      | ALLOC-07 | Per-transition throughput ≥ threshold | Complete (or Complete with exemptions) |
      | ALLOC-08 | alloc_params missing -> stop() | Complete |
      | ALLOC-09 | Standalone diagnostic script | Complete |
      | ALLOC-10 | Probability-map quality metrics | Complete |

    Edit 2 — Update `.planning/REQUIREMENTS.md` Traceability table rows for ALLOC-06..10 from `Pending` to:
    `| ALLOC-NN | Phase 3.3 | Complete (closed Phase 3.3 / Plan 03.3-05; live HPC BAU × <N> regions × 2022→2026: <per-region floor-met counts>) |`
    where `<N>` is the count of regions verified and `<per-region floor-met counts>` is the same one-line summary from the operator report.

    Edit 3 — Update `.planning/ROADMAP.md` Phase 3.3 row in the `## Progress` table from `4/5` to `5/5`, status from `Wave 1-3 plans complete; operator gate pending (03.3-05)` to `Complete`, and the completion date column from `partial — Wave 1-3 2026-05-26` to the actual completion date (today's date when this task runs).

    Edit 4 — Update the top-level `## Phases` list (line 21) for Phase 3.3 from `- [ ] **Phase 3.3: Probability Map Saturation & Allocation Throughput**` to `- [x] **Phase 3.3: Probability Map Saturation & Allocation Throughput**` (the checkbox flips from empty to ticked).

    Do NOT modify any other phase entry. Do NOT change the Phase 3.3 Goal or Success Criteria — those are anchored by the original roadmap insertion and CONTEXT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md &amp;&amp; grep -F "## Outcome" .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md &amp;&amp; grep -F "## Fixes Applied" .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md &amp;&amp; grep -F "## Verification" .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md &amp;&amp; grep -F "## Requirement Status" .planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md &amp;&amp; grep -F "ALLOC-06 | Phase 3.3 | Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -F "ALLOC-10 | Phase 3.3 | Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -F "5/5" .planning/ROADMAP.md &amp;&amp; grep -F "- [x] **Phase 3.3:" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `03.3-SUMMARY.md` exists with all required sections (Outcome, Fixes Applied with five plan subsections, Verification table with one row per region, Open Items, Evidence, Requirement Status).
    - The per-region Verification table has one row per region label from the Plan 01 reconnaissance file.
    - REQUIREMENTS.md traceability rows for ALLOC-06..10 all read `Complete (closed Phase 3.3 / Plan 03.3-05; ...)` with the per-region counts in the trailing parenthetical.
    - ROADMAP.md Phase 3.3 row in the Progress table reads `5/5` with a Complete status and the actual completion date.
    - The top-level `## Phases` list checkbox for Phase 3.3 is flipped from `- [ ]` to `- [x]`.
    - If any region required saturation_exempt additions, those additions are documented in 03.3-SUMMARY.md's Open Items section AND the config/local_config.yaml + config/hpc_config.yaml saturation_exempt lists have been updated with the new entries — verified by inspection of the YAML files (this is outside the autonomous edit if config changes are required; if the operator reported saturation_exempt additions, this task may itself need to delegate the YAML edit to the operator or pre-stage the diff for review).
    - The phase summary correctly references each prior plan's SUMMARY for detail (no duplication of content).
  </acceptance_criteria>
  <done>Phase 3.3 is closed. REQUIREMENTS.md traceability is updated with `Complete` status for all five ALLOC-* IDs. ROADMAP.md Progress table shows 5/5 with the actual completion date. 03.3-SUMMARY.md captures the per-region throughput numbers, the requirement status, and any open items (exempted transitions or regressions for downstream phases). The phase's empirical confirmation of the alignment fix is documented and committed.</done>
</task>

</tasks>

<verification>
- After Task 1: REQUIREMENTS.md has five new ALLOC-* entries with Pending traceability rows.
- After Task 2: ROADMAP.md Phase 3.3 entry has explicit Requirements list and five-plan checklist grouped by wave.
- After Task 3 (operator gate): all four regions verified on Euler with saturation_summary.csv evidence committed under .planning/phases/03.3-.../uat-evidence/.
- After Task 4: 03.3-SUMMARY.md written; REQUIREMENTS.md statuses flipped to Complete; ROADMAP.md Progress row reads 5/5; Phase 3.3 checkbox in the top-level Phases list is ticked.
- No file outside `files_modified:` is touched by the autonomous tasks (Tasks 1, 2, 4). Task 3 produces operator evidence files under .planning/phases/03.3-.../uat-evidence/ which are appended to files_modified by the operator as committed evidence.
</verification>

<success_criteria>
- All five Phase 3.3 requirement IDs (ALLOC-06, ALLOC-07, ALLOC-08, ALLOC-09, ALLOC-10) are present in .planning/REQUIREMENTS.md with traceability rows and `Complete` status after operator gate.
- .planning/ROADMAP.md Phase 3.3 entry is fully populated (Requirements list, Plans checklist with 5 plans in 4 waves, Progress table showing 5/5 Complete with completion date, top-level Phases checkbox ticked).
- 03.3-SUMMARY.md documents per-region throughput numbers and the root-cause confirmation that the alignment fix unlocks the ≥90% per-transition floor on BAU × all Peruvian regions × 2022→2026.
- Operator evidence (saturation_summary.csv per region + saturation_diagnosis.csv per region + audit log lines + posterior frequency tables) is committed under .planning/phases/03.3-.../uat-evidence/.
- Any transitions added to saturation_exempt are documented with rationale; any transitions that did NOT hit the floor are recorded as Phase 4 candidates for D-10 deferred remediation.
</success_criteria>

<output>
Task 3 (operator gate) produces evidence committed to `.planning/phases/03.3-probability-map-saturation-allocation-throughput/uat-evidence/`.
Task 4 produces `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-SUMMARY.md` as the phase closeout summary.
This plan's own per-plan SUMMARY (analogous to 03.3-01-SUMMARY through 03.3-04-SUMMARY) should ALSO be written at `.planning/phases/03.3-probability-map-saturation-allocation-throughput/03.3-05-SUMMARY.md` recording:
- Task 1 + Task 2 diffs to REQUIREMENTS.md and ROADMAP.md.
- The operator's per-region counts from Task 3.
- Any saturation_exempt additions or remediation notes carried forward to Phase 4.
- A confirmation that 03.3-SUMMARY.md was written and the top-level ROADMAP.md Phases checkbox is ticked.
</output>
