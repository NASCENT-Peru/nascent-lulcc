---
phase: 01.1-fix-dinamica-launch-contract
plan: 04
type: execute
wave: 2
depends_on: ["01.1-01", "01.1-02", "01.1-03"]
files_modified:
  - tests/testthat/test-dinamica-launch-contract-mirror.R
  - dinamica/container/README.md
  - docs/README_HPC.md
autonomous: true
requirements:
  - INFRA-01
  - OBS-02
  - PIPE-04
  - MEM-06
user_setup: []

must_haves:
  truths:
    - "tests/testthat/test-dinamica-launch-contract-mirror.R asserts that resolve_dinamica_launch() and scripts/smoke_test_dinamica.sh produce the same --home, --env DINAMICA_EGO_8_TEMP_DIR=, bash -c, and bin/DinamicaEGO.sh substrings against the same input."
    - "dinamica/container/README.md documents the new D-101 bootstrap (rocker/r-ver:4.5.3 + inlined %post) and the D-104 launch shape; the docker-save workaround is demoted to a fallback note."
    - "docs/README_HPC.md's Dinamica launch examples are updated to the D-104 apptainer-exec-bash-c shape; the smoke-test --ego example switches to dinamica/dinamica_model/smoketest.ego; setup_environments.sh operator notes document the --hpc flag and the HPC_SCRATCH_ROOT contract."
    - "Both READMEs reference the D-102 rebuild trigger conditions (Dinamica EGO version bump → update URL and @version; rocker/r-ver bump → update From tag)."
  artifacts:
    - path: "tests/testthat/test-dinamica-launch-contract-mirror.R"
      provides: "Cross-language mirror assertion test (R-side resolve_dinamica_launch vs shell-side smoke_test_dinamica.sh --dry-run output)"
      contains: "expect_match.*--home"
    - path: "dinamica/container/README.md"
      provides: "Operator-facing build flow + rebuild triggers; docker-save demoted to fallback note"
      contains: "rocker/r-ver:4.5.3"
    - path: "docs/README_HPC.md"
      provides: "Updated Dinamica launch command examples, smoke-test --ego flag pointing at smoketest.ego, setup_environments.sh --hpc flag doc"
      contains: "bin/DinamicaEGO.sh"
  key_links:
    - from: "tests/testthat/test-dinamica-launch-contract-mirror.R"
      to: "scripts/smoke_test_dinamica.sh --dry-run output"
      via: "system2('bash', c(script_path, '--dry-run', ...), stdout=TRUE)"
      pattern: "resolved command"
    - from: "dinamica/container/README.md"
      to: "dinamica/container/rocker-geospatial-dinamica.def"
      via: "build flow reference"
      pattern: "apptainer build"
    - from: "docs/README_HPC.md"
      to: "scripts/smoke_test_dinamica.sh"
      via: "live smoke test example"
      pattern: "smoketest.ego"
---

<objective>
Close the Phase 1.1 documentation surface (D-114) and add the cross-language mirror assertion test (RESEARCH Target 7) that proves `src/dinamica_utils.r:resolve_dinamica_launch()` and `scripts/smoke_test_dinamica.sh` produce the same launch shape. Update both READMEs (`dinamica/container/README.md` and `docs/README_HPC.md`) to reflect the new D-101 build flow and the D-104 launch command shape, switch all smoke-test `--ego` examples from `allocation.ego-decoded` to `smoketest.ego`, and demote the docker-save workaround to a fallback note.

Purpose: Close ROADMAP SC7 (documentation reflects new build flow + launch command shape) and add the drift-mitigation safety net (Target 7) that catches future R-side / shell-side launch contract divergence at test time.

Output: One new test file + two updated README files. Wave 2 because the mirror test needs both Plan 01 (R-side launch shape) and Plan 02 (shell-side LAUNCH_CMD mirror) AND the docs need Plan 03's smoke fixture name (`smoketest.ego`) to reference.
</objective>

<execution_context>
@C:/Users/bblack/.claude/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bblack/.claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-01-SUMMARY.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-02-SUMMARY.md
@.planning/phases/01.1-fix-dinamica-launch-contract/01.1-03-SUMMARY.md
@tests/testthat/test-dinamica-launcher.R
@scripts/smoke_test_dinamica.sh
@dinamica/container/README.md
@docs/README_HPC.md

<interfaces>
<!-- Outputs from Wave 1 plans the executor builds on. -->

After Plan 01: src/dinamica_utils.r resolve_dinamica_launch() HPC args contain `--home`, `--env DINAMICA_EGO_8_TEMP_DIR=`, `bash`, `-c`, `cd /opt/dinamica/usr && bin/DinamicaEGO.sh`.

After Plan 02: scripts/smoke_test_dinamica.sh --dry-run prints a `resolved command :` line containing the same substrings. The script accepts `--hpc` flag passthrough indirectly (only setup_environments.sh has --hpc), and supports `--runtime apptainer --artifact <sif> --ego <path>`.

After Plan 03: dinamica/dinamica_model/smoketest.ego{-decoded} exist as the new minimal fixtures.

Existing test loader pattern (verbatim from tests/testthat/test-dinamica-launcher.R lines 27-38):
```r
.repo_root <- (function() {
  here <- tryCatch(normalizePath(sys.frame(1)$ofile %||% "."), error = function(e) ".")
  if (is.null(here) || identical(here, "")) here <- "."
  is_dir <- tryCatch(file.info(here)$isdir, error = function(e) NA)
  if (isTRUE(is_dir)) here <- file.path(here, "x")
  normalizePath(file.path(dirname(dirname(dirname(here)))), mustWork = FALSE)
})()

.env <- new.env(parent = baseenv())
sys.source(file.path(.repo_root, "src", "setup.r"), envir = .env)
sys.source(file.path(.repo_root, "src", "utils.r"), envir = .env)
sys.source(file.path(.repo_root, "src", "dinamica_utils.r"), envir = .env)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests/testthat/test-dinamica-launch-contract-mirror.R (RESEARCH Target 7 mirror assertion)</name>
  <files>tests/testthat/test-dinamica-launch-contract-mirror.R</files>
  <read_first>
    - tests/testthat/test-dinamica-launcher.R (lines 27-38 — verbatim test-loader pattern to copy)
    - scripts/smoke_test_dinamica.sh (lines 239-256 — the `resolved command :` line shape and PLAN heredoc structure)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md (section "tests/testthat/test-dinamica-launch-contract-mirror.R (NEW test)" lines 346-409 — verbatim splice target)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-RESEARCH.md (Target 7 lines 685-701)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-01-SUMMARY.md (verify the R-side args shape that landed)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-02-SUMMARY.md (verify the shell-side LAUNCH_CMD shape that landed)
  </read_first>
  <behavior>
    Tests that MUST pass (all in the new file tests/testthat/test-dinamica-launch-contract-mirror.R):

    - test_that("R-side resolve_dinamica_launch() args and shell-side smoke_test_dinamica.sh --dry-run resolved command both contain --home"):
      * Source dinamica_utils.r into a private `.env` via the verbatim loader pattern from test-dinamica-launcher.R.
      * `withr::local_envvar(DINAMICA_EGO_8_HOME = "/tmp/dinamica.sif", DINAMICA_BACKEND = "hpc", HPC_SCRATCH_ROOT = withr::local_tempdir())`.
      * Call `launch <- .env$resolve_dinamica_launch(model_path = "/abs/test.ego", backend = "hpc", runtime_override = "apptainer", probe_runtime = FALSE)` and assert `any(grepl("--home", launch$args, fixed = TRUE))`.
      * Invoke shell: `shell_out <- system2("bash", c(file.path(.repo_root, "scripts/smoke_test_dinamica.sh"), "--dry-run", "--runtime", "apptainer", "--artifact", "/tmp/dinamica.sif", "--ego", "/abs/test.ego"), stdout = TRUE, stderr = TRUE)`.
      * Extract `resolved_line <- grep("^resolved command", shell_out, value = TRUE)`.
      * `expect_match(resolved_line, "--home", fixed = TRUE)`.

    - test_that("both sides include DINAMICA_EGO_8_TEMP_DIR= in their args/resolved command"):
      * Same setup.
      * `expect_true(any(grepl("DINAMICA_EGO_8_TEMP_DIR=", launch$args, fixed = TRUE)))`.
      * `expect_match(resolved_line, "DINAMICA_EGO_8_TEMP_DIR=", fixed = TRUE)`.

    - test_that("both sides include bash -c and bin/DinamicaEGO.sh"):
      * `expect_true(any(grepl("bin/DinamicaEGO.sh", launch$args, fixed = TRUE)))`.
      * `expect_match(resolved_line, "bin/DinamicaEGO.sh", fixed = TRUE)`.
      * `expect_match(resolved_line, "bash", fixed = TRUE)`.

    - test_that("both sides reference the same .sif path"):
      * `expect_true(any(grepl("/tmp/dinamica.sif", launch$args, fixed = TRUE)))`.
      * `expect_match(resolved_line, "/tmp/dinamica.sif", fixed = TRUE)`.

    - test_that("R-side args last element contains the absolute model path"):
      * `expect_match(launch$args[length(launch$args)], "/abs/test.ego", fixed = TRUE)`.

    Skip guards:
    - `skip_if_not(is.function(.env$resolve_dinamica_launch))` at the top of each test_that.
    - `skip_on_os("windows")` ONLY for the system2 bash invocation — bash is available on Linux + macOS + Git-Bash on Windows; the test should work on all three but the system2() bash spelling may behave differently. Prefer `skip_if_not(nzchar(Sys.which("bash")))` instead.
  </behavior>
  <action>
    Create `tests/testthat/test-dinamica-launch-contract-mirror.R` with the following content:

    ```r
    # tests/testthat/test-dinamica-launch-contract-mirror.R
    #
    # Phase 1.1 — RESEARCH §Target 7 mirror assertion.
    #
    # Proves that src/dinamica_utils.r:resolve_dinamica_launch() (R-side launch
    # contract) and scripts/smoke_test_dinamica.sh (shell-side LAUNCH_CMD)
    # produce the same launch shape for the same inputs. Drift between the two
    # is the Pitfall 1 failure mode this test catches.
    #
    # Locked decisions covered: D-104 (launch shape), D-105 (staged-home/tmp),
    # D-106 (absolute model path).

    library(testthat)

    .repo_root <- (function() {
      here <- tryCatch(normalizePath(sys.frame(1)$ofile %||% "."), error = function(e) ".")
      if (is.null(here) || identical(here, "")) here <- "."
      is_dir <- tryCatch(file.info(here)$isdir, error = function(e) NA)
      if (isTRUE(is_dir)) here <- file.path(here, "x")
      normalizePath(file.path(dirname(dirname(dirname(here)))), mustWork = FALSE)
    })()

    .env <- new.env(parent = baseenv())
    sys.source(file.path(.repo_root, "src", "setup.r"), envir = .env)
    sys.source(file.path(.repo_root, "src", "utils.r"), envir = .env)
    sys.source(file.path(.repo_root, "src", "dinamica_utils.r"), envir = .env)

    .smoke_script <- file.path(.repo_root, "scripts", "smoke_test_dinamica.sh")
    .have_bash <- nzchar(Sys.which("bash"))

    .resolve_pair <- function() {
      # Returns a list(launch_args, resolved_line) — both sides resolved against
      # the same fixed inputs.
      scratch <- withr::local_tempdir()

      withr::local_envvar(
        DINAMICA_EGO_8_HOME = "/tmp/dinamica.sif",
        DINAMICA_BACKEND    = "hpc",
        HPC_SCRATCH_ROOT    = scratch
      )

      launch <- .env$resolve_dinamica_launch(
        model_path       = "/abs/test.ego",
        backend          = "hpc",
        runtime_override = "apptainer",
        probe_runtime    = FALSE
      )

      shell_out <- suppressWarnings(system2(
        "bash",
        c(.smoke_script,
          "--dry-run",
          "--runtime", "apptainer",
          "--artifact", "/tmp/dinamica.sif",
          "--ego", "/abs/test.ego"),
        stdout = TRUE, stderr = TRUE,
        env    = c(paste0("HPC_SCRATCH_ROOT=", scratch))
      ))

      resolved_line <- grep("^resolved command", shell_out, value = TRUE)
      if (length(resolved_line) == 0L) resolved_line <- ""

      list(launch_args = launch$args, resolved_line = resolved_line, shell_out = shell_out)
    }

    test_that("R-side and shell-side both include --home in launch args", {
      skip_if_not(is.function(.env$resolve_dinamica_launch))
      skip_if_not(.have_bash, "bash not on PATH; skipping mirror assertion")

      p <- .resolve_pair()
      expect_true(any(grepl("--home", p$launch_args, fixed = TRUE)),
                  info = "R-side resolve_dinamica_launch() args must contain --home")
      expect_match(p$resolved_line, "--home", fixed = TRUE,
                   info = "Shell-side smoke test resolved command must contain --home")
    })

    test_that("R-side and shell-side both include DINAMICA_EGO_8_TEMP_DIR=", {
      skip_if_not(is.function(.env$resolve_dinamica_launch))
      skip_if_not(.have_bash, "bash not on PATH; skipping mirror assertion")

      p <- .resolve_pair()
      expect_true(any(grepl("DINAMICA_EGO_8_TEMP_DIR=", p$launch_args, fixed = TRUE)),
                  info = "R-side args must contain DINAMICA_EGO_8_TEMP_DIR=<staged-tmp>")
      expect_match(p$resolved_line, "DINAMICA_EGO_8_TEMP_DIR=", fixed = TRUE,
                   info = "Shell-side resolved command must contain DINAMICA_EGO_8_TEMP_DIR=")
    })

    test_that("R-side and shell-side both include bin/DinamicaEGO.sh and bash", {
      skip_if_not(is.function(.env$resolve_dinamica_launch))
      skip_if_not(.have_bash, "bash not on PATH; skipping mirror assertion")

      p <- .resolve_pair()
      expect_true(any(grepl("bin/DinamicaEGO.sh", p$launch_args, fixed = TRUE)),
                  info = "R-side args must include bin/DinamicaEGO.sh in the bash -c payload")
      expect_match(p$resolved_line, "bin/DinamicaEGO.sh", fixed = TRUE)
      expect_match(p$resolved_line, "bash", fixed = TRUE)
    })

    test_that("R-side and shell-side both reference the same .sif path", {
      skip_if_not(is.function(.env$resolve_dinamica_launch))
      skip_if_not(.have_bash, "bash not on PATH; skipping mirror assertion")

      p <- .resolve_pair()
      expect_true(any(grepl("/tmp/dinamica.sif", p$launch_args, fixed = TRUE)),
                  info = "R-side args must include the .sif path /tmp/dinamica.sif")
      expect_match(p$resolved_line, "/tmp/dinamica.sif", fixed = TRUE,
                   info = "Shell-side resolved command must include the .sif path")
    })

    test_that("R-side args include the absolute model path /abs/test.ego (D-106)", {
      skip_if_not(is.function(.env$resolve_dinamica_launch))
      skip_if_not(.have_bash, "bash not on PATH; skipping mirror assertion")

      p <- .resolve_pair()
      # The absolute path appears inside the bash -c payload (the last arg).
      payload <- p$launch_args[length(p$launch_args)]
      expect_match(payload, "/abs/test.ego", fixed = TRUE,
                   info = "Bash -c payload (last arg) must contain the absolute model path")
    })
    ```

    Run it:
    ```bash
    Rscript -e "testthat::test_file('tests/testthat/test-dinamica-launch-contract-mirror.R')"
    ```
    Expected: all 5 tests pass on a host with bash. On Windows without bash, the tests skip cleanly with the `skip_if_not(.have_bash, ...)` guard.
  </action>
  <verify>
    <automated>Rscript -e "testthat::test_file('tests/testthat/test-dinamica-launch-contract-mirror.R')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/testthat/test-dinamica-launch-contract-mirror.R` passes.
    - `grep -c 'test_that' tests/testthat/test-dinamica-launch-contract-mirror.R` ≥ 5.
    - `grep -c 'resolve_dinamica_launch' tests/testthat/test-dinamica-launch-contract-mirror.R` ≥ 1.
    - `grep -c 'smoke_test_dinamica.sh' tests/testthat/test-dinamica-launch-contract-mirror.R` ≥ 1.
    - `grep -c 'resolved command' tests/testthat/test-dinamica-launch-contract-mirror.R` ≥ 1.
    - `grep -c -- '--home' tests/testthat/test-dinamica-launch-contract-mirror.R` ≥ 2 (R-side + shell-side assertions).
    - `grep -c 'bin/DinamicaEGO.sh' tests/testthat/test-dinamica-launch-contract-mirror.R` ≥ 2.
    - `grep -c 'skip_if_not' tests/testthat/test-dinamica-launch-contract-mirror.R` ≥ 2 (function-existence + bash availability).
    - When Rscript is available with bash: `Rscript -e "testthat::test_file('tests/testthat/test-dinamica-launch-contract-mirror.R')"` exits 0 with all 5 tests passing (or all 5 skipped on a no-bash host).
  </acceptance_criteria>
  <done>
    tests/testthat/test-dinamica-launch-contract-mirror.R exists with 5 test_that blocks, all skip cleanly if Rscript or bash is missing, and all pass when both are present + Plan 01 + Plan 02 have landed. The test catches future R-vs-shell drift by re-resolving the launch in both languages and asserting matching substrings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update dinamica/container/README.md and docs/README_HPC.md to the D-101/D-104 contract (D-114)</name>
  <files>dinamica/container/README.md, docs/README_HPC.md</files>
  <read_first>
    - dinamica/container/README.md (full file — current Provenance + Build flow sections need rewriting)
    - docs/README_HPC.md (full file — current Dinamica launch / smoke-test / setup_environments sections need rewriting)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-PATTERNS.md (section "dinamica/container/README.md + docs/README_HPC.md" lines 412-425 — exact subsection update directives)
    - dinamica/container/rocker-geospatial-dinamica.def (Plan 03 output — reference for the new build flow)
    - scripts/smoke_test_dinamica.sh (Plan 02 output — for the smoke command example shape)
    - scripts/setup_environments.sh (Plan 02 output — for the --hpc flag doc reference)
    - .planning/phases/01.1-fix-dinamica-launch-contract/01.1-CONTEXT.md (D-114)
  </read_first>
  <action>
    Step A: Update `dinamica/container/README.md`.

    Per PATTERNS.md lines 416-420, edit these sections (use the Edit tool, not full rewrite):

    1. **Provenance section** (currently mentions GHCR / `ghcr.io/ethzplus/rocker-geospatial-dinamica:latest`):
       - REPLACE the GHCR reference with: "verbatim port of upstream Dockerfile (https://github.com/ethzplus/rocker-geospatial-dinamica) inlined in `%post` per D-101."
       - ADD a note: "Pinned base image: `rocker/r-ver:4.5.3` (D-102 — DD-1 resolved to r-ver over geospatial because the upstream Dockerfile already inlines geospatial install steps from `install_geospatial.sh`; using `rocker/geospatial:4.5.3` would duplicate work and triple the base layer pull)."

    2. **Build flow section**:
       - KEEP the `apptainer build dinamica-ego-8.sif dinamica/container/rocker-geospatial-dinamica.def` command verbatim.
       - REMOVE any GHCR-auth instructions (e.g. `apptainer remote add ghcr ...`, `apptainer remote login ghcr.io ...` — anything that requires authentication to GHCR before pulling).
       - ADD the rebuild-trigger conditions per D-102:
         ```
         When to rebuild:
           - Upstream Dinamica EGO bumps to 8.8+ → update DINAMICA_EGO_DOWNLOAD_URL number AND @version line in dinamica/dinamica_model/smoketest.ego-decoded.
           - rocker/r-ver bumps to 4.6+ → update the From tag in rocker-geospatial-dinamica.def.
         ```

    3. **Docker-save workaround section** (if present in the current README):
       - DEMOTE from canonical-build status to a fallback note. Add a heading like `## Fallback: Workstation docker save → docker-archive://` and prefix it with: "**Use only if** the Euler apptainer build host has no internet access OR the upstream AppImage URL is unreachable. The canonical path is `apptainer build … rocker-geospatial-dinamica.def` above."

    4. **`--ego` example** (anywhere in the README that shows `--ego allocation.ego-decoded`):
       - SWITCH to `--ego dinamica/dinamica_model/smoketest.ego` per DD-2.

    Step B: Update `docs/README_HPC.md`.

    1. **Dinamica launch command examples** — find every occurrence of `apptainer exec <sif> DinamicaConsole <model>` (or similar single-arg `DinamicaConsole` invocations) and REPLACE with the D-104 shape:
       ```
       apptainer exec \
           --home   "$HPC_SCRATCH_ROOT/dinamica-home" \
           --env    DINAMICA_EGO_8_TEMP_DIR="$HPC_SCRATCH_ROOT/dinamica-tmp" \
           "$DINAMICA_EGO_8_HOME" \
           bash -c 'cd /opt/dinamica/usr && bin/DinamicaEGO.sh /abs/path/to/model.ego'
       ```

    2. **Live smoke test command** — find `bash scripts/smoke_test_dinamica.sh --live` examples and:
       - Switch `--ego dinamica/dinamica_model/allocation.ego-decoded` to `--ego dinamica/dinamica_model/smoketest.ego`.
       - Add a note: "The smoke fixture is a no-op `.ego` (D-109/DD-2). The production `allocation.ego-decoded` is unchanged and continues to be loaded by `run_allocation_dinamica()`."

    3. **setup_environments.sh operator notes** (new subsection if missing) — document the Phase 1.1 D-112 contract:
       ```markdown
       ### Phase 1.1 — HPC env install root contract (D-112)

       `scripts/setup_environments.sh` detects HPC context via three OR'd signals:
         1. `SLURM_JOB_ID` or `SLURM_CLUSTER_NAME` environment variable is set
         2. `/cluster/scratch` directory exists on the host
         3. `--hpc` CLI flag or `FORCE_HPC=true` environment variable

       When HPC is detected AND `HPC_SCRATCH_ROOT` is unset, the script REFUSES to fall back to `$PROJECT_ROOT/.envs` and exits non-zero with a named-signal message:

       ```
       ERROR: HPC context detected (SLURM env var) but HPC_SCRATCH_ROOT is unset.
              Refusing to install conda envs under $HOME (home filesystem quota).
              Source the project .env or run: export HPC_SCRATCH_ROOT=/cluster/scratch/$USER/nascent-lulcc
       ```

       Source `.env` (which sets `HPC_SCRATCH_ROOT`) before running `bash scripts/setup_environments.sh --env allocation_env --non-interactive`.
       ```

    4. **Phase 1.1 changelog note** — add a short subsection at the top of the doc or in a "Recent Changes" section noting that the launch command shape changed in Phase 1.1 to the apptainer-exec-bash-c form and the docker-save workaround is no longer canonical.

    Step C: Cross-check both READMEs for any STILL-LIVE references to:
    - `ghcr.io/ethzplus` (should be 0 outside of attribution / fallback notes)
    - `apptainer exec <sif> DinamicaConsole` (should be 0 — replaced by the D-104 shape)
    - `--ego dinamica/dinamica_model/allocation.ego-decoded` (should be 0 in smoke-test examples — switched to smoketest.ego)
  </action>
  <verify>
    <automated>grep -c 'rocker/r-ver:4.5.3' dinamica/container/README.md | grep -qE '^[1-9]' &amp;&amp; grep -c 'smoketest.ego' dinamica/container/README.md | grep -qE '^[1-9]' &amp;&amp; grep -c 'bin/DinamicaEGO.sh' docs/README_HPC.md | grep -qE '^[1-9]' &amp;&amp; grep -c 'smoketest.ego' docs/README_HPC.md | grep -qE '^[1-9]' &amp;&amp; grep -c 'HPC_SCRATCH_ROOT' docs/README_HPC.md | grep -qE '^[2-9]|^[1-9][0-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'rocker/r-ver:4.5.3' dinamica/container/README.md` ≥ 1 (D-102 pinned base image).
    - `grep -c 'apptainer build dinamica-ego-8.sif' dinamica/container/README.md` ≥ 1 (canonical build command).
    - `grep -c 'smoketest.ego' dinamica/container/README.md` ≥ 1 (DD-2 fixture swap).
    - `grep -c 'When to rebuild' dinamica/container/README.md` ≥ 1 OR `grep -cE 'rebuild.*trigger' dinamica/container/README.md` ≥ 1 (D-102 rebuild conditions documented).
    - `grep -c 'docker save' dinamica/container/README.md` returns at most matches that appear under a "Fallback" / "fallback" heading (verify by reading the section context — if `grep -c 'docker save' ...` returns N, then `grep -B5 'docker save' dinamica/container/README.md | grep -ciE 'fallback|workaround|only if'` ≥ 1).
    - `grep -c -- '--home' docs/README_HPC.md` ≥ 1 (D-104 launch shape documented).
    - `grep -c 'DINAMICA_EGO_8_TEMP_DIR=' docs/README_HPC.md` ≥ 1.
    - `grep -c 'bin/DinamicaEGO.sh' docs/README_HPC.md` ≥ 1.
    - `grep -c 'smoketest.ego' docs/README_HPC.md` ≥ 1 (DD-2 fixture swap in smoke-test example).
    - `grep -c 'HPC context detected' docs/README_HPC.md` ≥ 1 (D-112 contract documented).
    - `grep -c -- '--hpc' docs/README_HPC.md` ≥ 1 (new CLI flag documented).
    - `grep -c 'D-101' dinamica/container/README.md` ≥ 1 OR `grep -c 'Phase 1.1' dinamica/container/README.md` ≥ 1.
    - `grep -c 'D-104' docs/README_HPC.md` ≥ 1 OR `grep -c 'Phase 1.1' docs/README_HPC.md` ≥ 1.
    - No live reference to the OLD launch shape in either doc: `grep -cE 'apptainer exec [^|]*DinamicaConsole [^|-]' dinamica/container/README.md docs/README_HPC.md` returns 0 (the old `apptainer exec <sif> DinamicaConsole <model>` form is gone; matches inside a "Recent Changes" / "previous behavior" note are acceptable but should be marked as DEPRECATED).
    - No `--ego dinamica/dinamica_model/allocation.ego-decoded` in smoke-test command examples: `grep -cE 'allocation\\.ego-decoded' docs/README_HPC.md` returns at most 1 (only if mentioned in a "production allocation is unchanged" note, NOT in smoke-test examples).
  </acceptance_criteria>
  <done>
    Both READMEs reflect the D-101 bootstrap (rocker/r-ver:4.5.3 + inlined %post) and the D-104 launch command shape; smoke-test --ego examples reference smoketest.ego; the docker-save workaround appears only under a Fallback heading; docs/README_HPC.md has a dedicated D-112 setup_environments.sh subsection documenting the three-signal detection and the --hpc flag; no references to the old `apptainer exec <sif> DinamicaConsole` form remain in canonical command examples.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan only modifies documentation and adds a test. No new runtime trust boundaries.

| Boundary | Description |
|----------|-------------|
| Mirror test → shell subprocess | The test invokes `bash scripts/smoke_test_dinamica.sh --dry-run` via system2; the shell script runs with the test process's env vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-15 | Tampering | Mirror test cannot detect future drift if it is silently skipped | mitigate | Both skip guards (`skip_if_not(is.function(.env$resolve_dinamica_launch))` and `skip_if_not(.have_bash, ...)`) print a message via the second `skip_if_not()` argument so the test runner surfaces WHY tests skipped. CI / operator-side full `testthat::test_dir('tests/testthat')` runs include the file. |
| T-01.1-16 | Repudiation | Stale documentation in either README confuses future operators about whether the launch shape changed | mitigate | The "Recent Changes" / Phase 1.1 changelog note in both READMEs ties the new shape to the D-104 decision ID; the docker-save workaround is explicitly demoted. |
</threat_model>

<verification>
- `Rscript -e "testthat::test_file('tests/testthat/test-dinamica-launch-contract-mirror.R')"` exits 0 with all 5 tests passing (or skipped cleanly on no-bash hosts).
- `dinamica/container/README.md` references `rocker/r-ver:4.5.3`, the new `apptainer build` command, rebuild triggers, and `smoketest.ego`; the docker-save workaround appears only under a Fallback heading.
- `docs/README_HPC.md` includes the D-104 launch command shape, the smoke-test command pointing at `smoketest.ego`, and a D-112 setup_environments.sh subsection.
- No live references to the old `apptainer exec <sif> DinamicaConsole <model>` form in either README.
</verification>

<success_criteria>
- ROADMAP SC7 (docs reflect new build flow + launch command shape; docker save demoted) — VERIFIED via grep gates on both READMEs.
- INFRA-01 documentation surface complete; cross-language drift safety net in place.
- Phase 1.1 closure ready for the final operator-side live HPC re-verification of the three deferred 01-HUMAN-UAT.md tests (smoke test, env solve, SLURM OOM probe).
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-fix-dinamica-launch-contract/01.1-04-SUMMARY.md` summarising:
- Number of tests in the new mirror file (≥ 5).
- Which sections of dinamica/container/README.md and docs/README_HPC.md changed.
- The Phase 1.1 closing summary: which D-XX decisions landed, which ROADMAP SCs are now provable via automated tests vs operator gate.
- Hand-off note: operator should re-run the three deferred 01-HUMAN-UAT.md tests on Euler to close INFRA-01 / SC6.
</output>
