.Rproj.user
.Rhistory
.RData
.Ruserdata
Gdrive_api_auth.json

#specific folders to ignore
data-raw
results
debug_*
log_*
data
preparation

# ignore cached DinamicEGO appimages
*.AppImage

# ignore compiled ego files
*.ego

.DS_Store
inst/doc
/doc/
/Meta/
/logs

# ignore potentially large files
*.csv
*.log
*.tif

renv*
.claude/settings.local.json
Agent-tool-input-(l9orum).txt
allocation_step_integration_plan.md
.claude/settings.json

# spatial-interventions working / planning files (local-only)
spatial_interventions_integration_protocol.md

# spatial_masks_misc/: preparation inputs, build scripts, build logs, and
# temp dirs. Everything here is local-only — needed to (re)build the masks
# but not at simulation time. Already covered by individual extension
# rules above (*.tif, *.log, *.R, etc.) but kept as a folder-level rule
# for clarity.
spatial_masks_misc/

# config/old/: legacy SSPx_interventions.yml files kept locally for
# reference. The active YAMLs are config/{BAU,NAT,CUL,SOC}_interventions.yml.
config/old/
