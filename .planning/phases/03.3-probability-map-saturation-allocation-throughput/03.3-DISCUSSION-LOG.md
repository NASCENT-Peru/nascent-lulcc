# Phase 3.3 — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents —
> CONTEXT.md is the canonical decisions record. This log preserves the back-and-forth
> for retrospectives.

**Date:** 2026-05-26
**Phase:** 03.3-probability-map-saturation-allocation-throughput
**Mode:** discuss (default)

## Gray Areas Selected

1. Alignment contract & fix shape
2. Saturation diagnostic
3. Reference scope & acceptance contract

(Skipped: "Post-alignment remediation knobs" — user implicitly deferred via reliance on
the empirically-validated alignment fix.)

## User's Empirical Anchor (volunteered mid-discussion)

> "A problem I have had previously [in] Dinamica is that the order of the transitions in
> trans_rates.csv and the probability maps is not aligned. Hence Dinamica tries to find
> cells for each transition but the vast majority have probability = 0 because the map is
> not correct."
>
> "Further evidence that the row misalignment is the issue. I manually corrected the row
> order and the majority of transition cell targets were achieved."

Corroborated by inline code trace: `src/allocation.r:2271` skips TIF write for
persistence + zero-rate rows while `trans_rates.csv` retains them, breaking the alphabetic-
filename-equals-transition-matrix-row contract that
`dinamica/dinamica_model/evoland_ego_Submodels/CreateCubeOfProbabilityMaps.ego-decoded`
line 3 documents. Captured as the phase anchor (CONTEXT.md D-01).

## Decisions

### Area 1 — Alignment Contract & Fix Shape

**Q1:** Where should the alignment contract be enforced?
- Options: Single-source write in allocation.r (Recommended) / Single-source + assertion /
  External audit script / Pass explicit transition_matrix.csv to Dinamica
- **Answer:** Single-source write inside allocation.r (Recommended)
- → CONTEXT.md D-02

**Q2:** How should persistence rows (from == to) and zero-rate rows be handled?
- Options: Drop both before any artifact (Recommended) / Keep zero-rate with real TIFs /
  Keep zero-rate with placeholder TIFs
- **Answer:** Drop both before any artifact is written (Recommended)
- → CONTEXT.md D-03

**Q3:** If an active id_trans has no matching alloc_params row, what should happen?
- Options: Stop hard with full list (Recommended) / Stop with config-flag fallback /
  Inject defaults silently
- **Answer:** Stop hard with full list of missing ids (Recommended)
- → CONTEXT.md D-04

### Area 2 — Saturation Diagnostic

**Q4:** Where should saturation metrics be computed and surfaced?
- Options: Both inline + standalone (Recommended) / Standalone only / Inline only
- **Answer:** Both: inline log line + standalone script (Recommended)
- → CONTEXT.md D-05

**Q5:** Which probability-map quality metrics should the diagnostic compute? (multiSelect)
- Options: Coverage (Recommended), Percentiles (Recommended), Demand-vs-capacity
  (Recommended), Spatial autocorrelation
- **Answer:** Coverage + Percentiles + Demand-vs-capacity ratio (the three recommended);
  spatial autocorrelation declined
- → CONTEXT.md D-06, D-07

**Q6:** How should the ≥90% throughput target be evaluated?
- Options: Per-transition floor with named exemptions (Recommended) / Aggregate only /
  Per-transition floor no exemptions
- **Answer:** Per-transition floor with named exemptions (Recommended)
- → CONTEXT.md D-08

### Area 3 — Reference Scope & Acceptance Contract

**Q7:** How many scenario × region × timestep combinations must hit the throughput floor
to declare Phase 3.3 done?
- Options: BAU × costa_peruana × 2022→2026 only (Recommended) / BAU × 3 regions × first
  timestep / BAU × costa_peruana × two timesteps / BAU + one non-BAU × costa_peruana
- **Answer:** Custom — "All four regions for one time step"
- **Interpretation:** BAU × all Peruvian regions (user named four; planner must confirm
  the authoritative region list from `inputs/regionalization/regions.json`) × first
  simulation timestep (2022 → 2026).
- → CONTEXT.md D-09

## Deferred Ideas Captured

See CONTEXT.md `<deferred>` — Patcher tuning, Expander rebalancing, probability post-
processing, monte-carlo calibration revival, spatial autocorrelation, multi-scenario
verification, setup_allocation_inputs stop promotion, auto-detection of capacity-bound
transitions.

## Claude's Discretion (delegated to planner)

- Exact placement of new helper functions (in `src/allocation.r` vs. new
  `src/saturation_diagnostics.r`)
- AUDIT log line format details (key=value ordering)
- `saturation_summary.csv` column schema
- Whether the new config keys live under `simulation_trans_rates_params` or a sibling
- Exact 4-region naming and order (must be confirmed from
  `inputs/regionalization/regions.json` during research)

---

*Phase: 03.3-probability-map-saturation-allocation-throughput*
*Discussion logged: 2026-05-26*
